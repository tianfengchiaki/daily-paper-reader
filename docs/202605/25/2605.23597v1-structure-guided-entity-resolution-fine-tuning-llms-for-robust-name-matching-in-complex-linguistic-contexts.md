---
title: "Structure-Guided Entity Resolution: Fine-Tuning LLMs for Robust Name Matching in Complex Linguistic Contexts"
title_zh: 结构引导的实体消解：微调LLM以在复杂语言上下文中实现稳健的姓名匹配
authors: "Shivam Chourasia, Hitesh Kapoor, Nilesh Patil"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.23597v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 微调大模型以完成实体解析任务
tldr: "本文针对复杂语言环境下的人名匹配难题，提出结构引导实体解析（SGER）框架，通过两阶段课程微调LLM：先学习人名语法语义结构，再优化实体匹配任务。在印度身份数据上达到99.02%准确率及0.994 F1，超越GPT-4o等基线，已部署于Dream11平台，服务2.5亿+用户。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.23597v1/fig-001.webp\", \"caption\": \"Figure 1: Structure-Guided Entity Resolution (SGER) methodology. Phase 1 fine-tunes Llama 3 8B to parse names into structured JSON using SFT with LoRA. The learned weights transfer to Phase 2, where the model performs binary name matching.\", \"page\": 4, \"index\": 1, \"width\": 779, \"height\": 492}]"
motivation: 在语言和文化复杂环境中，人名匹配面临命名惯例差异、音译不一致和录入错误等挑战，影响实体解析和KYC合规。
method: 提出SGER框架，通过两阶段课程微调LLM，先解析人名语法语义结构，再优化实体匹配任务。
result: "在5万对真实数据上达到99.02%准确率和0.994 F1，超过GPT-4o少样本提示和单阶段微调。"
conclusion: 课程引导训练能够实现高精度的实体解析，适用于现实世界多语言大规模系统。
---

## 摘要
在异构记录间匹配人名是实体消解的核心挑战，尤其是在语言和文化复杂的环境中。命名惯例的差异、跨文字音译的不一致性以及频繁的数据录入错误使得统一用户身份变得困难，而这正是客户身份认证（KYC）合规的基本要求。虽然大型语言模型在理解自然语言方面展现出潜力，但它们往往难以处理此类领域特定场景中的结构化歧义。本文提出了结构引导的实体消解（SGER）——一种新颖的框架，通过两阶段课程微调LLM。模型首先训练以解析人名的语法和语义结构，然后针对二元实体匹配的下游任务进行优化。我们在印度身份数据这一全球语言最多样化和噪声最大的环境中评估了SGER。在50000个真实世界配对组成的保留测试集上，SGER达到了99.02%的准确率和0.994的F1分数，优于GPT-4o少样本提示和单阶段微调基线。该系统已全面部署在全球最大的梦幻体育平台Dream11的生产环境中，服务于超过2.5亿用户。我们的结果表明，课程引导的训练能够在现实世界的多语言系统中实现大规模、稳健、高精度的实体消解。

## Abstract
Matching person names across heterogeneous records is a core challenge in entity resolution, especially within linguistically and culturally complex environments. Variations in naming conventions, inconsistent transliteration across scripts, and frequent data entry errors make it difficult to unify user identities, an essential requirement for Know Your Customer (KYC) compliance. While Large Language Models have shown promise in understanding natural language, they often struggle with the structured ambiguity present in such domain-specific settings. This paper introduces Structure-Guided Entity Resolution (SGER), a novel framework that fine-tunes an LLM through a two-phase curriculum. The model is first trained to parse the grammatical and semantic structure of personal names, then optimized for the downstream task of binary entity matching. We evaluate SGER in the challenging context of Indian identity data, one of the most linguistically diverse and noisy environments globally. SGER achieves 99.02% accuracy and an F1 of 0.994 on a held-out set of 50,000 real-world pairs, outperforming GPT-4o few-shot prompting and single-stage fine-tuning baselines. The system is fully deployed in production at Dream11, the world's largest fantasy sports platform, serving 250M+ users. Our results demonstrate that curriculum-guided training enables robust, high-precision entity resolution in real-world multilingual systems at scale.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在语言和文化高度复杂的环境中（如印度），人名匹配面临命名惯例差异大、跨脚本音译不统一、频繁的数据录入错误等挑战。传统的实体消解方法（如编辑距离、Jaro-Winkler、语音编码）难以处理结构歧义，导致高误报和高漏报，影响KYC（了解你的客户）合规流程的效率和准确性。
- **研究动机**：大型语言模型（LLM）虽然能理解自然语言，但在这种结构化模糊的领域特定场景下表现不佳。现有微调方法通常直接训练二元匹配，迫使模型同时学习名称结构和决策边界，性能受限。
- **整体含义**：通过将结构学习与匹配决策解耦，可以显著提升LLM在实体消解任务上的鲁棒性和精度，从而在真实世界的大规模多语言系统中实现生产级部署，具有重要的商业和合规价值。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**结构引导的实体消解（SGER）** 框架，采用两阶段课程学习（curriculum learning）微调LLM：
  - **阶段一：名称结构理解**：将原始名称字符串解析为结构化JSON（包含first_name, middle_name, last_name），让模型学习人名的语法和语义结构。
  - **阶段二：二元名称匹配**：基于阶段一的检查点，继续微调模型判断两个名称是否指向同一实体（输出“Yes”或“No”）。
- **关键技术细节**：
  - **基座模型**：Llama 3 8B Instruct。
  - **微调方法**：两次均使用监督微调（SFT）+ LoRA（秩=4，缩放因子alpha=8，混合精度fp16）。
  - **数据增强**：受计算机视觉中数据增强启发，设计了三种针对名称的增强策略：
    - 对调配对（Pair Swapping）：保证顺序不变性。
    - 组件排列（Component Permutation）：模拟结构变化。
    - 随机删除空格（Random Space Removal）：模拟常见录入错误。
  - **提示设计**：阶段二的输入包含指令、少量示例和目标名称对，保持与评估一致。

### 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 阶段一训练数据：约10,000个印度人名，手动标注结构组件，从匿名身份记录中采样并辅以合成数据以确保多样性。
  - 阶段二训练数据：初始20,000个标记的名称对（来自平台历史KYC工作流），通过三种数据增强扩展到50,000+对。
  - 评估集：50,000个真实世界名称对（held-out集），与训练集在个体层面无重叠，由内部风险运营团队手动标注。
- **基准方法**：
  - 莱文斯坦相似度（阈值0.8，调优F1）。
  - BERT微调（multilingual BERT-base，[CLS]头）。
  - GPT-4o少样本提示（同模板）。
  - Llama 3 8B少样本提示。
  - Llama 3 8B单阶段SFT（有/无数据增强）。
- **评估指标**：准确率、精确率、召回率、F1分数。

### 4. 资源与算力

- **GPU**：单块NVIDIA A100（80GB显存）用于训练。生产部署使用3块NVIDIA L4 GPU。
- **训练细节**：混合精度（fp16），早停（early stopping）基于验证F1。未明确说明训练时长，但提到使用默认超参数来自PEFT和Hugging Face Transformers。

### 5. 实验数量与充分性

- **实验数量**：主表比较了6种方法（包括SGER），并提供了消融研究（单阶段SFT vs 单阶段SFT+增强 vs SGER）。此外还有错误分析。
- **充分性与客观性**：
  - 消融设计合理：逐步增加数据增强和课程学习，清晰隔离每个组件的贡献。
  - 评估集规模大（50,000对），且严格防止数据泄露（个体级别无重叠）。
  - 对比方法涵盖传统、预训练模型、不同规模LLM及少样本提示，较全面。
  - 使用了真实生产数据，具有实际意义。
  - 不足：仅在一个领域（印度KYC）测试，未在其他语言或国家验证泛化性。

### 6. 论文的主要结论与发现

- **SGER达到99.02%准确率和0.994 F1**，显著优于所有基线（最佳基线单阶段SFT+增强为0.973 F1）。
- **课程学习贡献**：相比单阶段SFT，F1从0.946提升至0.994，数据增强进一步从0.946提升至0.973。
- **生产部署影响**：
  - 处理能力：10,000 RPM，P99延迟120ms。
  - 消除了人工审查，每年节省超过50万美元运营成本。
  - 高精度（99.95%）确保合规，低漏报（<1%）通过客服解决。

### 7. 优点：方法或实验设计上的亮点

- **方法创新**：两阶段课程学习将名称结构解析与匹配任务解耦，赋予模型更强的先验知识，在结构化歧义场景中表现优异。
- **数据增强的设计哲学**：借鉴计算机视觉中的不变性编码（对调、排列、噪声），针对名称领域定制，显著提升泛化能力。
- **实验严谨性**：
  - 严格的个体级别数据划分，避免泄漏。
  - 使用大规模真实生产数据评估（50,000对），远大于训练集。
  - 消融实验清晰，组件贡献可量化。
- **实际部署证明**：在2.5亿用户平台上线，展示了可扩展性和商业价值，不仅关注学术指标。

### 8. 不足与局限

- **领域局限性**：实验仅基于印度KYC数据，未在更多语言和地区（如中东、非洲、东亚）验证，泛化性有待证明。
- **错误分析揭示的弱点**：对音近词（如Saurabh vs Sorab）和多重扭曲复合错误（如同时缺空格、顺序颠倒、OCR错误）仍有困难，未提出融合语音等解决方案。
- **模型规模**：仅测试了8B模型，未探索更大LLM（如70B）是否能带来进一步收益。
- **算力成本**：单阶段SFT+增强已接近SGER，但未充分讨论训练成本差异（如阶段一额外开销对总成本的影响）。
- **偏差风险**：虽然声称设计旨在减少偏差，但未定量分析模型在不同社区/性别/种姓上的表现是否公平。
- **可重复性**：使用的数据为专有数据，无法公开验证结果，学术可再现性受限。

（完）
