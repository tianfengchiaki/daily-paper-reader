---
title: "Quantizing Intent: Cross-Domain Semantic IDs from Organic Activity for Industrial Ranking"
title_zh: 量化意图：面向工业排序的有机活动跨领域语义ID
authors: "Julie Choi, Haoran Ye, Zhiwei Ding, Bo Long, Benjamin Zelditch, Arpita Vats"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01396v1"
tags: ["query:q1"]
score: 9.0
evidence: 通过跨域语义ID从有机活动进行广告点击率预测
tldr: "针对广告点击率预测中用户监督稀疏及跨域迁移困难，本文提出从有机Feed活动中提取用户语义ID（SIDs），并引入RQ-FSQ方法离散化预训练嵌入，在匹配稠密嵌入AUC的同时大幅降低存储成本。结合层次离散嵌入模块，在工业广告排序系统中，冷启动用户AUC提升达1.522%，验证了跨域行为转移的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01396v1/fig-001.webp\", \"caption\": \"Figure 2: Integration of the HDE Module into the Ads Ranking Model. Left: The viewer SID (𝑐1, . . . , 𝑐𝐾 ) is encoded once per request by the HDE Module (Section 4.4), producing euser — a dense vector constant across all 𝐿 positions in the sequence. Right: Per-event features at position 𝑡 are encoded through standard embedding tables. Fusion (Eq. 10): At every position 𝑡 , the user embedding is summed with the event-level embeddings before a shared LayerNorm, yielding input tokens {h𝑡 }. The resulting sequence is processed by the Transformer stack and scored by the CTR head. All HDE embedding tables are trained end-to-end under the CTR objective.\", \"page\": 5, \"index\": 1, \"width\": 452, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-002.webp\", \"caption\": \"Table 2: RQ-FSQ on two heterogeneous pre-trained embedding sources. 𝚫AUC is measured against the no-SID baseline — the production ranking model with no member SID input. Storage is the per-user serialization footprint relative to the raw float32 embedding of the same source. RQ-FSQ matches the dense embedding’s AUC on both sources at 30–280× smaller storage.\", \"page\": 7, \"index\": 2, \"width\": 682, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-003.webp\", \"caption\": \"Table 1: Cross-domain user SID results (RQ-KMeans quantization, 𝐾=3 codes per source). ΔAUC relative to no-SID baseline. Upper block: single-source SIDs ordered by behavioral activity richness. Lower block: multi-source combinations. The RQ-FSQ comparison against the same sources appears in Table 2.\", \"page\": 6, \"index\": 3, \"width\": 740, \"height\": 267}]"
motivation: 解决广告CTR预测中用户行为稀疏与跨域有机信号难以有效迁移的问题。
method: 通过有机活动提取用户语义ID，提出残差有限标量量化（RQ-FSQ）离散化嵌入，并设计层次离散嵌入模块编码多级SIDs。
result: "基于Feed活动的SIDs提升AUC 0.213%；RQ-FSQ在约30倍存储压缩下实现同等甚至更优AUC；冷启动用户CTR增益最高达1.522%。"
conclusion: 跨域行为语义ID可作为稀疏历史排序的有效桥梁，显著提升广告系统冷启动性能。
---

## 摘要
广告点击率（CTR）预测受到用户监督稀疏性的制约：大多数用户很少与广告交互，同时在有机界面（如信息流）中产生密集的行为证据。由于领域不匹配、服务成本和工程复杂性，将这些跨领域信号迁移到广告排序中十分困难。我们引入了从有机信息流活动导出的跨领域用户语义ID（SID），并表明行为活动丰富度决定了跨领域迁移质量：来自用户画像文本的SID带来+0.036% AUC提升，基于活动调优的LLaMA用户嵌入模型的SID带来+0.107%，而直接来自信息流活动行为嵌入的SID带来+0.213%。我们进一步提出RQ-FSQ，一种残差有限标量量化方法，将预训练嵌入离散化，同时在存储大幅减少的情况下匹配稠密嵌入的AUC。在两种异构源上，RQ-FSQ匹配或略微超过稠密源嵌入，对于信息流活动实现了约30倍存储压缩下的+0.351% AUC提升，对于活动调优LLaMA实现了约280倍存储压缩下的+0.265% AUC提升。我们还引入了一个层次化离散嵌入模块，通过前缀n-gram稀疏嵌入表编码多级SID，这些嵌入表在CTR目标下端到端训练。在一个大规模工业广告排序系统中，冷启动段分析显示，对几乎没有广告交互历史的用户，增益高达+1.522%，验证了跨领域行为迁移作为稀疏历史排序的有效桥梁。

## Abstract
Ads click-through rate (CTR) prediction is constrained by sparse user supervision: most users engage with ads infrequently while generating dense behavioral evidence in organic surfaces such as feed. Transferring these cross-domain signals into ads ranking is difficult due to domain mismatch, serving cost, and production complexity. We introduce cross-domain user Semantic IDs (SIDs) derived from organic feed activity and show that behavioral activity richness governs cross-domain transfer quality: SIDs from user profile text yield +0.036% AUC, SIDs from an activity-tuned LLaMA-based user embedding model yield +0.107%, and SIDs from direct feed activity behavioral embeddings yield +0.213%. We further propose RQ-FSQ, a residual finite scalar quantization method that discretizes pre-trained embeddings while matching dense-embedding AUC at substantially smaller storage. Across two heterogeneous sources, RQ-FSQ matches or slightly exceeds dense source embeddings, achieving +0.351% AUC for Feed Activity at about 30x smaller storage and +0.265% AUC for Activity-Tuned LLaMA at about 280x smaller storage. We also introduce a Hierarchical Discrete Embedding module that encodes multi-level SIDs through prefix n-gram sparse embedding tables trained end-to-end under the CTR objective. In a large-scale industrial ads ranking system, cold-start segment analysis shows gains up to +1.522% for users with near-zero ad interaction history, validating cross-domain behavioral transfer as an effective bridge for sparse-history ranking.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：广告点击率（CTR）预测中，用户与广告的交互极为稀疏，而用户在有机界面（如信息流）中产生大量行为信号。如何有效利用这些跨域有机信号提升广告排序性能，面临领域漂移、服务成本和工程复杂性三大障碍。
- **整体含义**：本文提出了一种基于“用户语义ID（SID）”的跨域行为迁移框架，通过量化有机活动中的用户意图（Quantizing Intent），将离散化后的跨域信号注入工业级广告CTR排序模型，显著改善了冷启动用户的预估质量。工作验证了“行为活动丰富度是跨域迁移质量的关键决定因素”这一原则，并提供了可落地的量化方法。

## 2. 论文提出的方法论

### 2.1 核心思想
- **跨域用户语义ID（Cross-domain User Semantic IDs, SIDs）**：将用户在不同域（如信息流）的行为或画像嵌入通过残差量化转化为紧凑的离散token序列，作为广告排序模型的输入特征。
- **行为活动丰富度原则**：跨域SID的迁移效果与源表示中编码的行为活动量成正比，即直接行为聚合 > 活动调优的模型嵌入 > 纯文本画像嵌入。

### 2.2 关键技术细节

#### （1）SID构建方法
- **RQ-KMeans**：确定性残差量化，将用户嵌入v逐级分配到最近的质心，得到K级token序列（c₁, c₂, ..., c_K）。适用于从零训练。
- **RQ-FSQ（Residual + Finite Scalar Quantization）**：结合了残差VAE量化（RQ-VAE）和有限标量量化（FSQ）。
  - FSQ：对嵌入每个维度独立量化到有限整数集L（如{-L,...,L}），保持逐维精细结构。
  - RQ-VAE：逐级量化残差，保持全局几何。
  - 融合方式：下游模型中两分支嵌入加性融合（e_RQ-FSQ = e_c_RQ + f(c_FSQ)）。
  - 存储优势：对高维LLaMA源可达280倍压缩。

#### （2）层次化离散嵌入模块（HDE Module）
- 将K级SID（c₁,...,c_K）通过前缀n-gram哈希表转换为稠密用户嵌入：
  - Level 1：前缀unigram（c₁）直接查表。
  - Level k≥2：前缀k-gram（c₁,...,c_k）多项式哈希到固定大小表W_k。
  - 所有层级嵌入求和得到最终用户嵌入e_user。
- 哈希表大小由压缩因子α和内存上限H_max控制，支持端到端CTR训练。

#### （3）多源SID
- 将三个异构源（活动调优LLaMA、画像Qwen、信息流活动）各自3级token合并为9级结构化表示，各源独立使用HDE模块后求和。
- 缺失源通过骨干源（活动调优LLaMA）的专用RQ-VAE进行隐式补全，避免静默丢弃用户。

## 3. 实验设计

### 3.1 数据集与场景
- **主要实验**：某大规模工业推荐平台的广告日志。训练60天，评估后续1天。所有报告为相对于无SID基线的AUC相对提升（绝对AUC值因保密政策未公开）。
- **公共验证**：MovieLens-100K数据集，将用户对电影评分的二分类（评分≥4）作为预测任务。

### 3.2 基准与对比方法
- **基线模型**：生产级解码器仅Transformer广告CTR排序模型（无SID）。
- **对比方法**：
  - 单源SID：Profile Qwen SID（文本语义）、Activity-Tuned LLaMA SID（活动调优）、Feed Activity SID（直接行为聚合）。
  - 多源组合：独立组合（三个SID独立索引求和） vs. 结构化Multi-Source SID。
  - 量化方法：稠密浮点嵌入、RQ-KMeans、FSQ、RQ-FSQ（在两个源上对比）。
- **评估指标**：相对AUC提升，工业经验表明+0.1%离线AUC对应可衡量的在线CTR提升。

### 3.3 实验分组（估计）
- 单源SID × 3种。
- 多源组合 × 2种（独立组合和Multi-Source SID）。
- 量化方法比较：对Feed Activity源4种（稠密、RQ-KMeans、FSQ、RQ-FSQ），对Activity-Tuned LLaMA源同样4种，共8种。
- 冷启动分段：三个用户群（最冷启动、不频繁、频繁）各对比Feed Activity SID与基线。
- 公共数据集验证：4种方法（无SID、稠密、RQ-KMeans、RQ-FSQ）。
- 另有多源内部消融（未单列但体现在Multi-Source SID的+0.036%增量）。

## 4. 资源与算力

- **文中说明**：训练使用DDP/FSDP2（PyTorch Fully Sharded Data Parallel）在H200 GPU集群上进行。未明确给出具体GPU数量、训练时长和总计算量。
- **结论**：算力信息不完整，仅能确认使用了高端集群，实际资源消耗需参考行业标准（大规模工业系统通常需数十至数百GPU，训练数天）。

## 5. 实验数量与充分性

- **充分性分析**：
  - 实验覆盖了单源、多源、不同量化方法、冷启动分层、公共数据集验证，对照组完整。
  - 消融实验充分：通过独立组合与结构化多源SID的对比，验证了前缀n-gram表与骨干补全的有效性。
  - 冷启动分析直接支持核心假设（行为丰富度原则）。
  - 公共数据集实验（MovieLens）提升了结果的泛化可信度。
- **可能不足**：所有生产数据来自同一平台（LinkedIn），不同平台用户行为模式可能存在差异；公共数据集规模较小（100K）。总体实验设计合理，结论有足够证据支持。

## 6. 论文的主要结论与发现

1. **行为活动丰富度原则成立**：下游AUC增益随源嵌入中行为活动量单调递增——纯文本+0.036%，活动调优嵌入+0.107%，直接行为聚合+0.213%。
2. **RQ-FSQ匹配甚至超过稠密嵌入AUC**：在Feed Activity源上+0.351%（~30×压缩），在活动调优LLaMA源上+0.265%（~280×压缩），成为通用离散化工具。
3. **多源SID结构优于独立组合**：结构化9码表示在前者基础上再+0.036%，且灵活集成缺失源补全。
4. **跨域SID对冷启动用户效果最显著**：最冷启动段AUC提升+1.522%，而频繁段仅+0.131%，验证了跨域行为迁移作为稀疏历史桥梁的作用。
5. **HDE模块与生产融合可行**：仅增加少量内存查找，训练/推理开销可忽略。

## 7. 优点

- **方法创新**：
  - RQ-FSQ巧妙结合残差与标量量化，在压缩与保真度间取得优异平衡，可推广至任何预训练嵌入。
  - HDE模块利用前缀n-gram哈希，支持任意K级SID且内存可控，避免表爆炸。
  - 多源SID的骨干补全策略优雅解决覆盖率问题。
- **实验严谨**：大规模工业系统验证+公共数据集复现，冷启动分段分析强化因果解释。
- **原则提炼**：提出“行为活动丰富度”作为跨域迁移的设计原则，具有通用指导意义。
- **实用性强**：所有组件可离线预计算，无需定制预处理，与生产Transformer模型无缝集成。

## 8. 不足与局限

- **实验覆盖局限**：仅验证了三种特定的源表示（Profile Qwen、Activity-Tuned LLaMA、Feed Activity），未探索其他可能的源（如社交图、搜索查询）；仅在一个工业平台上实验。
- **缺少理论分析**：未从信息论或表示学习角度解释为何离散SID能有效跨域迁移，仅做了实证。
- **量化方法对比不全**：未与VQ-VAE、RQ-VAE、归一化FSQ等变体深入比较；公共数据集仅对比RQ-FSQ与RQ-KMeans，未对比其他离散化。
- **冷启动增益的过拟合风险**：文章未讨论极端冷启动段是否因记忆特定模式而损失泛化能力。
- **多源SID复杂度**：虽不需架构改动，但需维护三个独立量化模型和补全模型，工程复杂度增加；哈希冲突可能引入噪声（文中提及但未量化分析）。
- **资源与算力未透明**：未披露训练成本，不利于其他团队复现或评估可部署性。

（完）
