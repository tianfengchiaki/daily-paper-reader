---
title: "From Clicks to Intent: Cross-Platform Session Embeddings with LLM-Distilled Taxonomy for Financial Services Recommendations"
title_zh: 从点击到意图：利用LLM提炼分类法的跨平台会话嵌入用于金融服务推荐
authors: "Dianjing Fan, Yao Li, Kyaw Hpone Myint, Dwipam Katariya, Alexandre G. R. Day, Pranab Mohanty, Giri Iyengar"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.26277v1"
tags: ["query:llmapp"]
score: 9.0
evidence: LLM蒸馏分类法用于跨平台推荐
tldr: "针对金融服务中跨平台用户行为差异导致网站意图信号未被充分利用的问题，提出一个可扩展的双重意图预测框架：通过自监督Transformer将多模态点击流编码为紧凑会话嵌入，同时结合LLM生成可解释的意图标签。实验表明，会话嵌入在移动端首页排序任务上Recall@1提升1.88%、Log Loss降低13.38%，在转化预测任务上微F1比LLM标签高4.3%，且蒸馏层仅以7%性能损失实现超低延迟的可解释标签。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.26277v1/fig-001.webp\", \"caption\": \"Figure 1: System architecture overview. Left (Section 3.2): multi-modal clickstream events are fused and encoded into session embeddings. Right (Section 3.3): clustering-based sampling, iterative taxonomy generation, label assignment, and distillation.\", \"page\": 4, \"index\": 1, \"width\": 1056, \"height\": 667}]"
motivation: 现有方法难以同时满足量化推荐和定性理解需求，且跨平台实体解析困难导致网站意图信号未用于个性化。
method: 提出自监督Transformer编码多模态点击流为会话嵌入，并利用LLM生成并蒸馏可解释的意图标签。
result: "会话嵌入在移动端排序任务上Recall@1提升1.88%、Log Loss降低13.38%；在转化预测任务上微F1比LLM标签高4.3%，蒸馏层仅损失7%性能。"
conclusion: 自监督点击流嵌入结合LLM蒸馏标签能同时服务量化任务和定性理解，适用于生产环境。
---

## 摘要
序列化用户行为建模在工业推荐系统中被广泛采用；然而，在金融服务领域仍存在显著差距，因为登录前的网页交互与认证后的应用内体验截然不同。具体而言，登录前的网页用户通常探索新产品，而登录后的应用用户则专注于账户服务。由于跨渠道实体解析的挑战（例如，将匿名网页会话与认证的移动账户匹配），基于网页的意图信号在认证后的个性化中仍未得到充分利用。现有的捕捉网页意图的方法往往是临时和狭隘的，缺乏灵活性以同时支持量化下游推荐和规模化定性理解。在这项工作中，我们提出了一个可扩展的双重意图预测框架，用于基于网页的交互，并展示了其在个性化中的应用。我们的方法将原始网页点击流转化为两个输出：一个自监督Transformer将多模态点击流编码为紧凑的会话嵌入，而一个基于LLM的分类法生成和蒸馏流程产生可解释的意图标签。我们的系统表明，自监督点击流表示结合LLM提炼的分类法可以在生产中同时服务量化任务和定性理解：在移动首页磁贴排序任务中，会话嵌入相比生产基线提高了宏Recall@1 1.88%，并将对数损失降低了13.38%。在用户转化预测任务中，嵌入在微观F1上比LLM标签高出4.3%，而蒸馏层以超低延迟提供可解释标签，仅下降7%的性能。

## Abstract
Sequential user behavior modeling is widely adopted in industrial recommender systems; however, significant gaps remain in financial services, where pre-login web interactions and authenticated in-app experiences differ drastically. Specifically, pre-login web users typically explore new products, whereas logged-in app users focus on account servicing. Due to the challenge of cross-channel entity resolution (e.g., matching anonymous web sessions to authenticated mobile accounts), web-based intent signals remain underutilized for post-authentication personalization. Existing methods for capturing web-based intent are often ad-hoc and narrow, lacking the flexibility to support both quantitative downstream recommendations and qualitative understanding at scale. In this work, we propose a scalable and dual-purpose intent prediction framework for web-based interactions and demonstrate its applicability for personalization. Our approach transforms raw web clickstreams into two outputs: a self-supervised Transformer encodes multi-modal clickstreams into a compact session embedding, while an LLM-based taxonomy generation and distillation pipeline produces interpretable intent labels. Our system demonstrates that self-supervised clickstream representations combined with LLM-distilled taxonomies can jointly serve quantitative tasks and qualitative understanding in production: on the mobile homepage tile ranking task, the session embedding improves macro Recall@1 by 1.88% and reduces Log Loss by 13.38% over production baselines. On the user conversion prediction task, the embedding outperforms the LLM labels by 4.3% on micro F1, while the distillation layer delivers interpretable labels at ultra-low latency with only a 7% performance drop.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在金融服务领域，用户在登录前的网页浏览行为（产品探索）与登录后的移动应用体验（账户服务）存在巨大差异。由于跨平台实体解析困难（如匿名网页会话与认证移动账户难以匹配），登录前的网页意图信号被丢弃，未被用于登录后的个性化推荐。现有网页意图表示方法往往是临时和狭隘的（如简单统计访问产品页次数），缺乏既能用于量化推荐又能支持定性理解（如用户分群、分析）的可扩展、可解释的表示。
- **研究背景**：序列行为建模在工业推荐中广泛采用，但金融服务中跨平台数据鸿沟导致“为什么点击”的意图无法捕捉。同时，监管和用户体验要求模型可解释，而现有深度序列模型（如SASRec、BERT4Rec）缺乏可解释层。作者提出结合自监督Transformer编码与LLM生成分类法，旨在同时产出高质量密集嵌入和人类可读意图标签，弥合量化任务与定性分析之间的缺口。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：构建一个双重输出系统：一个自监督Transformer将多模态网页点击流编码为紧凑的会话嵌入（用于下游量化任务），并通过LLM生成并蒸馏可解释的意图标签（用于定性理解）。两个输出共享一个嵌入空间，蒸馏轻量级分类器直接从嵌入预测标签，无需LLM调用于大量推理。
- **关键技术细节**：
  - **多模态会话嵌入（Section 3.2）**：
    - 输入：登录前点击流会话，每个事件包含异构特征（页面信息、停留时间、事件类型、页面HTML内容等）。
    - 离散特征通过查询表嵌入为d维向量；页面内容通过预训练句子编码器（Sentence-BERT）提取d_url维向量，再经线性投影至d维；位置使用可学习位置嵌入。
    - 每个事件的特征向量拼接后由MLP融合为d维，加上位置嵌入，输入Transformer编码器。根据下游性能，最终采用双向注意力（可看见前后事件）和均值池化（避免最后事件偏差）来获得最终会话嵌入。
    - 训练方式：自监督的掩码语言建模（掩蔽事件预测），无需标注数据。
  - **意图分类法生成与蒸馏（Section 3.3）**：
    - **聚类采样**：对会话嵌入进行K-Means聚类（K=50），每簇采样N个代表性会话，确保LLM看到完整行为多样性。
    - **分类法生成**：借鉴TnT-LLM的迭代小批量方法，用LLM经过生成（初始分类法）、更新（融合新批次）、审查（完整性/互斥性检查）三个阶段，反复E轮，得到包含I个意图类别（最终I=35）的固定分类法。人工参与审查优化。
    - **标签分配与蒸馏**：用LLM零样本为每个采样会话分配多个意图标签及置信度（作为软目标）。训练轻量级MLP作为学生模型，输入是会话嵌入，输出各意图概率，使用逐标签二元交叉熵损失。推理时完全不需要LLM调用。

### 3. 实验设计

- **数据集**：内部金融服务网站点击流数据（单域第一方cookie），过滤后每日约10万包含登录前和登录后活动的会话；选取四个月数据，按时间划分训练/验证/测试集。
- **下游任务**：
  - **移动首页磁贴排序**（多标签分类）：预测用户登录后在移动首页点击哪个产品/内容磁贴。基线模型：LightGBM（基于点击率CTR + 站点重定向特征SRF）。加入会话嵌入变体（因果+最后隐藏状态、因果+均值池化、双向+均值池化），评估宏Recall@1/2/3和Log Loss。
  - **用户转化预测**（多标签分类）：预测登录前浏览后用户是否完成特定转化（如开信用卡、申请贷款等）。对比四种特征集：A) 9维SRF；B) 蒸馏意图（35维概率）；C) LLM意图教师（35维概率）；D) 64维会话嵌入。模型均为MLP+二元交叉熵+类别权重。
- **辅助验证**：人工审查LLM标签质量（97%严格一致率），蒸馏性能（学生F1=0.822 vs 教师）。
- **公平性**：所有转化预测模型使用相同MLP架构和超参数；按时间分割避免数据泄漏。

### 4. 资源与算力

- **文中未明确说明**训练所使用的GPU型号、数量及训练时长。
- **推理效率**：对10万会话，Transformer编码耗时60秒，蒸馏MLP耗时0.009秒，总计<61秒，相比直接LLM推理（需80+小时）速度提升>3000倍。

### 5. 实验数量与充分性

- **实验组**：
  - 磁贴排序：3种嵌入变体 + 1个基线，共4组对比，报告宏Recall@1/2/3和Log Loss。
  - 转化预测：4种特征集（A/B/C/D），加上A+B组合（+37%相对F1提升），共5组对比，报告微Precision、Recall、F1。
  - 蒸馏性能：学生vs教师微F1；人工标签一致性验证。
- **充分性评价**：实验设计较为客观，但范围有限——仅两个下游任务，没有在推荐系统中进行在线A/B测试；没有与其他序列模型（如GRU4Rec、SASRec、BERT4Rec）直接对比嵌入质量（仅对比了手写特征和LLM标签）；消融实验较少（如未比较不同聚类方法、不同蒸馏损失函数、不同Transformer层数等）。整体支持核心主张，但覆盖不够广泛。

### 6. 论文的主要结论与发现

- 会话嵌入优于基线：在磁贴排序中，双向注意力+均值池化变体使宏Recall@1提升1.88%、Log Loss降低13.38%。
- 在转化预测中，会话嵌入（64维）取得最佳微F1，比LLM教师标签高4.3%，比手写SRF高35.8%以上。
- 蒸馏意图（MLP学生）保留教师93%性能（微F1仅下降7%），但提供超低延迟（10万会话0.009秒），并产生可解释标签。
- LLM生成的分类法经人工审查具有高可靠性（97%严格一致率），可用于用户分群、营销策略。
- 系统成功连接登录前网页点击流与登录后个性化，证明跨平台意图信号可显著提升推荐和转化预测。

### 7. 优点

- **双重输出设计**：同一嵌入空间同时服务量化任务（最大预测能力）和定性理解（可解释标签），解决了工业界对可解释性的需求。
- **自监督学习**：不需要人工标注，可扩展到大规模点击流数据。
- **LLM+蒸馏实现可扩展解释**：通过聚类采样和迭代分类法生成，保证分类法覆盖行为多样性；蒸馏到轻量模型实现超低延迟推理，避免每次调用LLM。
- **跨平台链接**：利用first-party cookie和共享登录凭证连接网页浏览与移动应用行为，解决实体解析难题。
- **注入语义内容**：通过句子编码器提取页面文本语义，比仅用URL更丰富。

### 8. 不足与局限

- **通用性未验证**：仅基于单一金融机构数据，可能无法直接迁移到其他行业或不同用户行为模式。
- **在线验证缺失**：未做真实A/B测试，仅离线评估，实际推荐效果可能受多种因素影响。
- **蒸馏损失**：学生模型性能比LLM教师低7%，虽可接受但仍有优化空间；嵌入本身优于LLM标签，说明可解释标签牺牲了一定信息量。
- **分类法维护**：需要人工审查和定期更新，自动化程度有待提高（未来工作提及）。
- **未与其他深度序列模型对比**：未与GRU4Rec、SASRec、BERT4Rec等对比嵌入质量，难以判断是否达到最优。
- **实验覆盖有限**：仅两类下游任务，缺少跨场景验证；消融实验较少（如未比较不同池化策略、不同聚类方法对分类法的影响）。

（完）
