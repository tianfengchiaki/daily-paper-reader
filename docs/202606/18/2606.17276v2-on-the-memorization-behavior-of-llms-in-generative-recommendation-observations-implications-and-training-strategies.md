---
title: "On the Memorization Behavior of LLMs in Generative Recommendation: Observations, Implications, and Training Strategies"
title_zh: 关于生成式推荐中大型语言模型的记忆行为：观察、启示与训练策略
authors: "Sunwoo Kim, Sunkyung Lee, Clark Mingxuan Ju, Donald Loveland, Bhuvesh Kumar, Kijung Shin, Neil Shah, Liam Collins"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17276v2"
tags: ["query:llmapp"]
score: 9.0
evidence: 分析基于LLM的生成推荐中的记忆化
tldr: 生成式推荐中，LLM利用预训练知识但存在记忆倾向。本文发现LLM比非LLM模型更依赖单跳记忆，其性能提升主要来自可预测用户。为改善泛化，提出IIRG策略，学习多跳共现的协作关系和项目语义关系，显著提升对不可预测用户的推荐效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17276v2/fig-001.webp\", \"caption\": \"Figure 12: Training sample of semantic neighbor generation with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 14, \"index\": 1, \"width\": 950, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-002.webp\", \"caption\": \"Figure 11: Training sample of semantic neighbor generation with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 14, \"index\": 2, \"width\": 950, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-003.webp\", \"caption\": \"Figure 9: Training sample of collaborative neighbor generation with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 13, \"index\": 3, \"width\": 950, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-004.webp\", \"caption\": \"Figure 10: Training sample of collaborative neighbor generation with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 13, \"index\": 4, \"width\": 950, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-005.webp\", \"caption\": \"Figure 3: One-hop memorization and performance. LLMs trained solely with next-item prediction exhibit stronger one-hop memorization than TIGER, a nonLLM-based model (see (a)). The relative improvement of LLMs over TIGER is larger for one-hopmemorization-benefiting users than for non-one-hopmemorization-benefiting users in 5/6 settings (see (b)).\", \"page\": 4, \"index\": 5, \"width\": 452, \"height\": 630}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-006.webp\", \"caption\": \"Figure 18: Generalization to other semantic IDs (SIDs). The observation that LLMs rely more heavily on one-hop memorization than TIGER, as well as the effectiveness of IIRG in reducing memorization and improving recommendation performance, also holds under Residual-K-Means (R-K-Means) SIDs.\", \"page\": 19, \"index\": 6, \"width\": 934, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-007.webp\", \"caption\": \"Figure 8: Training sample of next-item prediction with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 12, \"index\": 7, \"width\": 950, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-008.webp\", \"caption\": \"Figure 7: Training sample of next-item prediction with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 12, \"index\": 8, \"width\": 950, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-009.webp\", \"caption\": \"Figure 4: Overview of IIRG. To train LLM-based GR models, IIRG uses three tasks and jointly optimizes them:\", \"page\": 5, \"index\": 9, \"width\": 794, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2606.17276v2/fig-010.webp\", \"caption\": \"Figure 13: Informativeness of our proposed neighbors. The ratio of users benefiting from our collaborative and semantic neighbors is comparable to that of users benefiting from one-hop memorization (see (a)). Moreover, the three neighbor types have a Jaccard similarity below 0.5, suggesting that they are not merely redundant (see (b)). In addition, among non-memorization-benefiting users, our neighbors benefit a group of users comparable in size to the memorization-benefiting group (see (c)).\", \"page\": 16, \"index\": 10, \"width\": 929, \"height\": 296}]"
motivation: LLM在生成式推荐中表现出过度的单跳记忆行为，限制了预训练知识的泛化，需要提升对非直接后继项目的推荐能力。
method: 提出IIRG训练策略，通过多跳共现和语义关系增强LLM对项目间复杂关联的学习。
result: IIRG相比标准下一项预测训练，在测试项目非单跳记忆的用户上获得显著提升。
conclusion: 通过引入协作和语义关系，可以有效缓解LLM的记忆瓶颈，提高生成式推荐的泛化性。
---

## 摘要
生成式推荐（GR）已成为推荐系统的一个有前景的方向。近年来，大型语言模型（LLMs）越来越多地被用于GR，其丰富的预训练知识有望帮助它们泛化到传统以记忆为导向的基线模型所能捕捉的常见用户行为模式之外。然而，现有的基于LLM的GR工作很大程度上忽略了LLMs众所周知的记忆倾向，如果这种倾向也出现在为GR微调的LLMs中，将限制它们对预训练知识的利用。在这项工作中，我们通过检查一跳记忆（即模型推荐训练数据中项目的直接后继项目）来研究这一问题。我们表明，LLMs比非基于LLM的GR模型更频繁地进行这种记忆——事实上，它们相对于GR基线的大部分增益实际上来自于那些目标项目可以通过一跳记忆预测的用户。我们直觉认为，提高其余用户的性能需要LLMs学习更丰富的项目-项目关系，超越一跳转换。为了实现这一点，我们提出了IIRG，一种新颖的训练策略，教导LLMs捕捉：（1）从用户序列中多跳项目共现推导出的协同关系，以及（2）具有相似主题的项目之间的语义关系，这两者都可以作为有用的推荐信号。我们表明，IIRG显著优于仅使用标准下一项目预测训练的LLMs，尤其是对于那些测试项目不在训练时一跳转换覆盖范围内的用户，增益尤为显著。

## Abstract
Generative recommendation (GR) has emerged as a promising direction for recommender systems. Recently, large language models (LLMs) have been increasingly adopted for GR, as their rich pretrained knowledge is expected to help them generalize beyond common user behavior patterns that traditional memorization-oriented baselines can capture. However, existing LLM-based GR works largely ignore LLMs' well-known tendency to memorize, which, if present in LLMs fine-tuned for GR, would restrict their utilization of pretrained knowledge. In this work, we investigate this concern by examining one-hop memorization, where a model recommends items that are direct successors of items in the training data. We show that LLMs do this more than non-LLM-based GR models-in fact, the vast majority of their gains over GR baselines are actually on users whose target items can be predicted through one-hop memorization. We intuit that improving performance on the remaining users requires LLMs to learn richer item-item relations beyond one-hop transitions. To achieve this, we propose IIRG, a novel training strategy that teaches LLMs to capture: (1) collaborative relations derived from item co-occurrences across multiple hops in user sequences, and (2) semantic relations among items with similar themes, both of which can serve as useful recommendation signals. We show that IIRG significantly improves over LLMs trained solely with standard next-item prediction, with especially large gains for users whose test items are not covered by train-time one-hop transitions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：在生成式推荐（GR）中，基于大型语言模型（LLM）的方法虽然拥有丰富的预训练知识，但它们存在一个被普遍忽视的问题——**记忆（memorization）**。特别是模型倾向于记住训练数据中的**单跳（one-hop）项目转换**（即某个项目后的直接后继项目），并在推理时直接推荐这些后继项。这种行为限制了LLM利用其预训练知识进行泛化，导致性能提升主要集中在那些目标项目可以通过单跳记忆预测的用户上，而对其他用户（非单跳记忆受益者）的提升非常有限。
- **研究背景**：生成式推荐（GR）是传统推荐系统的一个新兴方向，LLM因其强大的世界知识而受到青睐。然而，先前NLP领域已发现LLM会记忆训练数据，但这种现象在GR中未被充分探索。本文是第一篇系统分析LLM在GR中记忆行为的工作。
- **整体含义**：作者指出，过度依赖单跳记忆使LLM退化为“高级模式匹配器”，无法发挥预训练知识的优势。因此，需要设计新的训练策略，帮助LLM学习更丰富的项目-项目关系（多跳协作关系和语义关系），从而提升对所有用户（尤其是非单跳记忆覆盖用户）的推荐效果。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：提出**IIRG（Item–Item Relation Generation）**，通过引入两种辅助任务（协作邻居生成和语义邻居生成），在标准下一项预测训练之外，让LLM显式学习项目之间的非单跳关联。
- **关键技术细节**：
    - **协作邻居**：对于每个项目 $i_k$，定义其与另一项目 $i_q$ 的共现权重 $w_{kq}$ = 所有用户序列中在窗口大小 $W$ 内共现的次数。取权重最高的前 $N_1$ 个项目作为协作邻居序列 $n_k$。
    - **语义邻居**：使用预训练语言嵌入模型（如 Qwen-3.5-8B Embedding）将每个项目的文本描述编码为向量，计算余弦相似度，取最相似的 $N_2$ 个项目作为语义邻居序列 $m_k$。
    - **训练损失**：LLM同时优化三项损失：
        - 下一项预测损失 $\mathcal{L}_u$（标准自回归训练，见公式 (1)）。
        - 协作邻居生成损失 $\mathcal{L}_k^{(C)}$（给定项目 $i_k$，生成其协作邻居序列 $n_k$，公式 (2)）。
        - 语义邻居生成损失 $\mathcal{L}_k^{(S)}$（生成语义邻居序列 $m_k$，公式 (3)）。
    - **联合训练**：每个批次混合三个任务的数据，总损失为三者加权和，权重 $\lambda_1, \lambda_2$ 为超参数。
    - **项目标识符**：IIRG 同时支持语义ID（SID）和术语ID（TID）。
- **重要细节**：协作邻居包含单跳邻居，但实验显示去除单跳邻居反而降低性能（附录D.4），表明单跳信息仍有价值，但IIRG通过多跳和语义补充提供了更全面的信号。

### 3. 实验设计：数据集、场景、基准方法

- **数据集**：主要使用Amazon Reviews的三个子集（Sports, Toys, Beauty），额外在Yelp数据集上验证（附录D.10）。使用留一法（leave-one-out）评估：每个用户序列最后一项为测试，倒数第二项为验证，其余为训练。
- **基准方法**：共17种，分为四类：
    - **传统模型**：LightGCN, SimGCL, SASRec, FDSA, S3-Rec。
    - **非LLM GR模型**：TIGER, LETTER。
    - **使用辅助任务的LLM GR模型**：P5, ReAT, LC-Rec, EAGER-LLM。
    - **其他LLM GR模型**：PLUM, OneRec-Think, GRAM, GRLM, AgenticTagger，以及使用标准下一项预测的**Naive**（即仅微调LLM）。
- **实验场景**：全项目排名（full-item ranking），指标：Recall@K 和 NDCG@K（K=5,10）。
- **实验充分性**：
    - 主实验：在三个数据集上报告全部指标，并与所有基线对比，IIRG在所有设定下取得最佳。
    - 消融实验：分别去掉协作/语义邻居生成，或两者都去掉，验证每个组件必要性（表2）。
    - 泛化实验：在SID和TID下均表现一致（图5，图18）。
    - 设计目标验证：证明IIRG减少了单跳记忆依赖，并且对非单跳记忆受益用户的提升更大（图6）。
    - 分析实验：考察邻居信息量、正则化效果、模型规模影响、输出有效性、冷启动物品表现等。

### 4. 资源与算力

- 论文未明确给出总训练时长或GPU小时数，但提供了硬件配置：
    - 每个训练作业运行在**GCP a2-highgpu-8g**节点（96 vCPUs, 680 GB RAM, 8 × NVIDIA A100 40GB GPU）。
    - 对于9B模型，使用**a2-ultragpu-8g**节点（8 × A100 80GB GPU）。
- 训练使用DeepSpeed、bfloat16精度（9B模型用float32避免不稳定）。
- 超参数：训练3个epoch，batch size=128，梯度累积。不同数据集和学习率等有调优（见表4）。

### 5. 实验数量与充分性：合理性、公平性

- **实验数量**：论文进行了大量实验，包括：
    - 主对照实验 (Table 1)：3个数据集 × 18种方法 × 4个指标 = 216个数据点。
    - 消融实验 (Table 2)：3种变体 × 3数据集 × 4指标 = 36个数据点。
    - 泛化实验 (Fig.5, Fig.18)：SID和不同SID类型下验证。
    - 设计目标验证 (Fig.6)：2种ID类型 × 3数据集，对比单跳记忆比率和用户分组性能。
    - 进一步分析：冷启动物品、输出有效性、语义邻居检索效率、数据集扩展（Yelp）等。
- **充分性**：实验设计较为全面，覆盖了多个维度（数据集、模型、ID类型、用户分组、组件消融）。作者也承认了局限性：未探究记忆产生的根本原因、仅使用Qwen-3.5模型族、未测试其他LLM家族（如Llama, Mistral）。
- **公平性**：所有LLM基线使用相同骨干（Qwen-3.5-4B）和训练配置，超参数调优在验证集上进行。非LLM基线使用原论文配置。因此比较相对公平。

### 6. 论文的主要结论与发现

1. **LLM在生成式推荐中表现出更强的单跳记忆行为**：相比非LLM模型（TIGER），基于LLM的GR模型在Top-5推荐中更多包含训练数据中直接后继项目（图3a）。
2. **LLM的性能提升主要来自单跳记忆受益用户**：对于目标项目能被单跳记忆覆盖的用户，LLM相比TIGER提升显著；对于非覆盖用户，提升很小甚至为负（图3b）。
3. **IIRG有效降低依赖，提升泛化能力**：提出IIRG训练策略，通过让LLM生成协作邻居和语义邻居，显著减少单跳记忆依赖，并在所有数据集和指标上取得最佳结果（Table 1）。
4. **IIRG对非单跳记忆受益用户提升最大**：这些用户上的Recall@5提升高达50%，而单跳记忆受益用户上的提升仅为17%（图6b）。
5. **IIRG在冷启动物品推荐上表现更优**：对互动次数少的物品（冷启动），IIRG的Recall@10提升幅度远高于热门物品（图17）。

### 7. 优点

- **问题新颖**：首次系统研究LLM在GR中的记忆行为，揭示了一个被忽视的瓶颈。
- **方法简单实用**：IIRG不改变模型架构，仅通过设计辅助任务注入额外信号，可即插即用于不同标识符（SID/TID）。
- **分析深入**：不仅提出方法，还提供了多角度分析（邻居信息量、正则化效果、模型规模影响等），论证充分。
- **实验充分**：在多个公共数据集、多种指标、多组消融下验证，结果一致且显著。
- **可复现**：提供了代码和数据集链接，并详细说明了超参数和实现细节。

### 8. 不足与局限

- **背后的根本原因未明确**：论文观察到了LLM更依赖单跳记忆，但未解释为什么会出现这种现象（模型大小、重复数据、优化偏好？），仅排除了模型大小和过拟合假说。
- **模型种类单一**：所有实验基于Qwen-3.5家族（0.8B-9B），未验证其他LLM（如Llama, Mistral）是否表现出相同行为。
- **辅助任务构建依赖静态邻居**：协作和语义邻居是离线预计算的，无法随训练动态更新，可能限制模型捕捉变化的关系。
- **训练开销**：完全微调4B/9B模型需要8块A100 GPU，且需要额外生成邻居（虽然可离线），但对资源有限的研究者仍有一定门槛。
- **未涉及跨域/动态推荐**：实验仅使用静态序列推荐设定，未探讨在更复杂场景（如冷启动用户、时间漂移）下的表现。

（完）
