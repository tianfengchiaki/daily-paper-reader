---
title: "Token Factory: Efficiently Integrating Diverse Signals into Large Recommendation Models"
title_zh: Token Factory：高效地将多样信号集成到大型推荐模型中
authors: "Xilun Chen, Shao-Chuan Wang, Baykal Cakici, Lukasz Heldt, Lichan Hong, Raghu Keshavan, Aniruddh Nath, Li Wei, Xinyang Yi"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19635v2"
tags: ["query:llmapp"]
score: 9.0
evidence: 高效集成多种信号到大型推荐模型
tldr: 大型推荐模型（LRMs）在工业级推荐中表现优异，但高效集成传统信号仍是挑战。现有方法导致长提示、高内存和计算开销。本文提出Token Factory框架，将传统信号转化为软令牌，由LRM直接处理，实现高效集成与压缩，避免提示长度爆炸并提升性能。实验验证了其在生产环境中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19635v2/fig-001.webp\", \"caption\": \"Figure 1: Illustration of Generative Retrieval for next video recommendation from [4]. The input prompt is a sequence of interleaved SID tokens, text, and custom tokens for numerical features.\", \"page\": 1, \"index\": 1, \"width\": 506, \"height\": 335}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-002.webp\", \"caption\": \"Figure 6: ROC AUC comparison.\", \"page\": 6, \"index\": 2, \"width\": 506, \"height\": 432}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-003.webp\", \"caption\": \"Figure 2: Token Factory Architecture. Traditional signals are transformed into \\\"soft tokens\\\" as another modality and integrated with textual tokens in the prompt.\", \"page\": 2, \"index\": 3, \"width\": 506, \"height\": 374}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-004.webp\", \"caption\": \"Figure 9: Max attention scores across all layers/heads for textual SID inputs\", \"page\": 7, \"index\": 4, \"width\": 506, \"height\": 308}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-005.webp\", \"caption\": \"Figure 10: Mean attention scores across all layers/heads for textual SID inputs\", \"page\": 7, \"index\": 5, \"width\": 506, \"height\": 310}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-006.webp\", \"caption\": \"Figure 3: Token maker for user watch history.\", \"page\": 3, \"index\": 6, \"width\": 506, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-007.webp\", \"caption\": \"Figure 4: ROC AUC comparison between baseline (black line) and treatment (red line), keeping the same batch size.\", \"page\": 5, \"index\": 7, \"width\": 506, \"height\": 379}, {\"url\": \"assets/figures/arxiv/2606.19635v2/fig-008.webp\", \"caption\": \"Figure 5: ROC AUC comparison between baseline (black line) and treatment (red line), with increased batch size.\", \"page\": 5, \"index\": 8, \"width\": 506, \"height\": 325}]"
motivation: 传统信号集成到Transformer架构中存在提示过长、内存占用大和计算开销高的问题。
method: 提出Token Factory框架，将异构传统信号转化为软令牌，由LRM直接处理并压缩。
result: 在工业级推荐环境中验证了框架的有效性，避免了提示长度爆炸并提升了模型性能。
conclusion: Token Factory能高效集成多样信号，适用于大规模推荐系统。
---

## 摘要
大型推荐模型（LRMs）在工业规模的推荐任务中已展现出令人瞩目的能力。然而，如何全面且高效地将传统信号集成到这些基于Transformer的架构中仍然是一个重大挑战。传统方法直接将这些信号“文本化”或创建离散的项目表征，往往会导致提示过长、内存占用显著以及计算开销高昂。为了克服这些限制，我们提出了“Token Factory”框架，旨在将传统信号转换为可由LRMs直接处理的“软令牌”。这种方法能够高效地集成和压缩异构输入特征，防止提示长度爆炸，同时提升模型性能。我们详细介绍了Token Factory的架构，并展示了验证其在生产级推荐环境中有效性的实验结果。

## Abstract
Large Recommendation Models (LRMs) have demonstrated promising capabilities in industry-scale recommendation tasks. However, holistically integrating traditional signals into these transformer-based architectures effectively and efficiently remains a major challenge. Conventional approaches that "textualize" these signals directly or create discrete item representations often lead to excessively long prompts, substantial memory footprints, and high computational overhead. To overcome these limitations, we propose "Token Factory", a framework designed to transform traditional signals into "soft tokens" that can be directly processed by LRMs. This approach enables efficient integration and compression of heterogeneous input features, preventing prompt length explosion while enhancing model performance. We detail the architecture of Token Factory and present experimental results validating its effectiveness in a production-scale recommendation environment.

---

## 论文详细总结（自动生成）

## 论文详细总结

### 1. 论文的核心问题与整体含义（研究动机和背景）
- **背景**：大型推荐模型（LRMs）基于Transformer架构，已广泛应用于工业级推荐（如PLUM、HSTU等）。这些模型需要集成多种信号，包括文本化语义ID（SID）、密集/稀疏特征等。
- **问题**：传统方法将这些信号直接“文本化”或离散化为ID令牌，导致prompt长度爆炸、内存占用大、计算开销高。例如，一个观看历史项目需要多个文本令牌（如SID的8个令牌+通道名+密集特征），当历史序列较长时，prompt长度迅速增长，引发训练/推理瓶颈。
- **整体含义**：需要一种高效、可扩展的方式将异构传统信号融入LRM，同时保持或提升模型性能。作者提出Token Factory框架，将信号转化为“软令牌”（soft tokens）作为新模态，由LRM直接处理，从而避免长度爆炸并提升效率。

### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程（用文字说明）
- **核心思想**：将传统信号（密集、稀疏、序列特征）通过可学习的神经网络映射为嵌入向量（软令牌），这些软令牌位于Transformer的嵌入空间中，可直接作为输入令牌，而不需经过固定词汇表的tokenizer。软令牌长度固定（如每个项目一个令牌），且可压缩。
- **关键技术细节**：
  - **Token Factory架构**：包含多个“Token Maker”，每个负责一类特征。例如：
    - WH Token Maker：处理用户观看历史序列，每个项目转化为一个软令牌。
    - Query Token Maker：处理用户级特征（如年龄、性别）。
    - Candidate Token Maker：处理候选项目特征。
  - **Token Maker内部**：
    - 输入特征集合 \( F_{input} = [f_1, f_2, ..., f_n] \)，每个特征经变换函数 \( t_i \)（如归一化、嵌入查找）得到 \( E_{input} = Concat(t_1(f_1), ..., t_n(f_n)) \)。
    - 通过可微映射 \( G \)（如MLP、Transformer）将 \( E_{input} \) 转化为 \( N \) 个软令牌 \( T_{output} \in \mathbb{R}^{N \times d_{model}} \)。
    - \( N \) 为固定预算（如1或10），平衡容量与计算。
    - 所有参数与LRM端到端联合训练。
  - **序列压缩**：针对长历史，可进一步压缩：
    - MLP压缩：在序列维度用MLP将 \( N \) 个项目压缩为 \( M \) 个软令牌。
    - 注意力池化：对每K个项目用一个软令牌概括（类似LONGER）。
- **公式/流程**（文字说明）：每个Token Maker接收原始特征 → 分别变换 → 拼接 → 投影到固定数量软令牌 → 融入prompt的指定位置（如与文本令牌交错）。查询/用户级软令牌可前缀缓存提高效率。

### 3. 实验设计：使用了哪些数据集/场景，benchmark是什么，对比了哪些方法
- **场景**：YouTube视频推荐系统的两个任务：
  - **排名任务（CTR预测）**：预测用户对候选视频的点击率。
  - **生成检索任务**：预测用户下一次观看的视频的语义ID（SID），从候选集中检索。
- **基准模型**：PLUM（基于Gemini编码器/解码器，MoE结构）。基线使用文本化SID（每个项目8个SID令牌+通道名+密集特征共12个令牌），提示长度1536（排名）/768（检索）。提交历史较长时截断早期项目。
- **对比方法**：
  - 排名任务：对比“软令牌”处理（每个项目1个软令牌，提示长度480）与基线文本化方法。
  - 检索任务：同样对比软令牌（提示长度256）与基线文本化。
- **消融实验**：在排名任务中，在固定长度480条件下，比较以下变体：
  1. **Baseline**：使用软令牌，包含所有传统特征。
  2. **WH_SID**：使用文本化SID代替软令牌，其他特征保持不变。
  3. **NO_FEAT**：使用软令牌，但移除所有传统密集/稀疏特征（除了观看历史本身）。
  4. **WH_SID_NO_FEAT**：文本化SID，无传统特征。
  5. **NO_FEAT_STRICT**：软令牌，但仅包含SID和通道ID，无任何其他特征。
- **额外研究**：扩展历史长度从200到500，使用10%压缩率压缩新增的300个项目为30个软令牌，比较AUC。

### 4. 资源与算力：若文中有提到，请总结；若未明确，请指出
- 论文**未明确说明**使用的GPU型号、数量、训练时长或总算力。
- 仅提及模型规模：排名使用110M参数的MoE Gemini编码器，检索使用210M参数的MoE Gemini解码器。训练数据包含用户观看历史、传统特征、点击/目标SID。
- 指出论文未提供具体硬件和训练时间信息。

### 5. 实验数量与充分性：大概做了多少组实验，是否充分、客观、公平
- **主要实验**：
  - 排名任务：1组主要对比（基线 vs 软令牌，固定batch大小和增加batch大小），2组训练曲线比较（图4、5）。
  - 检索任务：1组离线（Recall@10）+ 线上A/B测试（表1，5个指标）。
  - 消融实验：5种变体的AUC比较（图6）。
  - 缩放研究：1组长历史实验（AUC +0.08%）。
  - 注意力可视化（附录）：软令牌与文本SID的注意力热图比较。
- **充分性评价**：实验覆盖了排名和检索两大任务，消融实验系统性地隔离了传统特征、SID格式、历史长度等因素，验证了软令牌的效率和效果。线上实验也证明了实际收益。但主要依赖YouTube单一平台，未在其他推荐场景或数据集上验证。未与最新的压缩方法（如LONGER、GenRec）直接对比。未报告统计显著性（如p值或置信区间），但AUC和百分比变化能说明趋势。总体而言实验设计合理、结果清晰，可认为是充分的。

### 6. 论文的主要结论与发现
- **效率**：Token Factory将prompt长度压缩至约30%（排名任务从1536降至480），训练速度提升约200%（相同batch size下）。
- **质量**：排名任务中，软令牌初始AUC略低但很快追上基线，当batch size增大时则超越基线。检索任务中，离线Recall@10提升2%，在线Unique Impressions提升16.8%，一天内新鲜视频曝光提升67.1%，同时保持用户满意度（Satisfied Watchers +0.04%，Satisfied Watch Time +0.05%）。
- **压缩好处**：软令牌可容纳更长历史（从200到500），带来AUC额外提升（+0.08%）。
- **注意力利用**：软令牌的注意力分布更均匀、更密集，避免了文本SID中大量令牌被忽略的问题，从而更有效利用模型容量。

### 7. 优点：方法或实验设计上的亮点
- **方法创新**：将异构信号统一转化为软令牌作为新模态，避免了传统文本化导致的prompt爆炸，同时保持信息丰富度。
- **灵活性**：Token Maker可扩展，支持MLP压缩、注意力池化等进一步压缩方式，且支持前缀缓存提高推理效率。
- **实验设计亮点**：
  - 同时评估排名和检索任务，覆盖推荐核心环节。
  - 消融实验逐步剥离特征，清晰显示软令牌在包含多种特征时的优势。
  - 线上实验验证真实用户收益，并关注新鲜内容推荐（一天内视频曝光的巨大提升）。
  - 注意力可视化直观展示了软令牌更有效利用模型容量。

### 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等
- **实验覆盖局限**：仅在YouTube视频推荐场景验证，未在电商、新闻、社交媒体等其他推荐领域测试，通用性有待验证。
- **对比基线有限**：仅与文本化基线对比，未与现有压缩方法（如LONGER、GenRec、HyMiRec等）进行直接比较，无法判断与最优方法的差距。
- **参数和计算成本**：软令牌引入了额外的Token Maker参数（MLP等），但论文未详细分析参数增加量或对训练/推理的额外开销，只强调prompt缩短带来的好处。
- **初始化偏差**：软令牌需要从零训练，初始AUC偏低，需要足够多的训练步数才能赶上基线，这可能对训练资源有限的应用不友好。
- **缺乏统计置信度**：未提供AUC的方差或置信区间，也未说明实验重复次数。
- **特征处理细节**：如何对密集特征进行归一化、稀疏特征嵌入的维度等未具体说明，可复现性存在一定障碍。
- **应用限制**：软令牌依赖Transformer架构，对于非Transformer的推荐模型（如传统深度神经网络）不直接适用。此外，软令牌缺乏可解释性，无法像文本令牌那样直接关联人类可读信息。

（完）
