---
title: "On the Memorization Behavior of LLMs in Generative Recommendation: Observations, Implications, and Training Strategies"
title_zh: 论大语言模型在生成式推荐中的记忆行为：观察、启示与训练策略
authors: "Sunwoo Kim, Sunkyung Lee, Clark Mingxuan Ju, Donald Loveland, Bhuvesh Kumar, Kijung Shin, Neil Shah, Liam Collins"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17276v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 研究基于LLM的生成推荐中的记忆行为
tldr: 本文研究大型语言模型（LLM）在生成式推荐中的记忆行为，发现LLM过度依赖一跳记忆（训练数据中的直接后继物品），导致泛化能力受限。为此提出IIRG训练策略，通过多跳协作关系和语义关系增强模型学习，显著提升了未覆盖用户的推荐性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17276v1/fig-001.webp\", \"caption\": \"Figure 12: Training sample of semantic neighbor generation with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 14, \"index\": 1, \"width\": 950, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-002.webp\", \"caption\": \"Figure 11: Training sample of semantic neighbor generation with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 14, \"index\": 2, \"width\": 950, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-003.webp\", \"caption\": \"Figure 9: Training sample of collaborative neighbor generation with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 13, \"index\": 3, \"width\": 950, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-004.webp\", \"caption\": \"Figure 10: Training sample of collaborative neighbor generation with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 13, \"index\": 4, \"width\": 950, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-005.webp\", \"caption\": \"Figure 3: One-hop memorization and performance. LLMs trained solely with next-item prediction exhibit stronger one-hop memorization than TIGER, a nonLLM-based model (see (a)). The relative improvement of LLMs over TIGER is larger for one-hopmemorization-benefiting users than for non-one-hopmemorization-benefiting users in 5/6 settings (see (b)).\", \"page\": 4, \"index\": 5, \"width\": 452, \"height\": 630}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-006.webp\", \"caption\": \"Figure 18: Generalization to other semantic IDs (SIDs). The observation that LLMs rely more heavily on one-hop memorization than TIGER, as well as the effectiveness of IIRG in reducing memorization and improving recommendation performance, also holds under Residual-K-Means (R-K-Means) SIDs.\", \"page\": 19, \"index\": 6, \"width\": 934, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-007.webp\", \"caption\": \"Figure 8: Training sample of next-item prediction with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 12, \"index\": 7, \"width\": 950, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-008.webp\", \"caption\": \"Figure 7: Training sample of next-item prediction with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 12, \"index\": 8, \"width\": 950, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-009.webp\", \"caption\": \"Figure 4: Overview of IIRG. To train LLM-based GR models, IIRG uses three tasks and jointly optimizes them:\", \"page\": 5, \"index\": 9, \"width\": 794, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2606.17276v1/fig-010.webp\", \"caption\": \"Figure 13: Informativeness of our proposed neighbors. The ratio of users benefiting from our collaborative and semantic neighbors is comparable to that of users benefiting from one-hop memorization (see (a)). Moreover, the three neighbor types have a Jaccard similarity below 0.5, suggesting that they are not merely redundant (see (b)). In addition, among non-memorization-benefiting users, our neighbors benefit a group of users comparable in size to the memorization-benefiting group (see (c)).\", \"page\": 16, \"index\": 10, \"width\": 929, \"height\": 296}]"
motivation: 现有LLM-based生成式推荐忽略LLM的记忆倾向，可能限制其利用预训练知识进行泛化。
method: 提出IIRG训练策略，教LLM捕捉物品间的多跳协作关系和语义关系，超越简单的一跳过渡。
result: IIRG显著优于仅用标准下一个物品预测训练的LLM，尤其对测试物品未被训练时一跳覆盖的用户提升巨大。
conclusion: 通过引入多跳关系和语义关系，LLM能更好地利用预训练知识，提升生成式推荐的泛化能力。
---

## 摘要
生成式推荐已成为推荐系统的一个有前景的方向。近年来，大语言模型越来越多地被用于生成式推荐，因其丰富的预训练知识有望帮助模型泛化到传统基于记忆的基线模型所能捕捉的常见用户行为模式之外。然而，现有的基于大语言模型的生成式推荐工作在很大程度上忽略了大语言模型众所周知的记忆倾向，如果在针对生成式推荐进行微调的大语言模型中存在这种倾向，将限制其对预训练知识的利用。本文通过考察一跳记忆行为来探究这一问题，即模型推荐训练数据中项目的直接后继项目。我们发现，大语言模型比非基于大语言模型的生成式推荐模型更频繁地表现出这种行为——事实上，它们相对于生成式推荐基线模型的大部分性能提升实际上来自于那些目标项目可通过一跳记忆预测的用户。我们推测，要提升其余用户的性能，需要大语言模型学习超越一跳转移的更丰富的项目间关系。为此，我们提出IIRG，一种新颖的训练策略，教导大语言模型捕捉：（1）从用户序列中多跳项目共现推导出的协同关系，以及（2）主题相似项目间的语义关系，这两者都可作为有用的推荐信号。我们证明，与仅用标准下一项目预测训练的大语言模型相比，IIRG显著提升性能，对于测试项目未被训练时一跳转移覆盖的用户，性能提升尤为显著。

## Abstract
Generative recommendation (GR) has emerged as a promising direction for recommender systems. Recently, large language models (LLMs) have been increasingly adopted for GR, as their rich pretrained knowledge is expected to help them generalize beyond common user behavior patterns that traditional memorization-oriented baselines can capture. However, existing LLM-based GR works largely ignore LLMs' well-known tendency to memorize, which, if present in LLMs fine-tuned for GR, would restrict their utilization of pretrained knowledge. In this work, we investigate this concern by examining one-hop memorization, where a model recommends items that are direct successors of items in the training data. We show that LLMs do this more than non-LLM-based GR models-in fact, the vast majority of their gains over GR baselines are actually on users whose target items can be predicted through one-hop memorization. We intuit that improving performance on the remaining users requires LLMs to learn richer item-item relations beyond one-hop transitions. To achieve this, we propose IIRG, a novel training strategy that teaches LLMs to capture: (1) collaborative relations derived from item co-occurrences across multiple hops in user sequences, and (2) semantic relations among items with similar themes, both of which can serve as useful recommendation signals. We show that IIRG significantly improves over LLMs trained solely with standard next-item prediction, with especially large gains for users whose test items are not covered by train-time one-hop transitions.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

大语言模型（LLM）凭借丰富的预训练知识，被越来越多地用于生成式推荐（Generative Recommendation, GR），期望它能超越传统基于记忆的模型，实现更好的泛化。然而，LLM 在自然语言处理中已知存在严重的记忆倾向——会记住训练数据中的片段并在推理时复现。这一现象在 LLM 用于 GR 时尚未被充分研究。作者担心，若 LLM 在 GR 中也过度依赖记忆，将无法利用其预训练知识，从而限制推荐性能。

**核心问题**：LLM 在 GR 中是否存在特殊的记忆行为？该行为如何影响推荐性能？能否通过训练策略缓解记忆依赖、提升泛化？

### 2. 论文提出的方法论

#### 核心思想
定义 **“一跳记忆”（one-hop memorization）**：模型直接推荐训练数据中某项物品的后继物品（即一项物品之后紧接着出现的物品）。作者发现 LLM 比非 LLM 的 GR 模型更依赖这种记忆，且其性能提升主要来自那些目标物品可通过一跳记忆预测的用户。为此，提出 **IIRG（Item–Item Relation Generation）** 训练策略，通过引入两种额外的生成任务，迫使 LLM 学习超越一跳关系的更丰富的物品间关系：
- **协作关系**：从用户交互序列中多跳窗口内共同出现的物品中提取频繁共现物品。
- **语义关系**：基于物品文本描述嵌入的相似性选择语义邻近物品。

#### 关键技术与算法流程
1. **构建邻居序列**：
   - 协作邻居 \( n_k \)：对每个物品 \( i_k \)，统计其在宽为 \( W \) 的时间窗口内与其他物品的共现频率，取 top-\( N_1 \) 高频物品。
   - 语义邻居 \( m_k \)：用预训练语言嵌入模型（Qwen-3-8B）编码物品文本描述，计算余弦相似度，取 top-\( N_2 \) 相似物品。
2. **联合训练损失**：
   - 原有**下一物品预测损失** \( \mathcal{L}_u \)（见公式 (1)）。
   - **协作邻居生成损失** \( \mathcal{L}_k^{(C)} \)（公式 (2)）：以物品标识符（SID 或 TID）和标题作为输入，输出其协作邻居序列。
   - **语义邻居生成损失** \( \mathcal{L}_k^{(S)} \)（公式 (3)）：输出语义邻居序列。
   - 总损失为三者加权和：\( \mathcal{L} = \frac{1}{B}\sum \mathcal{L}_u + \frac{\lambda_1}{B}\sum \mathcal{L}_k^{(C)} + \frac{\lambda_2}{B}\sum \mathcal{L}_t^{(S)} \)。
3. **推理**：采用束搜索（beam=20）生成物品标识符，映射到已有物品。

### 3. 实验设计

- **数据集**：Amazon Reviews 的 Sports、Toys、Beauty 三个子集，以及 Yelp（附录）。采用 leave-one-out 评估（最后一个交互为测试，倒数第二个为验证，其余为训练）。
- **评价指标**：Recall@K 和 NDCG@K（K=5,10），全物品排名设置。
- **对比方法**（共17种）：
  - 传统模型：LightGCN、SimGCL、SASRec、FDSA、S³-Rec
  - 非 LLM GR：TIGER、LETTER
  - 联合训练 GR：P5、ReAT、LC-Rec、EAGER-LLM
  - LLM-based GR（不同标识符）：PLUM（SID）、OneRec-Think（SID）、Naive（仅下一物品预测）、GRAM（TID）、GRLM（TID）、AgenticTagger（TID）
- **消融实验**：去掉协作或语义邻居生成、随机邻居、启发式方法、排除一跳转移等。
- **泛化验证**：在 SID（PLUM 和 Residual K-Means）和 TID 下均测试。

### 4. 资源与算力

- **硬件**：Google Cloud Platform。每项训练任务使用单台 **a2-highgpu-8g** 节点（8× NVIDIA A100 40GB）；9B 参数模型使用 **a2-ultragpu-8g** 节点（8× A100 80GB）。使用 DeepSpeed 和 bfloat16 混合精度。
- **训练时长**：论文未明确报告每个实验的具体训练时间，但提到训练 3 个 epoch，batch size 128。

### 5. 实验数量与充分性

论文进行了大量实验，覆盖多个维度和场景，充分性较高：
- **主实验**：Table 1 展示 3 个数据集 × 8 个指标 × 18 个方法（含 IIRG），共 432 个性能数据。
- **消融实验**：Table 2（协作/语义邻居的消融）；附录还包括随机邻居（Table 7）、启发式方法（Table 8）、排除一跳转移（Table 5）、正则化（Figure 14）、模型规模（Figure 15）等。
- **分析实验**：Figure 3/6/13/17 等给出记忆依赖、用户分组、冷启动、邻居信息量等分析。
- **泛化实验**：Figure 5（SID）、Figure 18（其他 SID）、Table 9（Yelp）。
- **公平性**：所有 LLM 基线使用相同骨干（Qwen-3.5-4B）并统一微调；对比方法按原文配置训练；结果若复现低于原文则引用原文数据。

结论：实验设计系统全面，对比充分，结果客观。

### 6. 论文的主要结论与发现

1. **LLM 在 GR 中存在更强的“一跳记忆”**：相比非 LLM 模型（TIGER），LLM 在 top-5 推荐中更频繁地推荐训练数据中直接的物品后继。
2. **性能提升主要来自“一跳记忆”覆盖的用户**：LLM 相对于非 LLM 基线的增益中，大量集中于那些目标物品在训练数据中至少在一个输入物品之后出现过的用户。
3. **IIRG 有效缓解对一跳记忆的依赖**：通过教导模型学习协作与语义关系，IIRG 显著降低了一跳记忆比例，同时提升了推荐性能。
4. **IIRG 对“非一跳记忆受益”用户提升巨大**：在 Sports 数据集上，IIRG 对这类用户的 Recall@5 相对提升达 50%，而对一跳受益用户仅 17%。
5. **IIRG 在多种标识符（TID/SID）上均有效**，且优于现有的所有辅助训练方法。
6. **冷启动推荐**：IIRG 对低频目标物品的性能提升更大。

### 7. 优点

- **发现问题新颖**：首次系统性地分析 LLM 在 GR 中的记忆行为并揭示其不利影响，为领域提供了重要洞察。
- **方法简单有效**：IIRG 仅需在标准下一物品预测基础上增加两个生成任务，不改变模型架构，即插即用。
- **实验严谨全面**：覆盖多种基线、标识符类型、数据集、消融分析，且验证了与模型规模、正则化等因素的独立性。
- **实用性高**：IIRG 适用于工业级 LLM 部署，支持两种主流标识符，且推理时几乎不产生无效输出（99% 以上可映射）。

### 8. 不足与局限

- **未揭示根本原因**：论文未解释为何 LLM 更易产生一跳记忆，仅排除了模型规模和过拟合作为主要原因。
- **模型类型有限**：所有实验基于 Qwen-3.5 系列（0.8B 到 9B），未验证在 LLaMA、Mistral 等其他主流 LLM 家族是否具有相同的记忆行为和 IIRG 的适用性。
- **可能的数据偏差**：仅使用 Amazon Reviews 和 Yelp 等英文公开数据集，未考虑跨域或多语言场景。
- **算力成本较高**：IIRG 需要全参数微调 4B/9B 模型，对资源要求较大；LoRA 在 GR 任务中失效（输出大量无效项），限制了低资源场景应用。

（完）
