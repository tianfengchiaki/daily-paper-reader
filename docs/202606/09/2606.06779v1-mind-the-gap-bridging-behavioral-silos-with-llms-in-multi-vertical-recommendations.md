---
title: "Mind the Gap: Bridging Behavioral Silos with LLMs in Multi-Vertical Recommendations"
title_zh: 注意鸿沟：利用大语言模型在多垂直领域推荐中弥合行为孤岛
authors: "Nimesh Sinha, Raghav Saboo, Martin Wang, Sudeep Das"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.06779v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 基于LLM的跨品类电子商务推荐
tldr: 针对多垂直电商平台新业务线的冷启动问题，提出利用LLM和分层RAG从用户餐厅历史中生成跨域特征，融入多任务学习排序模型，离线与在线实验证明该方法显著提升新兴领域的个性化推荐效果，有效填补行为数据鸿沟。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06779v1/fig-001.webp\", \"caption\": \"Table 4: LLM Evaluation of LLM-Generated Feature Personalization (GPT-4o). N=1000 samples per signal.\", \"page\": 4, \"index\": 1, \"width\": 1056, \"height\": 413}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-002.webp\", \"caption\": \"Table 2: Illustrative examples of LLM-generated category recommendations from consumer signals.\", \"page\": 4, \"index\": 2, \"width\": 1056, \"height\": 219}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-003.webp\", \"caption\": \"Figure 1: System flowchart illustrating the hierarchical RAG pipeline for generating user affinity features and their integration into the MTL ranking model.\", \"page\": 3, \"index\": 3, \"width\": 844, \"height\": 202}, {\"url\": \"assets/figures/arxiv/2606.06779v1/fig-004.webp\", \"caption\": \"Figure 4: Relative improvement (%) in online shadow traffic metrics for the Proposed Model versus the Baseline.\", \"page\": 5, \"index\": 4, \"width\": 506, \"height\": 273}]"
motivation: 解决多垂直平台中新兴垂直领域因行为数据稀疏导致的冷启动问题。
method: 利用LLM和分层RAG从数据丰富垂直域生成跨域特征，集成到多任务学习排序模型。
result: 离线和在线评估显示新兴垂直领域的个性化和用户参与度显著提升。
conclusion: 该方法有效弥合行为数据差距，提升多垂直领域的推荐质量。
---

## 摘要
在多垂直领域电商平台（如DoorDash）中，相对较新的产品垂直领域（如杂货和零售）为个性化创新提供了重要机遇。一个关键挑战在于解决用户的“冷启动”问题。本文引入了一种新颖的框架，通过从数据丰富的垂直领域（例如DoorDash上的餐厅）向数据稀疏的领域迁移知识来提升推荐质量。我们利用大语言模型（LLMs）进行生成式推理，合成能够封装潜在用户偏好的稀疏高维特征。具体而言，我们采用分层检索增强生成（RAG）流水线，从用户餐厅订单历史和搜索查询中推导出多级分类特征。这些生成的特征编码了长期的跨垂直偏好和短期意图，并被集成到一个生产环境中的多任务学习（MTL）排序模型中。通过广泛的离线和在线评估，我们证明该方法显著改进了新兴业务垂直领域的个性化和用户参与度，有效弥合了行为数据差距。

## Abstract
In multi-vertical e-commerce platforms like DoorDash, relatively newer product verticals such as grocery and retail present a significant opportunity for personalization innovation. A key challenge lies in solving the "cold start" problem for users. This paper introduces a novel framework for enhancing recommendation quality by transferring knowledge from data-rich verticals (e.g., restaurants at DoorDash) to data-sparse ones. We leverage Large Language Models (LLMs) to perform generative inference, synthesizing sparse, high-dimensional features that encapsulate latent user affinities. Specifically, we employ a hierarchical Retrieval-Augmented Generation (RAG) pipeline to derive multi-level taxonomic features from user restaurant order histories and search queries. These generated features, encoding both long-term cross-vertical preferences and short-term intent, are integrated into a production Multi-Task Learning (MTL) ranking model. We demonstrate through extensive offline and online evaluation that this approach significantly improves personalization and engagement in emerging business verticals, effectively bridging the behavioral data gap.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：多垂直领域电商平台（如DoorDash）在新兴垂直领域（如杂货、零售）面临用户行为数据稀疏导致的“冷启动”问题。而在已有成熟垂直领域（如餐厅）积累了丰富的用户行为数据，如何利用这些数据弥合行为孤岛，提升新兴领域的推荐个性化。
- **整体含义**：提出一种利用大语言模型（LLM）进行生成式推理，从用户餐厅订单历史和搜索查询中提取跨域分类偏好特征，并将其注入生产环境中的多任务学习（MTL）排序模型，从而有效传递知识，显著改善冷启动用户的推荐质量。

## 2. 论文提出的方法论

- **核心思想**：将用户在学习数据丰富的餐厅垂直领域的行为视为跨域偏好的潜在信号，使用LLM生成用户在不同产品分类层次上的显式偏好特征，弥补目标域的数据稀疏性。
- **关键技术细节**：
  - **分层检索增强生成（Hierarchical RAG）**：利用LLM将非结构化输入（餐厅订单、搜索查询）映射到内部四层产品分类树（L1~L4）。通过级联推理：先识别高层分类（L1/L2），再约束低层分类（L3）的生成，减少幻觉，提高精度。最终选择L2和L3特征（L1过粗，L4过稀疏）。
  - **特征类型**：长期跨垂直偏好（从历史餐厅订单生成）+ 短期意图（从近期搜索查询生成）。
  - **Prompt工程**：设置低温度（0.1）、输出置信度阈值（>0.8）、严格JSON格式、禁止捏造分类。通过改进prompt（如要求关注餐厅菜系、避免歧义）提升了特征相关性（例如从“三明治”修正为“特色面包（馕）”）。
  - **模型选择**：对比GPT-4o和GPT-4o-mini，后者以更低成本达到可比质量。采用prompt缓存（静态部分缓存）、增量更新（仅在新行为发生时重新计算），总成本降低约80%。
  - **特征集成**：每天批处理计算特征，存入数据湖用于训练，同时推送到在线特征存储用于实时推理。MTL排序模型将LLM生成的特征（u_LLM）与现有用户特征（u_eng）、物品特征（i_eng）拼接，通过共享Embedding表和平均池化处理变长分类特征，输入共享MLP和任务特定预测头。损失函数为加权多任务损失：L = Σ α_t L_t(ŷ_t, y_t)。

## 3. 实验设计

- **数据集/场景**：DoorDash平台内部生产数据（三个月的用户行为数据训练，15天holdout评估）。不涉及公开数据集。
- **Benchmark**：生产环境的MTL排序模型作为基线（仅使用历史用户行为和物品属性特征）。提出模型为相同架构加上LLM生成的特征。
- **对比方法**：只进行了基线 vs 提出的单变量对比，未与其他基于LLM的跨域推荐方法对比。
- **数据分割**：训练集为前三个月，测试集为后续15天。
- **评估维度**：总体用户、冷启动用户（非餐厅垂直领域新用户）、高活跃用户（该垂直领域高频用户）。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量、训练时长等算力信息。仅提到模型选择（GPT-4o-mini）和prompt缓存策略用于降低推理成本（约80%），但未给出具体硬件配置。这是一个信息缺失点。

## 5. 实验数量与充分性

- **实验组数**：
  - 离线评估：总体+两个用户群体（共3组），报告AUC-ROC和MRR两种指标。
  - 在线影子部署：1组（总体用户），报告AUC-ROC和MRR。
  - 定性评估：人类评估（N=1000样本）、LLM作为裁判（GPT-4o，N=1000样本），比较餐厅订单和搜索查询两种信号源的个性化程度。
  - 未进行消融实验（如只使用长期特征、只使用短期特征的影响）。
- **充分性评价**：实验设计覆盖了核心场景（冷启动、高活跃用户），验证了方法的有效性，但缺乏与其他跨域推荐方法的对比（如基于协同过滤的迁移、基于图网络的迁移），也缺少对特征组合的消融分析。总体而言，实验支持了核心结论，但对比基准单一，外部泛化性未充分验证。

## 6. 论文的主要结论与发现

- LLM生成的特征显著提升了MTL排序模型性能：
  - **总体用户**：离线AUC-ROC提升4.4%，MRR提升4.8%；在线影子部署AUC-ROC提升4.3%，MRR提升3.2%。
  - **冷启动用户**：从餐厅订单信号受益最大，AUC-ROC提升4.0%，MRR提升1.1%。
  - **高活跃用户**：从搜索查询信号受益最大，AUC-ROC提升5.2%，MRR提升2.2%。
- 搜索查询生成的特征个性化程度高于餐厅订单生成的特征（人类评估：70.7% vs 53.0%被评为“高度个性化”）。

## 7. 优点

- **创新性**：将LLM+分层RAG用于跨垂直领域特征生成，巧妙解决数据孤岛问题，而非直接使用LLM进行端到端推荐。
- **实用性**：面向工业级排序系统，考虑了成本优化（小模型、缓存、增量更新），证明了在真实生产环境中的有效性（通过影子评估）。
- **评估全面**：同时进行离线、在线、人工和LLM裁判评估，结果一致性强。
- **针对冷启动**：专门分析冷启动用户群体，展示了对业务关键痛点的解决能力。

## 8. 不足与局限

- **缺乏外部对比**：未与现有的跨域推荐方法（如基于矩阵分解的CDCF、图神经网络、基于文本嵌入的方法）进行定量比较，无法判断该方法的相对优势。
- **消融实验不足**：未评估仅使用长期特征或仅使用短期特征的效果，也未分析不同分类层级（L2 vs L3）的贡献。
- **特征质量局限**：特征生成依赖固定的产品分类树，灵活性有限；LLM可能产生偏差或遗漏小众品类。
- **计算资源未披露**：未报告训练成本（如GPU时长、内存），难以评估资源开销。
- **数据集单一**：仅在DoorDash平台验证，结论的跨平台通用性未知（不同平台分类体系、用户行为模式不同）。
- **时间动态性未深入**：文中提到未来会研究时间动态，但在当前实验中，特征为静态汇总周期（3个月），未捕捉用户偏好的实时演变。

（完）
