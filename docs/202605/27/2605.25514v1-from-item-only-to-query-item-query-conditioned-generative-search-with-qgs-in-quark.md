---
title: "From Item-Only to Query-Item: Query-Conditioned Generative Search with QGS in Quark"
title_zh: 从仅项目到查询-项目：Quark中基于查询条件的生成式搜索（QGS）
authors: "Yanglong Song, Zihao Yang, Shuo Meng, Rujun Guo, Jin Zhang, Bin Wang, Shaoyu Liu, Xiaozhao Wang, Guanjun Jiang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25514v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 查询条件生成式搜索用于推荐
tldr: "现有生成式搜索方法将查询和物品混为序列，忽略查询切换导致的意图偏移，造成监督噪声。本文提出查询条件生成搜索QGS，将交互编码为(查询,物品)对，用查询条件化下一物品预测；引入线性HSTU编码器降低复杂度至O(L)；设计HFG-Attention融合传统特征。在Quark搜索中上线，CTR提升0.62%，效果显著。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25514v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of QGS, a generative framework for search. Unlike prior generative recommendation/search methods that model item-only sequences, QGS autoregressively models query-conditioned next-item prediction, explicitly capturing the query-conditioned nature of search behavior.\", \"page\": 2, \"index\": 1, \"width\": 686, \"height\": 365}, {\"url\": \"assets/figures/arxiv/2605.25514v1/fig-002.webp\", \"caption\": \"Figure 4: Query-conditioned InfoNCE training with padding and collision masking. Part 1 illustrates the training flow: HSTU outputs are concatenated with the next-query semantic embedding, passed through a shared prediction head, ℓ2-normalized, and scored against in-batch item embeddings. Part 2 visualizes the two masking strategies—the padding mask excludes invalid (padded) tokens, the collision mask excludes false negatives caused by duplicate items across sequences, and their combination yields the final masked logits used for the contrastive objective.\", \"page\": 7, \"index\": 2, \"width\": 686, \"height\": 382}, {\"url\": \"assets/figures/arxiv/2605.25514v1/fig-003.webp\", \"caption\": \"Figure 2: Overall framework of QGS. (Top) Each search interaction is encoded as a query-item pair token via an Item LLM and feature projection, then sequentially modeled by the Linear HSTU encoder; the encoder output is concatenated with the next query and scored against in-batch negatives via an InfoNCE loss. (Bottom-left) Internal structure of a Linear HSTU block (Section 3.5), which reduces per-layer complexity from 𝑂 (𝐿2) to 𝑂 (𝐿). (Bottom-right) HFG-Attention block (Section 3.6), which fuses sparse cross features with dense sequential representations for the final multi-task prediction.\", \"page\": 4, \"index\": 3, \"width\": 848, \"height\": 529}, {\"url\": \"assets/figures/arxiv/2605.25514v1/fig-004.webp\", \"caption\": \"Figure 3: Training loss curves with and without queryconditioned next-item prediction. The item-only objective suffers from high and volatile loss due to noisy supervision caused by query switches, while the query-conditioned objective converges to a significantly lower loss, confirming that the query is a necessary condition for clean supervision in generative search.\", \"page\": 5, \"index\": 4, \"width\": 497, \"height\": 270}]"
motivation: 现有生成式推荐方法在搜索排序中因忽视查询边界，导致不同查询意图混合产生噪声监督，影响排序效果。
method: "QGS将交互编码为(查询,物品)对，采用查询条件化下一物品预测目标；并用线性HSTU编码器降低注意力复杂度，HFG-Attention融合传统特征。"
result: "在Quark搜索线上A/B测试中，CTR提升0.62%，点击搜索比提升0.38%，PV时长提升3.55%。"
conclusion: QGS通过查询条件化与线性复杂度编码器有效提升搜索排序性能，已在商业搜索引擎中部署验证。
---

## 摘要
生成式序列模型在推荐中表现出了强大的效果。将其应用于搜索排序更具挑战性。搜索行为本质上是查询驱动的。每次查询切换都会在用户交互历史中引入急剧的主题转变。现有生成方法将查询和项目扁平化为单个令牌序列，不区分查询边界，导致模型将不同的查询意图混合到一个预测目标中，产生噪声监督。我们提出了基于查询条件的生成式搜索（QGS）。QGS将每次交互编码为（查询，项目）成对令牌，并通过查询条件下的下一项目目标进行训练。预测目标从有噪声的边缘分布P(item_{t+1}|context_{<=t})变为清晰的条件分布P(item_{t+1}|context_{<=t}, query_{t+1})，直接消除了查询切换导致的语义不连续性。使用标准注意力机制对长交互历史进行编码具有二次成本，在严格的在线延迟预算下不可行。我们引入了线性HSTU编码器，用因果线性递归取代全注意力，每层复杂度从O(L^2)降至O(L)，且不损失排序质量。传统搜索排序依赖于手工特征，如文本匹配分数、统计信号和行为特征。我们提出了HFG-Attention以在生成框架中保留这些特征，将异构特征组织成语义组，并通过专用注意力块进行融合，从而桥接稀疏的工程化信号与密集的序列表示。QGS已部署在中国主要商业搜索引擎Quark Search的排序模块中。在线A/B测试显示，与生产环境的深度学习基线相比，CTR提升+0.62%，点击搜索比提升+0.38%，PV时长提升+3.55%，具有统计学显著性。

## Abstract
Generative sequence models have shown strong results in recommendation. Applying them to search ranking is more challenging. Search behavior is inherently query-driven. Each query switch introduces a sharp topic shift in the user's interaction history. Existing generative methods flatten queries and items into a single token sequence. They do not distinguish query boundaries. This causes the model to mix different query intents into one prediction target, resulting in noisy supervision.   We present Query-Conditioned Generative Search (QGS). QGS encodes each interaction as a (query, item) pair token. It trains with a query-conditioned next-item objective. The prediction target changes from a noisy marginal P(item_{t+1}|context_{<=t}) to a clean conditional P(item_{t+1}|context_{<=t}, query_{t+1}). This directly removes the semantic discontinuity caused by query switches.   Encoding long interaction histories with standard attention has quadratic cost. This is impractical under strict online latency budgets. We introduce a Linear HSTU encoder. It replaces full attention with causal linear recurrence. Per-layer complexity drops from O(L^2) to O(L) with no loss in ranking quality.   Traditional search ranking depends on hand-crafted features like text-matching scores, statistical signals, and behavioral features. We propose HFG-Attention to preserve them in the generative framework. It organizes heterogeneous features into semantic groups and fuses them through a dedicated attention block. This bridges sparse engineered signals with dense sequential representations.   QGS is deployed in the ranking module of Quark Search, a major commercial search engine in China. Online A/B tests show statistically significant gains: +0.62% CTR, +0.38% Click-Search Ratio, and +3.55% PV Duration over the production deep learning baseline.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

**背景**：  
搜索排序是信息检索系统的核心环节。工业界通常采用多级级联架构（召回→粗排→精排），精排阶段依赖深度学习模型（如 Wide&Deep、DeepFM、DIN）和大量人工特征（文本匹配、统计信号、行为特征）。近年来，生成式序列模型在推荐中取得了巨大成功，研究者试图将其迁移到搜索领域。

**核心问题**：  
现有生成式方法（如 UniSearch、OneSearch、HSTU、HLLM）将用户交互历史扁平化为单一物品序列，**不区分查询边界**，导致不同查询意图混杂在同一个预测目标中。搜索行为本质上是查询驱动的，每次查询切换都会引入急剧的主题跳跃（例如从“量子计算”跳到“古罗马历史”再到“热带冰沙”）。在物品序列中，这些跳跃被认为是无解释的语义不连续，使得下一物品预测的监督信号极为嘈杂。作者指出，搜索中的每个物品点击都由其对应的查询因果驱动，因此**查询不是可选的辅助信息，而是消除序列语义不连续的必要条件**。

## 2. 方法论

### 核心思想
将搜索排序重新定义为**查询条件下的生成式序列建模**问题。关键转变：从预测噪声边缘分布 \(P(item_{t+1} | context_{≤t})\) 变为预测清晰条件分布 \(P(item_{t+1} | context_{≤t}, query_{t+1})\)，直接建模查询驱动的行为结构。

### 关键技术细节

- **Query-Item Pair Token 构建**（Section 3.3）：  
  对每个历史交互（\(q_t, d_t\)），将查询文本和文档文本拼接后送入预训练BERT得到两个语义嵌入：CLS嵌入（项目级语义）和第一个SEP嵌入（查询级语义）。同时拼接数值/类别特征（位置、点击率、匹配分数等），形成统一的对令牌 \(x_t = [f_t; e^{cls}_t]\)，再通过线性投影加位置编码输入编码器。此举将查询信息嵌入到序列每个位置，使序列反映搜索的条件依赖结构。

- **查询条件下一个物品预测**（Section 3.4）：  
  训练时，将编码器最后位置输出 \(h_t^{(N)}\) 与下一查询的SEP嵌入 \(e^{sep}_{t+1}\) 拼接，经预测头得到 \(z_t\)。正样本为下一位置的对令牌 \(x_{t+1}\) 的投影，负样本来自批次内其他序列，使用InfoNCE对比损失优化。预测头权重与最终排序共享，避免训练测试不一致。同时使用填充掩码和碰撞掩码排除无效和重复负样本。论文通过图3对比表明，条件化目标损失更低、更稳定。

- **线性HSTU编码器**（Section 3.5）：  
  借鉴RWKV的线性递归范式，将HSTU的点式聚合注意力改为因果累积求和：  
  \(S_t = K_t ⊙ V_t\)，  
  \(C_t = γ C_{t-1} + S_t\)，  
  \(O_t = Q_t ⊙ C_t ⊙ U_t\)。  
  保留HSTU的softmax-free设计和U门控，每层复杂度从O(L²)降至O(L)。实验显示在序列长度1000时推理加速37%且排序质量无损。

- **HFG-Attention**（Section 3.6）：  
  为在生成框架中保留传统排序依赖的异构稀疏特征（如匹配分数、统计信号），将特征按语义分成G组，每组经嵌入和线性投影到统一维度，构成结构化令牌序列 \([ \tilde{r}_1 ; ... ; \tilde{r}_G ; \tilde{h}_{dnn} ]\)，送入单层HSTU注意力（保留FFN）进行跨组交互，再通过掩码平均池化融合。此举解决了稀疏特征与密集序列表示之间的语义鸿沟。

## 3. 实验设计

### 数据集与场景
- **来源**：Quark Search（中国主要商业搜索引擎）的生产日志
- **规模**：训练集 37.87M 用户，154.77M 搜索，488.58M 曝光，82.58M 点击；测试集 2.10M 用户，8.60M 搜索，27.14M 曝光，4.59M 点击（共18天日志）
- **为什么不用公开数据集**：需要长用户行为序列、丰富的上下文特征和统计信号，公开数据集缺乏这些；且核心结论依赖在线A/B测试验证。

### Benchmark与对比方法
- **生产基线（Base）**：当前部署的判别式深度排序模型（BERT&DNN架构 + 人工特征工程）
- **HSTU [26]**：标准点式聚合注意力，12层，8头，隐藏768维
- **HLLM [2]**：层次化LLM，Item LLM和User LLM初始化自预训练BERT，12层Transformer，12头，隐藏768维
- **未对比的方法**：OneSearch、UniSearch、GenSAR等因其任务设定（检索导向或统一搜索推荐）不适合工业排序场景

### 评估指标
- **离线**：AUC、GAUC（按搜索请求平均AUC）
- **在线**：CTR、Click-Search Ratio（有点击的搜索占比）、PV Duration（页面平均停留时长）

## 4. 资源与算力

文中明确说明：所有模型使用 **8张 NVIDIA A800 GPU**，Adagrad优化器，学习率0.01，batch size 100。训练步数未明确给出（从图3损失曲线可见约500k步收敛）。在线部署细节未详述，但提到A/B实验在2%流量上运行7天。

## 5. 实验数量与充分性

论文共进行四组实验（对应四个RQ）：

1. **总体性能对比**（表2）：QGS vs Base、HSTU、HLLM，单组对比。
2. **消融实验**（表3）：分别移除HFG-Attention、查询条件预测、对令牌中的上下文特征、将线性HSTU替换为原始HSTU或预训练LLM，共5个变体，覆盖所有核心组件。
3. **可扩展性**（表4）：变化编码器深度（1/4/12层）、隐藏维度（128/512）、序列长度（100/1000），共5种配置。
4. **在线A/B实验**（表5）：生产环境7天，三组指标。

**充分性评估**：  
- 消融实验单独测试了每个关键设计的影响，设计合理。  
- 可扩展性实验覆盖了三个主要维度，结论（加深、加宽、加长均有效）清晰。  
- 在线实验持续时间7天且流量足（百万级曝光/天），统计显著性（p<0.05）有保证。  
**不足**：  
- 仅在一个商业场景（Quark搜索）上验证，未在公开数据集或其它搜索引擎上测试，泛化性存疑。  
- 基线对比未包含最新的端到端生成检索方法（如TSG、QueStER等），因其任务不同，但可以讨论。  
- 消融实验未测试多个组件同时移除的联合效果，无法评估交互作用。

## 6. 主要结论与发现

1. QGS离线GAUC较生产基线提升+4.33点（0.7140→0.7573），AUC提升+5.01点，显著优于所有基线。
2. **查询条件下一个物品预测是最关键的模块**（移除后GAUC下降1.91），验证了搜索排序需要显式条件化查询。
3. **线性HSTU在无损排序质量下降低37%推理延迟**，实现了效率与效果的理想平衡。
4. 在线A/B测试：CTR +0.62%，Click-Search Ratio +0.38%，PV Duration +3.55%，所有提升均统计显著。
5. 更长序列（1000 vs 100）带来持续增益，深度和宽度也有正向贡献。

## 7. 优点（亮点）

- **问题洞察深刻**：明确指出现有生成式搜索方法忽略查询意图切换导致的监督噪声，并提出根因——缺乏查询条件依赖。
- **方法论完整**：从输入表示（成对令牌）、训练目标（条件化InfoNCE）、高效编码器（线性HSTU）到特征融合（HFG-Attention），形成闭环，且每个组件均有消融验证。
- **工程实用性强**：线性HSTU的设计既保留HSTU的精华（softmax-free、U门控），又满足工业级低延迟要求；HFG-Attention优雅地保留了传统核心特征，避免生成范式抛弃现有特征工程积累。
- **工业验证充分**：来自大厂真实场景，线上实验规模大、指标多元，结论可信度高。

## 8. 不足与局限

- **泛化性未验证**：仅在一个特定搜索场景（Quark）测试，不同领域（电商、视频）或语言（英文）的效果未知。
- **缺少与最新生成式搜索方法的公平对比**：未纳入UniSearch、OneSearch、GenSAR等同一任务范式的方法，虽然文中解释了原因（任务设定不同），但仍建议在适配条件下进行对比。
- **序列长度上限有限**：最大1000，对于超长用户生命周期（数月甚至数年）的效果未探索。
- **仅考虑点击信号**：未建模更细粒度的行为（如滑动、转化、停留时间分档），损失部分监督信息。
- **冷启动与长尾查询**：未讨论查询稀疏性或新物品/新用户场景下的表现，这些是工业落地关键问题。
- **消融实验粒度较粗**：例如移除上下文特征仅涉及一组变体，并未分离出哪类特征（位置 vs 时间 vs 统计信号）贡献最大。

（完）
