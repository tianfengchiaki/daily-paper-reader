---
title: Fine-Tuned LLM as a Complementary Predictor Improving Ads System
title_zh: 微调大语言模型作为互补预测器提升广告系统
authors: "Hui Yang, Daiwei He, Kevin Jiang, Taejin Park, Kungang Li, Jiajun Luo, Yuying Chen, Xinyi Zhang, Sihan Wang, Haoyu He, Yu Liu, Lakshmi Manoharan, David Xue, Shubham Barhate, Runze Su, Duna Zhan, Ling Leng, Siping Ji, Jinfeng Zhuang, Alice Wu, Leo Lu, Han Sun, Zhifang Liu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27856v1"
tags: ["query:q3"]
score: 9.0
evidence: 微调开源大语言模型作为广告特定预测器
tldr: 本文提出一种互补范式：微调开源LLM作为广告特定辅助预测器，从用户信息预测广告商，增强候选生成并为排序提供先验。在大规模生产广告系统中验证，取得离线改进和在线业务提升，证明LLM可有效利用世界知识，通过辅助预测实现检索和排序的端到端收益。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27856v1/fig-001.webp\", \"caption\": \"Figure 2: Serving architecture for large-scale inference. The deployment uses a distributed vLLM + Ray stackwith batching, prefix caching, fault tolerance, and incremental updates for efficient daily inference.\", \"page\": 5, \"index\": 1, \"width\": 482, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.27856v1/fig-002.webp\", \"caption\": \"Table 4: Feature ablation on the zero-shot 4B setup. Negative values indicate performance drops relative to the baseline.\", \"page\": 6, \"index\": 2, \"width\": 452, \"height\": 302}]"
motivation: 将LLM最新进展转化为广告推荐系统的实际收益仍很罕见，现有方法多限于生成检索、重排或信号增强，缺乏互补性利用。
method: 微调开源LLM作为辅助预测器，从用户画像和历史预测广告商，其输出增强候选生成并为下游排序提供先验信息。
result: 在大规模生产广告系统中实现显著离线改进和可衡量在线业务影响，验证了LLM的预测能力。
conclusion: 有针对性的辅助预测可解锁检索与排序的端到端增益，为大规模LLM增强推荐提供实用路径。
---

## 摘要
推荐系统为信息流、广告和短视频平台的用户参与和变现提供动力，但将大语言模型的最新进展转化为推荐系统的增益仍然罕见，特别是在广告和生产规模的现实工业场景中。此前大语言模型在现实世界中的成功通常分为三类：(a) 直接预测下一个项目以进行候选生成的生成式检索，(b) 使用大语言模型的后期重排序，以及(c) 利用大语言模型进行辅助信号增强。我们提出了一种针对广告的互补范式：微调的开源大语言模型并非用作排序器，而是作为广告特定的辅助预测器，根据用户画像和历史记录预测潜在广告主。这种大语言模型驱动的广告主预测增强了传统的候选生成，并为下游排序提供了信息丰富的先验信息。我们在一个大规模生产广告系统中开发了该方法，取得了显著的离线改进和可衡量的在线业务影响，证明大语言模型的世界知识和预测能力可以被高效利用。除了验证大语言模型在广告应用中的有效性，我们的结果表明，有针对性的辅助预测可以解锁检索和后期排序的全链路增益，为大规模增强大语言模型的推荐提供了一条实用路径。

## Abstract
Recommendation systems power engagement and monetization across feeds, ads, and short-video platforms, but translating the latest advances in Large Language Models into Recommendation Systems (RecSys) gains remains rare, particularly in advertising and production-scale real-world industry setups. Prior real-world LLM successes typically fall into three buckets: (a) generative retrieval that directly predicts the next items for candidate generation, (b) late-stage re-ranking that uses LLMs, and (c) auxiliary signal enrichment with LLMs. We introduce a complementary paradigm for ads: a fine-tuned open-source LLM used not as a ranker, but as an ads-specific ancillary predictor, forecasting likely advertisers from user profiles and histories. This LLM-driven advertiser prediction augments conventional candidate generation and provides informative priors to downstream ranking. Developed in a large-scale production advertising system, our approach produces substantial offline improvements and measurable online business impact, demonstrating that LLM world knowledge and predictive capacity can be efficiently harnessed. Beyond validating LLMs for ads applications, our results show that targeted ancillary predictions can unlock end-to-end gains across both retrieval and late-stage ranking, offering a practical path to LLM-enhanced recommendation at scale.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：尽管大型语言模型（LLM）在自然语言处理领域取得了巨大成功，但在工业级推荐系统（RecSys）尤其是广告系统中的实际落地仍然罕见。传统的工业推荐器严重依赖稀疏的 ID 特征（用户 ID、广告商 ID 等），难以与 LLM 的基于 token 的语义表示自然对齐；同时，LLM 的大参数量和高解码延迟不符合生产系统的实时性、内存和成本约束。现有将 LLM 用于推荐的成功案例主要分为三类：生成式检索（直接用 LLM 预测下一个物品）、后期重排序（LLM 作为昂贵的重排器）和辅助信号增强（LLM 推导标签或兴趣词）。但这些方法在广告推荐领域的效果仍有限，因为广告涉及广告商实体、定向约束和转化历史，引入额外的稀疏性和与纯文本语义的不匹配。
- **核心问题**：如何在不替换现有推荐栈、不牺牲延迟和成本的前提下，有效利用 LLM 的世界知识和预测能力来提升广告系统的整体效果（检索召回率和排名精度）。
- **本文贡献**：提出一种互补范式——微调开源 LLM 作为广告特定的**辅助预测器**，而非直接作为排序器。该 LLM 根据用户画像和历史行为预测最可能发生转化的广告商列表，并将这些预测注入早期检索和后期排序阶段，从而在检索覆盖和排序精度上同时获得端到端增益。

#### 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将 LLM 定位为传统推荐系统的“互补模块”，不替代现有检索和排序器，而是生成广告商先验信息，增强候选生成并为下游模型提供特征。
- **系统架构**（如图1所示）包含四个阶段：  
  1) **用户选择**：限定每日推理用户为过去90天内有活跃（on-Pinterest 和 off-Pinterest）转化的高价值活跃用户，平衡覆盖率和成本。  
  2) **特征编译**：将用户画像（年龄、性别、用户状态）和行为序列（站内搜索、归因转化、匹配转化、站外搜索/URL、类别、品牌等）转化为紧凑的文本表示。  
  3) **提示设计与后训练**：  
     - **SFT（监督微调）阶段**：使用简化提示，要求 LLM 预测一个“下一个广告商”，训练标签为 7 天窗口内的首个转化广告商。  
     - **GRPO（基于群组相对策略优化的强化学习）阶段**：提示模型输出 20 个排序后的广告商和最多 5 个用户兴趣（结构化 XML 格式）。虽然真实监督仍是单一广告商，但更长的输出列表提供更大的奖励方差。  
     - 奖励函数定义：  
       \(R_{\text{total}} = R_{\text{match}} - P_{\text{adv\_len}} - P_{\text{interest\_len}}\)  
       其中 \(R_{\text{match}}(i) = 0.1 \times (20 - i) + \text{bonus}(i)\)，bonus(i) 在 i≤4 时为 2.0，否则为 0；长度惩罚 \(P_{\text{len}}\) 在输出数量与要求不符时施加惩罚。  
     - **Semantic ID（SID）增强**：通过多阶段继续预训练（CPT）将多模态语义 ID（基于 PinCLIP 和 RQ-VAE 构建）融入 LLM。第一阶段仅更新 SID token 的嵌入，第二阶段全面微调以融合推荐知识，第三阶段任务特定微调（SFT + GRPO）。  
  4) **下游集成**：  
     - **检索**：开发一个新的候选生成器（CG），将 LLM 预测的广告商作为定向过滤器，由双塔模型检索该广告商下的 pin。该 CG 作为补充通道，尤其对信号覆盖弱的用户有效。  
     - **排序**：将 LLM 预测的广告商和用户兴趣作为特征输入下游转化模型（ctcvr, vtcvr）。  
- **服务化**：使用分布式 vLLM + Ray 进行大规模批量推理，支持前缀缓存、分页注意力、连续批处理，并通过增量更新减少每日推理量。

#### 3. 实验设计：数据集/场景、benchmark、对比方法
- **数据集**：  
  - **V0**：实验性数据集，仅包含序列特征足够长（匹配转化和归因转化序列长度均 ≥10）的用户，标签为 7 天窗口内的首个转化广告商。  
  - **V1**：与实际在线服务流量对齐的数据集，特征表达较弱但更真实，标签为高优先级转化。
- **基准方法**：零样本提示（zero-shot prompting）同一开源基座模型。
- **对比方法**：  
  - 零样本提示（无微调）  
  - SFT + 束搜索（beam search）  
  - SFT + 采样（sampling）  
  - GRPO（含推理/不含推理）  
  - SID 增强版 SFT + GRPO（仅离线评估）
- **评估指标**：  
  - 广告商预测：Recall@1, Recall@5, Recall@20  
  - 下游转化模型：损失（Loss）、AUC、PR-AUC  
  - 线上：美国购物广告片段的广告支出回报率（RoAS）

#### 4. 资源与算力
- **文中未明确说明**所使用的 GPU 型号、数量或训练时长。仅提及使用了分布式 vLLM + Ray 堆栈进行大规模批量推理，设计了前缀缓存、KV 缓存管理和连续批处理以优化效率。算力资源属于工业级部署细节，论文未公开。

#### 5. 实验数量与充分性
- **实验数量**：论文进行了多组实验，包括：
  - 2 个数据集（V0 和 V1）上的广告商预测对比（表2、表3）
  - 8 项特征消融研究（表4，涉及移除各特征组、序列重排序等）
  - 下游转化模型（ctcvr 和 vtcvr）的离线评估（表5）
  - 在线 A/B 实验（表6）
  - SID 增强的离线对比（仅表3中一行）
- **充分性评估**：实验覆盖了从零样本到 SFT + GRPO，再到 SID 增强的完整路径，消融实验系统性地量化了各特征贡献，在线实验报告了 p 值（p=0.021 和 p=0.029）表明显著性。但**不足**：SID 增强仅离线评估，未在线验证；线上实验仅限于美国购物广告片段、面向选择加入用户；数据版本 V0 和 V1 差异说明不够详尽。总体而言，实验设计较充分、客观、公平，但推广性受限于单一平台（Pinterest）。

#### 6. 论文的主要结论与发现
1. **后训练至关重要**：所有微调变体均显著优于零样本提示，SFT 主要提升精确召回（Recall@1），GRPO 主要提升广度召回（Recall@20）。  
2. **显式推理无益**：对于广告商预测任务（更接近偏好聚合而非长链推理），显式推理提示不仅无益甚至可能略差。  
3. **SID 增强大幅提升**：在 V1 上，SID 增强版 Recall@1/5/20 分别达到 0.248/0.515/0.755，相比无 SID 的 GRPO 相对提升约 11%～21%。  
4. **特征重要性排序**：活跃的历史转化广告商是最重要的单一特征；站外 URL 和站外搜索是次重要信号；用户画像（年龄/性别）影响微弱；类别特征可能引入噪声。  
5. **下游模型受益**：LLM 预测的广告商特征提升 ctcvr 和 vtcvr 的 PR-AUC（分别 +0.71% 和 +1.64%）。  
6. **线上业务指标提升**：LLM 候选生成器使美国购物广告的 RoAS 提升 4.94%（整体）和 6.69%（处理组），且统计显著。

#### 7. 优点
- **方法创新**：提出“互补预测器”的新范式，不替换现有系统，而是以低成本注入 LLM 预测能力，易于集成于工业流水线。
- **工业适配性**：考虑了延迟、成本、稀疏 ID 特征等实际约束，通过用户选择、增量更新、高效服务堆栈（vLLM+Ray）实现生产化。
- **多目标优化**：同时改进检索（增加高意图广告商覆盖）和排序（提供 rich features），达到端到端增益。
- **消融实验充分**：系统分析了每个特征组、后训练策略、解码方式的贡献，结论可信度较高。
- **在线验证**：报告了统计显著的在线业务指标，证明了实际价值。

#### 8. 不足与局限
- **平台与场景局限**：实验仅在 Pinterest 平台的美国购物广告片段上进行，结论是否适用于其他推荐场景（如视频、新闻、通用检索）或其他国家的广告主尚未验证。
- **在线验证不完整**：SID 增强仅在离线评估，未进行线上 A/B 测试，其在线效果未知。
- **覆盖用户有限**：每日推理仅针对活跃且近 90 天有转化的用户，对于新用户或低频用户无效，可能存在选择偏差。
- **奖励函数设计简化**：GRPO 奖励仅基于单一真值广告商的匹配排名，未考虑多标签转化或多广告商同时转化的情况。
- **特征工程未深入**：消融发现“top categories”特征反而有害，但解释仅说“噪声”，未进一步分析原因或改进。
- **资源消耗未公开**：缺乏对训练时长、GPU 消耗、推理延迟的具体量化，不利于他人复现或预估成本。
- **候选生成器配额敏感**：文中提到 L0 配额需手动微调，过大将导致广告多样性问题，说明该方法对系统超参数敏感。

（完）
