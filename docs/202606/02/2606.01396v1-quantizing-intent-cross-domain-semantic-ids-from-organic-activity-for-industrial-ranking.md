---
title: "Quantizing Intent: Cross-Domain Semantic IDs from Organic Activity for Industrial Ranking"
title_zh: 量化意图：来自有机活动的跨领域语义ID用于工业排序
authors: "Julie Choi, Haoran Ye, Zhiwei Ding, Bo Long, Benjamin Zelditch, Arpita Vats"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01396v1"
tags: ["query:q1"]
score: 9.0
evidence: 利用跨域用户语义ID提升广告点击率预测
tldr: "针对广告点击率预测中用户监督稀疏的问题，本文提出从有机信息流活动提取跨域用户语义ID（SID），并发现行为活动丰富度决定迁移质量。进一步提出残差有限标量量化（RQ-FSQ）方法，以极小存储代价匹配稠密嵌入性能，并设计层次离散嵌入模块（HDE）端到端学习。在大规模工业系统中，该方法在冷启动用户上取得高达+1.522%的AUC提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01396v1/fig-001.webp\", \"caption\": \"Figure 2: Integration of the HDE Module into the Ads Ranking Model. Left: The viewer SID (𝑐1, . . . , 𝑐𝐾 ) is encoded once per request by the HDE Module (Section 4.4), producing euser — a dense vector constant across all 𝐿 positions in the sequence. Right: Per-event features at position 𝑡 are encoded through standard embedding tables. Fusion (Eq. 10): At every position 𝑡 , the user embedding is summed with the event-level embeddings before a shared LayerNorm, yielding input tokens {h𝑡 }. The resulting sequence is processed by the Transformer stack and scored by the CTR head. All HDE embedding tables are trained end-to-end under the CTR objective.\", \"page\": 5, \"index\": 1, \"width\": 452, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-002.webp\", \"caption\": \"Table 2: RQ-FSQ on two heterogeneous pre-trained embedding sources. 𝚫AUC is measured against the no-SID baseline — the production ranking model with no member SID input. Storage is the per-user serialization footprint relative to the raw float32 embedding of the same source. RQ-FSQ matches the dense embedding’s AUC on both sources at 30–280× smaller storage.\", \"page\": 7, \"index\": 2, \"width\": 682, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-003.webp\", \"caption\": \"Table 1: Cross-domain user SID results (RQ-KMeans quantization, 𝐾=3 codes per source). ΔAUC relative to no-SID baseline. Upper block: single-source SIDs ordered by behavioral activity richness. Lower block: multi-source combinations. The RQ-FSQ comparison against the same sources appears in Table 2.\", \"page\": 6, \"index\": 3, \"width\": 740, \"height\": 267}]"
motivation: 广告CTR预测受限于用户稀疏监督，而有机活动蕴含丰富行为信号，但跨域迁移面临领域不匹配与存储成本问题。
method: 提出基于有机活动行为嵌入的语义ID（SID），并采用RQ-FSQ量化离散化嵌入，结合层次离散嵌入模块（HDE）进行端到端CTR训练。
result: "RQ-FSQ在Feed Activity上实现+0.351% AUC且存储缩小30倍，在Activity-Tuned LLaMA上实现+0.265% AUC且存储缩小280倍，冷启动用户增益高达+1.522%。"
conclusion: 跨域行为语义ID有效桥接有机活动与广告排序，尤其提升稀疏历史用户的排序性能。
---

## 摘要
广告点击率（CTR）预测受限于稀疏的用户监督：大多数用户不频繁与广告互动，但在有机表面（如信息流）中产生密集的行为证据。由于领域不匹配、服务成本和生产的复杂性，将这些跨领域信号转移到广告排序中十分困难。我们引入了从有机信息流活动派生的跨领域用户语义ID（SID），并表明行为活动丰富性决定了跨领域迁移的质量：来自用户画像文本的SID带来+0.036%的AUC提升，来自基于LLaMA的活动调优用户嵌入模型的SID带来+0.107%的提升，而来自直接信息流活动行为嵌入的SID带来+0.213%的提升。我们进一步提出RQ-FSQ，一种残差有限标量量化方法，将预训练嵌入离散化，同时以更小的存储空间匹配密集嵌入的AUC。在两个异构源上，RQ-FSQ匹配或略微超过密集源嵌入，在信息流活动上实现+0.351%的AUC提升（存储减少约30倍），在活动调优LLaMA上实现+0.265%的AUC提升（存储减少约280倍）。我们还引入了一个层次化离散嵌入模块，通过前缀n-gram稀疏嵌入表编码多级SID，并在CTR目标下进行端到端训练。在一个大规模工业广告排序系统中，冷启动片段分析显示，对于广告交互历史接近零的用户，增益高达+1.522%，验证了跨领域行为迁移作为稀疏历史排序的有效桥梁。

## Abstract
Ads click-through rate (CTR) prediction is constrained by sparse user supervision: most users engage with ads infrequently while generating dense behavioral evidence in organic surfaces such as feed. Transferring these cross-domain signals into ads ranking is difficult due to domain mismatch, serving cost, and production complexity. We introduce cross-domain user Semantic IDs (SIDs) derived from organic feed activity and show that behavioral activity richness governs cross-domain transfer quality: SIDs from user profile text yield +0.036% AUC, SIDs from an activity-tuned LLaMA-based user embedding model yield +0.107%, and SIDs from direct feed activity behavioral embeddings yield +0.213%. We further propose RQ-FSQ, a residual finite scalar quantization method that discretizes pre-trained embeddings while matching dense-embedding AUC at substantially smaller storage. Across two heterogeneous sources, RQ-FSQ matches or slightly exceeds dense source embeddings, achieving +0.351% AUC for Feed Activity at about 30x smaller storage and +0.265% AUC for Activity-Tuned LLaMA at about 280x smaller storage. We also introduce a Hierarchical Discrete Embedding module that encodes multi-level SIDs through prefix n-gram sparse embedding tables trained end-to-end under the CTR objective. In a large-scale industrial ads ranking system, cold-start segment analysis shows gains up to +1.522% for users with near-zero ad interaction history, validating cross-domain behavioral transfer as an effective bridge for sparse-history ranking.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：广告点击率（CTR）预测面临严重的用户监督稀疏问题——大多数用户与广告的交互频率低，而在有机表面（如信息流）上则产生大量行为证据。如何将这些跨领域（organic → ads）的行为信号有效迁移到广告排序模型中，同时克服领域不匹配、服务成本和集成复杂性，是工业界的关键挑战。
- **研究动机**：传统方法依赖同一领域内的用户行为，而跨域迁移的研究要么需要重叠用户/物品集，要么需要显式的域适应损失。本文尝试利用语义ID（SID）将有机行为离散化为紧凑的标记序列，作为广告CTR模型的输入特征，实现端到端微调，从而隐式完成域适应。
- **整体含义**：论文提出了一套完整的跨域用户语义ID框架，包括构建方法、量化技术、嵌入模块和多源融合策略，并验证了“行为活动丰富度”是跨域迁移质量的决定性因素。这项工作为工业广告排序中利用有机活动信号提供了一种实用且高效的解决方案。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想
- 将用户在有机信息流活动中的行为信号离散化为**语义ID（SID）**，作为请求级别的用户特征，通过端到端CTR训练实现跨域迁移。
- 提出**行为活动丰富性原则**：SID源嵌入中包含的行为活动越多，下游广告排序提升越大。
- 开发**RQ-FSQ**量化方法，以极小的存储代价匹配稠密嵌入的AUC。
- 设计**层次离散嵌入（HDE）模块**，高效编码多级SID，并支持多源SID的无缝融合。

### 2.2 关键技术细节

#### 2.2.1 SID构建方法
- 对用户密集嵌入 **v ∈ ℝᵈ** 进行残差量化，生成 **K** 级离散标记：  
  `SID(v) = (c₁, c₂, …, cₖ), cₖ ∈ {1,…,C}`  
  其中 **C** 为码本大小，**K** 为量化层级数。
- **RQ-KMeans**：确定性残差K均值，逐级分配最近邻中心，无重建目标，适用于从头训练的嵌入。
- **RQ-FSQ（残差+有限标量量化）**：结合RQ-VAE（残差向量量化，保存全局几何结构）和FSQ（有限标量量化，逐维量化，保存精细结构），融合后在下游模型中等价于 `e_RQ-FSQ = e_RQ + f(FSQ(v))`。适用于预先训练好的嵌入。

#### 2.2.2 多源SID
- 三个异构源：**Profile Qwen**（用户画像文本嵌入，+0.036% AUC）、**Activity-Tuned LLaMA**（基于LLaMA-3.1对比微调的活动嵌入，+0.107%）、**Feed Activity**（直接聚合一年信息流行为信号，+0.213%）。
- 结构化9码组合：c1-c3（LLaMA），c4-c6（Qwen），c7-c9（Feed Activity），每个源独立HDE模块，最后求和。
- 缺失插补：当Qwen或Feed Activity缺失时，使用Activity-Tuned LLaMA作为骨干，通过线性投影+残差VQ-VAE重建缺失嵌入，保证覆盖率。

#### 2.2.3 层次离散嵌入（HDE）模块
- 对于 **K** 级SID `s=(c₁,…,cₖ)`，每个前缀n-gram（unigram、bigram、trigram…）哈希到独立可训练稀疏表 **Wₖ**，限定最大表大小 **H_max**。
- 各级嵌入求和得到最终用户嵌入：`e_user = Σₖ eₖ`。
- 所有表在CTR目标下端到端训练，HDE表使用较高学习率（0.02 vs Transformer 4e-4）。

#### 2.2.4 集成到排序模型
- 用户SID为请求级别，广播到序列所有位置，与事件特征嵌入求和后经LayerNorm输入Transformer堆栈。无架构改变。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：大规模工业广告点击率预测平台的生产日志，60天训练，后续1天评估。绝对AUC值保密，报告相对提升。
- **基准**：无SID输入的工业级解码器Transformer排序模型（8层FlashAttention，Session Masking等）。
- **对比方法**：
  - 单源SID：Profile Qwen、Activity-Tuned LLaMA、Feed Activity（RQ-KMeans量化）。
  - 多种量化方法：原始稠密嵌入、RQ-KMeans、FSQ、RQ-FSQ（在两个异构源上）。
  - 多源组合：独立求和 vs 结构化多源SID。
  - 冷启动分段：按用户历史广告展示次数分为最冷启动（底部8%）、不频繁（中间64%）、频繁（顶部28%）。
- **公共验证**：MovieLens-100K，使用all-MiniLM-L6-v2将电影文本编码，用户嵌入取喜爱电影均值；训练MLP预测评分≥4，对比稠密嵌入、RQ-KMeans、RQ-FSQ。

## 4. 资源与算力

- 文中提到训练使用 **DDP/FSDP2 on H200 clusters**，但 **未明确说明具体的GPU数量、型号、训练时长等细节**。仅提及这是大规模工业系统，对学界复现参考有限。

## 5. 实验数量与充分性

- **实验组数**：
  - 单源SID对比（4种条件：无SID、文本、LLaMA、Feed Activity）。
  - 量化方法对比（4种方法×2个源，共8组）。
  - 多源组合对比（独立 vs 结构化，共2组）。
  - 冷启动分段（3个分段×1个源，共3组）。
  - 公共数据集验证（4种条件）。
  - 总计约20组实验，覆盖主要研究问题（RQ1-RQ5）。
- **充分性与公平性**：
  - 所有实验保持随机种子、批量大小、优化器、模型深度一致，仅变化SID输入。
  - 超参数（K=3, C=1024, D=64）基于内部扫描，但未公开扫描过程，存在一定调优偏差风险。
  - 冷启动分析直观且有说服力，公共数据集验证提升了泛化信心。
  - 缺少消融研究（如不同K、不同C的影响）和在线A/B测试报告，部分实验充分性可进一步加强。

## 6. 论文的主要结论与发现

- **行为活动丰富性决定跨域迁移质量**：AUC增益从画像文本（+0.036%）到活动调优LLaMA（+0.107%）到直接行为聚合（+0.213%）单调递增。
- **RQ-FSQ匹配或超过稠密嵌入AUC**：在Feed Activity上+0.351% AUC（30×存储缩小），在Activity-Tuned LLaMA上+0.265% AUC（280×存储缩小）。
- **结构化多源SID优于独立组合**：+0.296% vs +0.260%，提升来自避免哈希碰撞和骨干插补。
- **冷启动用户增益最大**：最冷启动用户+1.522%，不频繁用户+0.874%，频繁用户+0.131%。跨域SID有效弥补广告历史不足。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：RQ-FSQ巧妙结合RQ-VAE（全局）和FSQ（局部），兼顾几何结构和逐维细节，为预训练嵌入离散化提供通用方案。
- **验证链严密**：从三个信息源单调递增验证行为丰富性原则，实验设计层层递进，逻辑清晰。
- **工业规模验证**：在大规模生产系统上部署，冷启动分段分析直接证明机制有效性，结果可信度高。
- **可复现性考虑**：公开数据集（MovieLens-100K）验证了量化方法排序的一致性，增加泛化证据。
- **HDE模块设计务实**：前缀n-gram哈希+上限内存控制，平衡表达能力和部署成本，多源独立应用无需架构改动。

## 8. 不足与局限

- **实验覆盖有限**：所有实验来自同一工业平台，缺乏跨多个平台的验证，可能受平台特性影响。
- **缺少超参数敏感性分析**：未展示K、C、H_max等参数变化对结果的影响，难以判断鲁棒性。
- **缺失在线A/B测试**：仅报告离线AUC提升，未提供实际线上点击率、收入等指标验证，工业部署后的实际效果有待确认。
- **插补方法简单**：缺失源插注仅用线性投影+残差VAE，可能不是最优；未与其他插补策略（如直接回退到默认码）对比。
- **隐私讨论不足**：虽然作者认为SID相比稠密向量更隐私友好，但离散码仍可能被反推行为模式，需更深入讨论治理措施。
- **应用限制**：依赖预训练嵌入（LLaMA等），引入额外计算成本；对资源有限的团队部署门槛较高。

（完）
