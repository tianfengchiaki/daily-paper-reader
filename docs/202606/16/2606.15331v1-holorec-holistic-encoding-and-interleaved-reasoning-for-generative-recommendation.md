---
title: "HoloRec: Holistic Encoding and Interleaved Reasoning for Generative Recommendation"
title_zh: HoloRec：面向生成式推荐的全息编码与交错推理
authors: "Shuqi Zhao, Jingsong Su, Xiang Liu, Xingzhi Yao, Yiming Qiu, Huimu Wang, Liang Lin, Pengbo Mo, Mingming Li, Jiao Dai, Jizhong Han, Songlin Hu"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15331v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 使用LLM的生成式推荐，层次化编码
tldr: 针对生成式推荐中语义表示扁平、缺乏层次结构以及依赖外部链式推理的问题，提出HoloRec，通过多粒度嵌套残差量化和整体重构损失构建层次语义编码矩阵，实现了表示、推理和生成的统一。支持非思考模式（快速预测）和思考模式（内生链式推理），在多个数据集上超越基线，尤其在稀疏场景下表现突出。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15331v1/fig-001.webp\", \"caption\": \"Figure 3: Structure of the interleaved reasoning endogenous chain-of-thought model. At each decoding step, the fine-grained hidden state is first used to predict a coarse-grained semantic embedding, which is then fused with the decoder contextual state via a gating injection module. The resulting enhanced fine-grained representation is passed to the fine-grained prediction head to output the final recommendation.\", \"page\": 6, \"index\": 1, \"width\": 844, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-002.webp\", \"caption\": \"Table 4: Comparison of Time and Space Complexity\", \"page\": 11, \"index\": 2, \"width\": 732, \"height\": 411}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-003.webp\", \"caption\": \"Figure 6: Baseline Performance of Single-Granularity Coding\", \"page\": 10, \"index\": 3, \"width\": 492, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-004.webp\", \"caption\": \"Figure 7: Analysis of Supervision Task Weight Parameters\", \"page\": 10, \"index\": 4, \"width\": 492, \"height\": 473}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-005.webp\", \"caption\": \"Figure 1: Structure of Holistic Semantic Encoding\", \"page\": 3, \"index\": 5, \"width\": 790, \"height\": 554}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-006.webp\", \"caption\": \"Table 3: Ablation Study\", \"page\": 8, \"index\": 6, \"width\": 714, \"height\": 226}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-007.webp\", \"caption\": \"Table 2: Overall Performance Comparison\", \"page\": 8, \"index\": 7, \"width\": 864, \"height\": 317}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-008.webp\", \"caption\": \"Figure 4: Relative improvement (%) over the baseline (w/o aux) on Beauty and Instruments. Orange: w/o Interleaved; blue-green: HoloRec; red bars indicate negative changes. Both subplots share the same x-axis range (–2% to 16%). Exact percentage labels are shown on each bar.\", \"page\": 9, \"index\": 8, \"width\": 459, \"height\": 456}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-009.webp\", \"caption\": \"Figure 5: Performance comparison by sequence length on the Beauty dataset\", \"page\": 9, \"index\": 9, \"width\": 492, \"height\": 450}, {\"url\": \"assets/figures/arxiv/2606.15331v1/fig-010.webp\", \"caption\": \"Figure 2: Model structure for multi-granularity supervised alignment training. The fine-grained prediction head outputs the final recommendation, while the coarse-grained head provides hierarchical supervision only during training.\", \"page\": 5, \"index\": 10, \"width\": 794, \"height\": 382}]"
motivation: 现有生成式推荐模型缺乏层次化语义结构进行多步推理，且外部构建的链式推理需要昂贵标注且与生成目标脱节。
method: HoloRec采用多粒度嵌套残差量化构建层次语义编码矩阵，并通过整体重构损失优化，支持非思考模式（轻量级多粒度监督对齐）和思考模式（交错推理内生生成链式步骤）。
result: 在多个公开推荐数据集上，HoloRec一致优于基线，尤其在稀疏场景下增益显著，且思考模式在仅适度推理开销下获得更高准确率。
conclusion: HoloRec通过内生链式推理机制统一表示、推理和生成，有效提升生成式推荐性能，为序列化推荐提供新范式。
---

## 摘要
将任务建模为序列生成的生成式推荐模型克服了传统级联架构的目标碎片化问题，但现有方法仍存在两个缺陷：缺乏用于多步推理的层次化结构，导致语义表示扁平；以及外部构建的思维链（CoT）需要昂贵的人工标注且与生成目标脱节。我们提出HoloRec，一种内生式思维链推荐机制，通过多粒度嵌套残差量化构建层次化语义编码矩阵，并由整体重建损失进行优化，从而统一表示、推理和生成。HoloRec支持两种推理模式：非思考模式利用轻量级多粒度监督对齐实现快速预测；思考模式采用交错推理方案动态生成CoT步骤，将推理直接嵌入生成过程，无需外部数据。在多个公开推荐数据集上的实验表明，HoloRec始终优于基线模型，尤其在稀疏场景下提升显著，且思考模式在仅增加适度推理开销的情况下达到了比非思考模式更高的准确率。

## Abstract
Generative recommendation models that formulate the task as sequence generation overcome the objective fragmentation problem of traditional cascade architectures, yet existing approaches still suffer from flat semantic representations lacking hierarchical structure for multi-step reasoning and an externally constructed chain-of-thought (CoT) that requires expensive annotations and remains disconnected from the generation objective. We propose HoloRec, an endogenous chain-of-thought recommendation mechanism that unifies representation, reasoning, and generation by constructing a hierarchical semantic encoding matrix via multi-granularity nested residual quantization optimized by a holistic reconstruction loss. HoloRec supports two inference modes: a non-thinking mode that uses lightweight multi-granularity supervised alignment for fast prediction, and a thinking mode that employs an interleaved reasoning scheme to generate CoT steps on the fly, directly embedding reasoning into the generation process without external data. Experiments on multiple public recommendation datasets demonstrate that HoloRec consistently outperforms baselines, with especially significant gains in sparse scenarios, and the thinking mode achieves better accuracy than the non-thinking mode with only modest inference overhead.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有生成式推荐系统（如 TIGER、P5 等）虽然用序列生成范式替代了传统级联架构，但仍存在三个根本性缺陷：
  1. **语义表示扁平**：项目标识通常是单一向量或平面 token 序列，缺乏粗到细的层次结构，无法有效支撑多步推理。
  2. **链式思维数据稀缺**：高质量 CoT 数据依赖昂贵的人工标注，难以规模化，导致极稀缺。
  3. **推理与生成脱节**：即使获得 CoT 数据，现有方法也将推理步骤与最终预测分开在不同阶段执行，推理无法深度引导生成过程。
- **整体含义**：本文提出一种**内生式思维链机制**（HoloRec），通过构建层次化的语义编码矩阵，将表示、推理和生成统一在一个框架中，从而在不需外部标注的前提下提升推荐性能，尤其在数据稀疏场景下效果显著。

### 2. 论文提出的方法论

- **核心思想**：使用多粒度嵌套残差量化（Multi-granularity Nested Residual Quantization）为每个项目生成一个 M×L 的离散编码矩阵，其中行从粗到细对应不同语义粒度，列对应逐步细化的残差量化层。然后通过两种模式（非思考 / 思考）实现推理与生成的融合。

- **关键技术细节**：
  - **整体语义编码**（图 1）：
    - 通过编码器获取项目潜在向量 **z** ∈ R^D，定义维度序列 d₁<d₂<…<dₘ=D，对每个粒度提取前 dₘ 维作为初始表示。
    - 对所有粒度独立执行 L 层残差量化，但各粒度共享同层码本。
    - 训练损失包括：重建损失（L_recon）、量化损失（L_quant）、整体重建损失（L_holo），确保每层粒度的累积量化向量都能独立重建其对应初始切片。
    - 算法 1 给出了详细流程。
  - **非思考模式**（多粒度监督对齐）：
    - 在细粒度预测头之外添加一个或多个粗粒度预测头（仅训练时使用）。
    - 训练时同时优化细粒度交叉熵损失和粗粒度交叉熵损失，权重由 α 平衡。
    - 推理时移除粗粒度头，不增加任何额外开销（图 2，算法 2）。
  - **思考模式**（交错推理）：
    - 在每个解码步骤，先基于前一步的细粒度隐状态预测粗粒度分布（式 6），加权求和得到软嵌入（式 7）。
    - 软嵌入作为额外输入通过解码器得到上下文隐状态（式 8）。
    - 门控机制将粗信息融合进细粒度表示（式 9-10），再用于细粒度 token 预测。
    - 训练损失包括粗粒度 CE 损失、细粒度 CE 损失和对齐损失（使软嵌入接近真实粗嵌入）。
    - 推理时自动执行交错步骤，无需外部 CoT 数据（算法 3，图 3）。

- **复杂度**：
  - 离线 SID 训练复杂度 O(M L K D)，是标准 RQ-VAE 的 M 倍。
  - 在线推理时，非思考模式与基线持平（O(T² d_model)），思考模式增加 M 倍常数因子（多一次 decoder 前向）。
  - 参数增加主要来自粗粒度头（约 <1% backbone）和门控模块（两个线性层），影响很小。

### 3. 实验设计

- **数据集**：Amazon Reviews 中的 **Beauty**（22,363 用户，12,100 项目，密度 0.073%）和 **Instruments**（24,772 用户，9,921 项目，密度 0.084%）。五核过滤，留一法划分。
- **基准方法**：
  - 传统推荐：MF、HGN、BERT4Rec、SASRec
  - 生成式推荐：BIGRec（文本 ID）、P5-SemID（语义 ID）、TIGER（残差量化语义 ID）
- **评估指标**：Hit@5/10 和 NDCG@5/10。
- **实现细节**：T5 backbone，粒度 M=3（维度 8/16/32），量化层 L=4，码本大小 K=256，共享码本，K-Means 初始化。超参数：非思考 α=0.5，思考 λ₁=5, λ₂=1, λ₃=200。最大 200 epoch，早停 patience 20，学习率 5e-4，warmup 0.1，batch 1024。

### 4. 资源与算力

- **文中提及**：所有模型在 PyTorch 框架下使用 NVIDIA A100 GPU 训练。但**未明确说明 GPU 的数量、总训练时长或内存消耗**。
- **推测**：由于两个数据集均较小（约 20 万交互），200 epoch 结合 batch 1024 可在单张 A100 上合理时间内完成，但具体资源开销未给出。

### 5. 实验数量与充分性

- **实验组数**：
  - 整体性能表（Table 2）：2 数据集 × 4 指标 × 8 方法 = 64 个数值对比。
  - 消融研究（Table 3，图 4）：3 个变体（w/o aux、w/o Interleaved、完整 HoloRec）在 2 数据集 × 5 指标 = 30 个数值。
  - 序列长度分析（图 5）：2 分组 × 5 指标，比较 w/o aux vs HoloRec。
  - 单粒度编码分析（图 6）：3 种粒度在 2 数据集 × 5 指标。
  - 权重参数分析（图 7）：多种 α 组合在 3 种辅助粒度 + 多粒度融合，2 数据集 × 5 指标。
- **充分性评估**：
  - **优点**：覆盖了主要组件、不同数据稀疏度、序列长度、粒度选择、超参数敏感性，对比公平（骨干统一为 T5，与 TIGER 对比直接）。
  - **不足**：仅使用了两个亚马逊商品推荐数据集，领域有限，未在工业级或更多样化场景（如新闻、视频、社交）验证。此外，统计分析（如 p 值、置信区间）未报告，仅给出点估计。

### 6. 论文的主要结论与发现

- **整体性能**：HoloRec 在所有数据集和指标上一致优于所有基线，在稀疏数据集 (Beauty) 上提升最大（如 Hit@5 相对提升 14.2%）。
- **消融效果**：
  - 多粒度监督（w/o Interleaved vs w/o aux）带来中等提升，主要改善召回类指标（Hit@10）。
  - 交错推理（HoloRec vs w/o Interleaved）进一步提升了 Top 排名准确性（Hit@1/5），但长序列召回略有下降。
- **序列长度**：HoloRec 对短序列的召回和长序列的 Top-1 均有显著提升，验证了粗到细推理在不同历史长度下的适用性。
- **粒度分析**：细粒度 (32-dim) 单独作为目标最优，粗/中粒度作为辅助信号效果较好。
- **超参数**：任务权重 α=0.5 效果整体最优，多粒度融合平衡各指标。
- **复杂度**：思考模式推理开销增加常数倍，但参数数量增加小于 1%，内存占用几乎不变。

### 7. 优点

- **方法创新**：首次提出基于嵌套残差量化的**层次化语义编码**，显式提供粗到细表示，天然适配推理。
- **内生 CoT**：无需外部标注，推理与生成深度交织，通过门控机制直接引导细粒度预测。
- **双模式设计**：非思考模式无额外推理开销，思考模式以可控常数倍开销换更高准确率，灵活性高。
- **稳健性**：在稀疏数据上提升显著，表明层次结构提供了有效的语义正则化。
- **实验严谨**：消融逐步剥离组件，清晰归因贡献；对超参数、粒度、序列长度均做了细致分析。

### 8. 不足与局限

- **场景覆盖窄**：仅评估两个电商细分领域（美妆、乐器），未在新闻、视频、广告等更多领域验证泛化能力。
- **缺失实际推理耗时**：仅给出理论复杂度，未报告实际推理延迟（如 ms/请求），用户难以评估思考模式的效率损失。
- **统计可靠性不足**：未提供多次重复实验的标准差或显著性检验，点估计可能受随机影响。
- **工业级问题**：未在超大规模数据集（亿级用户/项目）上测试，层次编码矩阵的存储和搜索效率未讨论。
- **超参数敏感**：虽然找到较好设定，但 λ₁, λ₂, λ₃, α 等需要手动调优，自动自适应方法缺失。
- **可解释性有限**：虽然思考模式模拟了推理过程，但生成的粗粒度分布和门控向量缺乏直观解释，仍属隐式推理。

（完）
