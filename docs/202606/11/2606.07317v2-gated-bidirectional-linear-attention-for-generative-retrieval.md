---
title: Gated Bidirectional Linear Attention for Generative Retrieval
title_zh: 用于生成式检索的门控双向线性注意力
authors: "Artem Matveev, Vladislav Tytskiy, Sergei Makeev, Sergei Liamaev"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.07317v2"
tags: ["query:q1"]
score: 9.0
evidence: 推荐系统中的生成式检索
tldr: 2混合自注意力和GBLA的编码器保持质量，H100 GPU上长序列加速达8.2倍，且可泛化到公共数据集。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.07317v2/fig-001.webp\", \"caption\": \"Table 3: Comparison of bidirectional self-attention and linear attention for different sequence lengths.\", \"page\": 3, \"index\": 1, \"width\": 525, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2606.07317v2/fig-002.webp\", \"caption\": \"Figure 2: Encoder latency for different sequence lengths.\", \"page\": 3, \"index\": 2, \"width\": 489, \"height\": 341}, {\"url\": \"assets/figures/arxiv/2606.07317v2/fig-003.webp\", \"caption\": \"Figure 1: Gated Bidirectional Linear Attention architecture.\", \"page\": 2, \"index\": 3, \"width\": 504, \"height\": 417}]"
motivation: 双向注意力在生成式检索编码器中提升质量，但平方复杂度导致长序列下延迟瓶颈，需亚二次方法。
method: 提出GBLA，基于核化线性注意力，新增Conv1D局部因果混合、序列级键门控实现软遗忘、门控RMSNorm输出。
result: 2混合编码器匹配双向自注意力质量，长序列（32768）单层加速8.2倍（vs FlashAttention-v3）。
conclusion: 混合GBLA与自注意力的设计在推荐生成式检索中高效且通用，可推广至公共基准。
---

## 摘要
在推荐系统中，生成式检索通常使用编码器-解码器设置：编码器处理用户交互历史，自回归解码器随后生成推荐项。在大规模流媒体服务中，活跃用户随时间积累非常长的历史。随着历史增长，编码器成为主要延迟瓶颈，因为softmax注意力与序列长度呈二次方关系。在我们的实验中，在编码器中使用双向注意力显著提高了质量。然而，大多数次二次注意力方法专注于因果注意力。我们提出门控双向线性注意力（GBLA），这是一种线性时间双向注意力层，通过三个轻量级组件扩展了核化线性注意力：局部因果混合（Conv1D）、用于软遗忘的序列级键门控，以及门控RMSNorm输出。在大型Yandex Music数据集上，一个以1:2比例交错自注意力（SA）和GBLA的混合编码器（一个SA块后跟两个GBLA块）匹配了双向自注意力质量。在H100 GPU上，与FlashAttention-v3相比，GBLA在历史长度32768时实现了高达8.2倍的单层加速。最后，我们展示了相同的混合设计可推广到我们的专有设置之外，在公共Amazon基准测试上一致地保持了自注意力检索质量。

## Abstract
In recommender systems, generative retrieval typically uses an encoder-decoder setup: an encoder processes a user interaction history, and an autoregressive decoder then generates recommended items. In large-scale streaming services, active users accumulate very long histories over time. As histories grow, the encoder becomes a major latency bottleneck because softmax attention scales quadratically with sequence length. In our experiments, using bidirectional attention in the encoder substantially improves quality. However, most sub-quadratic attention methods focus on causal attention.   We propose Gated Bidirectional Linear Attention (GBLA), a linear-time bidirectional attention layer that extends kernelized linear attention with three lightweight components: local causal mixing (Conv1D), sequence-level key gating for soft forgetting, and a gated RMSNorm output. On a large-scale Yandex Music dataset, a hybrid encoder that interleaves self-attention (SA) and GBLA in a 1:2 ratio (one SA block followed by two GBLA blocks) matches bidirectional self-attention quality. On H100 GPUs, GBLA reaches up to an $8.2\times$ single-layer speedup at a history length of 32768, compared to FlashAttention-v3. Finally, we show that the same hybrid design generalizes beyond our proprietary setting, consistently preserving self-attention retrieval quality on public Amazon benchmarks.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

- **研究动机**：生成式推荐系统采用编码器-解码器架构，编码器处理用户交互历史。随着用户历史变长，标准softmax注意力的二次复杂度成为延迟瓶颈，特别是在大规模流媒体服务中。
- **核心问题**：如何在保持双向注意力质量收益的同时，将编码器复杂度降低到线性，从而支持长历史序列的实时推理。
- **整体含义**：提出了一种新的线性时间双向注意力层GBLA，通过混合自注意力与GBLA实现质量无损且显著加速，为工业级长序列生成式检索提供可行方案。

## 2. 论文提出的方法论

- **核心思想**：基于核化线性注意力（将softmax替换为elu+1激活函数），复杂度从O(L²)降至O(L)。但直接替换会损失质量，因此添加三个轻量级组件。
- **关键技术细节**：
  - **Conv1D局部混合**：在QKV投影前对输入序列应用一维卷积（kernel size=4），增强局部模式提取。
  - **序列级键门控（Key Gating）**：学习每个token的标量门控权重（通过softmax），对历史事件进行软遗忘，公式中键向量乘上门控因子 $g_j$。
  - **门控RMSNorm输出**：注意力输出后先做RMSNorm，再与SiLU激活的门控信号逐元素相乘。
- **算法流程（文字说明）**：
  1. 输入序列X经Conv1D得到 $\tilde{X}$；
  2. 计算Q、K、V投影，同时由 $\tilde{X}$ 通过线性层+softmax得到门控 $g$；
  3. 将 $\phi(K)$ 与 $g$ 逐元素相乘得到 $\tilde{K}$，然后计算线性注意力：$(\phi(Q)(\tilde{K}^\top V)) \oslash (\phi(Q)(\tilde{K}^\top \mathbf{1}))$；
  4. 输出经RMSNorm后与SiLU门控相乘，得到最终结果。
- **混合架构**：编码器采用 [SA, GBLA, GBLA] 的1:2交替堆叠，即每1个自注意力块后接2个GBLA块。

## 3. 实验设计

- **数据集与场景**：
  - 工业级：Yandex Music流媒体平台，7天训练数据（400M样本），下一天评估，最大历史长度设为2048（消融时扩展到8192）。
  - 公共基准：Amazon Beauty、Sports、Toys（5-core过滤），采用GRID框架，使用语义ID（Flan-T5嵌入+ RK-Means分词）。
- **基准方法**：
  - 双向自注意力（FlashAttention-v3，SA）
  - 因果自注意力（仅用于对比双向的重要性）
  - 普通BLA（移除所有组件的GBLA）
  - 完全GBLA（无SA混合）
- **对比指标**：Recall@10、Recall@100、Recall@1000（工业）；Recall@5/10、NDCG@5/10（Amazon）。
- **主要实验组**：
  1. 双向 vs 因果掩码（表1）
  2. 混合 vs 完全GBLA（表2）
  3. 不同序列长度（512/2048/4096/8192）下SA vs 混合GBLA（表3）
  4. GBLA各组件的消融实验（表4）
  5. 单层推理延迟对比（表5、图2）
  6. 全模型训练步骤时间对比（表6）
  7. Amazon数据集上Tiger vs Tiger+GBLA（表7）

## 4. 资源与算力

- **GPU型号**：NVIDIA H100（通过第三方租赁）。
- **训练配置**：未说明GPU数量，但提及有效batch size固定。对于L=2048，batch size=32；L=8192时batch size=8。
- **训练时长**：未明确总训练时间，仅报告了每个训练步骤的平均耗时（如SA 3000ms，GBLA 3100ms等）。
- **说明**：文中未提及具体GPU卡数或训练总小时数，信息不完整。

## 5. 实验数量与充分性

- **实验数量**：共7组主要实验（含消融、长度缩放、速度基准、公共数据集），此外还有图2的精细延迟曲线。消融实验中逐一移除三个组件，并对比完全去掉的BLA，设计较完整。
- **充分性**：
  - 工业数据规模大（400M样本），覆盖不同长度（512~8192），且验证了混合架构的通用性（Amazon）。
  - 速度测试覆盖单层和全模型，且对比了FlashAttention-v3，具备客观性。
  - 但Amazon数据集上历史长度≤128，未测速度，且结果差异很小（论文认为不显著），因此长序列优势未在公共场景验证。
- **公平性**：学习率、batch size等超参数分别调优（SA用7e-4，混合用5e-4），且平均5次运行报告结果，符合规范。

## 6. 论文的主要结论与发现

- 在生成式检索编码器中，**双向注意力至关重要**（因果掩码导致Recall@1000从0.8667降至0.8353）。
- **混合架构**（1:2比例）可匹配甚至略优于纯双向自注意力质量（在512/2048/4096上稍有提升，8192上轻微下降，差距<1%）。
- **GBLA各组件均贡献正向收益**，移除任何一个都会导致Recall下降，全部移除（BLA）下降最大。
- **延迟优势显著**：单层推理从长度4096起超越FlashAttention-v3，32768时速度提升8.2倍。全模型训练在长序列下也有1.29倍加速。
- 混合设计可推广到Amazon公共数据集，Tiger+GBLA的指标与纯Tiger几乎相同。

## 7. 优点

- **方法新颖**：首次针对生成式检索提出线性时间双向注意力，并设计了有效的混合架构。
- **工程实用**：在保持质量的前提下获得显著加速，且组件轻量（Conv1D kernel=4，门控仅增加少量参数）。
- **验证充分**：在工业大规模数据（Yandex Music）和公共基准（Amazon）上均进行评测，消融实验完整。
- **代码开源**：提供代码链接（GitHub仓库matfu-pixel/GRID），增强可复现性。

## 8. 不足与局限

- **短序列下无优势**：历史长度≤2048时，GBLA速度略慢于FlashAttention（0.95×），且自注意力占总延迟比例低，加速不明显。
- **完全移除自注意力不可行**：全GBLA编码器质量下降明显（表2），必须使用混合架构。
- **公共数据集验证不充分**：Amazon数据历史短（≤128），未测量延迟，且质量结果有微小下降（论文认为不显著，但未进行统计检验）。
- **仅限推荐领域**：方法在生成式检索场景提出，未验证其他NLP或CV任务。
- **算力信息不完整**：未说明训练使用的GPU数量、总时长，难以评估复现成本。
- **潜在偏差**：工业数据来自单一平台（Yandex Music），用户行为模式可能与其他服务不同。

（完）
