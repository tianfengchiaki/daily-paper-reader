---
title: Dual-Stream MLP is All You Need for CTR Prediction
title_zh: 双流MLP：点击率预测所需的一切
authors: "Kesha Ou, Zhen Tian, Wayne Xin Zhao, Long Zhang, Sheng Chen, Ji-Rong Wen"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04944v1"
tags: ["query:q1"]
score: 9.0
evidence: 在线广告点击率预测
tldr: 点击率预测在推荐系统中至关重要。现有双流架构面临高复杂度和模块不平衡问题。本文提出DS-MLP，通过知识蒸馏将显式特征交互能力整合到主MLP，并辅以并行MLP捕获隐式交互，同时设计两种对齐策略优化训练。实验表明，仅使用普通MLP结构即可达到SOTA性能，提供高效可扩展方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04944v1/fig-001.webp\", \"caption\": \"Fig. 5. Impact of the coefficients 𝜆 and 𝛼 in KD and Align Loss.\", \"page\": 21, \"index\": 1, \"width\": 813, \"height\": 884}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-002.webp\", \"caption\": \"Table 4. Performance comparisons grouped by dataset. Note that a higher AUC or lower Logloss at 0.001-level is significant for CTR prediction.\", \"page\": 14, \"index\": 2, \"width\": 744, \"height\": 1142}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-003.webp\", \"caption\": \"Fig. 4. Case study for our approach DS-MLP.\", \"page\": 18, \"index\": 3, \"width\": 658, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-004.webp\", \"caption\": \"Fig. 1. The architecture of our proposedDS-MLP. Overall, it is trainedwith twomain stages, namely knowledge distillation and fine-tuning.\", \"page\": 4, \"index\": 4, \"width\": 804, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-005.webp\", \"caption\": \"Table 6. The effectiveness of different components in DS-MLP.\", \"page\": 19, \"index\": 5, \"width\": 714, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-006.webp\", \"caption\": \"Fig. 2. The scaling effect for teacher v.s. student.\", \"page\": 17, \"index\": 6, \"width\": 801, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-007.webp\", \"caption\": \"Fig. 3. Comparison of MSE on each component in learning explicit feature interactions.\", \"page\": 17, \"index\": 7, \"width\": 567, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-008.webp\", \"caption\": \"Table 5. Compatibility Analysis of DS-MLP.\", \"page\": 16, \"index\": 8, \"width\": 664, \"height\": 323}]"
motivation: 现有双流架构特征交互学习复杂度高且易过拟合，且显式和隐式模块输出不平衡导致预测被主导。
method: 提出DS-MLP，利用知识蒸馏将显式交互学习能力整合到主MLP网络，并行MLP捕获隐式交互，并通过两种对齐策略优化双流兼容性。
result: 在三个广泛使用的基准数据集上达到当前最优性能，且仅需普通MLP结构即可实现。
conclusion: DS-MLP是一种高效可扩展的CTR预测框架，适用于大规模推荐系统。
---

## 摘要
点击率（CTR）预测在在线广告和推荐系统中扮演着关键角色，即使微小的改进也能显著提升收入。现有研究主要集中于设计双流架构，从显式和隐式两个角度捕获有效的复杂特征交互。然而，这些方法面临两大挑战：1）特征交互学习的高复杂度增加了计算需求和过拟合风险；2）显式与隐式模块之间的不平衡，即一个模块的输出可能主导最终预测。为解决这些问题，本文提出双流MLP（DS-MLP），一种用于CTR预测任务的新型特征交互框架。具体而言，它利用知识蒸馏将学习显式特征交互的能力整合到主MLP网络中，同时并行MLP捕获隐式特征交互作为补充。为有效优化双流MLP架构，我们进一步设计了一种特定的学习方法，包含两种对齐策略以增强两个MLP组件的兼容性。实验表明，DS-MLP虽然仅采用普通MLP结构（最终模型），但在三个广泛使用的基准测试中达到了最先进的性能，为大规模推荐系统提供了可扩展且高效的解决方案。我们的代码开源在 https://github.com/RUCAIBox/DS-MLP。

## Abstract
Click-through rate (CTR) prediction holds a pivotal role in online advertising and recommendation systems, where even small improvements can significantly boost revenue. Existing research primarily focuses on designing dual-stream architectures to capture effective complex feature interactions from both explicit and implicit perspectives. However, these approaches are faced with two major challenges: 1) the high complexity of feature interaction learning, which increases computational demands and the overfitting risk, and 2) the imbalance between explicit and implicit modules, where one module's output may dominate the final prediction. To address these issues, in this paper, we propose Dual-Stream MLP (DS-MLP), a novel feature interaction framework for the CTR prediction task. Specially, it leverages knowledge distillation to consolidate the capacity of learning explicit feature interaction into a main MLP network, while a parallel MLP simultaneously captures implicit feature interactions as a complement. To effectively optimize the dual-stream MLP architecture, we further design a specific learning approach with two alignment strategies for enhancing the compatibility of the two MLP components. Experiments demonstrate that DS-MLP, though merely a vanilla MLP structure (the final model), can achieve state-of-the-art performance across three widely used benchmarks, offering a scalable and efficient solution for large-scale recommendation systems. Our code is available at https://github.com/RUCAIBox/DS-MLP.

---

## 论文详细总结（自动生成）

# 论文总结：Dual-Stream MLP is All You Need for CTR Prediction

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：点击率（CTR）预测是推荐系统和在线广告中的关键任务。现有主流方法采用双流架构，分别从**显式特征交互**（如通过叉积运算）和**隐式特征交互**（如通过MLP）两个视角建模，但存在两大固有局限：
  - **高复杂度**：显式建模高阶特征交互需要枚举所有组合，在大规模特征空间下计算成本剧增，且易过拟合。
  - **不均衡融合**：显式和隐式模块的输出数值范围差异大——显式交互的输出随阶数指数放大，隐式MLP的输出被激活函数约束，导致显式模块主导最终预测，隐式模块被弱化。
- **整体含义**：本文旨在设计一种**简单、高效、强性能**的CTR模型，仅使用普通MLP结构，通过知识蒸馏和对齐策略统一显式与隐式交互的学习，解决上述挑战，为工业大规模部署提供可扩展方案。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程

### 2.1 核心思想
- 采用**双流MLP架构**（DS-MLP）：一个**主MLP**通过知识蒸馏从强教师模型中学习显式特征交互；一个**并行MLP**独立捕获隐式特征交互作为补充。
- 设计两阶段训练：**知识蒸馏**阶段 → **对齐微调**阶段，通过两种对齐策略（隐藏状态对齐和预测对齐）实现两流输出的兼容与平衡。

### 2.2 关键技术细节

#### 阶段一：通用特征交互学习（知识蒸馏）
- **教师模型**：选用GDCN（Gated Deep Cross Network），其输出 = 显式部分（GCN） + 隐式部分（DNN）。GDCN使用门控机制增强叉积，但显式部分数值范围大。
- **学生模型**：简单3层MLP（主MLP），旨在通过蒸馏学习教师模型的显式交互知识。
- **蒸馏方法**：最小化教师与学生logits之间的交叉熵损失，加入温度系数缩放logits。
- **联合优化**：总损失 = CTR损失 + λ × 蒸馏损失（公式6）。

#### 阶段二：容量补偿（双MLP微调）
- **架构**：主MLP + 并行MLP。最终预测 = σ(0.5×(y_M + y_P))（公式7）。
- **隐藏状态对齐**：对两个MLP的隐藏层分别施加**批归一化（BN）**，使输出数值范围接近，避免某一流主导。
- **预测对齐**：对每个MLP分支分别计算CTR损失（L_M, L_P，公式9），使其各自作为独立预测器适应任务目标，增强兼容性。
- **整体优化**：总损失 = CTR_loss + α·L_M + β·L_P（公式10）。

#### 算法流程（Algorithm 1）
- **阶段一**：迭代每个mini-batch，冻结教师网络，生成教师输出p_T；主MLP和并行MLP产生p_M, p_P；聚合预测p = 0.5p_M + 0.5p_P；计算蒸馏损失（KL散度）和CTR损失；仅更新主MLP。
- **阶段二**：生成双流隐藏输出；应用BN对齐；聚合预测；计算分支CTR损失（L_M, L_P）作为对齐损失；联合优化所有参数。

### 2.3 理论分析
- 利用万能逼近定理证明ReLU-MLP可在L2范数下逼近教师函数；通过二阶导数分析说明学生能继承教师的显式高阶交互；风险差由L2误差上界保证。

## 3. 实验设计

### 3.1 数据集
- **Criteo**：约4584万样本，39个字段，208.7万特征。广告点击数据。
- **Avazu**：约4043万样本，22个字段，154.4万特征。移动广告点击数据。
- **MovieLens**：约200.7万样本，3个字段，9万特征。标签推荐场景。

### 3.2 基准方法（Baseline）
- 涵盖三类：二阶交互模型（FmFM, AFM）、显式+隐式高阶交互模型（DeepFM, xDeepFM, DCNv2, GDCN, AutoInt+, FCN, Final, FinalMLP, WuKong, SFG）、知识蒸馏方法（ECKD）。共约17个对比方法。

### 3.3 评价指标
- **AUC**（越高越好）和**LogLoss**（越低越好），并报告RelaImpr相对提升和推理延迟（ms）。

### 3.4 实现细节
- 基于FuxiCTR库，嵌入维度10，batch size 4096，默认MLP结构[400,400,400]，Adam优化器。DS-MLP学生MLP隐藏层大小为[600,600,600]（对Criteo），并行MLP与教师隐式模块大小一致。超参数λ,α,β在不同数据集上分别调优。

## 4. 资源与算力

- **论文未明确说明**所使用的GPU型号、数量、训练时长等具体算力信息。仅在实验设置中提及使用服务器环境测量推理延迟，但未提供训练算力细节。

## 5. 实验数量与充分性

- **实验数量丰富**：
  - **整体性能对比**：在3个数据集上与17个基线方法比较（表4）。
  - **兼容性分析**：以5种不同教师模型（AutoInt, DCNv2, xDeepFM, GDCN, FinalMLP）验证DS-MLP的教师无关性（表5）。
  - **可扩展性分析**：对比DS-MLP与GDCN在不同参数量下的AUC曲线（图2）。
  - **显式交互保真度分析**：通过合成数据集计算MSE，比较主MLP、并行MLP和CrossNet模块对显式交互的拟合能力（图3）。
  - **案例研究**：展示双流预测如何纠正蒸馏带来的错误（图4）。
  - **消融实验**：5个变体（w/o alignment loss, w/o student MLP, w/o parallel MLP, w/o dual MLP, w/o KD），见表6。
  - **超参数分析**：对λ和α进行调优实验（图5）。

- **充分性与公平性**：
  - 所有基线方法的参数设置严格对齐BARS基准，嵌入维度、batch size等统一。
  - 实验覆盖了不同规模数据集、多种教师类型、多角度分析，结果客观且具有说服力。

## 6. 主要结论与发现

- **DS-MLP在所有三个数据集上均取得SOTA性能**（表4）：Criteo AUC 0.8152（RelaImpr 3.21%），Avazu AUC 0.7670（RelaImpr 2.73%），MovieLens AUC 0.9752（RelaImpr 6.02%），同时推理延迟与高效模型相当。
- **知识蒸馏有效**：主MLP能成功学习教师模型的显式交互，保真度实验（图3）证实其MSE远低于并行MLP，接近CrossNet。
- **双流对齐至关重要**：消融实验（表6）表明去除对齐损失、学生MLP、并行MLP、双MLP或KD均导致性能明显下降。
- **可扩展性好**：DS-MLP随参数增加性能持续提升（图2），而GDCN饱和甚至下降，说明简单结构更易优化。
- **教师无关性**：DS-MLP可适配不同教师模型，均带来稳定提升（表5）。

## 7. 优点

- **架构简洁高效**：最终模型仅为普通MLP，易于部署和维护，推理延迟低。
- **创新训练流程**：将知识蒸馏与双流对齐结合，解决传统双流模型的不平衡问题。
- **对齐策略巧妙**：隐藏状态层使用BN稳定数值范围，预测层使用独立CTR损失强制两流均作为有效预测器，避免主导。
- **理论支撑充分**：通过万能逼近定理和二阶导数分析证明蒸馏后MLP可恢复显式交互。
- **实验全面深入**：涵盖兼容性、可扩展性、保真度、案例、消融、超参数等多维度分析，结论可靠。

## 8. 不足与局限

- **算力资源未报告**：缺乏训练所需GPU型号、数量、时间等细节，不利于复现成本评估。
- **教师模型需预训练**：蒸馏过程依赖一个预训练的强教师模型（如GDCN），额外增加了前期计算开销。
- **超参数敏感**：λ和α需要针对不同数据集精细调节（表3给出最优值），实验虽展示鲁棒性但仍需调参工作。
- **理论假设较强**：理论分析依赖光滑性和正则化假设，实际中可能不完全满足。
- **应用限制**：仅在三个公开数据集上验证，未在真实大规模工业系统（如超十亿样本）中测试，可扩展性结论需进一步证实。
- **对比方法范围**：未与最新的大规模推荐系统（如Huatuo、OneRec等）对比，但已覆盖主流方法。
- **并行MLP容量未缩放**：实验中并行MLP大小固定为教师隐式模块大小，其最佳容量未单独探索。

（完）
