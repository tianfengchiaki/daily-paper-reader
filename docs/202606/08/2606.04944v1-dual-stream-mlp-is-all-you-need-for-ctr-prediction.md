---
title: Dual-Stream MLP is All You Need for CTR Prediction
title_zh: 双流MLP：CTR预测所需的一切
authors: "Kesha Ou, Zhen Tian, Wayne Xin Zhao, Long Zhang, Sheng Chen, Ji-Rong Wen"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04944v1"
tags: ["query:q1"]
score: 9.0
evidence: 在线广告和推荐系统中的点击率预测
tldr: 针对CTR预测中双流架构高复杂度和模块不平衡问题，提出DS-MLP，通过知识蒸馏将显式特征交互学习能力集成到主MLP，并行MLP捕获隐式交互，并设计两个对齐策略优化。实验表明最终仅用简单MLP结构即在三个基准上达到SOTA，为大规模推荐系统提供可扩展高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04944v1/fig-001.webp\", \"caption\": \"Fig. 5. Impact of the coefficients 𝜆 and 𝛼 in KD and Align Loss.\", \"page\": 21, \"index\": 1, \"width\": 813, \"height\": 884}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-002.webp\", \"caption\": \"Table 4. Performance comparisons grouped by dataset. Note that a higher AUC or lower Logloss at 0.001-level is significant for CTR prediction.\", \"page\": 14, \"index\": 2, \"width\": 744, \"height\": 1142}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-003.webp\", \"caption\": \"Fig. 4. Case study for our approach DS-MLP.\", \"page\": 18, \"index\": 3, \"width\": 658, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-004.webp\", \"caption\": \"Fig. 1. The architecture of our proposedDS-MLP. Overall, it is trainedwith twomain stages, namely knowledge distillation and fine-tuning.\", \"page\": 4, \"index\": 4, \"width\": 804, \"height\": 319}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-005.webp\", \"caption\": \"Table 6. The effectiveness of different components in DS-MLP.\", \"page\": 19, \"index\": 5, \"width\": 714, \"height\": 221}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-006.webp\", \"caption\": \"Fig. 2. The scaling effect for teacher v.s. student.\", \"page\": 17, \"index\": 6, \"width\": 801, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-007.webp\", \"caption\": \"Fig. 3. Comparison of MSE on each component in learning explicit feature interactions.\", \"page\": 17, \"index\": 7, \"width\": 567, \"height\": 333}, {\"url\": \"assets/figures/arxiv/2606.04944v1/fig-008.webp\", \"caption\": \"Table 5. Compatibility Analysis of DS-MLP.\", \"page\": 16, \"index\": 8, \"width\": 664, \"height\": 323}]"
motivation: 现有双流架构存在高复杂度和显隐模块输出失衡问题，影响性能与效率。
method: 采用知识蒸馏将显式交互能力注入主MLP，并设计并行MLP捕获隐式交互，辅以两个对齐策略增强模块兼容性。
result: 在三个公开基准上，DS-MLP（最终为普通MLP结构）取得最先进性能。
conclusion: DS-MLP证明了简化结构的有效性，为大规模CTR预测提供可扩展解决方案。
---

## 摘要
点击率（CTR）预测在在线广告和推荐系统中扮演着关键角色，即使是微小的改进也能显著提升收入。现有研究主要集中于设计双流架构，从显式和隐式两个角度捕获有效的复杂特征交互。然而，这些方法面临两大挑战：1）特征交互学习的高复杂度，增加了计算需求和过拟合风险；2）显式与隐式模块之间的不平衡，其中一个模块的输出可能主导最终预测。为解决这些问题，本文提出双流MLP（DS-MLP），一种用于CTR预测任务的新型特征交互框架。具体而言，它利用知识蒸馏将学习显式特征交互的能力整合到一个主MLP网络中，同时一个并行MLP捕获隐式特征交互作为补充。为了有效优化双流MLP架构，我们进一步设计了一种特定的学习方法，包含两种对齐策略以增强两个MLP组件的兼容性。实验表明，尽管DS-MLP最终模型仅为普通的MLP结构，但在三个广泛使用的基准上达到了最先进的性能，为大规模推荐系统提供了一种可扩展且高效的解决方案。我们的代码可在https://github.com/RUCAIBox/DS-MLP获取。

## Abstract
Click-through rate (CTR) prediction holds a pivotal role in online advertising and recommendation systems, where even small improvements can significantly boost revenue. Existing research primarily focuses on designing dual-stream architectures to capture effective complex feature interactions from both explicit and implicit perspectives. However, these approaches are faced with two major challenges: 1) the high complexity of feature interaction learning, which increases computational demands and the overfitting risk, and 2) the imbalance between explicit and implicit modules, where one module's output may dominate the final prediction. To address these issues, in this paper, we propose Dual-Stream MLP (DS-MLP), a novel feature interaction framework for the CTR prediction task. Specially, it leverages knowledge distillation to consolidate the capacity of learning explicit feature interaction into a main MLP network, while a parallel MLP simultaneously captures implicit feature interactions as a complement. To effectively optimize the dual-stream MLP architecture, we further design a specific learning approach with two alignment strategies for enhancing the compatibility of the two MLP components. Experiments demonstrate that DS-MLP, though merely a vanilla MLP structure (the final model), can achieve state-of-the-art performance across three widely used benchmarks, offering a scalable and efficient solution for large-scale recommendation systems. Our code is available at https://github.com/RUCAIBox/DS-MLP.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：点击率 (CTR) 预测在在线广告和推荐系统中至关重要，微小改进可显著增收。现有主流方法采用“双流架构”（显式特征交互 + 隐式特征交互）来捕获复杂特征关系。
- **核心问题**：双流架构面临两大挑战：
  1. **高复杂度**：显式交互枚举所有高阶组合，计算成本高、过拟合风险大。
  2. **模块输出失衡**：显式模块（如 CrossNet）的输出数值范围随阶数指数增长，隐式模块（如 MLP）输出受激活函数限制，导致显式模块往往主导最终预测，削弱隐式模块贡献。
- **整体含义**：本文旨在设计一种**简单、高效且强悍**的 CTR 模型，避免上述问题，同时保持高性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：利用知识蒸馏将复杂教师模型（如 GDCN）的显式交互能力迁移到轻量级 MLP 学生网络（主 MLP），并引入另一个并行 MLP 专门捕获隐式交互，通过两个对齐策略促进两者协同。

- **关键技术细节**：
  - **两阶段训练流程**：
    1. **阶段一：知识蒸馏**。以强大模型 GDCN 为教师，以三层 MLP 为主学生，通过最小化教师与学生 logits 之间的交叉熵损失（加温度系数）进行知识迁移。优化目标：`L = L_CTR + λ * L_KD`。
    2. **阶段二：双 MLP 微调（容量补偿）**。保留主 MLP，加入并行 MLP（与主 MLP 结构相同）。引入两个对齐策略：
       - **隐藏状态对齐**：对两个 MLP 的隐藏层分别应用批归一化 (BN)，使输出数值范围接近，避免某模块主导。
       - **预测对齐**：对每个 MLP 单独计算 BCE 损失（`L_M` 和 `L_P`），强制每个分支独立预测标签，使其输出与真实标签对齐，从而平衡两者。
     - 最终预测：`y_dual = σ(0.5(y_M + y_P))`。
     - 联合优化损失：`L = L_CTR + α L_M + β L_P`。

  - **教师模型选择**：实验中采用 GDCN（门控深度交叉网络），因其在主流基准上表现出色。方法本身是教师无关的，可灵活替换。

  - **理论分析**：通过通用逼近定理和 Hessian 级数分析，证明经过蒸馏的 MLP 可恢复教师模型的显式特征交互行为。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：三个公开 CTR 基准：
  - **Criteo**：~4600 万样本，39 个特征字段。
  - **Avazu**：~4040 万样本，22 个特征字段。
  - **MovieLens**：~200 万样本，3 个特征字段。

- **评测指标**：AUC（越大越好）、LogLoss（越小越好）、RelaImpr（相对提升百分比）、推理延迟（毫秒）。

- **对比方法**：涵盖多种范式：
  - 二阶模型：FmFM、AFM。
  - 显隐双流模型：DeepFM、xDeepFM、DCNv2、GDCN、FiBiNet、AutoInt+、FCN、FinalMLP、Final、WuKong、SFG。
  - 知识蒸馏方法：ECKD。

- **实现细节**：基于 FuxiCTR 和 BARS 基准，嵌入维度 10，batch size 4096，默认 MLP 大小 [400,400,400]。DS-MLP 学生 MLP 隐藏层大小设为 [600,600,600]（Criteo），[900,900,900]（Avazu），[1000,1000,1000]（MovieLens），并行 MLP 与教师 MLP 模块大小一致。

## 4. 资源与算力

- **文中未明确说明**：未提及 GPU 型号、数量、训练时长、显存消耗等具体算力信息。仅提到在相同服务器环境下测量推理延迟（单位毫秒），但未提供服务器配置。

## 5. 实验数量与充分性

- **实验组数**：较为充分，共包括：
  1. **主实验结果**（表 4）：在三个数据集上与 14+ 个基线对比，每个数据集报告 AUC、LogLoss、RelaImpr 和延迟。
  2. **兼容性分析**（表 5）：使用 5 种不同教师（AutoInt、DCNv2、xDeepFM、GDCN、FinalMLP）验证方法通用性，共 5×3=15 组数据。
  3. **可扩展性分析**（图 2）：对比 DS-MLP 与 GDCN 随参数增大的性能轨迹，绘制缩放曲线。
  4. **保真度分析**（图 3）：合成数据集上比较学生 MLP、并行 MLP 与 CrossNet 在拟合教师显式交互时的 MSE。
  5. **案例研究**（图 4）：展示双流对纠正教师错误预测的作用。
  6. **消融实验**（表 6）：5 种变体（去除对齐损失、去除学生 MLP、去除并行 MLP、去除双 MLP、去除知识蒸馏），覆盖三个数据集。
  7. **超参数分析**（图 5）：对 λ 和 α 进行范围搜索（0.1~10 和 0.5~1.5），展示趋势。

- **充分性与客观性**：
  - 数据集规模大、基线种类多，覆盖了主流方法。
  - 对比方法参数设置与 BARS 基准对齐，保证公平。
  - 消融实验设计合理，每个组件的作用被清晰验证。
  - 可扩展性分析使用了拟合曲线，结果客观。
  - 但未进行跨域泛化（如新闻推荐、视频推荐）或用户级 A/B 测试，未提供工业级线上验证。

## 6. 论文的主要结论与发现

- **主要结论**：DS-MLP 尽管最终仅用普通 MLP 结构，但在三个基准上均取得 SOTA 性能（最高 AUC 和最低 LogLoss），且推理延迟控制在可接受范围（如 Criteo 上 59.43ms，与 FinalMLP、DCNv2 接近）。
- **关键发现**：
  1. 知识蒸馏可成功将显式交互模式压缩进轻量 MLP。
  2. 并行 MLP 能有效补充隐式交互，并通过对齐策略避免主导问题。
  3. 双 MLP 结构在缩放时表现出稳定提升，而传统显式模型（如 GDCN）在大参数量下性能饱和甚至下降。
  4. 消除教师偏见：在案例中，蒸馏后的学生继承教师错误，但并行 MLP 通过数据驱动学习纠正了这些错误。

## 7. 优点：方法或实验设计上的亮点

- **方法优点**：
  - **简洁性**：最终模型仅由 MLP 组成，结构统一，易于优化和部署。
  - **教师无关性**：可灵活接入任意 SOTA 教师，适应未来更优模型。
  - **对齐策略创新**：同时使用 BN（隐藏状态对齐）和 BCE 损失（预测对齐），模块输出数值和任务目标双重视角平衡。
  - **理论支撑**：给出基于通用逼近定理和 Hessian 近似的理论分析，证明蒸馏后可恢复显式交互。

- **实验设计亮点**：
  - 对比基线全面，包括最新方法（如 FCN、WuKong、SFG）。
  - 可扩展性分析呈现清晰的缩放曲线，与 GDCN 对比凸显优势。
  - 保真度分析使用合成数据集验证学生 MLP 对显式交互的拟合能力，方法严谨。
  - 案例研究直观展示双流纠错能力，增强理解。

## 8. 不足与局限

- **实验覆盖局限**：
  - 仅使用三个公开基准（Criteo, Avazu, MovieLens），未涵盖工业级大规模（如十亿级）或不同领域（如视频、新闻）数据集。
  - 未进行线上 A/B 测试或真实流量验证，实际收益存疑。
  - 未与其他蒸馏方法（如 PKD、RKD）对比，ECKD 是唯一蒸馏基线，但对比不够广泛。

- **偏差风险**：
  - 教师模型选择（GDCN）可能引入偏置；虽然兼容性实验换用了 5 种教师，但未探索教师质量波动的影响。
  - 超参数 λ、α、β 仅针对每个数据集单独调优（表 3），通用规则未总结。

- **应用限制**：
  - 需要预训练一个大型教师模型，增加了离线训练成本（算力未报告）。
  - 两阶段训练流程（蒸馏 + 微调）复杂度高于端到端单阶段方法。
  - 并行 MLP 的引入使参数量翻倍，虽然推理延迟可控，但显存占用可能增加。

（完）
