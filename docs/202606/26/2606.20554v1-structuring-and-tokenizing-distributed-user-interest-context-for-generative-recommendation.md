---
title: Structuring and Tokenizing Distributed User Interest Context for Generative Recommendation
title_zh: 面向生成式推荐的分布式用户兴趣上下文结构化与分词化
authors: "Ruizhong Qiu, Yinglong Xia, Dongqi Fu, Hanqing Zeng, Ren Chen, Xiangjun Fan, Hong Li, Hong Yan, Hanghang Tong"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20554v1"
tags: ["query:q1"]
score: 9.0
evidence: 基于用户兴趣token化的生成式广告推荐
tldr: 生成式推荐是新兴范式，通过预测用户下一交互进行推荐，核心是物品标记化。现有方法难以有效组织用户行为和物品语义，存在扩展性差或语义表示不准确问题。本文提出G2Rec框架，统一图协同建模与语义标记化，捕获全局用户兴趣原型，无需真实兴趣标签。在线部署和实验表明其优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20554v1/fig-001.webp\", \"caption\": \"Table 2 Comparison with baseline methods on public datasets (best marked in bold). Our proposed G2Rec consistently outperforms all baseline methods on all four datasets.\", \"page\": 7, \"index\": 1, \"width\": 979, \"height\": 560}]"
motivation: 现有生成式推荐方法无法同时有效整合复杂用户行为与物品语义上下文，图方法有扩展性问题，语义标记化缺乏监督。
method: 提出G2Rec框架，将基于图的全局用户协同建模与语义标记化统一，学习用户兴趣原型。
result: 在线部署和多个公开数据集实验验证了G2Rec优于现有方法。
conclusion: G2Rec能够更全面准确地建模用户行为上下文，提升生成式推荐性能。
---

## 摘要
生成式推荐是一种新兴范式，在工业推荐系统中展现出前景，旨在从用户历史行为中预测其下一次交互。生成式推荐的核心是物品分词化，它连接了物品语义与推荐模型。然而，现有方法通常难以有效地组织并将复杂的用户行为和物品语义上下文同时注入推荐模型。一方面，现有的基于图的集成方法，如图序列化和图神经网络，要么存在可扩展性问题，要么仅利用局部图信息。另一方面，现有的语义分词化方法通常依赖启发式规则，缺乏明确的监督信号，可能导致不准确或次优的语义表示。为了解决用户兴趣上下文建模中的这些限制，我们提出了G2Rec，一个可扩展的框架，它统一了基于图的整体用户协同建模与语义分词化，用于工业规模的生成式推荐。总体而言，G2Rec使推荐模型能够捕捉整体且基于语义的用户兴趣原型，而无需真实用户兴趣，从而在工业序列推荐中提供更全面、更准确的用户行为上下文建模。跨产品表面的在线部署以及在公开数据集上的大量实验证明了G2Rec相对于现有方法的优越性。

## Abstract
Generative recommendation is an emerging paradigm that has shown promise in industrial recommendation systems, aiming to predict users' next interactions from their historical behaviors. At the core of generative recommendation lies item tokenization, which bridges item semantics and recommendation models. However, existing methods often struggle to effectively organize and inject complex user-behavioral and item-semantic contexts into recommendation models simultaneously. On the one hand, existing graph-based integration methods, such as graph serialization and graph neural networks, either suffer from scalability issues or exploit only local graph information. On the other hand, existing semantic tokenization methods typically rely on heuristics and lack explicit supervision signals, which may lead to inaccurate or suboptimal semantic representations. To address these limitations in user interest context modeling, we propose G2Rec, a scalable framework that unifies holistic graph-based user co-engagement modeling with semantic tokenization for industrial-scale generative recommendation. Overall, G2Rec enables recommendation models to capture holistic and semantically grounded user interest prototypes without requiring ground-truth user interests, thereby providing more comprehensive and accurate modeling of user behavior contexts in industrial sequential recommendation. Online deployment across product surfaces and extensive experiments on public datasets demonstrate the superiority of G2Rec over existing methods.

---

## 论文详细总结（自动生成）

## 论文核心问题与整体含义（研究动机和背景）

- **动机**：生成式推荐（Generative Recommendation）通过预测用户下一次交互实现个性化推荐，其核心是物品标记化（Item Tokenization），用于连接物品语义与推荐模型。然而现有方法存在两大不足：
  - 基于图的集成方法（如图序列化、GNN）面临可扩展性问题或仅利用局部图信息，无法有效捕捉全局用户行为结构。
  - 语义标记化方法通常依赖启发式规则，缺乏明确的监督信号，导致语义表示不准确或次优。
- **背景**：工业级推荐系统需要同时整合复杂的用户行为上下文和物品语义，但现有方法难以兼顾。因此需要一种既具备全局图结构建模能力，又能生成有监督语义令牌的可扩展框架。

## 方法论：核心思想与关键技术细节

- **核心思想**：提出G2Rec（Sparse Co-Engagement Graph Schema for Generative Recommendation）框架，通过构建稀疏的物品-物品共同参与图，并设计可扩展的软图聚类算法提取兴趣原型，再将这些兴趣原型注入生成式序列推荐模型。
- **关键技术细节**：
  1. **稀疏共同参与图构建**：基于用户历史交互，构建物品-物品共同参与图（Co-engagement Graph），边表示两个物品被同一用户交互过。通过采样每条用户交互序列中的`O(m log M)`个共同参与来稀疏化图，理论上保证Laplacian矩阵的近似保持（复杂度接近线性`O(M log M)`）。
  2. **可扩展软图聚类**：提出可微分的软模块度（Soft Modularity）目标函数，允许每个物品分配一个软聚类成员分布（而非硬分配），聚类表示兴趣原型。该目标函数可GPU加速，每轮复杂度`O(ρM log M)`（ρ是稀疏常数）。
  3. **物品兴趣画像生成**：利用软聚类得到的聚类成员分布作为物品的兴趣画像（Interest Profile），并以此计算兴趣原型嵌入（加权平均物品嵌入）和兴趣画像令牌（加权平均原型嵌入）。
  4. **序列格式与训练目标**：将用户交互序列扩展为交替包含物品嵌入和兴趣画像令牌的序列（`[BOS, x_i1, y_i1, ..., x_iN, y_iN]`）。训练目标包括下一物品预测的交叉熵损失和兴趣画像预测的交叉熵损失（以软聚类成员分布作为软标签），两者加权结合。

## 实验设计

- **数据集与场景**：
  - **公开数据集**：Amazon子类 Beauty、Sports、Toys 以及 Yelp 数据集。统计信息见表1（用户数约1.9万-3万，物品数1.2万-2万，交互数16万-31万，稀疏度>99.9%）。
  - **在线部署**：在 Meta 的多个产品表面（如 Instagram Reels）进行大规模 A/B 测试（7天短期和长期实验）。
- **基准方法**：
  - **经典方法**：POP（基于流行度）、MF（矩阵分解+BPR损失）。
  - **序列/标记化方法**：GRU4Rec、SASRec、BERT4Rec、Caser、EAGER（两流生成式推荐）。
  - **图方法**：LightGCN、HeLLM（超图增强LLM推荐）。
- **评估协议**：留一法（leave-one-out），每个用户最后一次交互作为测试，倒数第二次作为验证，其余训练。测试时对每个用户随机采样99个负样本与1个正样本排序。指标：Recall@1/5/10、NDCG@5/10、MRR。

## 资源与算力

- **模型**：使用 Llama 2 13B 作为推荐模型骨干，采用 LoRA（秩16，dropout 0.05）微调3个epoch。
- **优化**：Xavier初始化，Adam优化器，学习率0.0003，余弦学习率调度，100步预热。
- **其他**：序列长度限制为50，SASRec嵌入维度64。
- **算力说明**：论文未明确提及使用的GPU型号、数量或训练总时长。仅在效率研究中报告了单批次的训练/推理时间（Beauty数据集，批处理时间约0.8秒训练，0.056秒推理），未给出整体训练时间。

## 实验数量与充分性

- **实验数量**：
  - 离线实验：在4个公开数据集上对比了10种方法（含G2Rec），每个数据集报告6个指标，共24组对比。
  - 在线实验：在Meta产品表面进行多轮A/B测试（短期7天和长期）。
  - 消融实验：2组（软 vs 硬聚类，兴趣画像损失权重λ的影响）。
  - 效率研究：1组（训练/推理时间对比）。
- **充分性评估**：
  - 覆盖经典、序列、图方法，基准选择合理。
  - 使用标准留一法评估和随机负采样，指标全面。
  - 消融实验验证了软聚类和兴趣画像损失的有效性，实验较为充分。
  - 在线实验提供了实际工业场景验证，增强说服力。
  - 但公开数据集稀疏度极高，缺少对中等密度或长序列场景的测试；另外，负采样策略（随机99个负例）可能低估真实世界分布，与工业全量排序有差距。

## 主要结论与发现

- G2Rec在所有四个公开数据集上，在Recall、NDCG、MRR等所有指标上均显著优于所有基线方法，平均排名第一（平均排名1.04，远优于第二名GRU4Rec的2.46）。
- 在线A/B测试中，G2Rec使多项用户参与指标（如停留时长、点赞、分享）提升0.06%-0.19%，并改善了内容多样性和服务效率。
- 软图聚类在模块度上显著优于硬聚类（如Sports数据集提升23.8%）。
- 兴趣画像预测损失对推荐性能有正向贡献，且其权重越大效果越好（λ>0时MRR显著高于λ=0）。
- 引入兴趣画像令牌造成的计算开销几乎可忽略（训练每批多0.043秒，推理多0.0027秒）。

## 优点

- **理论严谨性**：对图稀疏化给出了Laplacian近似保持理论保证（定理2），软模块度具有封闭形式（命题3）和线性复杂度（命题4）。
- **工业可部署性**：离线图聚类/画像是周期性计算，不影响在线推理延迟；硬件的GPU加速支持；实际在Meta大规模产品上线并取得正向收益。
- **创新性**：将软图聚类与生成式序列推荐有机结合，利用软聚类作为监督信号（软标签）训练兴趣预测，无需真实兴趣标注。
- **实验充分性**：离线+在线+消融+效率实验，对比多种类型方法，结果一致且显著。

## 不足与局限

- **公开数据集局限性**：仅使用高度稀疏的评论数据集（Beauty等），未涵盖其他推荐场景（如新闻、视频流、音乐），且用户行为单一（仅隐式反馈）。未见在中等密度或长序列数据集上的结果。
- **负采样策略**：随机采样99个负例与工业全量排序差距大，可能高估相对性能。虽然在线实验弥补，但离线部分未使用更全面的负采样（如全量候选或动态负采样）。
- **冷启动问题**：论文假设所有物品都有交互（避免冷启动），但未讨论新物品/新用户场景下的处理方案。
- **可解释性**：兴趣原型是隐式的，难以解释其具体语义含义；虽然提到“兴趣原型”但未提供案例分析。
- **超参数敏感性**：兴趣原型数量由分辨率γ控制，但实验只给出了数据集对应的γ值（Beauty/Toys:0.8，Sports/Yelp:1.0），未做更多的灵敏度分析。
- **计算资源细节缺失**：未提及训练G2Rec（尤其是Llama 2 13B微调）的总GPU小时数或GPU型号，不利于复现和成本评估。
- **在线实验细节保密**：由于公司政策，不能公开具体设置和指标绝对值，削弱了独立验证能力。

（完）
