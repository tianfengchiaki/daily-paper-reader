---
title: "MAF: Multimodal Adaptive Few-shot Prompting for Sentiment Analysis with MLLMs"
title_zh: MAF：面向多模态大语言模型情感分析的多模态自适应少样本提示
authors: Hangling Xie
date: 2026-06-14
pdf: "https://arxiv.org/pdf/2606.15694v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 多模态自适应少样本提示用于情感分析
tldr: 多模态大语言模型（MLLMs）在情感分析中表现出色，但对提示设计敏感，静态提示难以捕捉输入差异。本文提出多模态自适应少样本提示（MAF）框架，动态检索与查询相关的示范，并引入唇部运动检测、轻量级权重生成网络和多数投票，提升情感分析性能。实验证明MAF显著优于基线，与强基线竞争力相当。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15694v1/fig-001.webp\", \"caption\": \"Fig. 4. Impact of the number of retrieved demonstrations (K) and the number of voting candidates (M) on MELD and CH-SIMS v2.0. The best trade-off is achieved with K = 3 and a stable voting setting around M = 8.\", \"page\": 11, \"index\": 1, \"width\": 652, \"height\": 350}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-002.webp\", \"caption\": \"Fig. 1. Illustration of sentiment analysis results from different prompts using ChatGLM3-6B. MAF adaptively retrieves multimodal demonstrations aligned with the query, yielding more accurate and consistent predictions.\", \"page\": 2, \"index\": 2, \"width\": 725, \"height\": 190}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-003.webp\", \"caption\": \"Table 2. Performance comparison of MAF against baseline methods. Within each backbone block, the best results are highlighted in bold.\", \"page\": 10, \"index\": 3, \"width\": 725, \"height\": 561}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-004.webp\", \"caption\": \"Fig. 2. The overview of the MAF framework.\", \"page\": 4, \"index\": 4, \"width\": 725, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2606.15694v1/fig-005.webp\", \"caption\": \"Fig. 3. Illustration of the few-shot prompt used in our work.\", \"page\": 5, \"index\": 5, \"width\": 725, \"height\": 519}]"
motivation: 静态、统一应用的提示无法充分捕捉输入中变化的细微多模态线索，导致MLLMs在情感分析中的性能不稳定。
method: MAF框架包括：唇部运动幅度检测识别说话人、轻量级系数生成网络实时输出查询条件融合权重，加权聚合多模态相似度检索top-K示范，并通过多数投票增强稳定性。
result: 在公开基准上，MAF相比对应骨干变体性能显著且一致提升，与强多模态情感分析基线保持竞争力。
conclusion: 多模态自适应少样本提示能有效激发MLLMs的情感推理能力，为情感分析提供更鲁棒的解决方案。
---

## 摘要
多模态大语言模型（MLLMs）在理解复杂多模态内容方面展现了卓越的能力。然而，它们在情感分析中的性能对提示设计高度敏感，导致静态、统一应用的提示本质上是次优的，无法捕捉因输入而异的多模态细微线索。为解决这一局限，我们提出了一种多模态自适应少样本提示（MAF）框架，该框架动态检索并整合与查询相关的示例，以上下文敏感的方式激发MLLMs的情感推理能力。MAF构建了一个示例检索模块，整体编码面部表情、场景上下文和文本语义，并引入嘴唇运动幅度检测机制以实现多人场景下的准确说话者识别。与传统的固定权重融合不同，我们训练了一个轻量级系数生成网络，该网络能实时输出与查询条件相关的融合权重，从而对多模态相似度分数进行加权聚合，以检索最具信息量的前K个示例。通过MLLM生成的多个候选输出进行多数投票，进一步增强了预测稳定性。在公开基准数据集上的大量实验表明，MAF相较于相应的骨干变体取得了显著且一致的性能提升，并且与强大的多模态情感分析基线保持竞争力。

## Abstract
Multimodal large language models (MLLMs) have demonstrated remarkable capabilities in understanding complex multimodal content. However, their performance in sentiment analysis exhibits acute sensitivity to prompt design, rendering static, uniformly applied prompts inherently suboptimal for capturing the nuanced multimodal cues that vary across inputs. To address this limitation, we propose a Multimodal Adaptive Few-Shot Prompting (MAF) framework, which dynamically retrieves and integrates query-relevant demonstrations to elicit the sentiment reasoning capabilities of MLLMs in a context-sensitive manner. MAF constructs a demonstration retrieval module that holistically encodes facial expressions, scene context, and textual semantics, with a lip movement amplitude detection mechanism introduced for accurate speaker identification in multi-person scenarios. Departing from conventional fixed-weight fusion, a lightweight coefficient generation network is trained to output query-conditioned fusion weights in real time, enabling weighted aggregation of multimodal similarity scores to retrieve the top-K most informative demonstrations. Prediction stability is further enhanced through majority voting over multiple candidate outputs generated by the MLLM. Extensive experiments on public benchmark datasets demonstrate that MAF achieves substantial and consistent performance improvements over the corresponding backbone variants and remains competitive with strong multimodal sentiment-analysis baselines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：多模态大语言模型（MLLMs）在情感分析任务中表现出色，但其性能对**提示设计**高度敏感。静态、统一应用的提示无法充分捕捉跨输入变化的细微多模态线索，导致预测不稳定且次优。
- **整体含义**：本文提出**多模态自适应少样本提示（MAF）** 框架，通过动态检索与查询相关的多模态示范（demonstrations），并自适应融合面部、场景和文本特征，有效激发MLLMs的上下文敏感情感推理能力，提升准确性和鲁棒性。

## 2. 方法论：核心思想、关键技术细节、算法流程

- **核心思想**：采用**检索增强生成（RAG）** 范式，为每个查询动态选择最相关的多模态示范嵌入提示模板，并通过可学习的自适应权重融合与多数投票机制增强预测稳定性。
- **关键技术细节**：
  - **混合示范检索模块**：从预构建的示范库中检索前K个最相关示范。特征提取包括：
    - 面部特征（OpenFace 3.0提取，含基于嘴唇运动幅度检测的说话人定位）；
    - 场景特征（PlaceCNN提取）；
    - 文本特征（LLM文本编码器 + 最大池化 + 归一化）。
    - 计算查询与每个示范之间的三类特征相似度（内积），然后加权融合得到排序分数 \( s_{\text{rank}} = \alpha_1 s_{\text{face}} + \alpha_2 s_{\text{scene}} + \alpha_3 s_{\text{text}} \)。
  - **自适应系数生成网络**：一个轻量级两层MLP（256维隐藏层），以查询的联合特征向量（4096维）为输入，输出C（=66）个预定义权重组合的概率。采用多标签二元交叉熵损失训练，预测每个权重组合是否正确。推理时选择概率最高的组合作为融合系数。
  - **多数投票模块**：MLLM为同一提示生成M个候选情感标签（通过概率采样），然后采用多数投票确定最终预测，以平滑输出波动。
- **算法流程**：给定查询（图像序列+文本）→ 提取多模态特征 → 自适应系数生成器输出权重 → 加权融合多模态相似度 → 从示范库检索top-K示范 → 构造包含示范的提示 → MLLM生成M个候选标签 → 多数投票得到最终预测。

## 3. 实验设计

- **数据集**：三个公开基准数据集：
  - **CMU-MOSEI**（英文独白，23,453样本，情感强度[-3,+3]）
  - **CH-SIMS v2.0**（中文电影/电视剧，4,361样本，[-1,+1]）
  - **MELD**（英文多说话人对话，13,707样本，7个情绪类别）
- **Benchmark**：报告了各数据集的标准指标（MAE、Corr、Acc-7、Acc-2、F1等），并与其他多模态情感分析方法对比。
- **对比方法**：包括传统方法（TFN、LMF、MulT）、轻量级方法（MAG-BERT、Self-MM、AV-MC、UFEN-MTFN、TextMI、CHFN、UniMSE）、MLLM相关方法（UniSA系列、MMGCN、MM-DFN、EmoKEG、GA2MIF、MSE、ATGFB），以及基于不同骨干（Qwen-1.8B、LLaMA2-7B、ChatGLM3-6B）的MSE和ATGFB变体。

## 4. 资源与算力

- 文中明确提到：所有实验运行在**NVIDIA GeForce RTX 4090**上。
- **未说明**：具体使用的GPU数量、训练时长、总计算量。仅提到“所有实验都使用了固定种子以保证可重复性”，但未提供训练系数生成网络的具体迭代次数或时间。

## 5. 实验数量与充分性

- **实验组数**：
  - **主要结果**：三个数据集 × 三种骨干变体（Qwen/LLaMA2/ChatGLM3）的MAF与MSE/ATGFB对比，共9组核心对比。
  - **消融实验**：一个表包含5种消融设置（w/o RAG、w/o adaptive retrieval、w/o adaptive weighting、w/o voting、完整MAF），在三个数据集上各报告5个指标。
  - **超参分析**：分析了K（1~5）和M（1~10+）对MELD和CH-SIMS v2.0的影响（两个子图）。
  - **总计**：约3种数据集 × 5种消融 × 5个指标 + 主表9组 + 两个超参曲线。实验数量较充分。
- **充分性与公平性**：
  - 对比方法覆盖了传统和现代基线，并保持骨干一致（在MSE/ATGFB框架下比较）。
  - 消融实验设计合理，逐步验证各模块贡献。
  - 但未在更多MLLM骨干上测试（如GPT-4V），也未进行跨数据集泛化实验。
  - 评估指标多样，但MELD仅报告Acc和WF1，略显单一。

## 6. 主要结论与发现

- **MAF框架在所有三个数据集上均显著且一致地提升了对应骨干变体的性能**，且在多数指标上优于或匹敌强基线。
- **自适应检索质量**：使用错误的示范会误导模型，自适应检索比固定策略更可靠。
- **自适应权重融合**：动态融合比固定融合更适应多样的情感表达模式，尤其在回归指标（MAE）上改善明显。
- **多数投票**：有效平滑概率波动，显著提升预测稳定性，尤其是在MOSEI和CH-SIMS v2.0上。
- **超参影响**：最佳检索数量K=3，投票数M=8后增益边际递减。

## 7. 优点

- **方法新颖性**：将RAG思想引入多模态情感分析，并提出自适应权重生成网络，实现了查询驱动的个性化示范选择。
- **技术细节**：基于嘴唇运动幅度检测实现多人场景下的说话人定位，提升了面部特征提取的准确性。
- **轻量高效**：系数生成网络仅为一个两层MLP，训练成本低，推理时快速；多数投票通过并行API调用几乎不增加延迟。
- **泛化性验证**：在中文、英文独白、英文多说话人对话三种场景均有效，证明框架具有跨语言、跨任务鲁棒性。
- **消融彻底**：详细分析了各组件贡献，支持了方法设计的合理性。

## 8. 不足与局限

- **实验覆盖偏少**：仅在三种骨干上测试，未涵盖更大规模或更先进的MLLMs（如GPT-4V、Qwen2-VL）。同时，未在纯音频或更多模态（如语音）上验证。
- **算力细节缺失**：未报告训练系数生成网络的具体耗时、GPU数量，影响可复现性评估。
- **数据集局限性**：MELD实验仅使用Acc和WF1，缺少细粒度指标（如F1 per class）；CH-SIMS v2.0样本量较小（4361），可能存在偏差。
- **应用限制**：依赖预定义权重组合离散化（66种），可能无法覆盖所有最优融合；示范库大小为1000，扩展性未充分验证。
- **公平性风险**：文中提到“Ties are resolved randomly”，可能引入随机性，影响小样本实验稳定性。

（完）
