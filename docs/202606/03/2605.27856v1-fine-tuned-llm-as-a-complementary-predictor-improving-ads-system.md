---
title: Fine-Tuned LLM as a Complementary Predictor Improving Ads System
title_zh: 微调大语言模型作为补充预测器改进广告系统
authors: "Hui Yang, Daiwei He, Kevin Jiang, Taejin Park, Kungang Li, Jiajun Luo, Yuying Chen, Xinyi Zhang, Sihan Wang, Haoyu He, Yu Liu, Lakshmi Manoharan, David Xue, Shubham Barhate, Runze Su, Duna Zhan, Ling Leng, Siping Ji, Jinfeng Zhuang, Alice Wu, Leo Lu, Han Sun, Zhifang Liu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27856v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 针对广告预测微调大语言模型
tldr: 广告推荐系统受益于LLM的进展但实际应用少。本文提出微调开源LLM作为广告商预测器，从用户资料和历史预测广告商，增强候选生成并为下游排序提供先验。在大规模生产系统中实现，离线改进显著且带来在线业务提升，验证了LLM在广告中的有效性，并展示了辅助预测的端到端增益。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27856v1/fig-001.webp\", \"caption\": \"Figure 2: Serving architecture for large-scale inference. The deployment uses a distributed vLLM + Ray stackwith batching, prefix caching, fault tolerance, and incremental updates for efficient daily inference.\", \"page\": 5, \"index\": 1, \"width\": 482, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.27856v1/fig-002.webp\", \"caption\": \"Table 4: Feature ablation on the zero-shot 4B setup. Negative values indicate performance drops relative to the baseline.\", \"page\": 6, \"index\": 2, \"width\": 452, \"height\": 302}]"
motivation: 利用LLM的世界知识和预测能力改进广告推荐系统，尤其是作为辅助预测器而非直接排序器。
method: 微调开源LLM作为广告特定的辅助预测器，从用户画像和历史中预测可能的广告商，增强候选生成并提供先验给下游排序。
result: 在大规模生产广告系统中，方法产生显著的离线改进和可测量的在线业务影响。
conclusion: LLM作为辅助预测器能有效提升广告系统的检索和排序，为实现LLM增强推荐规模化提供了实用路径。
---

## 摘要
推荐系统支撑着信息流、广告和短视频平台的用户参与和变现，但将大语言模型的最新进展应用于推荐系统的增益仍然罕见，尤其是在广告和生产规模的真实行业场景中。以往真实世界的LLM成功案例通常分为三类：(a) 生成式检索，直接预测下一个候选生成项；(b) 使用LLM的后期重排序；(c) 利用LLM增强辅助信号。我们提出了一种广告领域的补充范式：一个微调的开源LLM，不作为排序器，而是作为广告特定的辅助预测器，根据用户画像和历史预测可能的广告主。这种LLM驱动的广告主预测增强了传统的候选生成过程，并为下游排序提供了信息丰富的先验。该方法在大规模生产广告系统中开发，产生了显著的离线改进和可衡量的在线业务影响，证明LLM的世界知识和预测能力可以被高效利用。除了验证LLM在广告应用中的有效性，我们的结果表明，有针对性的辅助预测可以在检索和后期排序阶段实现端到端收益，为大规模LLM增强推荐提供了一条实用路径。

## Abstract
Recommendation systems power engagement and monetization across feeds, ads, and short-video platforms, but translating the latest advances in Large Language Models into Recommendation Systems (RecSys) gains remains rare, particularly in advertising and production-scale real-world industry setups. Prior real-world LLM successes typically fall into three buckets: (a) generative retrieval that directly predicts the next items for candidate generation, (b) late-stage re-ranking that uses LLMs, and (c) auxiliary signal enrichment with LLMs. We introduce a complementary paradigm for ads: a fine-tuned open-source LLM used not as a ranker, but as an ads-specific ancillary predictor, forecasting likely advertisers from user profiles and histories. This LLM-driven advertiser prediction augments conventional candidate generation and provides informative priors to downstream ranking. Developed in a large-scale production advertising system, our approach produces substantial offline improvements and measurable online business impact, demonstrating that LLM world knowledge and predictive capacity can be efficiently harnessed. Beyond validating LLMs for ads applications, our results show that targeted ancillary predictions can unlock end-to-end gains across both retrieval and late-stage ranking, offering a practical path to LLM-enhanced recommendation at scale.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：推荐系统（尤其是广告推荐系统）受益于大语言模型（LLM）的快速发展，但将LLM直接部署到工业级广告推荐系统中仍面临挑战。主要障碍包括：工业推荐器严重依赖稀疏的ID特征（用户ID、商品/广告主ID等），与LLM的语义表示不匹配；LLM的参数规模和推理成本难以满足实时服务的延迟和成本约束；广告推荐中广告主实体、定向约束和转化历史的特殊性增加了与纯文本语义的错位。
- **研究动机**：现有LLM在推荐中的成功主要局限于三类：(a) 生成式检索（直接预测下一个候选项）；(b) 后期重排序（使用LLM对紧凑列表重排）；(c) 辅助信号增强（用LLM推断侧信息作为特征）。但在广告领域，大规模、可验证的业务影响仍然罕见。论文希望探索一种互补范式：不将LLM作为直接排序器，而是作为广告特定辅助预测器，利用LLM的世界知识和预测能力来改进现有广告系统。
- **整体含义**：本文提出并验证了一种实用的、生产级的LLM增强广告系统方案，表明大规模LLM可以低成本、低风险地整合到现有工业流水线中，在检索和排序两个阶段带来端到端业务收益，为LLM在广告推荐中的规模化应用提供了新路径。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：微调一个开源LLM，使其作为广告特定的辅助预测器，根据用户画像和行为历史预测最可能发生转化的广告主（以及用户兴趣）。这些预测结果被注入到广告系统的两个关键阶段：
    1.  **检索阶段**：作为广告主定向过滤，驱动一个基于LLM的候选生成器（LLM-based Candidate Generator, CG），补充现有检索通道。
    2.  **排序阶段**：作为额外特征输入下游转化模型（如ctcvr, vtcvr），提升预测精度。

- **关键技术细节**：
    - **数据流水线**：
        - **用户选择**：每日推理限制为高价值用户（过去90天内有活跃转化的美国Pinterest用户），以控制成本。
        - **标签构造**：预测窗口（第x+1天至x+7天）内第一个发生转化的广告主，采用下一个广告主预测任务形式。
        - **用户特征编译**：构建结构化用户表示，包括用户画像（年龄、性别、用户状态）和行为序列（站内搜索、站外归因转化、站外匹配转化、站外搜索相关、URL、顶部类别/品牌等）。使用两个广告主池：过去转化的活跃广告主和预设的每日高营收美国广告主池。
    - **提示设计**：三阶段不同提示模板：
        - **SFT阶段**：预测单个下一个广告主，输出纯文本。
        - **GRPO阶段**：预测20个排名广告主和5个用户兴趣，输出结构化XML。
        - **推理阶段**：与GRPO输出格式一致。
    - **后训练策略**：
        - **监督微调（SFT）**：在单广告主预测任务上训练。
        - **基于GRPO的强化学习**：在SFT基础上进一步优化，奖励函数 \( R_{\text{total}} = R_{\text{match}} - P_{\text{adv\_len}} - P_{\text{interest\_len}} \)。其中匹配奖励 \( R_{\text{match}}(i) = 0.1 \times (20-i) + 2.0 \)（若排名前4）或0；长度惩罚 \( P_{\text{len}}(n,n^*) = 0 \)（若相等），否则 \(\min(0.1 \times |n-n^*|, 1.0) + 1.0\)。
    - **语义ID增强（SID）**：通过多阶段继续预训练（CPT）将SID知识融入LLM，SID来自多模态PinCLIP嵌入和RQ-VAE量化。三阶段：第一阶段冻结除SID嵌入外的参数；第二阶段全参数预训练混合推荐和通用数据；第三阶段任务特定微调（预测下一个广告主或下一个SID）。
    - **服务与生产化**：使用分布式vLLM + Ray栈进行大规模批量推理，支持前缀缓存、分页注意力、连续批处理。采用虚拟epoch和检查点实现容错，增量推理仅对活动变动的用户重新计算。

## 3. 实验设计

- **数据集/场景**：论文使用Pinterest的内部生产数据。
    - **V0**：离线实验数据集，包含行为序列较长的用户（匹配转化和归因转化序列长度≥10）。
    - **V1**：与在线服务流量对齐的数据集（日期x的快照，标签为x+1至x+7的高优先级转化）。V1更接近生产环境但特征表现力较弱。
    - 在线实验在真实美国市场中进行，涵盖Home Feed、Related Pins、Search三种视图，仅限opt-in（选择加入）用户。
- **基准方法**：
    - Zero-shot prompting（零样本提示，基座模型直接预测）。
    - SFT + beam search / sampling。
    - GRPO（带/不带显式推理提示）。
    - 消融实验中，通过逐一删除特定特征（如活跃广告主、站外URL、站外搜索等）测量性能变化。
- **对比方法**：无外部公开基准，主要是内部对比不同后训练策略和特征组合。

## 4. 资源与算力

- 论文未明确说明使用的GPU型号、数量或训练时长。仅提及使用开源LLM（未指明具体模型大小，从“4B”的提及推测可能为4B参数模型），以及分布式vLLM + Ray栈用于推理。未提供训练算力细节。

## 5. 实验数量与充分性

- **实验数量**：论文进行了多组实验：
    - 广告主预测质量对比（V0和V1上，表2、表3）。
    - 特征消融实验（表4，共12种设置，包括删除单个特征、重新排序等）。
    - 下游转化模型影响实验（表5，两个模型ctcvr和vtcvr，报告ΔLoss、ΔAUC、ΔPR-AUC）。
    - 在线部署实验（表6，两个指标：US Shopping Slice RoAS和Opt-in US Shopping Slice RoAS）。
    - 此外提及了检索阶段实验（多种训练目标对比）和SID增强的离线对比。
- **充分性与公平性**：
    - 实验设计较为系统：有离线、在线、消融、下游评估，覆盖了论文声称的检索和排序两个阶段。
    - 使用用户级随机分割（9:1训练/评估）防止泄漏，公平。
    - 特征消融实验采用控制变量法，客观。
    - 在线实验有p值（p=0.021, p=0.029），统计显著性较好。
    - 不足之处：未与外部先进方法（如其他LLM推荐方法）进行横向对比；仅在一个平台（Pinterest）上验证，通用性待考证；V0和V1用户覆盖和特征质量不同，可能引入偏差。

## 6. 论文的主要结论与发现

- 后训练（SFT+GRPO）显著优于零样本提示，显式推理对广告主预测任务帮助不大，SFT+优化简单的训练即可接近最好效果。
- 特征消融表明：**活跃广告主（过去转化过的广告主）是最重要的输入特征**；站外URL和站外搜索次之；画像特征贡献较小；顶部类别特征可能引入噪声。
- 引入语义ID（SID）进一步提升广告主预测Recall（Recall@1从0.223提升至0.248，Recall@5从0.461提升至0.515，Recall@20从0.683提升至0.755），改善超过10%。
- LLM预测的广告主作为特征输入下游转化模型，显著提升PR-AUC（ctcvr +0.71%，vtcvr +1.64%），AUC也有微增。
- 在线部署LLM-based候选生成器后，美国购物广告的RoAS提升4.94%（全量用户）和6.69%（opt-in用户），p值显著。
- 核心结论：微调LLM作为辅助预测器是可行的生产级方案，能够在不替换现有堆栈的情况下，同时改善检索和排序阶段的性能。

## 7. 优点

- **实用性**：方法直接整合到现有生产系统中，不要求LLM成为主排序器，降低了风险。
- **巧妙的定位**：利用LLM的世界知识和文本理解能力预测广告主，而非直接处理稀疏ID或深度特征交叉，绕过LLM与ID特征不匹配的问题。
- **多阶段增益**：同时服务于检索和排序两个关键环节，端到端改进且可分解验证。
- **特征工程细致**：详细比较不同行为信号的重要性，提供实际指导（如强调站外URL和站外搜索）。
- **生产化考量周到**：用户选择策略、增量更新、分布式推理、容错机制等，确保大规模部署可行。
- **实验设计严谨**：包含离线、消融、下游和在线A/B测试，在线结果有统计显著性。

## 8. 不足与局限

- **平台依赖**：仅在Pinterest平台验证，方法可能依赖于特定广告生态（如美国购物广告、opt-in用户），通用性未证明。
- **缺失全量用户评估**：在线实验仅限opt-in用户，对普通用户的收益未知。
- **计算成本未披露**：未给出训练LLM所需GPU资源、时间、成本，读者难以评估可行性。
- **缺乏外部基准对比**：未与传统SOTA推荐方法（如仅用双塔检索、无LLM的模型）进行公平对比，难以量化“LLM部分”带来的绝对增益。
- **广告主预测任务简化**：只预测第一个转化广告主，忽略多广告主或跨广告主竞争关系，可能丢失信息。
- **语义ID增强未在线验证**：SID版本仅在离线评估中表现出色，缺乏在线业务影响数据，实际效果待确认。
- **召回指标覆盖有限**：离线广告主预测使用Recall@1/5/20，未报告精度或NDCG等更全面的排名指标。
- **潜在偏差风险**：用户选择（仅活跃转化用户）和特征构建（90天窗口、两周URL等）可能引入选择性偏差，影响预测的泛化能力。

（完）
