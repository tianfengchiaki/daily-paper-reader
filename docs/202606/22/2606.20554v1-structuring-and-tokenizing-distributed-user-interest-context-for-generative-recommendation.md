---
title: Structuring and Tokenizing Distributed User Interest Context for Generative Recommendation
title_zh: 面向生成式推荐的分布式用户兴趣上下文结构化与分词化
authors: "Ruizhong Qiu, Yinglong Xia, Dongqi Fu, Hanqing Zeng, Ren Chen, Xiangjun Fan, Hong Li, Hong Yan, Hanghang Tong"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20554v1"
tags: ["query:q1"]
score: 9.0
evidence: 面向广告的用户兴趣生成式推荐与令牌化
tldr: 生成式推荐是新兴范式，核心在于物品标记化。现有方法难以同时有效组织和注入用户行为与物品语义上下文。本文提出G2Rec，统一了基于图的用户协同建模与语义标记化，可扩展且无需真实用户兴趣，实现了更全面的用户行为上下文建模。在线部署和实验表明其优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20554v1/fig-001.webp\", \"caption\": \"Table 2 Comparison with baseline methods on public datasets (best marked in bold). Our proposed G2Rec consistently outperforms all baseline methods on all four datasets.\", \"page\": 7, \"index\": 1, \"width\": 979, \"height\": 560}]"
motivation: 现有生成式推荐方法在用户兴趣上下文建模方面存在图集成方法可扩展性差或只利用局部信息，以及语义标记化缺乏监督信号的问题。
method: 提出G2Rec框架，融合全局图结构的用户协同建模与语义标记化，通过可扩展方式捕捉用户兴趣原型。
result: 在多个产品表面的在线部署和公开数据集实验表明，G2Rec优于现有方法。
conclusion: G2Rec实现了更全面准确的用户行为上下文建模，适用于工业级序列推荐。
---

## 摘要
生成式推荐是一种新兴范式，在工业推荐系统中展现出潜力，旨在从用户历史行为预测其下一次交互。生成式推荐的核心在于物品分词化，它连接了物品语义与推荐模型。然而，现有方法往往难以同时有效地组织和注入复杂的用户行为及物品语义上下文。一方面，现有的基于图的集成方法，如图序列化和图神经网络，要么面临可扩展性问题，要么仅利用局部图信息。另一方面，现有的语义分词化方法通常依赖启发式规则，缺乏明确的监督信号，可能导致不准确或次优的语义表示。为解决用户兴趣上下文建模中的这些局限，我们提出G2Rec，这是一个可扩展的框架，将基于图的全局用户共同参与建模与语义分词化统一起来，适用于工业级生成式推荐。总体而言，G2Rec使推荐模型能够在无需真实用户兴趣标签的情况下，捕捉全局且具有语义基础的用户兴趣原型，从而在工业序列推荐中提供更全面、更准确的用户行为上下文建模。跨产品表面的在线部署以及公开数据集上的大量实验表明，G2Rec优于现有方法。

## Abstract
Generative recommendation is an emerging paradigm that has shown promise in industrial recommendation systems, aiming to predict users' next interactions from their historical behaviors. At the core of generative recommendation lies item tokenization, which bridges item semantics and recommendation models. However, existing methods often struggle to effectively organize and inject complex user-behavioral and item-semantic contexts into recommendation models simultaneously. On the one hand, existing graph-based integration methods, such as graph serialization and graph neural networks, either suffer from scalability issues or exploit only local graph information. On the other hand, existing semantic tokenization methods typically rely on heuristics and lack explicit supervision signals, which may lead to inaccurate or suboptimal semantic representations. To address these limitations in user interest context modeling, we propose G2Rec, a scalable framework that unifies holistic graph-based user co-engagement modeling with semantic tokenization for industrial-scale generative recommendation. Overall, G2Rec enables recommendation models to capture holistic and semantically grounded user interest prototypes without requiring ground-truth user interests, thereby providing more comprehensive and accurate modeling of user behavior contexts in industrial sequential recommendation. Online deployment across product surfaces and extensive experiments on public datasets demonstrate the superiority of G2Rec over existing methods.

---

## 论文详细总结（自动生成）

# 论文总结：面向生成式推荐的分布式用户兴趣上下文结构化与分词化

## 1. 论文的核心问题与整体含义（研究动机和背景）
生成式推荐（Generative Recommendation, GR）通过预测用户的下一次交互来提升个性化推荐体验，其核心在于物品分词化——将物品语义与模型输入连接起来。然而现有方法面临双重挑战：
- **图集成方法**：如图序列化（graph serialization）计算成本高，图神经网络（GNN）仅能利用局部子图信息，难以捕捉全局用户行为模式。
- **语义分词化方法**：通常依赖启发式目标，缺乏监督信号，导致语义表示不准确或次优。
为此，论文提出 **G2Rec** 框架，旨在**统一全局图结构的用户协同建模与语义分词化**，在工业级规模下实现对用户兴趣上下文的全面、准确建模，且无需真实兴趣标签。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 核心思想
- 构建物品-物品的**稀疏共同参与图**（sparsified item co-engagement graph）作为物品模式（schema）。
- 设计可扩展的**软聚类算法**（soft clustering），从图中提取**兴趣原型**（interest prototypes）和物品的**软隶属度分布**（interest profiles）。
- 将物品的兴趣原型嵌入（token）与原始交互序列交替排列，作为生成式推荐模型的输入序列，使模型同时学习物品转换和兴趣过渡模式。

### 关键技术细节
#### (a) 稀疏共同参与图构造
- 定义共同参与边：若用户 `u` 同时交互过物品 `i` 与 `j`，则 `(i,j)` 为一条边。
- 原始图边数为 O(M²)（M 为总交互次数），论文采用**每用户采样 m 条边**的策略，并以理论证明（Theorem 2）只需 **O(M log M)** 条边即可在 Loewner 序下近似保持拉普拉斯矩阵，保证了近乎线性的复杂度。

#### (b) 可扩展软图聚类算法
- 目标是为每个物品学习属于 C 个兴趣原型的**软隶属度分布** `p_i ∈ [0,1]^C`（和为1），参数化为 `p_i = softmax(z_i)`。
- 提出**可微模块度目标函数**（differentiable modularity） `Q_soft(P)`，将经典硬模块度 `Q_hard`（包含指示函数 1[h_i = h_j]）替换为期望形式得出闭式解：
  ```
  Q_soft(P) = (1/|E|) * Σ_{(i,j)∈E} p_i^T p_j  - γ * ∥P^T k∥₂² / |E|²
  ```
  其中第一项鼓励高频共现的物品对具有高同簇概率，第二项基于 Newman–Girvan 零模型进行惩罚。
- 该目标可基于 GPU 高效优化，每轮时间复杂度 **O(ρ M log M)**（ρ为软隶属度分布的非零元素数）。

#### (c) 兴趣原型嵌入与序列格式化
- 每个原型嵌入 `v_a` 为该原型下所有物品嵌入的加权平均（权重为隶属度）。
- 每个物品的兴趣配置文件令牌（interest profile token）`y_i` 为其所属原型嵌入的加权平均。
- 交互历史序列改写为：`R_u = [<BOS>, x_{i1}, y_{i1}, ..., x_{iN}, y_{iN}]`，即将物品嵌入与其兴趣令牌交替排列。
- 训练采用两项损失之和：
  - **物品预测损失** `L_item`：预测下一物品。
  - **兴趣配置损失** `L_profile`：以物品的兴趣配置作为软标签，预测当前交互的兴趣原型分布。

## 3. 实验设计
### 数据集
- **公开数据集**：Beauty、Sports、Toys（来自 Amazon 评论）、Yelp（2019年至今）。每个数据集均高度稀疏（>99.9%），采用5-core过滤，按时间排序并留一法划分（最后一项为测试，倒数第二项为验证）。
- **工业环境**：Meta 多个产品表面（如 Instagram Reels）的在线 A/B 测试。

### Benchmark 与对比方法
共对比 9 个基线方法，分为三类：
- **经典方法**：POP（流行度）、MF（BPR loss）。
- **序列/分词方法**：GRU4Rec、SASRec、BERT4Rec、Caser、EAGER（两流生成式推荐）。
- **图方法**：LightGCN（图协同过滤）、HeLLM（超图增强LLM推荐）。

### 评估指标
- 召回率（Recall@1/5/10）、归一化折损累计增益（NDCG@5/10）、平均倒数排名（MRR）。
- 测试时每用户 1 个正样本 + 99 个随机负样本。

### 实现细节
- 推荐模型主干为 **Llama 2 13B**，使用 LoRA（rank=16, rank dropout=0.05）微调 3 个 epoch。
- 优化器：Adam，学习率 3e-4，余弦调度含100步预热。
- 物品嵌入维度 d=64，最大序列长度 50。

## 4. 资源与算力
论文**未明确报告**具体的 GPU 型号、数量及总训练时长。仅提及使用 Llama 2 13B 作为 backbone，并在 GPU 上运行软聚类优化。同时提到了工业部署时使用自研的高性能图处理引擎进行离线分布式执行，但未公开集群规模。

## 5. 实验数量与充分性
### 实验数量
- **公开数据集实验**：4个数据集 × 6个指标 × 10种方法（含G2Rec），完整结果在 Table 2 和 Figure 1。
- **效率实验**：Table 3 展示了训练/推理每批时间对比（仅 Beauty 数据集）。
- **消融实验**：
  - Table 4：软聚类 vs 硬聚类（Leiden）在4个数据集上的模块度对比。
  - Figure 2：兴趣配置损失权重 λ 的消融（MRR 变化）。
- **在线 A/B 测试**：7天短期 + 长期实验，报告多个用户参与度指标的提升（0.06% ~ 0.19%）。

### 实验充分性评价
- **客观性**：使用了公开基准和标准的留一法评估协议，对比了足够广泛且具代表性的基线方法（经典/序列/图模型），涵盖多类指标。
- **公平性**：所有方法采用相同序列长度和留一法划分，G2Rec 超参数（γ, λ）在验证集上调优，未额外泄露信息。
- **局限性**：在线测试仅提供相对提升百分比，未给出绝对数值或置信区间，且“因公司政策无法公开详细设置”，降低了可复现性。

## 6. 论文的主要结论与发现
1. **性能领先**：G2Rec 在所有公开数据集上一致超越所有基线，例如在 Sports 上 NDCG@5 比最佳基线高出 14.9%；平均排名（1.04）远优于第二名（2.46）。
2. **软聚类优于硬聚类**：软模块度显著高于硬聚类（如 Sports 上提升 23.8%），证明软隶属度能更精细地捕捉物品的多原型归属。
3. **兴趣配置损失有效**：加入 `L_profile` 显著提升 MRR（λ=0 时最低），且随 λ 增大性能继续改善。
4. **计算开销极小**：训练仅增加 0.043 秒/批，推理仅增加 0.0027 秒/批，适合工业部署。
5. **在线效果显著**：多个用户参与度指标（时间花费、点赞、分享等）获得 0.06%~0.19% 的增量收益，且观察到内容多样性和服务效率的提升。

## 7. 优点
- **理论扎实**：给出了图稀疏化的近似保证（Theorem 2）和软模块度的闭式解（Proposition 3），复杂度分析清晰。
- **创新性结合**：将图结构信息（全局共同参与模式）与可微软聚类直接融入生成式推荐序列，而非仅仅作为预训练或特征嵌入。
- **可扩展性**：所有关键步骤（图构造、软聚类优化、令牌生成）均具有近乎线性的复杂度，可通过 GPU 加速。
- **工业验证**：在 Meta 多产品表面成功部署并取得显著线上收益，证明了实用性。
- **消融设计完整**：分别验证了软聚类、兴趣配置损失的作用，且效率实验量化了额外开销。

## 8. 不足与局限
- **缺乏算力细节**：未报告 GPU 型号、数量及总训练耗时，难以评估资源需求。
- **在线结果透明性不足**：仅给出相对百分比提升，未提供具体指标绝对值或统计显著性检验，且因政策限制未公布实验设置。
- **负采样影响**：测试采用 1+99 负采样，可能高估模型区分能力；未进行全排序评估。
- **超参数敏感度**：仅对 λ 进行了消融，但对聚类数 C、γ 等关键超参数的鲁棒性未作深入分析。
- **长尾覆盖问题**：虽然提到内容多样性改善，但未专门分析 G2Rec 对低频物品的推荐效果。
- **冷启动问题**：文中假设所有物品至少被一个用户交互过（`∪_u I_u = I`），未讨论全新物品的初始嵌入及兴趣配置如何获取。
- **可复现性**：软聚类优化细节（迭代次数、学习率等）未完全公开，工业部署的代码和配置属内部资源。

（完）
