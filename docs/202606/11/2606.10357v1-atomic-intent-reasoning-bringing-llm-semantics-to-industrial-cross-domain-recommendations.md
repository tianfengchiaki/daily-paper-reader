---
title: "Atomic Intent Reasoning: Bringing LLM Semantics to Industrial Cross-Domain Recommendations"
title_zh: 原子意图推理：将LLM语义引入工业跨域推荐
authors: "Zhuohang Jiang, Yuxin Chen, Shijie Wang, Haohao Qu, Zhou Jindong, Wenqi Fan, Li Qing, Dongxu Liang, Jun Wang"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10357v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 将LLM语义引入工业跨域推荐
tldr: "跨域推荐面临语义鸿沟和LLM高延迟问题。本文提出AIR框架，将LLM推理离线化，通过在线检索组合动态构建用户意图，实现400倍加速。在多个公开数据集上达到最优性能，在快手电商真实业务中GMV提升3.446%，验证了其工业级应用价值。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10357v1/fig-001.webp\", \"caption\": \"Figure 1: Kuaishou spans multiple heterogeneous domains, making cross-domain recommendation crucial for intent transfer, but facing two key challenges: (1) Prohibitive RealTime Inference Expanse of Large Language Models, and (2) Large-Scale and Noisy User Behavior Sequences.\", \"page\": 2, \"index\": 1, \"width\": 502, \"height\": 438}, {\"url\": \"assets/figures/arxiv/2606.10357v1/fig-002.webp\", \"caption\": \"Table 2: The Overall Recommendation Performance on the Amazon Dataset.\", \"page\": 7, \"index\": 2, \"width\": 852, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2606.10357v1/fig-003.webp\", \"caption\": \"Table 6: Industry Online A/B Test Results.\", \"page\": 8, \"index\": 3, \"width\": 509, \"height\": 403}, {\"url\": \"assets/figures/arxiv/2606.10357v1/fig-004.webp\", \"caption\": \"Figure 2: Our proposed AIR (Atomic Intent Reasoning) tackles cross-domain recommendation via a four-stage pipeline: (1) Atomic Intent Pair Generation, (2) Real-time Unified User Intent Tree Construction, (3) Target-aware Intent Retrieval, and (4) MHA-based Intent Extraction.\", \"page\": 4, \"index\": 4, \"width\": 1054, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2606.10357v1/fig-005.webp\", \"caption\": \"Figure 3: Online serving in AIR with separated offline/online serving stages. Offline, LLMs generate and index atomic behavior–intent pairs. Online, user behaviors are decomposed into parallel intent queries and efficiently retrieved to summarize user intent, achieving an effect equivalent to real-time LLM inference while supporting low-latency, high-QPS recommendation.\", \"page\": 5, \"index\": 5, \"width\": 1052, \"height\": 384}]"
motivation: 解决工业跨域推荐中不同领域间的语义鸿沟以及LLM在线推理延迟过高的问题。
method: 提出AIR框架，将LLM推理迁移至离线阶段，在线通过高效检索和组合动态构建用户意图表示。
result: "实现约400倍推理加速，在多个公开数据集上取得最优性能，快手电商在线A/B测试中GMV提升3.446%。"
conclusion: AIR方法有效提升跨域推荐效果，并具备工业级部署的实用价值。
---

## 摘要
跨域推荐是内容到电子商务平台的核心问题，其目标是利用用户与内容的交互来推断其在电商侧的潜在购买意图，从而提升转化率和商业价值。然而，在实际工业场景中，跨域推荐面临多重挑战：不同领域之间存在显著的语义鸿沟，用户跨域行为序列通常规模庞大且噪声丰富。尽管大型语言模型（LLM）拥有强大的语义理解和推理能力，但其毫秒级的推理延迟使得直接应用于在线推荐系统变得困难。为解决这些问题，本文提出了AIR（原子意图推理），一个专为工业级部署设计的LLM驱动的跨域推荐框架。通过将LLM推理迁移到离线阶段，并在在线操作期间通过高效检索与组合动态构建用户意图表示，该框架在保持语义一致性的同时实现了约400倍的推理加速。在多个公共数据集上的实验结果表明，我们的方法在跨域推荐任务中达到了最先进的性能。此外，在快手电商真实业务场景中开展的大规模在线A/B测试显示，我们的方法在多个核心业务指标上均取得了稳定且显著的提升，包括GMV提升+3.446%，充分验证了其在工业级推荐系统中的有效性和实用价值。

## Abstract
Cross-domain recommendation is a core problem in content-to-e-commerce platforms. Its objective is to leverage user interactions with content to infer potential purchasing intent on the e-commerce side, thereby enhancing conversion rates and commercial value. However, in real industrial scenarios, cross-domain recommendation faces multiple challenges: significant semantic gaps exist between different domains, and user cross-domain behavior sequences are often massive in scale and rich in noise. Although large language models (LLMs) possess powerful semantic understanding and reasoning capabilities, their millisecond-level inference latency makes direct application in online recommendation systems difficult. To address these issues, this paper introduces AIR (Atomic Intent Reasoning), an LLM-driven cross-domain recommendation framework designed for industrial-grade deployment. By migrating LLM inference to the offline phase and dynamically constructing user intent representations through efficient retrieval and composition during online operations, it achieves approximately 400* inference acceleration while maintaining semantic consistency. Experimental results across multiple public datasets demonstrate that our method achieves state-of-the-art performance in cross-domain recommendation tasks. Furthermore, large-scale online A/B testing conducted in Kuaishou E-commerce's real-world business scenarios shows that our approach delivers stable and significant improvements across multiple core business metrics, including a +3.446% increase in GMV, fully validating its effectiveness and practical value in industrial-scale recommendation systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题**：在内容到电子商务（如快手）的跨域推荐场景中，用户行为横跨多个异构域（如视频观看、直播、商品购买），不同域间存在显著的语义鸿沟。同时，用户跨域行为序列规模庞大、噪声丰富，使得传统基于ID协同的跨域推荐方法难以捕捉细粒度的意图转移。
- **挑战**：大型语言模型（LLM）虽然具备强大的语义理解和推理能力，但其在线推理延迟（毫秒级要求下不可接受）和成本（约8秒每次调用）使其无法直接应用于工业级在线推荐系统。
- **整体含义**：论文旨在设计一个LLM驱动的跨域推荐框架，在保持LLM级语义推理能力的同时，将推理延迟降低到工业可接受水平（毫秒级），从而在内容-电商平台上提升转化率和商业价值。

## 2. 方法论：核心思想、关键技术细节、算法流程

### 核心思想
提出**离线-在线混合策略**：离线阶段利用LLM将用户行为事件（含用户属性、对象描述）蒸馏为细粒度原子行为-意图对并缓存；在线阶段通过轻量级检索和组合实时构建用户意图表示，避免在线调用LLM，同时实现语义缓存的高效复用。

### 关键技术细节（四个阶段）

1. **Atomic Intent Pair Generation（原子意图对生成）**  
   - 将用户事件 `e = (action, object, user_profile)` 输入LLM，生成一组分层意图路径 `P(e) = {p1,...,pm}`，每条路径 `p = [c1 → c2 → ... → ck]` 是粗到细的类别轨迹。  
   - 按动作类型（click, long-view, order）构建意图缓存 `P_a = ∪_{e: a_e=a} P(e)`，供在线检索。

2. **Real-time Unified User Intent Tree Construction（实时统一用户意图树构建）**  
   - 在线从缓存中聚合用户近期行为历史的意图路径，动态构建层次化意图树。每个节点存储动作统计数据，并计算偏好权重：  
     - `w_u = log(1 + N(u)) * Σ_{k=1}^{n_u} exp(-λ Δt_k)`  
     - 其中 `N(u) = α*N_click(u) + β*N_order(u) + γ*N_view(u)`。  
   - 对数项稳定长尾频率，时域衰减强调近期行为。

3. **Target-aware Intent Retrieval（目标感知意图检索）**  
   - 将目标商品嵌入与意图节点嵌入计算余弦相似度 `s(u,v) = cos(e_u, e_v)`，选出Top-M候选节点。  
   - 对候选节点递归回溯到根节点形成意图链，并用评分函数 `score(u;v) = s(u,v) * g(w_u)`（`g(w)=log(1+w)`）排序，得到紧凑层次化意图链。  
   - 从节点关联的行为对象ID集合中检索支撑证据，并去噪（低语义相似度、陈旧、限定预算）。

4. **MHA-based Intent Extraction（基于多头注意力的意图提取）**  
   - 目标商品嵌入作为查询，检索到的意图链令牌嵌入（融合动作和时间特征）作为键/值，通过多头注意力得到目标条件化表示 `z_v = MHA(q_v, X, X)`。  
   - 该表示与用户/商品特征拼接后输入下游CTR模型或排序模型。

### 算法流程总结
离线：LLM解析行为→生成意图对→索引存入本地检索结构。  
在线：用户请求→分解行为序列为并行意图查询→从索引检索→动态构建意图树→目标感知检索提取高证据意图链→多头注意力融合→预测。

## 3. 实验设计

### 数据集
- **公开基准**：Amazon 2014数据集中的四个域构成两个跨域对：
  - Movie–Book（电影-书籍）
  - Food–Kitchen（食品-厨房）
  - 统计数据：用户约1万~10万，物品数万~数十万，平均序列长度8-9。
- **工业数据集**：来自快手电商平台，用户超4亿，物品数十亿，行为序列数千。

### Benchmark与对比方法
- **单域SR模型**：FPMC, Caser, GRU4Rec, SRGNN, FEARec, SASRec
- **跨域CDSR模型**：TPUF, π-Net, C2DSR, MGCL, LLMCDSR

### 评估协议
- 留一法（leave-one-out）：每个验证/测试实例随机采样999个负样本与正样本共同排序。
- 指标：Hit Ratio@10（HR@10）、NDCG@10。
- 数据划分：时间顺序80%/10%/10%训练/验证/测试，防止信息泄露。

### 实验设置
- 使用Qwen3-4B模型进行离线意图生成，BAAI/bge-m3嵌入模型进行语义映射。
- 剔除缺少文本元数据的物品，过滤低频用户/物品（Movie–Book阈值10，Food–Kitchen阈值5）。
- 保留重叠和非重叠用户，用固定时间窗口分割行为序列。

## 4. 资源与算力

- **未明确说明**：论文未报告离线LLM推理所使用的GPU型号、数量或训练时长。
- **仅提及**：离线阶段使用Qwen3-4B模型（约40亿参数）；在线推理延迟从直接调用LLM的约8秒降至20.134毫秒（约400倍加速）。但对离线阶段的算力消耗未做量化分析。

## 5. 实验数量与充分性

### 实验组数
- **主要对比实验**（表2）：在Movie–Book和Food–Kitchen两个场景下，与11个基线方法进行全面比较。
- **消融实验**（表3）：在Food–Kitchen场景下，移除MHA、Intent Retrieval、Intent Tree及其组合共4组变体。
- **类别稀疏性分析**（表4）：对比Base模型与AIR在4个层级上的HHI指标。
- **用户活跃度分析**（表5）：将用户分为低、中、高、超高4组，分别计算GMV提升率。
- **在线A/B测试**（表6）：在快手真实流量中部署，测量商业指标（付费订单数、GMV、GPM、OPM）。

### 充分性评价
- **优点**：数据集涵盖公开基准和工业级数据，消融实验覆盖关键组件，用户分群分析考虑了冷启动和长序列场景，在线A/B测试验证了实际效果。
- **不足**：
  - 消融实验仅在Food–Kitchen场景进行，未在Movie–Book上重复，可能无法完全排除场景影响。
  - 未进行超参数敏感性分析（如α, β, γ, λ, top-M等）。
  - 仅使用单一LLM模型（Qwen3-4B），未对比不同LLM（如GPT-4、Llama等）对结果的影响。
  - 未报告离线LLM生成意图对的准确率或覆盖率，存在语义偏差风险。
- **客观公平性**：采用了标准留一法、随机负采样、时间顺序分割，与基线方法设置对齐，对比结果可信。

## 6. 论文的主要结论与发现

1. **AIR在所有公开数据集上均达到SOTA**：在Movie–Book和Food–Kitchen场景中，HR@10和NDCG@10全面超越所有基线，尤其在非重叠用户场景下优势明显。
2. **消融实验确认组件必要性**：单独移除Intent Tree或Intent Retrieval导致HR@10下降17-27%，同时移除则性能崩溃（NDCG@10下降40-64%）。
3. **类别多样性提升**：AIR在所有层级上降低HHI（更均衡），尤其在细粒度层级（Level 3下降5.99%），促进更广泛的类别覆盖。
4. **跨用户活跃度鲁棒**：在低活跃度用户（+4.32%）和高/超高活跃度用户（+8.04%/+7.21%）均实现正收益，证明意图树有效缓解冷启动，且目标感知检索可压缩长序列噪声。
5. **在线A/B测试工业验证**：付费订单数+1.043%，GMV+3.446%，GPM+3.662%，OPM+1.254%，稳定且显著提升核心商业指标。

## 7. 优点

1. **高效解耦设计**：将LLM推理剥离到离线，在线仅需检索和组合，实现400倍加速，同时保持与实时LLM调用等效的语义推理能力。
2. **层次化意图建模**：意图树支持从粗粒度到细粒度的多级语义结构，并融合动作类型权重和时域衰减，捕捉用户兴趣演化。
3. **目标感知压缩**：针对每个候选商品进行语义检索，从长噪声序列中提取高证据意图链，有效减少无关行为干扰。
4. **工业部署友好**：系统架构（图3）详细说明了离线/在线分离的管道，支持高并发、低延迟、高QPS的在线服务。
5. **充分的工业验证**：在快手亿级用户平台上开展在线A/B测试，覆盖核心商业指标，证明实际价值。

## 8. 不足与局限

1. **实验覆盖局限性**
   - 消融实验仅在Food–Kitchen场景进行，未在Movie–Book上验证，可能缺乏场景间一致性分析。
   - 未与更多LLM-based CDR方法（如LLM4CDSR）在同一设置下进行细粒度比较（文中的LLMCDSR是2025年作品，但论文发表于2026年，时间差异较小）。
2. **评估偏差风险**
   - 仅使用NDCG@10和HR@10，未报告召回率、多样性或用户满意度等指标。
   - 公开数据集的负采样策略（999随机）可能导致估计偏差，工业在线指标更可靠但未披露实验时长和置信区间。
3. **LLM依赖与成本未量化**
   - 离线LLM推理的计算开销（GPU小时、存储需求）未报告，无法评估整体资源成本。
   - 仅用了Qwen3-4B，未探究不同尺寸或类型LLM对意图质量的影响。
4. **应用限制**
   - 框架设计依赖于内容域与商品域间存在可映射的语义空间，对极度异构或缺乏文本描述的场景可能不适用。
   - 在线意图树构建需在毫秒级完成聚合，大规模用户下可能存在瓶颈（论文未提供QPS上限压力测试）。
5. **其他缺失**
   - 未讨论失败案例或意图路径生成的错误传播问题。
   - 未提供代码或伪代码，部分公式细节（如MHA实现中的融合方式）不够清晰。

（完）
