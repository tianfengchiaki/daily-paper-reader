---
title: "ANCHOR: Agentic Noise Creation Framework for Human Simulation and Denoising Recommendation"
title_zh: "ANCHOR: 面向人类模拟与去噪推荐的智能体噪声生成框架"
authors: "Xiangming Li, Hua Chu, Chengyu Feng, Jianan Li, Yangtao Zhou"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05621v1"
tags: ["query:q1"]
score: 9.0
evidence: 通过主动噪声创建实现推荐去噪的范式级重构
tldr: 针对推荐系统中从噪声隐式反馈提取用户偏好的瓶颈，现有方法依赖启发式导致噪声误识别。本文提出Creation-Recognition范式，基于ANCHOR智能体框架模拟用户行为生成带标签噪声，将去噪转化为监督学习。通过推荐器在环的噪声创建和参数化识别器训练，有效检测真实交互中的噪声模式，提升准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05621v1/fig-001.webp\", \"caption\": \"Table 2: Overall performance comparison of different baselines on the backbone models. Bold numbers indicate the best performance, and underlined numbers indicate the second-best performance. \\\"R\\\" and \\\"N\\\" stand for Recall and NDCG, respectively.\", \"page\": 9, \"index\": 1, \"width\": 1046, \"height\": 448}, {\"url\": \"assets/figures/arxiv/2606.05621v1/fig-002.webp\", \"caption\": \"Figure 2: The overall framework of the proposed ANCHOR framework.\", \"page\": 5, \"index\": 2, \"width\": 1014, \"height\": 482}]"
motivation: 现有推荐去噪方法依赖无监督侧信息或手工启发式，导致噪声误识别和用户偏好表征失真。
method: 提出Creation-Recognition范式，基于ANCHOR框架通过推荐器在环的智能体架构合成多样噪声，并训练参数化识别器进行监督去噪。
result: ANCHOR能够生成包括超出偏好和边界邻近噪声的标签，并训练识别器整合协同信号与语义表示检测真实噪声。
conclusion: ANCHOR将推荐去噪从启发式过滤转变为监督学习，有效提升了噪声识别准确性和泛化能力。
---

## 摘要
从含噪隐式反馈中提取准确的用户偏好仍然是推荐系统的根本瓶颈，凸显了推荐去噪的必要性。然而，真实世界数据缺乏显式噪声标注，迫使现有方法依赖无监督的辅助信息或手工设计的启发式规则。这些方法通常导致高昂的外部成本、泛化能力差或依赖不可靠的先验，从而造成噪声误判并破坏真实的用户偏好表示。为解决这些限制，我们提出了推荐去噪的范式级重构。我们的“创建-识别”范式不是通过启发式间接推断噪声交互，而是主动创建带标签的噪声交互，并训练专用识别器加以识别，将去噪从启发式过滤转变为监督学习。基于此范式，我们提出ANCHOR，一个受近期LLM-as-User研究启发的智能体框架。ANCHOR模拟用户行为生成真实噪声标签，并通过两个阶段实现监督去噪：噪声创建与噪声识别。在噪声创建阶段，ANCHOR采用推荐器在环的智能体架构，合成多样化的偏好外噪声和富有信息的边界邻近噪声。对于偏好外噪声，它实现五种可扩展的模拟机制以近似含噪隐式反馈的主要来源。对于边界邻近噪声，一种对抗性边界细化机制生成模糊交互，挑战识别器并瞄准决策边界。在噪声识别阶段，ANCHOR利用生成的标签训练一个可重用的参数化识别器，该识别器整合协同信号与语义表示，以检测真实交互数据中的噪声模式。

## Abstract
Distilling accurate user preferences from noisy implicit feedback remains a fundamental bottleneck in recommendation systems, highlighting the need for recommendation denoising. However, real-world data lack explicit noise annotations, forcing existing methods to rely on unsupervised side information or handcrafted heuristics. These approaches often incur high external costs, generalize poorly, or depend on unreliable priors, causing noise misidentification and corrupting true user preference representations. To address these limitations, we propose a paradigm-level reformulation of recommendation denoising. Instead of indirectly inferring noisy interactions through heuristics, our Creation-Recognition paradigm proactively creates labeled noisy interactions and trains a dedicated recognizer to identify them, transforming denoising from heuristic filtering into supervised learning. Based on this paradigm, we present ANCHOR, an agent-based framework inspired by recent LLM-as-User research. ANCHOR simulates user behaviors to generate realistic noise labels and enables supervised denoising through two stages: noise creation and noise recognition. In the noise creation stage, ANCHOR adopts a recommender-in-the-loop agentic architecture to synthesize both diverse out-of-preference noise and informative boundary-adjacent noise. For out-of-preference noise, it implements five extensible simulation mechanisms to approximate major sources of noisy implicit feedback. For boundary-adjacent noise, an adversarial boundary refinement mechanism generates ambiguous interactions that challenge the recognizer and target the decision boundary. In the noise recognition stage, ANCHOR leverages the generated labels to train a reusable parametric recognizer that integrates collaborative signals and semantic representations to detect noise patterns in real interaction data.

---

## 论文详细总结（自动生成）

# ANCHOR: 面向人类模拟与去噪推荐的智能体噪声生成框架——论文详细总结

## 1. 核心问题与整体含义
- **研究动机**：推荐系统依赖隐式反馈（如点击、购买），但这些交互常包含噪声（误点击、好奇心驱动、从众行为等），导致用户偏好建模不准确。
- **现有方法局限**：去噪方法主要分为两类：基于侧信息（如停留时间、社交网络）和基于启发式假设（如高训练损失即为噪声）。两者均存在成本高、泛化差、先验不可靠的问题，根本原因在于真实数据缺乏噪声标注，无法直接学习噪声模式。
- **本文贡献**：提出“创建-识别”范式，**主动合成带标签的噪声交互**，将推荐去噪从无监督/启发式过滤转变为监督学习，从根本上解决噪声标注缺失的瓶颈。

## 2. 方法论
### 核心思想
- 利用 LLM 驱动智能体模拟用户非偏好驱动行为，生成逼真的噪声标签；然后训练一个可复用的参数化噪声识别器，在真实交互数据中检测并移除噪声。
- 整体流程分为两个阶段：**噪声创建**（合成带标签数据）和**噪声识别**（监督学习识别器）。

### 关键技术细节
1. **用户画像与候选集构建**：对每个用户，从历史交互中提取文本语义，用 LLM 生成用户画像 $p_u$；用预训练的 LightGCN 为用户生成推荐候选列表 $C_u$。
2. **偏好外噪声生成**（5 种机制）：
   - 误点击噪声：随机选择候选项。
   - 好奇心驱动噪声：选择与用户兴趣语义相邻但非核心的项。
   - 标题偏见噪声：选择表面文本吸引人但内容不匹配的项。
   - 流行度偏见噪声：选择全局流行度高的项。
   - 位置偏见噪声：选择列表中排名靠前的项。
   - 每种机制通过特定 prompt 引导智能体生成噪声，最终合并为合成噪声集 $\tilde{\mathcal{N}}$。
3. **边界邻近噪声生成**：为模拟难以区分的“硬噪声”，引入**创作器-识别器迭代对抗机制**：
   - 创作器基于用户画像、候选集和历史反馈记忆，生成表面相似但实质不符的交互。
   - 识别器对生成样本打分，反馈哪些样本被正确/错误检测。
   - 创作器通过 prompt 级别的反思和记忆更新，逐步生成更具迷惑性的样本。
   - 重复多轮，将生成的边界噪声加入识别器训练集。
4. **噪声识别器训练**：
   - 训练数据：原始交互（标签 0）+ 合成噪声（标签 1）。
   - 架构：结合协作 ID 嵌入（LightGCN）和语义嵌入（预训练语言模型编码用户画像和物品文本），通过 MLP 输出噪声概率 $s_{u,i}$。
   - 损失函数：$\mathcal{L}_{\text{noise}} = \mathcal{L}_{\text{CE}} + \alpha \mathcal{L}_{\text{BPR}}$，其中 $\mathcal{L}_{\text{CE}}$ 为二分类交叉熵，$\mathcal{L}_{\text{BPR}}$ 为 BPR 排序损失以保留协同信号。
5. **去噪应用**：训练好的识别器对原始交互数据预测噪声概率，移除高于阈值 $\tau$ 的交互，用清洗后的数据训练下游推荐模型。

## 3. 实验设计
- **数据集**：三个公开真实数据集——DBbook2014、Book-Crossing、MovieLens-1M。三者均提供物品文本信息，支持智能体语义推理。
- **推荐骨干**：GMF（广义矩阵分解）和 LightGCN（图卷积网络）。
- **对比方法**：T-CE（截断交叉熵）、DeCA（跨模型一致）、BOD（双层优化）、DCF（双重校正）、LLaRD（LLM+信息瓶颈）。共 5 种主流去噪方法。
- **评价指标**：Recall@10、NDCG@10、Recall@20、NDCG@20。
- **主要实验组**：
  - 表 2：整体性能对比（3 数据集 × 2 骨干 × 5 基线 + 本文）。
  - 表 3：消融实验（4 个变体：去除噪声生成、去除边界细化、去除语义、去除 BPR 损失）。
  - 图 3：超参数敏感性（生成噪声数量、BPR 损失权重 $\alpha$、边界细化迭代次数 $T$）。
  - 图 4：噪声鲁棒性（在训练集中注入 5%~20% 额外噪声，对比性能变化）。

## 4. 资源与算力
- **论文未明确说明**训练所使用的 GPU 型号、数量、具体训练时长等算力资源。仅提及使用 GPT-3.5 Turbo 作为 LLM 进行用户画像与噪声生成，未报告推理成本。
- 注意：该方法在噪声创建阶段需要大规模 LLM 调用（采样 1000 用户，每人生成多个噪声），实际部署可能面临较高的 API 成本和时间开销。

## 5. 实验数量与充分性
- **数量**：总共呈现了 4 类实验（性能对比、消融、超参、鲁棒性），覆盖 3 个数据集、2 个骨干、5 个基线，消融包含 4 个变体，超参试验了 3 个参数的不同取值，鲁棒性试验了 4 个噪声比例。
- **充分性评价**：实验设计较为全面，对比了当前主流方法，消融和超参分析验证了各模块贡献。但缺少以下方面：
  - 未在更大规模或工业级数据集（如 Amazon、Yelp 等）上验证。
  - 未报告统计显著性检验（如 t 检验），无法判断改进是否显著。
  - 边界细化迭代次数较少（最优 T=4），可能未充分探索靠近边界的样本。
  - 未分析模拟噪声与真实噪声分布的相似度（仅通过下游性能间接体现）。

## 6. 主要结论与发现
- **ANCHOR 在所有组合上一致超越 SOTA 方法**，平均提升约 10%~20%（以 Recall 为例，LightGCN 上 MovieLens-1M 的 R@10 从 0.1903 提升至 0.2015）。
- **消融实验表明**：去除噪声生成组件（w/o Gen）导致性能下降最严重，验证了合成噪声标签的核心价值；边界细化（w/o IBR）和语义（w/o Sem）均显著影响性能。
- **超参分析**：生成 4 个噪声/用户最佳；BPR 损失权重 $\alpha=0.2$ 最优；边界细化 4 轮后性能饱和。
- **噪声鲁棒性**：在额外注入噪声高达 20% 时，ANCHOR 仍保持较平稳的性能，优于基线。

## 7. 优点
- **范式创新**：将推荐去噪从被动推理转变为主动创建，提供了全新的监督学习视角，避免了无监督方法的固有限制。
- **噪声模拟全面且可扩展**：覆盖 5 种主要噪声来源，机制可扩展至更多行为模式；对抗边界细化生成硬负样本，提升辨别力。
- **识别器设计巧妙**：融合协同 ID 与语义表示，并通过 BPR 目标保留推荐结构，实现可迁移的“黑盒”去噪模块。
- **模型无关性**：不依赖特定推荐骨干，可即插即用于不同模型。
- **实验验证扎实**：在多个数据集和骨干上均取得显著提升，消融分析完整。

## 8. 不足与局限
- **算力与成本未报告**：依赖 LLM 进行大规模模拟，实际应用可能面临高昂的 API 成本和时间开销，论文未提供量化分析或优化方案。
- **模拟与真实噪声的差距未评估**：合成噪声的分布是否真正匹配真实噪声？仅通过最终推荐性能间接验证，缺乏直接对比。
- **实验覆盖有限**：仅在三个中等规模数据集上验证，缺少大规模或工业级场景（如百万用户以上）的测试；也未在更多样化的推荐任务（如序列推荐、多行为推荐）上检验。
- **边界细化迭代次数少**：最优仅 4 轮，可能未充分学习复杂边界；且对抗机制基于 prompt 而非梯度，理论上可控性弱于 GAN。
- **可迁移性验证不足**：识别器在不同数据集间的迁移未实验；需验证是否需对每个新数据集重新生成噪声。
- **潜在偏差**：智能体行为由 prompt 和 LLM 控制，可能引入人类设计者的主观偏见，导致某些噪声类型被过度或不足表示。

（完）
