---
title: "GRACE: Boosting Video MLLMs with Grounded Action-Centric Evidence for Viewer Sentiment Prediction"
title_zh: GRACE：用定位的动作中心证据增强视频MLLMs以进行观众情感预测
authors: "Ruoxuan Yang, Tieyuan Chen, Xiaofeng Huang, Haibing Yin, Jun Wang, Xiping Chen, Jun Yin, Xuesong Gao, Weiyao Lin"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16198v1"
tags: ["query:q6"]
score: 9.0
evidence: 使用多模态大模型预测视频广告中的观众情感
tldr: 针对视频广告观众情感预测任务，提出GRACE框架，通过提取显式的动作三元组（SVO）和视觉实体裁剪，增强多模态大模型对细粒度情感线索的提取和推理能力。在Pitts数据集上显著提升Qwen2.5-VL和Qwen3-VL基线性能，消融与跨数据集实验验证其泛化性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16198v1/fig-001.webp\", \"caption\": \"Fig. 5. Evidence-level answer-to-evidence attention for the example in Fig. 3.\", \"page\": 11, \"index\": 1, \"width\": 717, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2606.16198v1/fig-002.webp\", \"caption\": \"Fig. 1. Illustration of sparse frame-only input and our grounded action-centric evidence augmentation. Our method makes action events and visible textual cues explicit, helping the MLLM infer the intended viewer sentiment.\", \"page\": 2, \"index\": 2, \"width\": 712, \"height\": 313}, {\"url\": \"assets/figures/arxiv/2606.16198v1/fig-003.webp\", \"caption\": \"Table 1. Main results on the Pitts dataset.\", \"page\": 7, \"index\": 3, \"width\": 547, \"height\": 293}, {\"url\": \"assets/figures/arxiv/2606.16198v1/fig-004.webp\", \"caption\": \"Fig. 4. Layer-wise attention to grounded action-centric evidence components. Columns show prefill and decoding attention; rows report mean and sum attention.\", \"page\": 10, \"index\": 4, \"width\": 710, \"height\": 421}, {\"url\": \"assets/figures/arxiv/2606.16198v1/fig-005.webp\", \"caption\": \"Fig. 2. Overview of our grounded action-centric evidence augmentation framework.\", \"page\": 4, \"index\": 5, \"width\": 694, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2606.16198v1/fig-006.webp\", \"caption\": \"Fig. 3. Qualitative example of an online-bullying advertisement. Our grounded actioncentric evidence makes the victim-centered narrative explicit and helps infer the intended “Empathetic” viewer response.\", \"page\": 9, \"index\": 6, \"width\": 692, \"height\": 372}]"
motivation: 标准MLLMs依赖整体帧表示，缺乏对细粒度、情感相关事件（如人物-物体交互）的显式建模，导致情感推理不精确。
method: 从动作中心的视频描述中提取时序有序的主-谓-宾三元组及辅助文本线索，并对主语和宾语实体进行视觉裁剪，作为结构化证据输入MLLM进行情感推理。
result: 在Pitts数据集上，GRACE持续优于Qwen2.5-VL和Qwen3-VL基线；消融、AdsQA交叉验证及TVQA情感子集迁移实验证实有效性。
conclusion: 显式结构化动作线索与局部视觉证据能够有效提升视频MLLMs的细粒度情感推理能力，且方法具有良好的泛化性。
---

## 摘要
视频广告中的观众情感预测旨在推断观众中引发的潜在情感反应。为了弥合所展示与所感知之间的差距，模型必须从显式的视觉叙事、具体的人物-物体交互以及可见的文本线索中推断出隐藏的观众情感。然而，标准的多模态大语言模型（MLLMs）通常依赖于整体帧表示，这使得这些细粒度的、与情感相关的事件变得隐式，并使精确的情感推理复杂化。为了解决这个问题，我们提出了一种基于定位的动作中心证据增强框架，通过引入显式的事件结构和局部化视觉证据，增强了视频MLLMs的线索提取和理解能力。我们的方法从动作中心的视频描述中提取时间排序的主语-动词-宾语（SVO）三元组和辅助的可见文本线索，将主语和宾语实体定位为视觉实体裁剪，然后使MLLM能够基于这些提取的结构化线索执行线索增强的情感推理。通过这种方式，动作三元组指定“发生了什么”，而定位的视觉实体裁剪将“谁或什么参与了每个事件”锚定到具体的视觉证据上。在Pitts数据集上的实验显示，相对于Qwen2.5-VL和Qwen3-VL基线，性能持续提升。消融研究、在AdsQA上的跨数据集评估以及在以情感为重点的TVQA子集上的迁移实验进一步支持了我们方法的有效性和泛化能力。

## Abstract
Viewer sentiment prediction in video advertisements aims to infer the latent affective response evoked in the audience. To bridge the gap between what is shown and what is felt, models must deduce hidden viewer emotions from explicit visual narratives, concrete character-object interactions, and visible textual cues. However, standard Multimodal Large Language Models (MLLMs) typically rely on holistic frame representations, which leave these fine-grained, affect-relevant events implicit and complicate precise emotional reasoning. To address this, we propose a grounded action-centric evidence augmentation framework that enhances video MLLMs' clue extraction and comprehension by introducing explicit event structure and localized visual evidence. Our method extracts temporally ordered subject-verb-object (SVO) triplets and auxiliary visible textual cues from action-centric video descriptions, grounds subject and object entities as visual entity crops, and then enables the MLLM to perform clue-enhanced emotional reasoning based on these extracted structured clues. In this way, action triplets specify "what happens", while grounded visual entity crops anchor "who or what participates in each event" to concrete visual evidence. Experiments on the Pitts dataset show consistent improvements over Qwen2.5-VL and Qwen3-VL baselines. Ablation studies, cross-dataset evaluation on AdsQA, and transfer experiments on an emotion-focused TVQA subset further support the effectiveness and generalization of our approach.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将严格按照您的要求，对提供的论文进行结构化、深入且客观的中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

-   **核心问题**：视频广告的**观众情感预测**。与识别屏幕上角色的情绪不同，观众情感是一种潜在的、间接从广告叙事中引发的反应。论文旨在弥合“可见的视觉叙事”与“不可见的观众情感”之间的鸿沟。
-   **研究动机**：
    -   **现有方法的不足**：标准的多模态大语言模型（MLLMs）通常依赖于整体的视频帧表示（如稀疏采样帧）。这种做法将细粒度的、与情感相关的关键事件（如人物与物体的互动、背景中的文本提示）隐藏在全局特征中，导致模型难以进行精确的情感推理。
    -   **角色情感的误导性**：单纯依赖角色的面部表情或肢体动作不可靠（如库勒肖夫效应，Kuleshov effect），相同的表情在不同场景下会引发不同的观众情感。
    -   **符号落地问题（Symbol Grounding Problem）**：仅用纯文本描述动作事件（如主语-动词-宾语三元组）缺乏对事件参与实体的具体视觉感知证据，限制了模型的推理能力。
-   **整体含义**：为了更准确地推断广告所触发的潜在观众情感，模型需要显式地理解和利用结构化的动作事件线索，并将这些符号化的线索与具体的、局部的视觉证据（如实体裁剪图）相结合。

### 2. 论文提出的方法论：核心思想、关键技术细节

-   **核心思想**：提出**GRACE**（Grounded Action-Centric Evidence）框架，通过提取**定位（grounded）的、以动作为中心的证据**来增强视频MLLMs的推理能力。核心在于将视频内容转化为显式的“事件结构”（谁-对谁-做了什么）和“视觉锚点”（实体具体是什么样），而不是依赖模糊的整体帧表示。
-   **关键技术流程**（如图2所示）：
    1.  **动作中心语义提取**：
        -   **密集帧采样**：从视频中采样大量密集帧（M=64，多于基线）。
        -   **动作中心字幕生成**：使用一个冻结的MLLM（`f_cap`）和一个动作聚焦的指令（`I_cap`）生成一个按时间顺序的、以动作为中心的视频描述（`c`）。
        -   **三元组与文本线索提取**：使用一个冻结的LLM（`f_parse`）从描述中解析出时间排序的**主语-动词-宾语（SVO）三元组（E）** 和**辅助的可见文本线索（T）**。例如：`(car, collide, car)`, `"Always wear a seat belt."`。
    2.  **视觉实体定位**：
        -   **关键帧选择**：对每个三元组`ei`，使用一个冻结的MLLM（`f_sel`）从稀疏帧集（`X`，K=8）中选择最能支持该事件的最佳帧（`τi`）。
        -   **开放集定位**：在选中的帧上，对主语和宾语实体使用开放集定位模型**GroundingDINO**（`f_grd`）进行视觉定位，获得实体边界框（`̂bri`）。
        -   **视觉证据裁剪**：从对应帧上根据定位框裁剪出实体的局部视觉图像（`Crop`）。如果定位失败，则回退为使用该实体的文本名称。
        -   **预算控制**：为避免计算开销过大，设置裁剪预算上限C（如24），保留置信度最高的C个裁剪图，其余仍用文本表示。
    3.  **证据增强与推理**：
        -   **序列化**：将定位后的实体图像（或文本回退）、动词、另一个实体图像（或文本回退）按时间顺序组合成一个结构化的输入单元（`Zi`）。
        -   **输入组织**：将原始稀疏帧（X）、序列化的定位动作事件（H）和可见文本线索（T）按时间顺序拼接，形成增强后的提示，输入给下游的MLLM进行情感分类。

### 3. 实验设计：数据集、基准及对比方法

-   **数据集与场景**：
    -   **主数据集**：**Pitts**（2,034个成功下载视频），广告观众情感预测任务，60/40划分训练/测试集。评价指标为`Acc_clean`（多数投票标签）和`Acc_raw`（所有原始标签）。
    -   **跨数据集泛化**：**AdsQA**（998个情感导向的问答对），评价`Acc_raw`。
    -   **跨任务迁移**：**TVQA**（构造1,536个情感聚焦的子集），评价`Acc_clean`。
-   **基准（Benchmark）与对比方法**：
    -   **先前方法**：SVM（早期方法）、GPT-4o（闭源模型）。
    -   **开源MLLM基线**：**Qwen2.5-VL-7B** 和 **Qwen3-VL-8B**。这两个模型在同样的设置下进行稀疏帧微调，作为对比基准。
    -   **消融研究对象**：GRACE框架本身，对比了仅添加字幕（Cap.）、仅添加文本结构化证据（文本三元组，Trip.）以及最终添加定位视觉裁剪（Crop）的效果。

### 4. 资源与算力

-   **文中未明确说明训练所用的GPU型号、数量和具体训练时长**。仅提及使用BF16精度、AdamW优化器、学习率1e-4、批量大小1和梯度累积16，并训练3个epoch。
-   **隐含的计算开销**：GRACE框架在离线提取阶段会引入额外计算（密集帧字幕生成、LLM解析、GroundingDINO定位等），但推理时仅将定位后的图像作为输入，不会在MLLM主模型中引入额外的视觉帧。

### 5. 实验数量与充分性

-   **实验数量**：
    -   **主实验**：在Pitts数据集上，对Qwen2.5和Qwen3两个骨干网络进行对比，共4组主要结果。
    -   **消融实验**：在Pitts数据集上，对比了基于Qwen3的4种配置（基线、+字幕、+文本线索、+定位裁剪）。
    -   **泛化实验**：在AdsQA和TVQA两个额外数据集上进行评估，共8组结果。
    -   **推理预算分析**：在Pitts上，分析了不同视觉预算和裁剪预算下的性能与FLOPs关系，共4组结果。
    -   **定性分析**：一个详细的例子展示（图3）。
    -   **注意力分析**：组件级和证据级注意力分析（图4，图5）。
-   **实验充分性与客观性**：
    -   **充分**：覆盖了主任务、消融、跨数据集/任务泛化、计算成本等多种场景，实验设计比较系统。
    -   **客观公平**：所有基线模型都与GRACE采用相同的微调设置（学习率、epoch等），且结果是三个随机种子的平均值。对比方法（SVM, GPT-4o）也是公开可复现的。但仅使用3个种子，统计显著性不够强，可能存在一定偏差。跨数据集实验直接用Pitts训练好的模型在AdsQA上测试，场景限制较大，可能低估模型的真实泛化潜力。

### 6. 论文的主要结论与发现

1.  **GRACE有效**：在Pitts数据集上，GRACE持续显著地提升了Qwen2.5-VL和Qwen3-VL的基线性能（`Acc_clean`提升约3-4个百分点，`Acc_raw`提升约3-6个百分点）。
2.  **组件贡献**：消融实验证明，从基线到添加字幕，再到结构化文本证据，最后到定位视觉证据，每个组件的加入都带来性能提升，其中结构化线索和定位视觉证据贡献互补。
3.  **泛化能力强**：在AdsQA和TVQA上的跨数据集/跨任务迁移实验中，GRACE同样带来了性能提升，证明了其通用性。
4.  **效率更高**：推理预算分析表明，GRACE在降低整体计算开销（0.79x FLOPs）的同时，依然能超越原始全预算下的基线性能。这表明GRACE通过更有效的视觉预算分配（将资源集中在关键实体上）而非简单的增加信息量来提升性能。
5.  **可解释性**：注意力分析显示，MLLM在浅层关注动词，在深层关注实体裁剪图像，表明模型确实在利用动作结构和局部视觉证据进行推理。

### 7. 优点

1.  **方法设计巧妙**：创造性地将视频情感推理分解为“提取动作结构”和“锚定视觉证据”两个互补的步骤，解决了标准MLLM“看不清”细粒度事件和“不认识”具体实体的两个关键问题。
2.  **效率导向设计**：强调所有视觉实体裁剪均来自基线MLLM已使用的稀疏帧，不引入额外视觉信息，并通过预算控制计算开销，体现了对效率和效果的平衡。
3.  **实验设计全面**：涵盖了主任务、消融、跨数据集/任务迁移、计算开销分析等多种场景，验证了方法的有效性和泛化性，具有较强的说服力。
4.  **结果优异**：在多个视频情感推理基准上取得了显著优于标准MLLM基线的结果，展示了结构化推理在视频理解中的重要性。

### 8. 不足与局限

1.  **离线处理依赖性**：框架严重依赖于离线、冻结的模型（字幕生成、LLM、GroundingDINO）进行证据提取。如果这些模型存在错误（如错误的地名、错误的实体定位），会直接影响最终性能。
2.  **泛化评估的局限性**：跨数据集实验仅限于将Pitts模型直接迁移到其他数据集，未见对模型进行微调后的泛化能力分析（除了TVQA子集）。此外，TVQA是构造的子集，可能不能完全代表真实的视频问答场景。
3.  **潜在的数据集偏差**：主实验仅在Pitts数据集上分析，而未提及是否使用了其他现有的大规模广告数据集。实验结果可能受限于Pitts数据集的特性和标签分布。
4.  **计算资源开**销：尽管推理时降低了FLOPs，但离线证据提取阶段仍需要运行多个模型（字幕、LLM、GroundingDINO），增加了开发和预处理成本。文中未提及这部分成本。
5.  **种子数量较少**：虽然取了3个随机种子平均，但对于说服力的统计严谨性而言，种子数量略少。
6.  **对“文本线索”的依赖**：方法中显式提取了可见的文本线索，这在视频广告中很常见，但对于完全不包含文字的广告视频，该组件的贡献可能会降低。

（完）
