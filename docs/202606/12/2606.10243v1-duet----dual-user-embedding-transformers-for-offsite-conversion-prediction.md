---
title: DUET -- Dual User Embedding Transformers for Offsite Conversion Prediction
title_zh: DUET -- 用于站外转化预测的双用户嵌入Transformer
authors: "Reazul Hasan Russel, Mingwei Tang, Rostam Shirani, Xinlong Liu, Navid Madani, Leo Ding, Yawen He, Xiangyu Wang, Mustafa Acar, Ashish Katiyar, Yuhai Li, Alan Yang, Metarya Ruparel, Derek Qiang Xu, Rupert Wu, Rui Yang, Liang Tao, Xinyi Zhao, Larry Zhang, Sri Reddy, Rob Malkin"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10243v1"
tags: ["query:q1"]
score: 9.0
evidence: 双编码器预测推荐中的站外转化
tldr: "针对离线转化率预测中点击信号密集但转化信号稀疏且延迟的问题，提出DUET框架，将用户行为分为点击和转化两个域，分别使用多层自注意力与交叉-自注意力transformer预训练，生成互补嵌入供下游排序器使用。实验表明相比最强基线归一化熵降低0.38%，A/B测试验证了预测准确性提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10243v1/fig-001.webp\", \"caption\": \"Table 2 Ablation study on the upstream encoders.\", \"page\": 11, \"index\": 1, \"width\": 632, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-002.webp\", \"caption\": \"Figure 3 End-to-end system: recurring pre-training with checkpoint validation followed by SIDE-based embedding compression and finally event-triggered inference and serving.\", \"page\": 7, \"index\": 2, \"width\": 968, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-003.webp\", \"caption\": \"Figure 6 Eval NE gains (%) on Different Types and Stages of downstream rankers. Higher is better.\", \"page\": 10, \"index\": 3, \"width\": 627, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-004.webp\", \"caption\": \"Figure 1 DUET unified framework: data routing, representation pre-training, and downstream ranker.\", \"page\": 4, \"index\": 4, \"width\": 894, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-005.webp\", \"caption\": \"Figure 4 Principal components of quantized user embeddings. ConvAUE and ClickAUE occupy distinct regions, indicating non-redundant information. Figure5 Orthogonality between ConvAUE and ClickAUE. Degree of complementarity varies, higher orthogonality indicating greater potential for additive information gain.\", \"page\": 9, \"index\": 5, \"width\": 977, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-006.webp\", \"caption\": \"Figure 2 DUET architecture. Left: the DLRM backbone with parallel sequence and non-sequence branches. Right: event tower structure with stacked attention blocks and DCN user summarization.\", \"page\": 5, \"index\": 6, \"width\": 968, \"height\": 518}]"
motivation: 现有方法对点击和转化信号使用统一编码器，忽略了二者在稀疏性、延迟等方面的统计差异。
method: 将点击和转化行为分为两个域，分别采用多层自注意力（点击）与交叉-自注意力（转化）transformer预训练，生成互补嵌入由下游排序器联合使用。
result: "相比最强基线，归一化熵降低0.38%，A/B测试显示离线转化率预测准确性持续提升。"
conclusion: DUET在严格的延迟约束下有效提升了离线转化率预测性能。
---

## 摘要
站外转化率（OCVR）预测是计算推荐系统中的一个重要排序问题。该任务存在建模挑战：点击信号丰富且时间窗口短，而转化信号本质稀疏、延迟长且常无法归因。尽管存在这些统计差异，两种信号都必须服务于严格服务延迟约束的模型。以往的预训练方法采用单一、无差别的编码器统一处理两种数据流。我们提出DUET（双用户嵌入Transformer）框架，将用户行为数据显式划分为两个领域一致的数据流——点击和转化，并预训练专用的Transformer编码器，其架构针对各数据流的统计特性设计：密集点击流采用多层自注意力，稀疏转化流采用交错交叉注意力和自注意力。生成的互补嵌入由下游排序器联合使用，且不超出服务延迟预算。评估表明，相对于最强基线，归一化熵（NE）降低达0.38%，A/B测试显示OCVR预测精度持续提升。

## Abstract
Offsite conversion rate (OCVR) prediction is an important ranking problem in computational recommendation systems. This task presents a modeling challenge: click signals are abundant and exhibit short temporal horizons, whereas conversion signals are inherently sparse, long-delayed, and frequently unattributed. Despite these statistical disparities, both signal types must inform models that operate within strict serving-latency constraints. Prior pre-training approaches address this heterogeneity with a single, undifferentiated encoder applied uniformly across both data streams. We propose DUET (Dual User Embedding Transformers), a framework that explicitly partitions user behavioral data into two domain-coherent streams -- clicks and conversions -- and pre-trains dedicated transformer encoders with architectures tailored to each stream's statistical characteristics: multi-layer self-attention for the dense click stream and interleaved cross- and self-attention for the sparse conversion stream. The resulting complementary embeddings are jointly consumed by a downstream ranker without exceeding serving-latency budgets. Evaluation demonstrates up to 0.38% normalized entropy (NE) reduction relative to the strongest baseline, and A/B test shows consistent improvements in OCVR prediction accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：站外转化率（Offsite Conversion Rate, OCVR）预测是推荐系统中一项关键排序任务。其核心挑战在于：点击信号（click）丰富且时间窗口短，而转化信号（conversion）则极其稀疏、延迟长且常常无法归因（unattributed）。这两种信号在统计特性上存在显著差异，但必须同时被模型用于预测，且模型需要满足严格的服务延迟约束。
- **已有方法局限**：以往的预训练方法使用单一的、无差别的编码器统一处理点击和转化数据流，忽略了二者之间的统计异质性，导致三个问题：
  1. 信号主导：密集团点击信号压制了稀疏的转化信号。
  2. 架构统一：单一编码器无法为不同统计特性的数据流提供合适的归纳偏置。
  3. 跨域转移浅层：知识转移方式有限，未充分利用有机行为和内容语义信号。
- **论文目标**：提出DUET框架，通过对异构行为数据进行域感知数据路由，为每个数据流预训练专用编码器，生成互补的用户嵌入，在不突破在线延迟预算的前提下提升OCVR预测精度。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将用户行为数据划分为两个领域一致的数据流——点击流（含站内转化）和转化流（站外转化），分别训练专用编码器，产生互补的用户嵌入（ClickAUE和ConvAUE），在下游排序器联合使用。
- **关键技术细节**：
  - **域感知数据路由**：根据标签语义和归因窗口划分数据。
    - CTR/Onsite-CVR流：密集、短窗口，保留所有正样本，负样本下采样。
    - OCVR流：稀疏、长窗口，保留全部样本，并包含基于未归因转化生成的合成数据作为弱监督。
  - **上游编码器架构**：基于DLRM骨干（序列分支+非序列分支），输入包括事件基础特征（EBF）、时间戳、ID属性、语义ID等。
    - **ClickAUN**（点击编码器）：采用多层自注意力（基于LLaTTE范式），层数n=2，头数h=2，模型维度d_model=256。
    - **ConvAUN**（转化编码器）：采用交错交叉注意力与自注意力，层数n=1，头数h=8，d_model=128。交叉注意力将静态特征作为查询，序列特征作为键值，提供隐式正则化并降低计算成本。
  - **训练目标**：多任务BCE损失。
    - ClickAUN：CTR任务 + 站内转化任务。
    - ConvAUN：归因转化任务 + 未归因转化任务。
  - **下游集成**：冻结的ClickAUE和ConvAUE作为额外输入特征，下游排序器保持原有架构，仅多任务损失（主任务+辅助任务）。
  - **异步服务**：事件触发推理（ETI），将训练与服务解耦；嵌入压缩（SIDE实现4倍压缩）；服务时解码+MLP投影。

### 3. 实验设计：使用了哪些数据集/场景、benchmark、对比方法

- **数据集/场景**：论文使用Meta内部工业级数据，未公开具体数据集名称。涉及6个不同的下游站外CVR排序模型（不同排序阶段、优化目标、流量特征）。
- **Benchmark**：以归一化熵（NE）为主要指标，定义NE = 平均log损失 / 零模型熵。报告相对NE降低百分比（%ΔNE）。
- **对比方法**（所有方法共享相同下游DLRM架构和输入特征集）：
  1. **Baseline**：无预训练嵌入的现有下游排序器。
  2. **ClickAUE Only**：仅添加ClickAUE。
  3. **ConvAUE Only**：仅添加ConvAUE。
  4. **DUET**：同时添加ClickAUE和ConvAUE。
- 排除了对比学习和掩码预训练方法（因其目标函数不同）。
- 还进行了A/B测试（两个模型）验证在线提升。

### 4. 资源与算力

- **GPU型号与数量**：ClickAUN和ConvAUN均在128块NVIDIA H100 GPU上训练。
- **训练吞吐量**：离线训练QPS约200K，足以支持持续训练和及时部署。
- **具体训练时长**：论文未明确给出总训练时长，仅提到可“continuous training”和“timely deployment”。
- **优化器**：Distributed Shampoo（二阶优化器），学习率0.04，线性warmup 20,000步。

### 5. 实验数量与充分性

- **实验组数**：
  - 主结果表（Table 1）：4种配置（Baseline, ClickAUE Only, ConvAUE Only, DUET）在主要OCVR任务上报告训练NE增益。
  - 跨模型评估（Figure 6）：在6个不同下游排序器上评估3种配置（ClickAUE Only, ConvAUE Only, DUET）的评估NE增益。
  - 消融研究（Table 2）：对ConvAUE进行4项消融（移除合成数据、减少1层注意力、移除用户旅程事件、移除曝光事件），对ClickAUE进行3项消融（缩放序列/非序列架构、移除曝光事件、移除页面事件），报告预训练（PT）和下游（DR）NE退化。
  - 嵌入分析（Figure 4,5）：主成分可视化、余弦距离分布。
  - A/B测试（Table 3）：2个模型在线CVR提升。
- **充分性与公平性**：
  - 消融实验覆盖了数据、架构、特征三个层面，且分别报告了PT和DR效果，清晰地度量了组件贡献。
  - 对比方法设置公平：所有配置共享相同下游架构和输入特征，仅改变预训练嵌入注入。
  - 跨6个不同下游模型验证，涵盖不同阶段和优化目标，增加了结论的泛化性。
  - A/B测试在工业真实流量上进行，统计显著性p<0.05。
  - 但数据集未公开可能影响可复现性；消融实验仅针对单一模型（未在6个模型上都做），但主要设计点已覆盖。

### 6. 论文的主要结论与发现

- **主结果**：DUET（双嵌入）相对于最强基线（ConvAUE Only）实现0.38% NE降低，超过单独使用ClickAUE (0.21%)或ConvAUE (0.30%)，恢复理论上限（0.51%）的75%，表明两个嵌入互补。
- **跨模型评估**：DUET在所有6个下游排序器上一致优于单嵌入，且ClickAUE提供额外提升（0.04%-0.21%），说明点击和转化信号携带独立预测信息。
- **嵌入分析**：ClickAUE和ConvAUE在PCA空间中占据不同区域，余弦距离接近均匀分布，表明二者信息近似正交，进一步支持互补性。
- **消融发现**：
  - 转化流中用户旅程事件最为关键（PT NE涨0.30%），合成数据提供有价值弱监督（0.13%）。
  - 点击流中页面事件（0.07%）和架构缩放（0.13%）影响最大。
  - 不同数据流的组件重要性差异反映了其统计特性：稀疏流依赖少数高信号事件，密集流平均受益于多个中等信号事件。
- **A/B测试**：两个模型分别实现+0.66%和+0.15%的站外CVR提升，在线延迟无增加。

### 7. 优点

- **创新性方法**：明确将异构行为数据划分为领域一致流，并为每个流设计专用注意力架构（自注意力 vs. 交叉-自注意力），而非使用单一编码器。
- **弱监督利用**：将未归因转化作为合成数据纳入训练，有效缓解稀疏性问题。
- **异步服务架构**：通过事件触发推理、检查点验证、嵌入量化和独立管道，实现复杂上游模型与低延迟排序器的解耦，对工业部署友好。
- **充分消融**：不仅消融组件，还分别度量对预训练和下游的传递效果，揭示不同模块的迁移率。
- **多模型验证**：在6个不同下游排序器和A/B测试中验证，增强了结论的鲁棒性和实用性。

### 8. 不足与局限

- **数据路由静态**：当前基于规则划分，未来可探索学习式、多流路由。
- **注意力配置未全面比较**：仅验证了“自注意力用于点击、交叉注意力用于转化”的配置，未对比其他组合（如交换或混合配置）。
- **嵌入冻结**：下游训练时嵌入冻结，可能牺牲对分布偏移的适应性，论文提出可考虑轻量微调（adapter层）作为未来方向。
- **实验可复现性**：使用私有工业数据集，核心方法和结果难以独立复现；未提供公开代码或数据。
- **模型对比有限**：未与近期其他预训练范式（如对比学习、掩码学习、多任务共享编码器）进行直接对比，仅排除了它们。
- **消融范围**：主要消融在一个模型上完成，跨模型一致性未完全验证（但跨模型评估显示DUET一致优于单嵌入，间接支持）。
- **A/B测试模型数量**：仅报告2个模型在线结果，样本量有限（但统计显著）。
- **理论部分**：附录中证明合并与分离模型近等价时基于线性假设（点积），忽略了实际DLRM中的非线性交互层，可能不完全准确。

（完）
