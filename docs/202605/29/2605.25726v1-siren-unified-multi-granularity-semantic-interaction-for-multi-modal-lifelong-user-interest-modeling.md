---
title: "SIREN: Unified Multi-Granularity Semantic Interaction for Multi-Modal Lifelong User Interest Modeling"
title_zh: "SIREN: 面向多模态终身用户兴趣建模的统一多粒度语义交互"
authors: "Yaqian Zhang, Ruyi Yu, Tianyi Li, Bohan Liu, Maoquan Ye, Ke Wang, Shifeng Wen, Junwei Pan, Lijie Wang, Qi Zhou, Yeshou Cai, Chengguo Yin, Lifeng Wang, Hui Li, Lei Xiao, Haijie Gu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25726v1"
tags: ["query:q6"]
score: 9.0
evidence: 多模态终身用户兴趣建模用于推荐
tldr: "本文针对工业推荐系统中多模态特征与终身用户兴趣建模的集成难题，提出SIREN统一多粒度语义交互框架。在一般搜索单元采用多模态相似度软检索或SemID硬检索，在精确搜索单元通过粗粒度相似度桶和细粒度前缀编码SemID实现目标感知交互。离线达到SOTA GAUC，在线微信朋友圈等场景GMV提升2.28%-3.87%，已全量部署于腾讯广告平台。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25726v1/fig-001.webp\", \"caption\": \"Figure 2: CTR distributions of Semantic ID groups within target–behavior similarity buckets. Although pairs in the same bucket have similar multi-modal proximity to the target item, their CTRs vary substantially across Semantic ID groups, especially in high-similarity regions. This withinbucket heterogeneity shows that similarity buckets provide only coarse target-aware relevance, motivating the use of fine-grained SemIDs to preserve semantic and collaborative distinctions.\", \"page\": 2, \"index\": 1, \"width\": 529, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2605.25726v1/fig-002.webp\", \"caption\": \"Figure 3: Overview of SIREN. SIREN follows the two-stage GSU–ESU paradigm. The GSU retrieves target-relevant behaviors via similarity-based soft retrieval or SemID-based hard retrieval. The ESU incorporates SemIDs and similarity buckets as side information for unified target-conditioned sequence modeling.\", \"page\": 3, \"index\": 2, \"width\": 519, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.25726v1/fig-003.webp\", \"caption\": \"Table 1: Overall offline performance comparison on GAUC. The best result is highlighted in bold.\", \"page\": 5, \"index\": 3, \"width\": 508, \"height\": 248}]"
motivation: 现有方法因多模态与协同空间不对齐，仅通过分离建模和后期融合导致多模态表示粗粒度、集成有限。
method: 提出SIREN框架，包含一般搜索单元（多模态相似度软检索或SemID硬检索）和精确搜索单元（目标感知粗粒度相似度桶与细粒度前缀编码SemID）。
result: "离线GAUC达SOTA，在线A/B测试在微信朋友圈、公众号、视频号分别获得+2.28%、+3.87%、+1.61%的GMV提升。"
conclusion: SIREN有效统一了多模态语义交互，自2025年7月起已全流量服务腾讯广告平台。
---

## 摘要
工业推荐系统越来越多地利用终身用户行为历史和丰富的多模态内容来捕捉不断变化的用户偏好。然而，由于多模态空间与协同空间之间存在固有的不对齐，将多模态特征有效融入终身兴趣建模仍然具有挑战性。现有范式通常依赖对多模态序列和行为序列的单独建模，并通过后期融合来缓解模态差异，这导致了粗粒度的多模态表示和有限的集成。本文提出SIREN，一个面向多模态终身用户兴趣建模的统一多粒度语义交互框架。在通用搜索单元阶段，我们引入了两种可选的检索策略：基于多模态相似性的软检索以提升检索效果，以及基于语义ID（SemID）的硬检索以实现高效的工业服务。在精确搜索单元阶段，我们通过粗粒度相似性桶和细粒度前缀编码的SemID显式引入目标感知相关性，从而在目标条件transformer架构内实现与协同ID特征的统一交互。离线数据集上的大量实验表明，SIREN达到了最先进的GAUC。在线A/B测试进一步展示了在多个生产场景中持续的GMV提升，包括微信朋友圈+2.28%、微信公众号+3.87%和微信视频号+1.61%。自2025年7月起，SIREN已在腾讯广告平台的全流量服务中全面上线。

## Abstract
Industrial recommender systems increasingly leverage lifelong user behavior histories and rich multi-modal content to capture evolving user preferences. However, effectively integrating multi-modal features into lifelong interest modeling remains challenging due to the inherent misalignment between multi-modal and collaborative spaces. Existing paradigms typically rely on separate modeling of multi-modal sequence and behavior sequence, and late fusion to alleviate the modality gap, which results in coarse-grained multi-modal representation and limited integration. In this paper, we propose SIREN, a unified multi-granularity semantic interaction framework for multi-modal lifelong user interest modeling. In the General Search Unit stage, we introduce two alternative retrieval strategies: multi-modal similarity-based soft retrieval for retrieval effectiveness, and Semantic ID (SemID)-based hard retrieval for efficient industrial serving. For the Exact Search Unit stage, we explicitly incorporate target-aware relevance via coarse similarity buckets and fine-grained prefix-encoded SemIDs, enabling unified interaction with collaborative ID features within the target-conditioned transformer architecture. Extensive experiments on the offline dataset demonstrate that SIREN achieves a state-of-the-art GAUC. Online A/B tests further demonstrate consistent GMV gains across multiple production scenarios, including +2.28% in Weixin Moments, +3.87% in Weixin Official Accounts, and +1.61% in Weixin Channels. From July 2025, SIREN has been fully launched for full-traffic serving in Tencent's advertising platform.

---

## 论文详细总结（自动生成）

# SIREN: 面向多模态终身用户兴趣建模的统一多粒度语义交互框架——论文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：工业推荐系统需要利用终身用户行为历史和多模态内容来捕捉用户偏好，但多模态信号（如图像、文本）与ID协同信号之间存在固有的空间不对齐，导致现有方法只能采用分离建模（分别处理ID序列和多模态序列）并在后期进行粗糙融合，限制了多模态与协同特征的细粒度交互。
- **核心问题**：如何将多模态语义信号有效、高效地融入终身用户兴趣建模的两阶段范式（通用搜索单元GSU + 精确搜索单元ESU），同时解决检索效率、粗粒度表示及多模态-协同空间对齐问题。
- **整体含义**：本文提出统一多粒度语义交互框架SIREN，通过多模态相似度软检索/SemID硬检索在GSU阶段提升检索质量与效率，在ESU阶段通过粗粒度相似度桶与细粒度前缀编码SemID实现与ID特征的早期融合及目标条件交互，从而增强模型对用户偏好的捕捉能力，最终在离线与在线场景均取得显著提升，并已全流量部署于腾讯广告平台。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在工业两阶段GSU-ESU范式内，将多模态信号以互补的多粒度形式（相似度桶提供目标感知的粗粒度相关信号，SemID提供语义层面的细粒度信号）在项级别进行早期融合，而非后期聚合，并通过目标条件Transformer实现统一的序列建模。
- **关键技术细节**：
  1. **多模态特征构建**：
     - **Semantic ID（SemID）**：使用RQ-VAE将预训练多模态嵌入（128维）量化成分层离散编码（如`[c1, c2, ..., cM]`），再通过前缀编码生成多层级语义令牌，保留粗到细的语义结构。
     - **目标感知相似度桶**：计算目标物品与历史行为物品的多模态余弦相似度，并将其离散化为固定数量的桶索引，映射为可学习嵌入。
  2. **通用搜索单元（GSU）**：
     - **相似度软检索**：基于余弦相似度从完整行为序列中检索Top-K最相关行为；效果好但计算开销大。
     - **SemID硬检索**：利用目标物品的顶级语义码（c1）查询倒排索引，检索共享相同顶级码的行为；将在线复杂度从`O(|B|·d)`降至近常数，存储开销降低>90%。
  3. **精确搜索单元（ESU）**：
     - **统一项表示**：将ID特征向量与SemID前缀编码特征拼接作为行为表示和目标表示。
     - **目标感知序列编码**：采用多头目标注意力机制，在注意力计算中将行为-目标对的相似度桶嵌入作为额外拼接信号，实现早期融合。
     - **目标条件交互**：对用户兴趣表示与目标表示进行逐元素乘积（element-wise multiplication），捕捉细粒度相关性，用于最终CTR预测。
- **公式/算法流程（文字说明）**：
  - 输入：用户行为序列、目标物品；预训练多模态嵌入。
  - 步骤1：通过RQ-VAE生成每物品的SemID，并构建前缀编码。
  - 步骤2：GSU阶段：选择软检索（余弦相似度Top-K）或硬检索（顶级SemID匹配）。
  - 步骤3：计算每个行为-目标对的余弦相似度并离散化为桶索引。
  - 步骤4：构建统一表示：`h_i = concat(ID_emb_i, prefix_sem_emb_i)`。
  - 步骤5：目标注意力：`α_i = Attn(concat(h_i, sim_bucket_emb_i), concat(h_t, sim_bucket_emb_t))`，用户兴趣`u_t = Σα_i h_i`。
  - 步骤6：目标条件交互：`Ũ_t = u_t ⊙ h_t`。
  - 步骤7：与用户/上下文特征拼接后经MLP+Sigmoid输出点击概率。

## 3. 实验设计
- **数据集与场景**：
  - **离线**：Taobao-MM数据集（99M交互，8.79M用户，35.4M物品，每用户历史序列最长1000，128维多模态嵌入）。
  - **在线**：腾讯微信广告系统三大场景（朋友圈、公众号、视频号），覆盖广告、视频、内容Feed等多域行为序列（最长每域4000，覆盖两年历史）。
- **基准方法**：DIN（短序列）、SIM-Hard、SIM-Soft、TWIN、MISS、MUSE（多模态SOTA）。
- **评价指标**：
  - 离线：GAUC（分组AUC），并报告标准差。
  - 在线：GMV提升（相对百分比）。
- **对比变体**：SIREN SemID-GSU（SemID硬检索）、SIREN Sim-GSU（相似度软检索），以及消融实验中的多个中间配置。

## 4. 资源与算力
- **文中未明确说明**：论文未提及具体的GPU型号、数量、训练时长等算力信息。仅提到使用AdamW优化器（学习率2e-4）和SparseAdam（学习率2e-3），batch size=1000，训练1个epoch。离线实验在Taobao-MM上完成，但未描述硬件环境。在线部署部分只提了系统规模和效率对比（SemID硬检索降低>90%延迟和存储），未提到训练阶段的算力消耗。

## 5. 实验数量与充分性
- **实验数量**：
  1. **整体性能对比**：8种方法在单数据集上的GAUC对比（Table 1）。
  2. **消融实验**：逐步添加SemID、相似度桶、目标交互（Table 2），共5个配置。
  3. **表示可判别性分析**：使用互信息（MI）对用户表示聚类后与点击标签计算，包括与MUSE的比较、交互机制比较、组件消融（Figure 4左三幅子图）。
  4. **细粒度SemID必要性分析**：条件熵分析、桶内CTR分布（Figure 2）、桶粒度影响实验（Figure 4右）。
  5. **在线A/B测试**：三大场景、多个管道阶段（pCTR/pCVR/LTR）的GMV提升，以及冷启动分段分析（低活跃用户、冷启动用户、新广告）。
- **充分性**：
  - 离线实验覆盖了主要SOTA基线，消融充分，且有深入的分析（表示质量、桶粒度限制），实验设计较为严谨。
  - 在线实验覆盖多个主流生产场景，并做了冷启动分解，充分验证了实际效果。
  - 但离线只用了单一数据集（Taobao-MM），缺少其他公开数据集（如Amazon、MovieLens）的验证，可能限制泛化性。

## 6. 论文的主要结论与发现
- **核心结论**：SIREN通过统一多粒度语义交互（相似度桶+SemID+目标条件交互）有效弥补了多模态与协同空间的不对齐，在离线GAUC达到SOTA（0.6155，相对提升+2.48%），在线全场景GMV显著提升（朋友圈+2.28%、公众号+3.87%、视频号+1.61%），且SemID硬检索在保持可比效果的同时大幅降低服务成本。
- **关键发现**：
  - 多模态相似度桶提供强目标感知信号；SemID提供互补的细粒度语义信息，两者联合优于单独使用。
  - 桶内CTR分布分析表明：相同相似度桶内的行为-目标对仍具有巨大协作异质性，高相似区域异质性更显著，因此单纯增加桶粒度无法替代SemID。
  - 模型在冷启动场景（低活跃用户、新广告）的增益尤为突出，证实多模态信号能缓解数据稀疏问题。

## 7. 优点：方法或实验设计上的亮点
- **方法亮点**：
  1. **早期融合策略**：将多模态信号在项级别（而非序列聚合后）与ID特征拼接并参与注意力计算，实现了真正的统一建模，避免了后期融合的信息损失。
  2. **多粒度互补特征设计**：相似度桶提供目标感知的粗粒度相关性，SemID提供语义层面的细粒度信息，两者在注意力中同时发挥作用，且前缀编码保留了层级语义。
  3. **灵活的GSU设计**：同时提供软检索（高质量）和硬检索（高效率），为工业部署提供实用权衡，且SemID硬检索通过倒排索引实现近常数时间检索。
  4. **目标条件交互**：元素级乘积操作进一步增强了用户兴趣与目标表示的细粒度对齐。
- **实验亮点**：
  1. **全面的分析实验**：不仅报告性能，还深入分析表示可判别性（MI）、桶粒度限制、条件熵，理论支撑充分。
  2. **大规模在线验证**：覆盖微信三大主流场景，并提供冷启动分段分析，实证价值高。
  3. **部署效率数据**：明确给出SemID硬检索降低90%以上延迟和存储，说明实用性。

## 8. 不足与局限
- **离线数据集单一**：仅使用Taobao-MM一个数据集，未在Amazon、MovieLens等基准上验证，通用性存疑。
- **计算资源未报告**：训练所需GPU型号、数量及时间未披露，不利于复现。
- **在线实验设计细节有限**：未说明A/B测试的分流方式、显著性检验方法（仅提“rigorous significance testing”），GMV提升的具体统计显著性未展示。
- **多模态嵌入来源固定**：预训练嵌入由SCL生成，未探讨不同多模态模型或对齐策略的影响。
- **冷启动分析粒度**：低活跃用户(<50)和冷启动用户(<10)定义较粗略，未深入分析不同冷启动类型。
- **长尾物品效果未单独评估**：虽然提到冷启动新广告，但未专门分析长尾物品（高频/低频）上的表现差异。
- **模型复杂度与扩展性**：未讨论序列长度进一步增加（如>10k）时，软检索的性能瓶颈及硬检索的覆盖度是否足够。

（完）
