---
title: Adaptive Loss Balancing for Noise-Robust GRPO in Generative Recommendation
title_zh: 生成式推荐中噪声鲁棒GRPO的自适应损失平衡
authors: "Kewei Xu, Junbo Qi, Yanyan Zou, Pengfei Zhang, Xingzhi Yao, Shengjie Li"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08480v1"
tags: ["query:q1"]
score: 9.0
evidence: 生成式推荐强化学习应用于广告
tldr: "针对生成推荐中强化学习因奖励模型对曝光偏差日志评估不准确而失效的问题，提出AdaGRPO框架。该方法通过策略难度和奖励可区分性两个诊断指标，对每个样本决定是否应用GRPO目标，否则使用监督学习。在电商数据集上，HR@10从11.01%提升至12.18%，幻觉率低于0.22%，并在A/B测试中显著提升点击率和停留时间。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08480v1/fig-001.webp\", \"caption\": \"Figure 1: Two failure modes of uniform reward application. (a) On easy samples the ground truth already ranks among top rollouts; rewards cluster tightly and the GRPO advantage is dominated by RM noise. (b) On hard samples where the RM is unreliable, exposure bias causes it to score popular distractors above the ground truth, pushing the policy in the wrong direction.\", \"page\": 2, \"index\": 1, \"width\": 498, \"height\": 337}, {\"url\": \"assets/figures/arxiv/2606.08480v1/fig-002.webp\", \"caption\": \"Table 4: Offline performance evaluated at the final training checkpoint. Best intermediate checkpoints are discussed in Figure 3 and the surrounding text; this table reports the final state to highlight training stability. Standard GRPO eventually collapses, trading target retrieval and generation validity for reward maximization, whereas AdaGRPO maintains a stable and superior final-checkpoint trade-off across retrieval accuracy, reward improvement, and hallucination control. The two AdaGRPO rows correspond to the two variants shown in Figure 3: difficulty-only clipping (w. 𝑓1) and the full clip with both conditions (w. 𝑓1 & 𝑓2).\", \"page\": 7, \"index\": 2, \"width\": 1044, \"height\": 223}, {\"url\": \"assets/figures/arxiv/2606.08480v1/fig-003.webp\", \"caption\": \"Figure 3: Offline training dynamics over 2,500 steps. Standard GRPO increases reward-model scores but is accompanied by a decline in HR@10 and an increase in hallucination rate, suggesting over-optimization to the reward model. GRPO+NLL with a fixed mixing coefficient reduces part of this degradation but does not eliminate the late-stage increase in hallucination. AdaGRPO reaches its best checkpoint at an intermediate training step, improving HR@10 from the Base model’s 11.01% to 12.18% while keeping hallucination at or below 0.22%. At the final checkpoint, AdaGRPO remains stable and achieves the best retrieval accuracy among the final models, as detailed in Table 4. Adding the reward-discriminability condition 𝑓2 further reduces variance and improves the HR–reward–hallucination trade-off.\", \"page\": 7, \"index\": 3, \"width\": 1056, \"height\": 475}, {\"url\": \"assets/figures/arxiv/2606.08480v1/fig-004.webp\", \"caption\": \"Figure 2: Overview of AdaGRPO. Two rank-based diagnostics—one probing policy-side difficulty (𝑓 (𝑖 ) 1 ), the other probing reward-model discriminability (𝑓 (𝑖 ) 2 )—are evaluated on each training instance and combined into a binary sample-level clip. The clip decides, at instance granularity, whether the GRPO loss is admitted into the update or clipped to zero.\", \"page\": 5, \"index\": 4, \"width\": 989, \"height\": 313}]"
motivation: 强化学习依赖可靠的奖励模型，但生产级排序器因训练于有偏日志导致样本依赖的评估错误，均匀应用会带来有害梯度。
method: AdaGRPO以监督负对数似然为基础，通过策略难度和奖励可区分性两个诊断指标为每个样本生成二进制门控，决定是否应用GRPO目标，否则退化为纯监督。
result: "在电商数据集最佳中间检查点，HR@10从11.01%提升至12.18%，幻觉率低于0.22%；最终检查点保持鲁棒性（HR@10 11.63%，幻觉率0.27%），A/B测试中点击率和停留时间显著提升。"
conclusion: AdaGRPO通过选择性应用强化学习目标，有效缓解了奖励模型噪声，在检索准确性和有效性上取得平衡，证明了实际可靠性。
---

## 摘要
强化学习为超越监督式模仿的生成式推荐提供了有前景的途径，通过利用奖励信号指导策略改进。然而，其有效性关键取决于奖励模型对所评估样本的可靠性。实践中，生产级排序器（广泛采用的奖励模型）是在有曝光偏差的日志上训练的，导致样本依赖的不准确性，违反了这一假设。我们的分层分析揭示了一个一致的模式：当策略表现出不确定性且排序器能有效区分真实物品与生成负样本时，奖励指导最为有益。对于其他样本，奖励信号要么可忽略不计，要么有害，凸显了统一应用强化学习的风险。为解决这一问题，我们引入了AdaGRPO，一种将奖励引导优化视为选择性准入而非统一压力的新颖框架。训练以监督负对数似然为基础，而GRPO目标则由基于两项生成诊断的每个样本二进制裁剪门控：策略侧难度和奖励可区分性。任一诊断失败的样本默认采用纯监督，从而确保稳定性并减轻噪声梯度的放大。我们在大规模电商数据集上验证了AdaGRPO。在最佳中间检查点，它将HR@10从11.01%提升至12.18%，同时将幻觉率控制在0.22%以下，并在最终检查点保持鲁棒性（HR@10 11.63%，幻觉率0.27%），在检索-有效性前沿上优于固定的NLL-GRPO混合方案。在生产A/B测试中，AdaGRPO在点击率和停留时间上取得了统计显著的增益，证实了其实用价值。

## Abstract
Reinforcement learning (RL) presents a promising avenue for enhancing generative recommendation beyond supervised imitation, leveraging reward signals to guide policy improvement. However, its efficacy is critically contingent on the trustworthiness of the reward model for the samples it evaluates. In practice, production rankers, the widely adopted reward models, are trained on exposure-biased logs, leading to sample-dependent inaccuracies that violate this assumption. Our stratified analysis uncovers a consistent pattern: reward guidance is most beneficial when the policy exhibits uncertainty and the ranker can effectively discriminate the ground-truth item from rollout negatives. On other samples, the reward signal is either negligible or detrimental, highlighting the risk of uniform RL application. To address such an issue, we introduce AdaGRPO, a novel framework that treats reward-guided optimization as selective admission rather than uniform pressure. Training is anchored in supervised negative log-likelihood, while the GRPO objective is gated by a binary, per-sample clip determined by two rollout diagnostics: policy-side difficulty and reward discriminability. Instances failing either diagnostic default to pure supervision, ensuring stability and mitigating the amplification of noisy gradients. We validate AdaGRPO on a large-scale e-commerce dataset. At the best intermediate checkpoint, it elevates HR@10 from 11.01% to 12.18% while constraining hallucination below 0.22%, and maintains robustness at the final checkpoint (HR@10 11.63%, hallucination 0.27%), outperforming fixed NLL--GRPO mixtures across the retrieval--validity frontier. In production A/B tests, AdaGRPO achieves statistically significant gains in click-through rate and dwell time, confirming its practical utility.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：生成式推荐（GR）通过自回归解码生成推荐候选，但受限于参数共享，其细粒度区分能力弱于排序模型。强化学习（RL，如GRPO）常被用来利用排序模型的奖励信号来优化策略，期望弥补这一差距。
- **核心问题**：生产级排序器（奖励模型）训练于有曝光偏差的日志，导致对部分样本的评估不可靠。如果对所有样本统一应用RL目标，在“简单”样本上奖励信号趋同，噪声主导梯度；在“困难”样本上奖励模型可能偏好流行物品，误导策略。因此，关键挑战不是设计更强的奖励，而是**判断何时可以信任奖励信号**。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将奖励引导优化视为“选择性准入”而非“统一压力”。训练以监督负对数似然（NLL）为基础，而GRPO目标通过每个样本的二进制门控（clip）决定是否激活。门控由两个从rollout生成中计算出的诊断信号联合判定。
- **关键技术细节**：
    - **策略侧诊断（困难度 \( f_1 \))**：比较真实物品在策略当前log-likelihood排序中的位置。若排名低于阈值 \(\lfloor \tau K \rfloor\)（即落在前\(\tau\)分数之外），则认定该样本对策略是困难的，需要RL帮助。
    - **奖励侧诊断（可区分性 \( f_2 \))**：要求奖励模型将真实物品排在rollout组和批内负样本的奖励排序中前\(\lfloor\tau K\rfloor\)位，同时所有负样本（从其他实例采样）排名均高于\(\lfloor\rho K\rfloor\)（即底部）。只有同时满足真实物品突出和负样本被压制，才认为本地奖励可靠。
    - **样本级门控**：\(\alpha_i = f_1^{(i)} \cdot f_2^{(i)}\)，只有两个诊断均为1时才允许GRPO梯度通过；否则退化为纯NLL更新。公式为 \(L_i = L_{\text{NLL}}^{(i)} + \lambda \alpha_i L_{\text{GRPO}}^{(i)}\)。
    - **设计特点**：超参数仅\(\tau, \rho, \lambda, M\)，且基于rank分位数，具有尺度无关性；门控可解释且计算开销低（利用已有rollout统计量）。

## 3. 实验设计

- **数据集**：来自电商平台约175K个用户-物品交互序列（一周窗口），测试集为后续两周。
- **基准模型**：基于物品语义ID的decoder-only LLM，先做SFT作为基础策略。
- **奖励模型**：生产级encoder-decoder Transformer排序器的CTR头（多任务MoE，AUC≈0.76）。
- **对比方法**：
    - Base：仅SFT。
    - GRPO：标准GRPO。
    - GRPO+NLL：固定混合系数\(\lambda\)的混合目标。
    - AdaGRPO (w. \(f_1\))：仅困难度诊断。
    - AdaGRPO (w. \(f_1 \& f_2\))：完整双诊断。
- **离线指标**：HR@1/10/50、ClkRwd@k、OrdRwd@k、幻觉率。
- **在线A/B测试**：在生产环境中对比GRPO+NLL和AdaGRPO（不同时间段，分别对各自控制组），测量有效IPV、UCTR、停留时间、覆盖类目数。

## 4. 资源与算力

- 论文中**未明确说明GPU型号、数量或具体训练时长**，仅提到训练步数为2500步。从规模（175K序列）推断，算力需求可能不高，但无法精确描述。

## 5. 实验数量与充分性

- **离线实验**：
    - 主实验：训练动态曲线（2500步，图3）、最终检查点对比（表4）。
    - 分层分析：按预测困难度分5个bin，对比HR@10改进（表5）。
    - 聚合 vs 条件影响分析（表1-3）作为动机验证。
- **在线实验**：两组A/B测试（GRPO+NLL vs 控制，AdaGRPO vs 控制），时间窗口不同。
- **充分性评价**：
    - **优点**：涵盖离线、在线、消融、分层分析，指标全面（命中率、奖励分、幻觉、用户行为）。
    - **不足**：
        - 仅在一个数据集（自有电商数据）上验证，缺少公共基准。
        - 奖励模型仅一种（CTR头），未探索不同类型RM。
        - 在线实验并非头对头对比，而是不同时段分别对照，无法直接比较方法间差异。
        - 超参数（\(\tau, \rho, \lambda, M\)）敏感性分析只在文本中提到“经验稳定”，未展示具体消融。
    - **总体评价**：实验设计合理，证据链清晰，但泛化性和鲁棒性验证有一定局限。

## 6. 主要结论与发现

- **激励性发现**：奖励模型对策略的整体影响近乎为零（表1），但在策略困难且自身可区分的子集上平均提升约30个排名（表2-3），说明**奖励信号有条件的价值**。
- **离线结果**：AdaGRPO（双条件）在最佳中间检查点将HR@10提升至12.18%（Base 11.01%），幻觉率≤0.22%；最终检查点（表4）HR@10 11.63%，幻觉率0.27%，优于所有基线。
- **在线结果**：AdaGRPO在有效IPV（+0.43%）、UCTR（+0.27%）、停留时间（+0.23%）上取得统计显著提升，且覆盖类目数增加，表明未坍缩到少数流行物品。
- **主要观点**：将RL用于生成式推荐的核心挑战不是设计更强奖励，而是**判断何时可以信任奖励信号**。

## 7. 优点

- **方法新颖且实用**：将PPO的信任域从token级别提升到样本级别，通过简单诊断实现自适应门控，计算开销极低（利用已有rollout统计量）。
- **可解释性强**：每个样本的clip决策是二进制的，且由两个直观条件组成，便于调试和归因。
- **缓解奖励模型噪声**：有效避免了在简单样本上奖励噪声主导梯度，以及在不可靠区域误导策略，兼顾了检索准确性和生成有效性。
- **超参数精简且稳健**：仅需调节少数分位数阈值，且与rollout数量无关，易应用于不同规模。

## 8. 不足与局限

- **实验覆盖有限**：仅在一个电商数据集、一个奖励模型上验证，且在线测试时间窗口不统一，难以完全确认方法通用性。
- **超参数依赖**：\(\tau, \rho, \lambda, M\)仍需验证集调优，论文未给出详细敏感性分析。
- **依赖地面真值**：诊断需要已知真实物品，无法直接应用于纯探索或无监督场景。
- **批内负样本影响**：\(f_2\)中的负样本从同一batch采样，batch组成可能引入噪声，影响诊断稳定性。
- **训练窗口限制**：为稳定训练，仅使用一周数据（175K），未解决更长窗口下的概念漂移或reward hacking问题。
- **在线实验偏差**：GRPO+NLL和AdaGRPO的A/B测试运行于不同时间段，对比时无法排除外部因素干扰。

（完）
