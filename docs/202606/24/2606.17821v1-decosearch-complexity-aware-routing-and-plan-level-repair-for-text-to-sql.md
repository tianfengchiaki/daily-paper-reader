---
title: "DecoSearch: Complexity-Aware Routing and Plan-Level Repair for Text-to-SQL"
title_zh: DecoSearch：面向文本到SQL的复杂度感知路由与计划级修复
authors: "Esteban Schafir, Xu Zheng, Hojat Allah Salehi, Zhuomin Chen, Mo Sha, Wei Cheng, Dongsheng Luo"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.17821v1"
tags: ["query:llmapp"]
score: 6.0
evidence: LLM驱动的复杂Text-to-SQL路由
tldr: "针对大语言模型在Text-to-SQL任务中对复杂多步查询处理不佳的问题，本文提出DecoSearch无训练框架。它通过复杂度感知路由，将简单问题直接生成，复杂问题分解为有向无环图原子子问题，并利用拓扑精炼器修复执行失败的计划。在BIRD和Spider上分别取得70.53%和88.31%的执行准确率，且消耗数量级更少的token，同时可作为模型无关包装器提升微调模型。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17821v1/fig-001.webp\", \"caption\": \"Table 1: Comprehensive comparison of Text-to-SQL frameworks on BIRD and Spider benchmarks, categorized by learning approach. “Model Avail.” indicates whether model weights are publicly available.\", \"page\": 7, \"index\": 1, \"width\": 950, \"height\": 342}, {\"url\": \"assets/figures/arxiv/2606.17821v1/fig-002.webp\", \"caption\": \"Figure 3: Cumulative Execution Accuracy vs. Cumulative Token Usage (log scale) on BIRD (a) and Spider (b). DecoSearch reaches its final accuracy with fewer tokens than all compared methods on both benchmarks, demonstrating superior token efficiency.\", \"page\": 7, \"index\": 2, \"width\": 939, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2606.17821v1/fig-003.webp\", \"caption\": \"Figure 4: Execution Accuracy on BIRD when pairing DecoSearch with fine-tuned (a) and open-source (b) backbone models, compared against their DirectSQL baseline. DecoSearch consistently improves over the direct generation baseline regardless of the backbone, demonstrating its model-agnostic nature.\", \"page\": 8, \"index\": 3, \"width\": 939, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2606.17821v1/fig-004.webp\", \"caption\": \"Figure 1: The pipeline begins with Pre-Processing via evidence-aware schema pruning. A Judger determines if the question is manageable; if so, the Direct Path generates and executes a single SQL query and exits if successful. Otherwise, the Decomposer produces a DAG of atomic sub-questions linked by @[n] dependency placeholders. At the Sub-Question Solving stage, each node is executed in topological order; upstream results are resolved as inline value lists for small result sets or materialized as temporary tables for large ones before the downstream SQL is generated. Persistent execution failures trigger the Topology Refiner to rewrite the DAG and retry.\", \"page\": 3, \"index\": 4, \"width\": 888, \"height\": 361}]"
motivation: 现有方法在处理需要多步骤、数据感知推理的复杂查询时表现不佳。
method: DecoSearch通过复杂度感知路由将简单问题直接生成，复杂问题分解为DAG原子子问题，并利用拓扑精炼器修复执行失败的计划。
result: "在BIRD和Spider数据集上分别达到70.53%和88.31%的执行准确率，超越所有无训练基线，且消耗数量级更少的token。"
conclusion: DecoSearch作为无训练、模型无关的框架，有效提升Text-to-SQL性能，并可作为包装器改进微调模型。
---

## 摘要
大型语言模型（LLM）在将自然语言翻译为SQL方面展现了卓越能力，但现有方法在处理需要多步骤、数据感知推理的复杂查询时仍显不足。我们提出DecoSearch，一种无需训练的框架，通过将每个查询路由到适当级别的推理努力来解决这一问题。轻量级模式选择器首先将完整数据库模式剪枝为相关表和列。然后，LLM评判器判断问题是否需要分解：直接问题走直接生成路径，复杂问题则升级为原子子问题的有向无环图（DAG），每个子问题通过目标SQL生成步骤解决。RAG组件利用语义相似的训练示例来支撑分解器，而拓扑优化器则在执行失败表明是分解缺陷而非可修复SQL错误时重构推理计划。DecoSearch在BIRD上达到70.53%的执行准确率，在Spider上使用DeepSeek骨干网络达到88.31%，超越了所有无需训练的基线方法，同时消耗的令牌数比竞争方法低一个数量级。它还可作为模型无关的封装器，在不修改流水线的情况下持续改进微调后的SQL生成骨干网络。

## Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities in translating natural language to SQL, yet existing methods still falter on complex queries requiring multi-step, data-aware reasoning. We introduce DecoSearch, a training-free framework that addresses this by routing each query to the appropriate level of reasoning effort. A lightweight Schema Selector first prunes the full database schema to the relevant tables and columns. An LLM Judger then decides whether the question requires decomposition: straightforward questions follow a direct generation path and complex ones are escalated to a Directed Acyclic Graph (DAG) of atomic sub-questions, each solved by a targeted SQL generation step. A RAG component grounds the decomposer with semantically similar training examples, and a Topology Refiner restructures the reasoning plan when execution failures signal a flawed decomposition rather than a fixable SQL error. DecoSearch achieves 70.53% execution accuracy on BIRD and 88.31% on Spider with a DeepSeek backbone, surpassing all training-free baselines while consuming an order of magnitude fewer tokens than competing methods. It also functions as a model-agnostic wrapper, consistently improving fine-tuned SQL generation backbones without any modification to the pipeline.