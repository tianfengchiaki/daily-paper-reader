---
title: Generative Long-term User Interest Modeling for Click-Through Rate Prediction
title_zh: 面向点击率预测的生成式长期用户兴趣建模
authors: "Jiangli Shao, Kaifu Zheng, Hao Fang, Huimu Ye, Zhiwei Liu, Bo Zhang, Shu Han, Xingxing Wang"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15905v1"
tags: ["query:q1"]
score: 9.0
evidence: 专注于广告和推荐中的点击率预测
tldr: 针对CTR预测中长序列用户兴趣建模的检索效率低和兴趣不完整问题，提出GenLI模型，包含兴趣生成模块（IGM）生成多样性兴趣分布、行为检索模块（BRM）实现O(1)复杂度检索、兴趣融合模块（IFM）融合特征，无需目标中心化搜索，平衡了准确率和效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15905v1/fig-001.webp\", \"caption\": \"Figure 1: The framework of GenLI. GenLI consists of an interest generation module (IGM), a behavior retrieval module (BRM), and an interest fusion module (IFM).\", \"page\": 3, \"index\": 1, \"width\": 1056, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2605.15905v1/fig-002.webp\", \"caption\": \"Figure 3: Performance of GenLI when taking different lengths of short-term behaviors as the input of interest generation module.\", \"page\": 8, \"index\": 2, \"width\": 506, \"height\": 454}]"
motivation: 现有目标为中心的兴趣建模忽略了潜在兴趣，且基于匹配的检索随行为增长耗时严重，缺乏行为间交互信息。
method: GenLI通过IGM生成目标无关的多种兴趣分布，BRM使用简单查找实现O(1)检索，IFM通过门控机制融合兴趣特征。
result: 在准确率和效率之间取得了更好平衡，避免了复杂匹配检索，提升了兴趣多样性。
conclusion: GenLI通过生成式多兴趣建模和高效检索，有效提升了CTR预测性能。
---

## 摘要
利用海量历史用户行为建模长期用户兴趣可以提升广告和推荐系统中的点击率（CTR）预测性能。目前广泛采用一种两阶段框架：首先通过通用搜索单元（GSU）检索与目标物品最相关的 top-k 个行为，然后通过精确搜索单元（ESU）利用定制化的注意力机制生成兴趣特征。然而，当前以目标为中心（target-centered）的 GSU 会忽略其他潜在的用户兴趣，导致兴趣特征不完整且有偏。此外，GSU 中基于匹配的检索过程依赖于目标物品与每个历史行为之间的逐对相似度评分，这不仅随着用户行为的持续增长而变得耗时，而且忽略了用户行为之间的交互信息。针对这些问题，我们提出了一种名为 GenLI 的生成式长期用户兴趣模型（GenLI）用于 CTR 预测。GenLI 包括兴趣生成模块（IGM）、行为检索模块（BRM）和兴趣融合模块（IFM）。IGM 生成多个兴趣分布以表征实时用户兴趣的不同方面，该模块独立于目标物品，并融合了行为间的交互信息，确保了兴趣特征完整且多样化。BRM 通过简单的查找操作选择相关行为，将每个行为权重大小的时间复杂度降低至 O(1)。最后，IFM 使用精巧的门控机制生成兴趣特征。基于生成过程，GenLI 提升了用户兴趣的多样性，避免了复杂的基于匹配的行为检索，从而在 CTR 预测的准确性和效率之间实现了更好的平衡。

## 速览
**TLDR**：长期用户兴趣建模对点击率预测至关重要，但现有两阶段框架存在兴趣偏差和检索效率低的问题。本文提出GenLI模型，包括兴趣生成模块（IGM）生成多元兴趣分布、行为检索模块（BRM）实现O(1)时间复杂度检索、兴趣融合模块（IFM）精细融合特征。GenLI提升了兴趣多样性，避免了复杂匹配，在准确性和效率之间取得了更好平衡。 \
**Motivation**：现有目标中心化检索忽略潜在兴趣，且基于匹配的检索耗时并忽略行为间交互，导致兴趣特征不全且效率低。 \
**Method**：提出GenLI，由IGM生成与目标无关的多兴趣分布，BRM通过简单查找实现常数时间检索，IFM利用门控机制融合兴趣特征。 \
**Result**：GenLI提高了用户兴趣多样性，避免了复杂匹配检索，在CTR预测中平衡了准确性和效率。 \
**Conclusion**：GenLI通过生成式建模有效解决了现有框架的兴趣偏差和效率问题，实现了更优的点击率预测性能。

---

## Abstract
Modeling long-term user interests with massive historical user behaviors enhances click-through rate (CTR) prediction performance in advertising and recommendation systems. Typically, a two-stage framework is widely adopted, where a general search unit (GSU) first retrieves top-$k$ relevant behaviors towards the target item, and an exact search unit (ESU) generates interest features via tailored attention. However, current target-centered GSU would ignore other latent user interests, leading to incomplete and biased interest features. Additionally, the matching-based retrieval process in GSUs depends on the pairwise similarity score between target item and each historical behavior, which not only becomes time-consuming for online services as user behaviors continue to grow, but also overlooks the interaction information among user behaviors. To combat these problems, we propose a \textbf{Gen}erative \textbf{L}ong-term user \textbf{I}nterest model named GenLI for CTR prediction. GenLI consists of an interest generation module (IGM), a behavior retrieval module (BRM), and an interest fusion module (IFM). The IGM generates multiple interest distributions to indicate different aspects of real-time user interests, which is target-independent and incorporates interaction information among behaviors, ensuring complete and diverse interest features. The BRM selects related behaviors via a simple lookup operation, reducing the time complexity for weighting each behavior to $O(1)$. Finally, the IFM uses delicate gating mechanisms to generate interest features. Based on the generation process, GenLI improves the diversity of user interests and avoids complex matching-based behavioral retrieval, achieving a better balance between accuracy and efficiency for CTR prediction.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：点击率（CTR）预测是广告和推荐系统的核心任务，用户兴趣建模是提升CTR的关键。现有方法多采用两阶段框架：通用搜索单元（GSU）先检索与目标物品最相关的top‑k历史行为，再由精确搜索单元（ESU）生成兴趣特征。
- **问题**：
  - **兴趣不完整**：GSU以目标为中心（target-centered），只考虑与目标相关的兴趣，忽略用户的其他潜在兴趣，导致兴趣特征有偏。
  - **效率瓶颈**：GSU需要计算目标与每个历史行为的逐对相似度，复杂度随行为序列增长而急剧上升，影响在线服务延迟。
  - **忽略交互信息**：逐对比较方式无法捕捉行为序列内部的交互信息，影响检索准确性。
- **整体含义**：本文提出一种**生成式长期用户兴趣模型GenLI**，通过目标无关的生成方式直接产生多样化的兴趣分布，并利用查找操作实现O(1)级别的行为检索，在提升兴趣完整性的同时大幅降低计算开销，从而在CTR预测中兼顾准确率与效率。

### 2. 论文提出的方法论

- **核心思想**：摒弃传统的目标‑行为匹配范式，采用生成式方法从短期行为中推断用户实时兴趣分布，再根据分布索引直接从长序列中拾取行为，避免逐对相似度计算。
- **模型架构（GenLI）** 包含三个模块：
  1. **兴趣生成模块（IGM）**  
     - 输入：最近 \(l\) 个短期行为（如Amazon用10个，TaoBao用100个）。  
     - 流程：将短期行为嵌入堆叠为Key/Value，结合最新行为嵌入和可学习查询向量，通过多头注意力（MHA）得到隐藏兴趣表示 \(h\)，再经MLP和softmax生成离散分布 \(p \in \mathbb{R}^{N}\)（N=4096）。  
     - 生成三种分布（公式7‑10）：  
       - **显式兴趣 \(p_E\)**：以点击物品为标签，最小化负对数似然（公式8）。  
       - **隐式兴趣 \(p_I\)**：以曝光物品为标签，最小化负对数似然（公式9）。  
       - **相对兴趣 \(p_R = \text{softmax}(p_E - p_I)\)**：反映兴趣变化趋势和决策过程。
  2. **行为检索模块（BRM）**  
     - 对每个历史行为 \(b_i\)，通过查找操作 \(s(b_i, p) = p_{\text{ID}(b_i)\,\%\,N}\) 直接从分布中获取得分，时间复杂度O(1)。  
     - 分别在三种分布下取top‑k行为，共 \(K = 3k\) 个（默认k=20, K=60）。
  3. **兴趣融合模块（IFM）**  
     - 对每类检索出的行为，以目标物品嵌入为Query，通过MHA聚合得到三种兴趣嵌入 \(z_I, z_E, z_R\)。  
     - 拼接后经门控机制（sigmoid+MLP）生成最终长期兴趣特征 \(x_l\)（公式13）。  
     - 最终CTR由 \(x_l\) 与其他特征经MLP预测（公式14），损失函数为CTR损失加两种辅助损失（公式15，权重 \(\alpha=\beta=1.0\)）。
- **复杂度**：总复杂度约O(L + (l+K)d_h)，远低于传统方法的O(L·d_h)或O(L·m)。

### 3. 实验设计

- **数据集**：
  - **Amazon (Book)**：75k用户、358k物品、15万实例，行为序列长度截断/填充至100，短期行为取10。
  - **TaoBao**：987k用户、34.2M物品、796万实例，行为序列长度500，短期行为取100。
  - **Industrial (美团)**：2.4亿用户、110万物品、11亿样本，行为序列长度1000，短期行为取100。曝光物品已知，用于隐式损失。
- **Benchmark对比方法**：
  - **短兴趣模型**：DIN、DIEN。
  - **长兴趣基准**：Avg‑Pooling‑long、SIM‑hard/soft、ETA、SDIM、TWIN（SOTA）。
- **评价指标**：离线AUC；在线CTR和RPM（千次展示收入）；推理时间（ms/8192样本）。
- **实验设置**：统一嵌入维度8，MLP隐藏层200/80，多头注意力4头，隐藏维度8。训练批次256，Adam优化器，学习率0.001。所有方法保持检索总数一致（K=60）。在线A/B测试部署30%流量6天。

### 4. 资源与算力

- **未明确说明**：论文未提及使用的GPU型号、数量、训练时长等硬件信息。仅提到训练批次为256，使用Adam优化器，无更多算力细节。

### 5. 实验数量与充分性

- **实验组数及类型**（总计约6大类实验）：
  - **整体性能对比**（表3、表4）：在两个公开数据集和工业数据集上对比9种方法，重复多次取均值标准差。
  - **推理时间对比**（图2）：不同行为序列长度下（50~1000）与其他方法的延时曲线。
  - **IGM模块消融**（表5）：移除IGM、分别移除三种分布，观察AUC下降。
  - **短期行为长度影响**（图3）：l从1变化到50，AUC趋向饱和。
  - **超参数K和N的影响**（图4、图5）：K从15到75、N从256到65536的AUC变化。
  - **在线A/B测试**（表6）：真实流量下的CTR和RPM提升。
- **充分性与公平性**：
  - 实验覆盖多数据集、多基线、多维度消融，设计较全面。
  - 控制其他模块一致，只改变长期兴趣建模部分，确保公平。
  - 但未报告统计显著性检验（如t检验），且工业数据集仅给出一次结果（无标准差）。

### 6. 论文的主要结论与发现

- **性能提升**：GenLI在所有数据集上取得最优AUC。Amazon上比TWIN高0.61%，TaoBao高1.03%，工业集高0.22%。
- **效率优势**：推理时间仅略高于简单平均池化，远低于TWIN和SIM，且随着序列长度增加优势更明显。
- **兴趣多样性**：三种分布（隐式、显式、相对）均贡献性能，显式兴趣最重要。
- **在线收益**：在美团广告系统部署后，CTR提升0.776%，RPM提升1.567%，带来实际商业价值。
- **生成式检索的有效性**：在检索数量较少时（K=15），GenLI相比TWIN提升更显著（1.65%），证明目标无关的生成方式能更全面捕捉用户兴趣。

### 7. 优点

- **创新性**：首次将生成式方法用于CTR长期兴趣建模，脱离目标‑行为匹配范式。
- **兴趣完整性**：生成多种兴趣分布，覆盖隐式、显式及相对兴趣，避免仅关注目标相关兴趣。
- **高效性**：行为检索仅需O(1)查找，整体复杂度线性，适合在线部署。
- **可插拔性**：GenLI可作为即插即用模块，易于集成到任意CTR模型。
- **端到端训练**：IGM、BRM、IFM联合优化，且辅助损失与CTR损失同步训练。
- **充分实验验证**：离线+在线实验，多数据集、多基线、多角度消融，结论可靠。

### 8. 不足与局限

- **算力信息缺失**：未提供GPU型号、训练时间等，不利于复现性能或资源评估。
- **隐式损失近似**：Amazon和TaoBao无曝光数据，采用“最近相似物品”模拟曝光，可能引入偏差。工业场景中曝光数据已知，因此更具优势。
- **哈希碰撞影响**：分布维度N通过模运算映射，较低N时碰撞概率高，可能降低检索区分度。论文虽实验N=4096，但未深入分析碰撞对检索精度的影响。
- **超参数敏感性**：K、N、短期行为长度l均需调优，且随不同场景可能需重新调整，未见通用设置。
- **在线实验范围**：仅在一个平台（美团）测试，其他领域（如电商、视频）的通用性待验证。
- **统计显著性未报告**：未给出p值或置信区间，AUC的0.1%~1%差异是否显著未有严谨统计检验。
- **局限性提及**：论文未讨论模型对冷启动或短序列用户的适用性，长期行为建模本身依赖大量历史数据。

（完）
