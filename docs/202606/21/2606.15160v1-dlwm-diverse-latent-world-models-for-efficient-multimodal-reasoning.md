---
title: "DLWM: Diverse Latent World Models for Efficient Multimodal Reasoning"
title_zh: DLWM：用于高效多模态推理的多样化潜在世界模型
authors: "David Huang, Lianlei Shan"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15160v1"
tags: ["query:llmapp"]
score: 8.0
evidence: 用于多模态大语言模型高效推理的多样化潜在世界模型
tldr: "针对多模态大模型推理中视觉输入存在模糊性、现有方法依赖单一潜在假设导致计算资源浪费的问题，提出DLWM框架，通过正交正则化构建多样潜在世界假设，并利用强化学习策略自适应分配计算资源，在多个基准上准确率提升2-5%，内存减少24%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15160v1/fig-001.webp\", \"caption\": \"Figure 1: Comparison of prior multimodal reasoning methods and our proposed DLWM framework. Explicit chain-of-thought methods reason through a single textual path, while existing latent reasoning methods typically rely on a single latent state or fixed rollout process. In contrast, DLWM constructs multiple diverse latent world hypotheses and uses a resource-aware RL controller to adaptively expand, stop, merge, or answer across reasoning paths.\", \"page\": 2, \"index\": 1, \"width\": 826, \"height\": 406}, {\"url\": \"assets/figures/arxiv/2606.15160v1/fig-002.webp\", \"caption\": \"Table 1: Main benchmark results and efficiency comparison. Best results are in bold.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2606.15160v1/fig-003.webp\", \"caption\": \"Figure 2: Overview of DLWM. Given a multimodal input, the encoder produces a joint representation z, from which the Latent World Generator creates K diverse world hypotheses. Each world initializes an independent reasoning trajectory updated by a shared reasoning module. An RL controller dynamically allocates computation by selecting EXPAND, STOP, MERGE, or ANSWER actions under a budget constraint. Active worlds are aggregated via learned weighted fusion to produce the final prediction.\", \"page\": 4, \"index\": 3, \"width\": 826, \"height\": 416}]"
motivation: 现有推理方法假设单一潜在解读，无法处理视觉输入的歧义性，且统一计算策略导致资源浪费。
method: 构建多样潜在世界假设并施加正交正则化防止坍塌，通过资源感知强化学习动态决策推理路径的扩展、终止或合并。
result: "在多个多模态推理基准上准确率提升2-5%，同时内存使用降低24%。"
conclusion: DLWM通过多样假设和资源自适应分配，实现了更高效、更具鲁棒性的多模态推理。
---

## 摘要
近年来，多模态大语言模型（MLLMs）的推理能力有了显著提升。现有方法通常依赖于显式的思维链或连续的潜在空间轨迹来增强多步推理。然而，这些方法通常假设输入具有单一的潜在解释，并沿着固定路径或在统一计算预算下展开推理。在现实世界的多模态环境中，视觉观测往往受到遮挡、模糊、视角变化或语义歧义的影响，产生多种合理的解释。统一的推理策略不仅限制了模型探索多种假设的能力，还会导致高内存使用率和展开成本。我们提出了DLWM（多样化潜在世界模型），这是一种结合潜在空间推理与强化学习的多模态推理框架。首先，我们在连续潜在空间中构建一组多样化的潜在世界假设，每个假设捕捉视觉输入的一种不同合理解释，并独立地在每个假设上展开潜在推理。基于正交性的多样性正则化器明确防止假设崩溃。其次，我们将潜在推理过程形式化为一个资源约束的序列决策问题，并引入一种资源感知的强化学习策略，该策略能在假设间自适应地分配计算资源，动态决定是否扩展、终止或合并推理路径，从而显著减少内存占用并提高展开效率。在多个多模态推理基准上的实验表明，DLWM在准确率上比现有方法高出2-5个百分点，同时内存使用量减少了24%。

## Abstract
Reasoning capabilities of multimodal large language models (MLLMs) have improved considerably in recent years. Existing approaches typically rely on explicit chain-of-thought or continuous latent-space trajectories to enhance multi-step reasoning. However, these methods generally assume that an input admits a single latent interpretation and unfold reasoning along a fixed path or under a uniform computation budget. In real-world multimodal settings, visual observations are often subject to occlusion, blur, viewpoint variation, or semantic ambiguity, giving rise to multiple plausible interpretations. A uniform reasoning strategy not only limits the model's ability to explore multiple hypotheses but also incurs high memory usage and rollout cost. We present DLWM (Diverse Latent World Models), a multimodal reasoning framework that combines latent-space reasoning with reinforcement learning. First, we construct a set of diverse latent world hypotheses in continuous latent space, each capturing a different plausible interpretation of the visual input, and unfold latent reasoning independently on each hypothesis. An orthogonality-based diversity regularizer explicitly prevents hypothesis collapse. Second, we formulate the latent reasoning process as a resource-constrained sequential decision problem and introduce a resource-aware reinforcement learning policy that adaptively allocates computation across hypotheses, dynamically deciding whether to expand, terminate, or merge reasoning paths, thereby substantially reducing memory footprint and improving rollout efficiency. Experiments on multiple multimodal reasoning benchmarks demonstrate that DLWM outperforms existing methods by 2-5 points in accuracy while reducing memory usage by 24%.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多模态推理中，视觉输入常受遮挡、模糊、视角变化或语义歧义的影响，导致同一输入存在多种合理解释。现有的显式链式思维（CoT）或连续潜在空间推理方法通常假设单一潜在解读，并沿着固定路径或在统一计算预算下展开推理，这不仅限制了模型探索多个假设的能力，还会造成高内存使用和低效的展开成本。
- **整体含义**：论文旨在解决“在不确定的视觉环境下，如何有效建模并探索多种潜在解释，并自适应分配计算资源”这一核心问题，从而提升多模态推理的鲁棒性与效率。

---

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：DLWM将潜在空间推理与强化学习相结合，显式构建多个多样化的潜在世界假设（每个假设对应一种输入的可能性），并对每个假设独立展开潜在推理；同时将推理过程建模为资源约束的序列决策问题，通过资源感知的RL策略动态决定各推理路径的扩展、终止或合并，以减少冗余计算。
- **关键技术细节**：
  - **多模态编码**：冻结的视觉-语言编码器生成联合表示 \( z = Enc(I, q) \)。
  - **潜在世界生成**：一个两层MLP生成器 \( G_\phi \)，结合可学习的世界嵌入 \( e_i \)，为同一输入 \( z \) 生成 \( K \) 个不同的世界表示 \( w_i = G_\phi(z, e_i) \)。
  - **潜在推理**：每个世界初始化独立推理轨迹 \( h^{(i)}_0 = w_i \)，由共享的Transformer块（参数 \( \theta \)）迭代更新。
  - **世界多样性正则化**：采用正交性损失 \( \mathcal{L}_{orth} = \sum_{i \neq j} \left( \frac{w_i^\top w_j}{\|w_i\|\|w_j\|} \right)^2 \)，防止世界表示坍塌为同一点。
  - **多世界聚合**：推理结束后，通过轻量评分网络 \( s_\psi \) 计算每个活跃世界的权重，进行加权求和得到最终预测：
    \[
    p(y|I,q) = \sum_{i \in A} \alpha_i p_i(y), \quad \alpha_i = \text{softmax}(s_\psi(h^{(i)}_T))
    \]
  - **资源感知RL控制**：
    - 状态 \( s_t \)：包含活跃世界表示集与剩余预算。
    - 动作空间：Expand(i)、Stop(i)、Merge(i,j)、Answer。
    - 奖励设计：\( R = R_{acc} - \lambda_T T - \lambda_K \bar{K} \)（鼓励准确同时惩罚步数与活跃世界数）。
    - 训练采用两阶段：先监督预训练（固定展开长度），再用REINFORCE（带移动平均基线）微调RL策略。

---

### 3. 实验设计：数据集、基准与对比方法

- **数据集**：四个多模态推理基准：
  - **MMVP**：侧重视觉歧义与精细感知。
  - **GQA**：组合式、多跳关系推理。
  - **VQAv2**：日常场景，语言先验强。
  - **ScienceQA**：跨模态科学知识推理。
- **对比方法**：
  - 显式CoT方法：[1]。
  - 文本域潜在推理：Coconut [4]、CoLaR [5]。
  - 多模态潜在推理：LVR [6]、LaRe [7]、Heima [8]。
  - 推理时潜在控制：Soft Thinking [9]。
- **评估指标**：任务准确率（按各数据集官方标准），以及效率指标（平均展开步数、活跃世界数、GPU内存、推理吞吐量）。

---

### 4. 资源与算力

- GPU：8块A100 GPU。
- 优化器：AdamW，学习率 \( 2\times 10^{-5} \)。
- 批量大小：64。
- 精度：混合精度训练。
- 骨干：ViT-L/14视觉编码器冻结，仅优化后续模块。
- 训练策略：两阶段（监督预训练 + RL微调），具体轮次或总步数未明确给出，但提到与基线总训练步数一致。

---

### 5. 实验数量与充分性

- **实验组数**：主结果（4个数据集，8个方法对比）、效率对比表、组件消融（6种变体）、世界数量消融（K=1,2,4,6,8）、计算预算消融（B=4,6,8,10,12）、动作空间消融（5种策略）、训练策略对比（4种）、聚合方法与多样性正则化对比，以及附录中的更多消融（共约10+组实验）。
- **充分性与公平性**：
  - 所有方法使用相同骨干、相同输入预处理、相同评估脚本、相同最大推理预算。
  - 消融实验覆盖了核心组件，验证了多样世界、正交正则、RL分配、动作设计的必要性。
  - 多次随机种子重复（附录提及），但正文未报告标准差，不过仍可认为实验设计较为全面客观。
- **不足之处**：仅测试静态图像，未覆盖视频或交互场景；未报告训练收敛曲线或具体训练时间。

---

### 6. 论文的主要结论与发现

- DLWM在所有四个基准上取得最高准确率，尤其在MMVP上提升最大（比LVR高5.2点，比LaRe高4.4点），证明多假设建模对处理视觉歧义的有效性。
- 效率方面，DLWM平均展开步数5.0、活跃世界数2.2，内存14.5GB，吞吐量8.9样本/秒，在性能-效率平衡上优于所有基线（如Heima虽快但精度低）。
- 消融实验表明：多世界、正交正则、RL分配、完整动作空间均为关键组件，缺失任一都会导致性能或效率显著下降。
- 资源感知策略能根据实例难度动态调整计算：简单样本提前终止，困难样本保留更多路径。

---

### 7. 优点：方法或实验设计上的亮点

- **方法亮点**：
  - **显式多样假设建模**：与单路径或隐式多路径方法不同，DLWM在潜在空间显式构造多个语义不同的假设，并通过正交正则防止坍塌，增强了模型探索多种可能性的能力。
  - **资源感知动态分配**：将推理视为预算约束的MDP，用RL控制扩展、终止、合并等操作，实现了“按需分配算力”，避免了固定展开的线性开销。
  - **两阶段训练策略**：先监督预训练稳定表示，再RL微调优化分配，缓解了RL初期不稳定性。
  - **并行世界共享推理模块**：共享参数使参数量不随K线性增长。
- **实验设计亮点**：
  - 在多个难度不同、侧重不同的基准上评估，结果有说服力。
  - 消融实验系统全面，覆盖组件、超参数、动作空间、训练策略等多方面。
  - 效率指标（步数、内存、吞吐量）与准确率并重，充分展示实用价值。

---

### 8. 不足与局限

- **实验覆盖**：仅在4个静态图像基准上评测，未涉及视频理解、视觉对话或交互式推理任务，泛化性有待验证。
- **硬件效率**：尽管内存比LVR降低24%，但生成K个世界并维持并行推理仍带来额外开销；在极大规模模型或实时响应场景下可能需要进一步优化。
- **RL稳定性**：RL训练仍对超参数敏感（如奖励系数），附录承认存在不稳定性，且两阶段训练增加了调参复杂度。
- **可解释性**：RL策略可能产生不透明的决策（如为何终止或合并某路径），缺乏可视化或归因分析。
- **统计报告**：正文未给出多次运行的标准差或置信区间，虽附录提及但略显不足。
- **未探讨更复杂的多样性正则化**：正交损失简单有效，但可能存在更优的选择（如对比学习、互信息最大化），论文未深入比较。

（完）
