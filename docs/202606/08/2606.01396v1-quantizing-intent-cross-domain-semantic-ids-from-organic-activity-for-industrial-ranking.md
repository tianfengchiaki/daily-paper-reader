---
title: "Quantizing Intent: Cross-Domain Semantic IDs from Organic Activity for Industrial Ranking"
title_zh: 量化意图：用于工业排序的来自有机活动的跨域语义ID
authors: "Julie Choi, Haoran Ye, Zhiwei Ding, Bo Long, Benjamin Zelditch, Arpita Vats"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01396v1"
tags: ["query:q2"]
score: 9.0
evidence: 来自有机活动的跨域语义ID用于工业广告排序
tldr: "针对广告点击率预测中用户监督稀疏问题，提出从有机feed活动中提取跨域用户语义ID（SID），并采用RQ-FSQ方法高效量化嵌入，在工业排名系统中冷启动用户取得最高+1.522% AUC提升，验证了跨域行为迁移的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01396v1/fig-001.webp\", \"caption\": \"Figure 2: Integration of the HDE Module into the Ads Ranking Model. Left: The viewer SID (𝑐1, . . . , 𝑐𝐾 ) is encoded once per request by the HDE Module (Section 4.4), producing euser — a dense vector constant across all 𝐿 positions in the sequence. Right: Per-event features at position 𝑡 are encoded through standard embedding tables. Fusion (Eq. 10): At every position 𝑡 , the user embedding is summed with the event-level embeddings before a shared LayerNorm, yielding input tokens {h𝑡 }. The resulting sequence is processed by the Transformer stack and scored by the CTR head. All HDE embedding tables are trained end-to-end under the CTR objective.\", \"page\": 5, \"index\": 1, \"width\": 452, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-002.webp\", \"caption\": \"Table 2: RQ-FSQ on two heterogeneous pre-trained embedding sources. 𝚫AUC is measured against the no-SID baseline — the production ranking model with no member SID input. Storage is the per-user serialization footprint relative to the raw float32 embedding of the same source. RQ-FSQ matches the dense embedding’s AUC on both sources at 30–280× smaller storage.\", \"page\": 7, \"index\": 2, \"width\": 682, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-003.webp\", \"caption\": \"Table 1: Cross-domain user SID results (RQ-KMeans quantization, 𝐾=3 codes per source). ΔAUC relative to no-SID baseline. Upper block: single-source SIDs ordered by behavioral activity richness. Lower block: multi-source combinations. The RQ-FSQ comparison against the same sources appears in Table 2.\", \"page\": 6, \"index\": 3, \"width\": 740, \"height\": 267}]"
motivation: 广告CTR预测受限于用户稀疏的广告交互监督，而用户在其他界面有丰富行为，但跨域信号迁移因领域不匹配和成本问题难以实现。
method: 通过有机feed活动生成用户语义ID（SID），并提出残差有限标量量化（RQ-FSQ）将预训练嵌入离散化，同时保持相近AUC并大幅压缩存储；设计层次离散嵌入模块端到端学习多级SID。
result: "RQ-FSQ在Feed Activity嵌入上实现+0.351% AUC且存储减少约30倍，在Activity-Tuned LLaMA嵌入上实现+0.265% AUC且存储减少约280倍；冷启动用户获得最高+1.522% AUC提升。"
conclusion: 跨域行为语义ID能有效桥接稀疏历史行为，提升排序效果，尤其对无历史交互用户显著。
---

## 摘要
广告点击率（CTR）预测受到稀疏用户监督的限制：大多数用户很少点击广告，但在有机场景（如信息流）中产生密集的行为证据。由于领域不匹配、服务成本和工业复杂性，将这些跨域信号迁移到广告排序中十分困难。我们引入了从有机信息流活动导出的跨域用户语义ID（SIDs），并证明行为活动丰富程度决定了跨域迁移质量：基于用户画像文本的SIDs提升AUC +0.036%，基于活动微调的LLaMA用户嵌入模型的SIDs提升+0.107%，而基于直接信息流活动行为嵌入的SIDs提升+0.213%。我们进一步提出RQ-FSQ，一种残差有限标量量化方法，将预训练嵌入离散化，同时以大幅减小的存储空间匹配稠密嵌入的AUC。在两个异构来源上，RQ-FSQ匹配或略微超过稠密源嵌入，在信息流活动上以约30倍存储精简实现+0.351% AUC提升，在活动微调LLaMA上以约280倍存储精简实现+0.265% AUC提升。我们还引入了一个层次化离散嵌入模块，通过前缀n-gram稀疏嵌入表对多级SIDs进行编码，并在CTR目标下端到端训练。在一个大规模的工业广告排序系统中，冷启动片段分析显示，对于广告交互历史几乎为零的用户，提升高达+1.522%，验证了跨域行为迁移作为稀疏历史排序的有效桥梁。

## Abstract
Ads click-through rate (CTR) prediction is constrained by sparse user supervision: most users engage with ads infrequently while generating dense behavioral evidence in organic surfaces such as feed. Transferring these cross-domain signals into ads ranking is difficult due to domain mismatch, serving cost, and production complexity. We introduce cross-domain user Semantic IDs (SIDs) derived from organic feed activity and show that behavioral activity richness governs cross-domain transfer quality: SIDs from user profile text yield +0.036% AUC, SIDs from an activity-tuned LLaMA-based user embedding model yield +0.107%, and SIDs from direct feed activity behavioral embeddings yield +0.213%. We further propose RQ-FSQ, a residual finite scalar quantization method that discretizes pre-trained embeddings while matching dense-embedding AUC at substantially smaller storage. Across two heterogeneous sources, RQ-FSQ matches or slightly exceeds dense source embeddings, achieving +0.351% AUC for Feed Activity at about 30x smaller storage and +0.265% AUC for Activity-Tuned LLaMA at about 280x smaller storage. We also introduce a Hierarchical Discrete Embedding module that encodes multi-level SIDs through prefix n-gram sparse embedding tables trained end-to-end under the CTR objective. In a large-scale industrial ads ranking system, cold-start segment analysis shows gains up to +1.522% for users with near-zero ad interaction history, validating cross-domain behavioral transfer as an effective bridge for sparse-history ranking.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

### 研究动机与背景
- **核心问题**：广告点击率（CTR）预测面临严重的用户监督稀疏性问题。大多数用户很少点击广告，但会在有机场景（如信息流、Feed）中产生丰富的交互行为。如何有效利用这些跨域行为信号来提升广告排序性能，是一个具有挑战性的工业级难题。
- **三个主要障碍**：1) Feed与广告域之间的特征空间不匹配；2) 稠密行为嵌入的高维度与高服务成本；3) 大规模生产训练管道难以容忍对每个特征源进行定制化预处理。
- **现有工作的局限**：语义ID（SID）此前主要应用于单域推荐场景（如物品ID的量化），而跨域用户视角的SID研究尚属空白。
- **本文贡献**：首次在工业广告CTR模型中实证研究跨域用户SID，提出了核心设计原则（行为活动丰富度）与实用方法（RQ-FSQ、HDE模块、多源SID），在大型生产系统上取得了显著提升。

## 2. 论文提出的方法论

### 2.1 跨域用户SID构建
- 对稠密用户嵌入 v ∈ R^d 使用残差量化，生成 K 级离散令牌序列：SID(v) = (c1, c2, ..., cK)，其中每个 ck ∈ {1,...,C}，C为码本大小，K为量化层级数（本文默认K=3，C=1024）。

### 2.2 两种量化方法
- **RQ-KMeans**：确定性残差K均值。逐级分配最近邻质心并计算残差，简单高效，适合从零训练SID。
- **RQ-FSQ（残差有限标量量化）**：针对预训练嵌入提出，结合两种互补机制：
  - **残差VAE量化（RQ-VAE）**：捕获全局几何结构，多级残差量化。
  - **有限标量量化（FSQ）**：对每个维度独立标量量化到有限整数集 L = {-L,...,L}，保留每维度精细结构。
  - 融合方式：下游模型中两个分支的嵌入相加：e_RQ-FSQ = e_{RQ} + f(e_{FSQ})，f为线性投影。
  - 优点：在约30~280倍存储压缩下，匹配甚至略微超过稠密浮点嵌入的AUC。

### 2.3 多源SID（Multi-Source SID）
- 结合三个互补的跨域用户嵌入源，形成9码结构化表示（每源3码）：
  - **c1-c3：Activity-Tuned LLaMA SID**（基于用户画像文本，但通过对比微调学习隐式行为意图，覆盖最广）
  - **c4-c6：Profile Qwen SID**（纯文本语义，无行为信号）
  - **c7-c9：Feed Activity SID**（一年窗口直接聚合的Feed交互行为，行为最丰富）
- 缺失源通过基于Activity-Tuned LLaMA的**骨干-推理机制**进行补全：训练残差VAE从现有源映射到缺失源，避免用户零填充。
- 级联回退：当骨干也缺失时，使用填充码（padding code, ck=0），HDE模块将其映射为零嵌入，不影响其他源。

### 2.4 层次化离散嵌入模块（HDE Module）
- 将K级SID转化为稠密用户嵌入，使用前缀n-gram稀疏嵌入表，哈希查找大小有界（内存上限H_max）。
  - **Level 1**（前缀unigram）：直接查表 W1[ c1 ]。
  - **Level k ≥ 2**（前缀k-gram）：多项式哈希 (c1,...,ck) 到索引，查表 W_k[hash]。
  - 所有层嵌入求和：e = Σ e_k。
- 多源SID：每源独立应用HDE模块，最终所有源嵌入求和：e_user = Σ_s HDE(s^(s))。
- **集成方式**：请求级SID（用户维度恒定）广播到序列所有位置，与事件级嵌入加和后经 LayerNorm 输入Transformer栈，CTR目标端到端训练。

## 3. 实验设计

### 3.1 数据集与场景
- **主要实验**：来自大型工业推荐平台的广告日志，60天训练，后续1天评估。所有AUC增益相对于无SID的生产基线。
- **公开验证**：MovieLens-100K数据集，用作RQ-FSQ与RQ-KMeans对比的补充验证。

### 3.2 基准模型
- **生产基线**：解码器-only Transformer广告CTR排序模型（8层，FlashAttention，session masking），无用户SID输入。
- **对比方法**：
  - 无SID参考
  - 单源SID（Profile Qwen SID, Activity-Tuned LLaMA SID, Feed Activity SID）
  - 多源组合（独立组合 vs 结构化多源SID）
  - RQ-FSQ对比：稠密浮点嵌入、RQ-KMeans、FSQ单独。

### 3.3 评估指标
- 相对AUC增益（ΔAUC）。工业标准：+0.1%离线AUC可靠对应在线CTR影响。

### 3.4 超参数
- 默认：码本C=1024，层次K=3，嵌入维度D=64，哈希表H由内存上限H_max决定。
- RQ-FSQ：每维度量化等级L=16（4 bits），承诺损失系数β=0.25。
- 学习率：HDE表η_HDE=0.02，Transformer权重η_TR=4×10^{-4}。

## 4. 资源与算力

- **文中说明**：模型训练使用DDP/FSDP2（fully sharded data parallel）在H200 GPU集群上进行。
- **未明确给出**：具体GPU数量、训练时长、总计算量等均未披露。仅提及“大规模工业部署”“H200集群”，属于典型工业配置，但无法精确量化。

## 5. 实验数量与充分性

### 实验组数
- **单源SID实验**：3种源（Profile Qwen、Activity-Tuned LLaMA、Feed Activity），共3组。
- **多源组合实验**：独立组合、结构化多源SID，共2组。
- **RQ-FSQ对比实验**：在两个源上分别对比稠密、RQ-KMeans、FSQ、RQ-FSQ，共2×4=8组。
- **冷启动分段分析**：3个用户分段（最冷启动、低频、高频），1组。
- **公开验证**：MovieLens-100K，1组（4种方法）。
- **消融/未明确**：未展示超参数扫描、不同K/C组合、不同哈希表大小等的系统性消融，仅在文中提及基于内部初步实验选择默认值。

### 充分性与客观性
- **优点**：实验覆盖了核心假设验证（行为丰富度原理）、多源有效性、量化方法对比、冷启动效果、公开数据集验证，设计较为完整。
- **潜在不足**：① 未报告标准差或置信区间，未说明重复次数；② 缺少对RQ-KMeans与RQ-FSQ在不同码本大小下的详细敏感性分析；③ 未与其他跨域方法（如EMCDR、CoNet）进行直接对比；④ 公开验证（MovieLens-100K）规模小，且与工业场景差异较大；⑤ 未展示多源SID的独立贡献分析（如仅去掉某源的影响）。

## 6. 论文的主要结论与发现

1. **行为活动丰富度原则**：跨域SID的AUC增益单调依赖于源嵌入中编码的行为活动量：纯文本+0.036%，活动微调LLaMA+0.107%，直接行为聚合+0.213%。什么用户做（行为）比用户是什么（画像）更重要。
2. **RQ-FSQ作为通用离散化器**：在两个异构源上（低维Feed Activity、高维Activity-Tuned LLaMA），RQ-FSQ匹配甚至略微超过稠密浮点AUC，同时实现30~280倍存储压缩。
3. **结构化多源SID优于独立组合**：9码结构化表示（+0.296% AUC）比三个独立单源求和（+0.260%）高出+0.036%，源于避免跨源哈希冲突和骨干推理补全。
4. **冷启动用户获益最大**：最冷启动用户段（近零广告历史）AUC提升高达+1.522%，远高于高频用户的+0.131%，验证跨域行为SID作为行为桥梁的有效性。

## 7. 优点

- **方法创新性**：首次将跨域用户SID用于工业广告CTR排序，提出RQ-FSQ结合残差与标量量化的混合方案，在存储与AUC之间取得优越平衡。
- **设计原则明确**：行为活动丰富度原则提供了清晰、可操作的源选择指南。
- **生产部署友好**：HDE模块使用标准哈希稀疏嵌入表，端到端训练，不改变现有Transformer骨干，无在线延迟成本，易于集成。
- **实验完整且具说服力**：从单一源到多源、从离线AUC到冷启动段分析、从私有数据到公开数据集，验证了方法的泛化性和有效性。
- **隐私优势**：离散SID相比稠密向量暴露更少原始行为信息。

## 8. 不足与局限

- **缺乏统计显著性报告**：未给出AUC的方差或置信区间，无法判断结果是否显著。
- **消融实验不充分**：对K、C、哈希表大小等超参数敏感性分析缺失；未展示去掉某单个源对多源SID的影响。
- **对比基线有限**：未与其他跨域推荐方法（如EMCDR、CoNet、GenCDR）直接对比，仅与无SID基线对比。
- **公开验证**：仅用MovieLens-100K一个简单基准，且量化参数经缩小，不能完全代表工业场景。
- **分布偏移风险**：实验数据为60天训练+1天评估，但未讨论长期时间漂移或鲁棒性。
- **应用限制**：方法依赖预训练的用户嵌入（LLaMA、Qwen、Feed聚合），这些嵌入的获取需要复杂的基础设施和大量计算资源，中小机构可能难以复现。另外，跨域SID可能引入用户兴趣关联，需遵守隐私与合规审查。

（完）
