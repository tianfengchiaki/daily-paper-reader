---
title: "Quantizing Intent: Cross-Domain Semantic IDs from Organic Activity for Industrial Ranking"
title_zh: 量化意图：来自自然活动的跨领域语义ID用于工业排序
authors: "Julie Choi, Haoran Ye, Zhiwei Ding, Bo Long, Benjamin Zelditch, Arpita Vats"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01396v1"
tags: ["query:q2"]
score: 9.0
evidence: 利用有机活动跨域语义ID和LLaMA进行广告点击率预测
tldr: "论文提出从有机Feed活动中提取跨域用户语义ID（SID），用于广告点击率预测。实验表明，行为活动丰富度决定迁移质量：活动行为嵌入的SID带来+0.213% AUC提升。提出RQ-FSQ离散化方法，在存储减少30-280倍下匹配或超越原始嵌入。引入分层离散嵌入模块，在工业系统中冷启动用户获得+1.522%提升。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01396v1/fig-001.webp\", \"caption\": \"Figure 2: Integration of the HDE Module into the Ads Ranking Model. Left: The viewer SID (𝑐1, . . . , 𝑐𝐾 ) is encoded once per request by the HDE Module (Section 4.4), producing euser — a dense vector constant across all 𝐿 positions in the sequence. Right: Per-event features at position 𝑡 are encoded through standard embedding tables. Fusion (Eq. 10): At every position 𝑡 , the user embedding is summed with the event-level embeddings before a shared LayerNorm, yielding input tokens {h𝑡 }. The resulting sequence is processed by the Transformer stack and scored by the CTR head. All HDE embedding tables are trained end-to-end under the CTR objective.\", \"page\": 5, \"index\": 1, \"width\": 452, \"height\": 292}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-002.webp\", \"caption\": \"Table 2: RQ-FSQ on two heterogeneous pre-trained embedding sources. 𝚫AUC is measured against the no-SID baseline — the production ranking model with no member SID input. Storage is the per-user serialization footprint relative to the raw float32 embedding of the same source. RQ-FSQ matches the dense embedding’s AUC on both sources at 30–280× smaller storage.\", \"page\": 7, \"index\": 2, \"width\": 682, \"height\": 227}, {\"url\": \"assets/figures/arxiv/2606.01396v1/fig-003.webp\", \"caption\": \"Table 1: Cross-domain user SID results (RQ-KMeans quantization, 𝐾=3 codes per source). ΔAUC relative to no-SID baseline. Upper block: single-source SIDs ordered by behavioral activity richness. Lower block: multi-source combinations. The RQ-FSQ comparison against the same sources appears in Table 2.\", \"page\": 6, \"index\": 3, \"width\": 740, \"height\": 267}]"
motivation: 用户广告交互稀疏，而有机表面行为丰富，但跨域信号迁移存在领域不匹配、成本高和复杂度问题。
method: 从有机Feed活动提取用户语义ID，使用RQ-FSQ量化预训练嵌入，并通过分层离散嵌入模块编码多级SID。
result: "活动行为SID提升AUC 0.213%，RQ-FSQ在30-280倍存储压缩下达到相近或更好性能，冷启动用户提升1.522%。"
conclusion: 基于有机活动的跨域语义ID能有效桥接稀疏历史，提升广告排序效果。
---

## 摘要
广告点击率（CTR）预测受限于稀疏的用户监督：大多数用户很少与广告互动，同时在自然界面（如信息流）中产生密集的行为证据。由于领域不匹配、服务成本和生成复杂性，将这些跨领域信号迁移到广告排序中较为困难。我们引入了从自然信息流活动派生的跨领域用户语义ID（SIDs），并表明行为活动丰富性决定了跨领域迁移质量：来自用户画像文本的SIDs提升AUC +0.036%，来自基于活动调优的LLaMA用户嵌入模型的SIDs提升+0.107%，而来自直接信息流活动行为嵌入的SIDs提升+0.213%。我们进一步提出了RQ-FSQ，一种残差有限标量量化方法，将预训练嵌入离散化，同时以显著更小的存储量匹配密集嵌入的AUC。在两个异构源上，RQ-FSQ匹配或略微超过密集源嵌入，信息流活动以约30倍存储压缩实现+0.351% AUC提升，活动调优LLaMA以约280倍存储压缩实现+0.265% AUC提升。我们还引入了一个层次离散嵌入模块，通过前缀n-gram稀疏嵌入表编码多级SIDs，并在CTR目标下进行端到端训练。在一个大规模工业广告排序系统中，冷启动片段分析显示，对于广告交互历史近乎为零的用户，提升高达+1.522%，验证了跨领域行为迁移作为稀疏历史排序的有效桥梁。

## Abstract
Ads click-through rate (CTR) prediction is constrained by sparse user supervision: most users engage with ads infrequently while generating dense behavioral evidence in organic surfaces such as feed. Transferring these cross-domain signals into ads ranking is difficult due to domain mismatch, serving cost, and production complexity. We introduce cross-domain user Semantic IDs (SIDs) derived from organic feed activity and show that behavioral activity richness governs cross-domain transfer quality: SIDs from user profile text yield +0.036% AUC, SIDs from an activity-tuned LLaMA-based user embedding model yield +0.107%, and SIDs from direct feed activity behavioral embeddings yield +0.213%. We further propose RQ-FSQ, a residual finite scalar quantization method that discretizes pre-trained embeddings while matching dense-embedding AUC at substantially smaller storage. Across two heterogeneous sources, RQ-FSQ matches or slightly exceeds dense source embeddings, achieving +0.351% AUC for Feed Activity at about 30x smaller storage and +0.265% AUC for Activity-Tuned LLaMA at about 280x smaller storage. We also introduce a Hierarchical Discrete Embedding module that encodes multi-level SIDs through prefix n-gram sparse embedding tables trained end-to-end under the CTR objective. In a large-scale industrial ads ranking system, cold-start segment analysis shows gains up to +1.522% for users with near-zero ad interaction history, validating cross-domain behavioral transfer as an effective bridge for sparse-history ranking.

---

## 论文详细总结（自动生成）

# 基于论文《Quantizing Intent: Cross-Domain Semantic IDs from Organic Activity for Industrial Ranking》的详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：广告点击率（CTR）预测面临严重的数据稀疏性——大多数用户与广告的交互极少，但在自然界面（如信息流）中却产生大量行为信号。如何将这些跨域（有机→广告）的行为信号高效、低成本地迁移到工业级广告排序模型中，是一个尚未被充分研究的挑战。
- **研究背景**：已有工作主要关注单域语义ID（SID）用于推荐，而跨域视角——从有机行为中提取用户SID并用于广告CTR——缺乏实证研究。传统的跨域迁移方法（如EMCDR、CoNet）需要显式对齐或重叠用户/物品集，且引入额外复杂性。离散SID作为紧凑、可端到端微调的接口，有可能成为解决该问题的桥梁。
- **整体含义**：论文首次系统验证了跨域用户SID在工业广告排序中的有效性，并提出了一系列可复用的技术组件（RQ-FSQ量化、HDE模块、多源SID结构），为大规模推荐系统利用外部行为信号提供了新范式。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- **行为活动丰富性原则**：跨域SID的迁移质量单调依赖于源表示中编码的“行为活动”数量。即，直接的行为聚合优于隐式行为编码，优于纯文本画像。
- **离散化为迁移接口**：将用户嵌入通过残差量化（RQ-KMeans或RQ-FSQ）离散为紧凑的整型码序列，作为特征输入广告排序模型，通过CTR损失端到端微调，自动实现领域适应。

### 2.2 关键技术细节
#### 2.2.1 SID构建方法
- **RQ-KMeans**：确定性残差k-means，逐级分配最近邻码字，不优化重建损失，适用于从零训练的场景。
- **RQ-FSQ**（核心贡献）：结合残差VAE量化（RQ-VAE）与有限标量量化（FSQ）。  
  - FSQ对每个维度独立量化：`c_FSQ = round(tanh(v)·L)`，L为量化级别（论文取16）。  
  - RQ-VAE对残差进行K级码本查找。  
  - 下游融合：`e_RQ-FSQ = e_RQ + f(c_FSQ)`，f为线性投影。  
  - 优势：保留全局几何（RQ-VAE）与每维精细结构（FSQ），在存储压缩30-280倍下匹配甚至超越原始浮点嵌入的AUC。

#### 2.2.2 多源SID（Multi-Source SID）
- 三源组成：Activity-Tuned LLaMA（隐式行为，覆盖最广）、Profile Qwen（纯文本）、Feed Activity（直接行为聚合）。
- 结构化9码（每源3级）：`c1-c3`（LLaMA）、`c4-c6`（Qwen）、`c7-c9`（Feed Activity）。
- 缺失时基于LLaMA主干进行VAE重建，保留人口覆盖；若主干也缺失则用零填充。
- 独立HDE模块编码每源，嵌入求和：`e_user = Σ HDE(s^{(s)})`。

#### 2.2.3 层次离散嵌入模块（HDE Module）
- 输入：K级SID `(c1,...,cK)`。  
- 第1级：前缀unigram直接查表 `e1 = W1[c1]`。  
- 第k级（k≥2）：前缀k-gram多项式哈希 `idx_k = Σ (c_j-1)·C^{k-j} mod H`，查表 `e_k = W_k[idx_k]`。  
- 所有表大小受`H_max`上限控制，压缩因子α=4，确保有限内存。  
- 所有表随机初始化，以更高学习率（0.02 vs Transformer 4e-4）端到端训练。

#### 2.2.4 集成到广告排序模型
- 用户SID为请求级（对所有序列位置常数），与事件级特征嵌入相加后经LayerNorm输入Transformer解码器。
- 数据管线：离线预计算SID，广播为`(B, L, K)`整数张量。

## 3. 实验设计：数据集/场景、基准、对比方法

### 3.1 数据集与场景
- **主要实验**：工业级广告平台真实生产日志，60天训练，后1天评估。所有AUC增益相对于无SID基线，绝对值因保密不披露。
- **公开验证**：MovieLens-100K（用户-电影评分数据集），使用all-MiniLM-L6-v2编码电影文本，用户嵌入为喜欢电影的平均，预测评分≥4的二分类。

### 3.2 基准（Baseline）
- 无SID生产基线（decoder-only Transformer排序模型，具有上下文条件注意力、RoPE、会话掩码、FlashAttention）。
- 此外，在RQ-FSQ对比中包含：原始浮点嵌入（dense）、RQ-KMeans、FSQ单独使用。

### 3.3 对比方法
- **单源SID比较**：Profile Qwen SID、Activity-Tuned LLaMA SID、Feed Activity SID（三者行为活动丰富度递增）。
- **量化方法比较**：RQ-KMeans、FSQ、RQ-FSQ（论文方法）。
- **多源SID比较**：独立组合（三个SID独立索引后求和） vs 结构化多源SID（论文方法）。
- **冷启动分段**：按广告历史交互频率分为最冷启动（底部8%）、不频繁（中间64%）、频繁（顶部28%）。

## 4. 资源与算力

- 文中仅在第3节提到训练使用“DDP/FSDP2 on H200 clusters”，未明确给出GPU数量、具体型号（H200推断为NVIDIA H200）、训练时长等细节。
- HDE模块的额外训练开销几乎为零（与标准嵌入表共享SGD步骤），推理延迟可忽略。因此整体算力需求与基线模型相当。

## 5. 实验数量与充分性

### 5.1 实验组数
- **主实验**：单源SID 3种、多源组合2种（独立+结构化）、冷启动分段分析3类、RQ-FSQ在两种源上的4种方法对比 → 共约10+组对照。
- **公开数据集验证**：4种方法（无SID、dense、RQ-KMeans、RQ-FSQ）在MovieLens上的对比。

### 5.2 充分性评价
- **充分**：系统验证了“行为活动丰富性原则”的三个梯度，证明单调趋势；冷启动分段分析直接验证迁移机制；RQ-FSQ在异构源（低维Feed Activity、高维LLaMA）上一致表现；公开数据复现了RQ-FSQ > RQ-KMeans的顺序。
- **客观/公平**：控制随机种子、批大小、学习率调度、模型深度等所有超参数；RQF-SQ的FSQ分支使用线性投影而非复杂网络；严格固定的生产模型 backbone。
- **不足**：未在更多公开推荐数据集（如Amazon、Yelp）上进行跨域SID验证；缺少对量化过程中信息损失的定量分析（如重建误差）；未探讨不同K和C组合的全面敏感性（仅在内部试探后固定K=3, C=1024, D=64）。

## 6. 论文的主要结论与发现

1. **行为活动丰富性决定迁移质量**：纯文本画像SID → +0.036% AUC，活动调优LLaMA SID → +0.107% AUC，直接Feed活动聚合SID → +0.213% AUC。
2. **RQ-FSQ通用离散化器**：在Feed Activity和Activity-Tuned LLaMA两种源上，RQ-FSQ匹配或略超原始浮点AUC，且存储压缩30×–280×，优于RQ-KMeans和单独FSQ。
3. **多源结构化SID优于独立组合**：结构化9码多源SID（+0.296% AUC）比独立求和（+0.260% AUC）额外提升+0.036%，归因于避免跨源哈希冲突和缺失时的主干填充。
4. **跨域SID缓解冷启动**：最冷启动用户获得+1.522% AUC提升，远高于不频繁用户（+0.874%）和频繁用户（+0.131%），说明跨域行为信号是稀疏历史的有效替代。
5. **公开数据验证**：MovieLens-100K上RQ-FSQ相比无SID提升+6.54% AUC，高于RQ-KMeans（+5.26%）和dense用户嵌入（+3.89%）。

## 7. 优点：方法或实验设计上的亮点

- **理论的简洁性与可操作性**：“行为活动丰富性原则”提供了清晰的选择规则，可直接指导工程实践。
- **RQ-FSQ的创新性**：巧妙结合两种量化策略，互补保留全局与局部结构，且可热启动（从已有RQ-KMeans系统出发），具备即插即用特性。
- **工业级集成设计**：HDE模块使用前缀n-gram哈希表，在有限内存下实现层次编码；多源SID通过独立前缀表实现零架构修改，通过主干填充处理缺失，确保了100%覆盖。
- **冷启动分段分析**：直接揭示了迁移机制的实际价值，增强了结果的说服力。
- **公开数据复现**：在MovieLens上使用开放模型（all-MiniLM-L6-v2）复现了RQ-FSQ的优越性，提升了结果的可信度和可推广性。

## 8. 不足与局限

- **实验覆盖的局限性**：仅在一个工业级广告平台数据集上进行主实验，缺乏跨平台/跨领域（如电商搜索、视频推荐）的验证；公开实验仅使用小型MovieLens-100K，且任务简化为二分类，与工业CTR差距较大。
- **超参数敏感性未充分探讨**：K=3、C=1024、L=16等关键参数选择基于内部试点，但未系统性展示在不同设置下的鲁棒性；Hash表压缩因子α和上限H_max的选取也未给出消融。
- **迁移假设依赖预训练嵌入质量**：SID受益于嵌入本身的质量，若源嵌入（如Profile Qwen）未经活动调优，增益可能缩小；文中未分析嵌入质量对迁移的下界影响。
- **隐私与公平性考量有限**：虽指出离散令牌比密集向量更有利隐私，但未探讨SID可能放大关联推断或对不同人群的偏差影响（如冷启动用户收益大，但频繁用户收益微小，可能加剧差异）。
- **量化损失分析缺失**：RQ-FSQ如何影响嵌入空间拓扑（如保持类间距离）未见定量分析；重建误差或码本利用率未报告。
- **可复现性限制**：因工业数据不可公开，外部研究者无法完全复现主实验结果；公开数据的实验规模小，且使用不同模型（MLP而非Transformer），结论的普适性需更多验证。

（完）
