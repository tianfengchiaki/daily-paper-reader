---
title: "On the Memorization Behavior of LLMs in Generative Recommendation: Observations, Implications, and Training Strategies"
title_zh: 大语言模型在生成式推荐中的记忆行为：观察、启示与训练策略
authors: "Sunwoo Kim, Sunkyung Lee, Clark Mingxuan Ju, Donald Loveland, Bhuvesh Kumar, Kijung Shin, Neil Shah, Liam Collins"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17276v3"
tags: ["query:llmapp"]
score: 9.0
evidence: 研究生成式推荐中LLM的记忆行为
tldr: 本文研究LLM在生成式推荐中的记忆行为，发现其倾向于通过单跳记忆预测用户目标，但忽略了更丰富的物品关系。作者提出IIRG训练策略，结合多跳协作关系和语义关系，显著提升了LLM在推荐任务上的表现。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17276v3/fig-001.webp\", \"caption\": \"Figure 12: Training sample of semantic neighbor generation with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 14, \"index\": 1, \"width\": 950, \"height\": 246}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-002.webp\", \"caption\": \"Figure 11: Training sample of semantic neighbor generation with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 14, \"index\": 2, \"width\": 950, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-003.webp\", \"caption\": \"Figure 9: Training sample of collaborative neighbor generation with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 13, \"index\": 3, \"width\": 950, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-004.webp\", \"caption\": \"Figure 10: Training sample of collaborative neighbor generation with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 13, \"index\": 4, \"width\": 950, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-005.webp\", \"caption\": \"Figure 3: One-hop memorization and performance. LLMs trained solely with next-item prediction exhibit stronger one-hop memorization than TIGER, a nonLLM-based model (see (a)). The relative improvement of LLMs over TIGER is larger for one-hopmemorization-benefiting users than for non-one-hopmemorization-benefiting users in 5/6 settings (see (b)).\", \"page\": 4, \"index\": 5, \"width\": 452, \"height\": 630}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-006.webp\", \"caption\": \"Figure 18: Generalization to other semantic IDs (SIDs). The observation that LLMs rely more heavily on one-hop memorization than TIGER, as well as the effectiveness of IIRG in reducing memorization and improving recommendation performance, also holds under Residual-K-Means (R-K-Means) SIDs.\", \"page\": 19, \"index\": 6, \"width\": 934, \"height\": 187}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-007.webp\", \"caption\": \"Figure 8: Training sample of next-item prediction with semantic IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 12, \"index\": 7, \"width\": 950, \"height\": 206}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-008.webp\", \"caption\": \"Figure 7: Training sample of next-item prediction with term IDs. Given the instruction and input, the LLM is trained to generate the output autoregressively using teacher forcing.\", \"page\": 12, \"index\": 8, \"width\": 950, \"height\": 208}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-009.webp\", \"caption\": \"Figure 4: Overview of IIRG. To train LLM-based GR models, IIRG uses three tasks and jointly optimizes them:\", \"page\": 5, \"index\": 9, \"width\": 794, \"height\": 283}, {\"url\": \"assets/figures/arxiv/2606.17276v3/fig-010.webp\", \"caption\": \"Figure 13: Informativeness of our proposed neighbors. The ratio of users benefiting from our collaborative and semantic neighbors is comparable to that of users benefiting from one-hop memorization (see (a)). Moreover, the three neighbor types have a Jaccard similarity below 0.5, suggesting that they are not merely redundant (see (b)). In addition, among non-memorization-benefiting users, our neighbors benefit a group of users comparable in size to the memorization-benefiting group (see (c)).\", \"page\": 16, \"index\": 10, \"width\": 929, \"height\": 296}]"
motivation: 现有LLM-based GR方法忽视了LLM的过度记忆倾向，导致无法有效利用预训练知识，限制了模型对非单跳模式用户的推荐性能。
method: 提出IIRG训练策略，同时捕获物品间的多跳协作关系（基于序列共现）和语义关系（基于主题相似性），增强LLM的泛化能力。
result: IIRG相比标准下一项预测训练，在测试集上显著提升，尤其对单跳未覆盖的用户表现突出。
conclusion: 通过抑制LLM的过度记忆并引导其学习更丰富的物品关系，可显著提升生成式推荐的泛化性能。
---

## 摘要
生成式推荐（GR）已成为推荐系统的一个有前景的方向。近年来，大语言模型（LLMs）越来越多地被用于GR，因为其丰富的预训练知识有望帮助它们超越传统以记忆为导向的基线模型所能捕捉的常见用户行为模式。然而，现有的基于LLM的GR工作很大程度上忽略了LLMs众所周知的记忆倾向，如果这种倾向出现在为GR微调的LLMs中，将限制其对预训练知识的利用。在这项工作中，我们通过考察一跳记忆（即模型推荐训练数据中物品的直接后继物品）来研究这一担忧。我们发现，LLMs比非基于LLM的GR模型更常出现这种情况——实际上，它们相对于GR基线的大部分增益实际上来自那些目标物品可以通过一跳记忆预测的用户。我们推测，提高其余用户的性能需要LLMs学习超越一跳转移的丰富物品-物品关系。为此，我们提出了IIRG，一种新颖的训练策略，教导LLMs捕捉：（1）来自用户序列中多跳物品共现的协同关系，以及（2）主题相似物品之间的语义关系，这两者都可以作为有用的推荐信号。我们证明，IIRG显著优于仅使用标准下一项预测训练的LLMs，尤其是对于那些测试物品未被训练时一跳转移覆盖的用户，其增益尤为显著。

## Abstract
Generative recommendation (GR) has emerged as a promising direction for recommender systems. Recently, large language models (LLMs) have been increasingly adopted for GR, as their rich pretrained knowledge is expected to help them generalize beyond common user behavior patterns that traditional memorization-oriented baselines can capture. However, existing LLM-based GR works largely ignore LLMs' well-known tendency to memorize, which, if present in LLMs fine-tuned for GR, would restrict their utilization of pretrained knowledge. In this work, we investigate this concern by examining one-hop memorization, where a model recommends items that are direct successors of items in the training data. We show that LLMs do this more than non-LLM-based GR models-in fact, the vast majority of their gains over GR baselines are actually on users whose target items can be predicted through one-hop memorization. We intuit that improving performance on the remaining users requires LLMs to learn richer item-item relations beyond one-hop transitions. To achieve this, we propose IIRG, a novel training strategy that teaches LLMs to capture: (1) collaborative relations derived from item co-occurrences across multiple hops in user sequences, and (2) semantic relations among items with similar themes, both of which can serve as useful recommendation signals. We show that IIRG significantly improves over LLMs trained solely with standard next-item prediction, with especially large gains for users whose test items are not covered by train-time one-hop transitions.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：大语言模型（LLMs）在生成式推荐（GR）中展现出潜力，但其固有的**记忆倾向**（memorization）可能导致模型过于依赖训练数据中的局部模式（如“一跳记忆”），从而无法充分运用预训练知识，限制了泛化能力。
- **整体含义**：本文首次系统分析LLM-based GR中的记忆行为，发现LLM比非LLM GR模型（如TIGER）更依赖一跳记忆，且其性能提升主要集中在此类用户上，而对其他用户帮助有限。为此提出**IIRG**训练策略，引导LLM学习更丰富的物品关系（多跳协作关系和语义关系），以提升整体推荐质量。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：在标准下一项预测（next-item prediction）之外，增加两个辅助生成任务，迫使LLM捕获**物品间的协作关系**和**语义关系**，从而超越仅依赖直接相邻跳的记忆。
- **关键技术细节**：
  - **协作邻居生成**：对每个物品 \( i_k \)，统计与其在用户序列中距离不超过窗口 \( W \)（默认5）的共现频率，选取频率最高的 \( N_1 \) 个物品作为协作邻居序列 \( n_k \)。训练LLM根据物品 \( i_k \) 的标识符（ID + 标题）生成该序列。
  - **语义邻居生成**：利用预训练语言模型（Qwen-3-8B embedding）对物品描述编码，计算余弦相似度，选取最相似的 \( N_2 \) 个物品作为语义邻居序列 \( m_k \)。训练LLM生成该序列。
  - **联合训练**：每个batch混合三个任务样本，总损失为：
    \[
    \mathcal{L} = \sum_{u \in \mathcal{U}^{(b)}} \frac{\mathcal{L}_u}{B} + \sum_{i_k \in \mathcal{I}_C^{(b)}} \lambda_1 \frac{\mathcal{L}_k^{(C)}}{B} + \sum_{i_t \in \mathcal{I}_S^{(b)}} \lambda_2 \frac{\mathcal{L}_t^{(S)}}{B}
    \]
    其中 \(\mathcal{L}_u\) 为下一项预测损失，\(\mathcal{L}_k^{(C)}\) 和 \(\mathcal{L}_t^{(S)}\) 分别为协作和语义邻居生成任务的交叉熵损失，\( \lambda_1, \lambda_2 \) 为超参数。
  - **标识符支持**：IIRG支持语义ID（SID）和基于术语的ID（TID），无需修改模型结构。
- **算法流程**（文字说明）：数据预处理阶段为每个物品生成协作和语义邻居列表；训练阶段，LLM每个batch从三个任务中随机采样样本，同时优化所有损失；推理阶段采用束搜索生成Top-K推荐。

## 3. 实验设计

- **数据集**：Amazon Reviews的三个子领域（Sports、Toys、Beauty）以及Yelp（附录）。采用留一法（leave-one-out）划分训练/验证/测试集，最后一个行为作为测试，倒数第二个作为验证。
- **Benchmark**：全物品排名（full-item ranking），使用Recall@K和NDCG@K（K=5,10）。
- **对比方法**：共17个基线，涵盖：
  - 传统模型：LightGCN、SimGCL、SASRec、FDSA、S3-Rec
  - 非LLM生成式推荐：TIGER、LETTER
  - 带辅助任务的LLM GR：P5、ReAT、LC-Rec、EAGER-LLM
  - LLM-based GR：PLUM、OneRec-Think、Naive（标准下一项预测）、GRAM、GRLM、AgenticTagger
- **骨干模型**：Qwen-3.5-4B（默认），也验证了0.8B、2B、9B等规模。

## 4. 资源与算力

- 论文在Appendix C.2中明确：所有训练在Google Cloud Platform上运行。
  - 4B及以下模型：单节点 **a2-highgpu-8g**（96 vCPUs, 680 GB内存, 8×NVIDIA A100 40GB GPU）。
  - 9B模型：单节点 **a2-ultragpu-8g**（96 vCPUs, 1,360 GB内存, 8×NVIDIA A100 80GB GPU）。
- 训练轮数：3个epoch，batch size 128（使用梯度累积）。
- 未给出精确训练时长，但基于上述硬件可推断为小时级。

## 5. 实验数量与充分性

- **主要实验**：Table 1展示了3个数据集上Recall@5/10和NDCG@5/10的全表比较（共24个指标）。
- **消融实验**：Table 2验证了协作生成、语义生成各自及组合的贡献；附录D.8还比较了随机邻居、直接使用邻居作为推荐等变体。
- **分析实验**：
  - 验证LLM一跳记忆占比高于TIGER（Figure 3a）
  - 将用户分为“从中受益”和“不从中受益”两组，展现性能差距（Figure 3b）
  - 展示IIRG降低记忆依赖并提升非受益用户性能（Figure 6）
  - 测试SID通用性（Figure 5，附R-K-Means SID的Figure 18）
  - 冷启动物品分析（Appendix D.9）
  - 正则化技术效果（Appendix D.2）
  - 模型大小影响（Appendix D.3）
  - 输出有效性（Appendix D.7）
  - Yelp数据集验证（Appendix D.10）
- **充分性评价**：实验数量充足，覆盖多数据集、多基线、多维度分析，消融设计合理。公平性方面，所有LLM方法采用相同骨干和调参策略。但仅使用Qwen系列模型，未测试其他LLM家族（如Llama、Mistral），是潜在局限。

## 6. 论文的主要结论与发现

- LLM-based GR模型比非LLM模型（TIGER）更依赖一跳记忆，且该依赖不能通过增大模型规模或常规正则化消除。
- LLM相对于TIGER的性能提升主要来自那些目标物品能被一跳记忆覆盖的用户，对非覆盖用户提升有限。
- IIRG通过引入协作和语义邻居生成任务，显著降低一跳记忆依赖，并在所有数据集上取得最优性能，对非受益用户的提升（平均约40%）远大于受益用户（平均约16%）。
- IIRG支持多种标识符（TID/SID），并且在冷启动物品推荐上表现出色。

## 7. 优点

- **方法简洁有效**：仅增加两个生成任务，无需改变模型架构或增加推理复杂度。
- **标识符无关**：兼容语义ID和术语ID，可应用于不同GR框架。
- **实验全面**：涵盖大量基线、多数据集、多维度分析，结果具有说服力。
- **启发性分析**：首次揭示LLM在GR中的记忆行为及其与性能的关系，为后续研究提供洞察。
- **代码开源**：提供完整代码和数据集，利于复现。

## 8. 不足与局限

- **未知因素**：论文未识别导致一跳记忆的底层原因（如数据重复、模型结构等），仅指出模型大小不是主因。
- **LLM种类有限**：仅使用Qwen-3.5系列，未验证Llama、Mistral等其他主流模型，结论的泛化性待确认。
- **数据集局限**：主要基于Amazon电商和Yelp餐饮数据，缺乏新闻、视频等场景，可能存在领域偏差。
- **协作邻居计算依赖序列数据**：在稀疏序列场景下可能不够稳定，未深入分析。
- **辅助任务增加训练压力**：需要为每个物品预计算邻居列表，且邻居可能包含噪声（尤其语义邻居依赖嵌入质量）。

（完）
