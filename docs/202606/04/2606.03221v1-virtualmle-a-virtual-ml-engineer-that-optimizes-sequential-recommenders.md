---
title: "VirtualMLE: A Virtual ML Engineer that Optimizes Sequential Recommenders"
title_zh: VirtualMLE：优化序列推荐器的虚拟机器学习工程师
authors: "Shiteng Cao, Jingwen Liu, Junda She, Zhiheng Li"
date: 2026-06-02
pdf: "https://arxiv.org/pdf/2606.03221v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 用于优化序列推荐的LLM智能体框架
tldr: 本文提出VirtualMLE，一个基于LLM的智能体框架，用于自动化优化序列推荐模型。它通过执行-反思-记忆更新的闭环，利用LLM的推理能力进行调参，在SASRec和HSTU上取得与人工调参相当的推荐质量且需要更少的试验次数，并展示了调参启发式跨数据集迁移的潜力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.03221v1/fig-001.webp\", \"caption\": \"Table 3: Ablation study of VirtualMLE on Baby and Beauty.\", \"page\": 5, \"index\": 1, \"width\": 494, \"height\": 263}, {\"url\": \"assets/figures/arxiv/2606.03221v1/fig-002.webp\", \"caption\": \"Table 1: Overall performance on three datasets. R@K and N@K denote Recall@K and NDCG@K, respectively. The best results are in bold and the second-best results are underlined. “Improv.” denotes the relative improvement of VirtualMLE over the best baseline within the same backbone group.\", \"page\": 4, \"index\": 2, \"width\": 1044, \"height\": 611}, {\"url\": \"assets/figures/arxiv/2606.03221v1/fig-003.webp\", \"caption\": \"Table 2: Search efficiency on Baby and Beauty. “#Trials” is the trial index at which the best validation NDCG@10 was first reached. Lower is better.\", \"page\": 4, \"index\": 3, \"width\": 494, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2606.03221v1/fig-004.webp\", \"caption\": \"Table 4: Robustness of VirtualMLE across LLM backbones on the SASRec backbone. “Cost” is the monetary cost (USD) of LLM calls for one complete optimization session, computed from public token pricing.\", \"page\": 6, \"index\": 4, \"width\": 496, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2606.03221v1/fig-005.webp\", \"caption\": \"Figure 1: Overview of VirtualMLE compared with conventional AutoML paradigms. Conventional AutoML (left) treats each (dataset, model) pair as an independent black-box problem, whereas VirtualMLE (right) closes a reflect & plan → act → observe → update loop over a hierarchical memory system, redirecting compute from exhaustive exploration to cognitive pruning.\", \"page\": 3, \"index\": 5, \"width\": 1056, \"height\": 525}]"
motivation: 手工调参序列推荐模型效率低下，需要自动化智能体替代人工试错。
method: 构建LLM智能体框架，通过执行、反思和层级记忆更新形成闭环优化。
result: 在Amazon三个基准上，以更少试验达到与人工调参相当的推荐质量。
conclusion: 具备反思和记忆的LLM智能体可作为虚拟工程师，自动化序列推荐优化中的启发式学习。
---

## 摘要
大型语言模型（LLM）的最新进展在推理、反思和工具利用方面展现了卓越的能力，为自动化复杂工程工作流程开辟了新的范式。然而，在序列推荐（SR）领域，针对新数据集的模型调优仍然严重依赖经验丰富的机器学习工程师的手动试错。为弥补这一差距，我们提出 VirtualMLE，一个LLM智能体框架，利用LLM的认知能力将推荐优化组织为执行、反思和记忆更新的闭环。每次试验后，智能体明确分析观察到的结果，并将简洁的启发式反馈存储在层次化记忆系统中。我们在三个亚马逊SR基准上使用两个代表性骨干模型SASRec和HSTU评估了VirtualMLE。VirtualMLE以显著更少的试验次数达到了具有竞争力的推荐质量。此外，我们观察到从先前数据集中提炼的认知摘要可以显著加速未见数据集上的搜索过程，展示了调优启发式迁移的潜力。总体而言，我们的结果提供了有力证据，表明配备反思和记忆的LLM智能体可以作为实用的虚拟工程师，在SR优化中自动化并摊销启发式学习。我们的代码已公开。

## Abstract
Recent advancements in Large Language Models (LLMs) have demonstrated remarkable capabilities in reasoning, reflection, and tool utilization, unlocking new paradigms for automating complex engineering workflows. However, in the domain of sequential recommendation (SR), tuning models on new datasets still relies heavily on the manual trial-and-error of experienced machine learning engineers. To bridge this gap, we propose \textbf{VirtualMLE}, an LLM-agent framework that leverages the cognitive capabilities of LLMs to organize recommender optimizing into a closed loop of execution, reflection, and memory update. After each trial, the agent explicitly analyzes the observed outcomes and stores concise heuristic feedback in a hierarchical memory system. We evaluate VirtualMLE on three Amazon SR benchmarks with two representative backbones, SASRec and HSTU. VirtualMLE reaches competitive recommendation quality with substantially fewer trials. Furthermore, we observe that cognition summaries distilled from previous datasets can significantly accelerate the search process on unseen datasets, demonstrating the potential of transferring tuning heuristics. Overall, our results provide compelling evidence that LLM agents equipped with reflection and memory can serve as practical virtual engineers to automate and amortize heuristic learning in SR optimization. Our codes are available.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：在序列推荐（Sequential Recommendation, SR）领域，模型调优（如 SASRec、HSTU）高度依赖工程师手动试错，需要大量试验；传统超参数优化方法（如网格搜索、贝叶斯优化）效率低、无法理解因果关系，且每次新任务从头开始。
- **动机**：利用 LLM 的推理、反思和工具使用能力，构建一个虚拟 ML 工程师（VirtualMLE），将调优过程自动化、认知化，并实现跨数据集的知识迁移，从而减少人工成本和试验次数。

## 2. 方法论：核心思想、关键技术细节与流程

- **核心思想**：将调优视为一个闭环过程，包含**执行（act）→ 观察（observe）→ 反思（reflect）→ 计划（plan）→ 更新记忆（update）**，通过 LLM 驱动的因果分析和层次化记忆，将试验经验转化为可复用的启发式知识。
- **关键技术细节**：
  - **沙盒执行协议**：调优仅基于验证集，测试集完全隔离，防止信息泄漏。
  - **反思与认知摘要（Cognition Summary）**：每次试验后，LLM 分析变化与指标提升/下降的因果关系，将经验提炼为可迁移的规则（如稀疏数据偏好强正则化、低注意力学习率等）。
  - **层次化记忆系统**：
    - **长期记忆**：存储跨数据集、跨骨干模型的持久知识（包括数据集观察、模型级知识）。
    - **短期记忆**：记录当前会话的试验轨迹（配置、验证指标、反思结果）。
  - **闭环自优化流程**：每次试验更新短期记忆；反思后改进局部理解；完成一个数据集后，将经验蒸馏为认知摘要写入长期记忆，实现跨数据集零样本迁移。
- **公式/算法逻辑**：文中给出了成本对比公式：
  - 传统 AutoML 成本 = |S| × C_train
  - VirtualMLE 成本 = |S_effective| × C_train + |S_trials| × C_LLM_reflection
  其中 |S_effective| 远小于 |S|，从而显著降低训练试验次数。

## 3. 实验设计

- **数据集**：Amazon-2014 评论基准中的三个子集：**Baby**、**Beauty**、**Pet**。预处理：移除交互少于5的用户和物品，按留一法（leave-one-out）划分。
- **骨干模型**：**SASRec** 和 **HSTU**（两种代表性序列推荐模型）。
- **对比方法**：
  - **大型生成式推荐器**：Tiger、ReaRec、Lc-Rec、OpenOneRec。
  - **AutoML 调优的轻量骨干**：普通 SASRec/HSTU、+Grid Search、+Bayesian Optimization、+OPRO（LLM作为优化器，无反思/记忆）。
  - **VirtualMLE** 自身及其消融变体。
- **评价指标**：Recall@K 和 NDCG@K（K=5,10），最终模型通过验证集 NDCG@10 选择。

## 4. 资源与算力

- **未明确说明**：文中没有提及具体使用的 GPU 型号、数量、训练时长等算力信息。仅提到 LLM 调用成本（GPT-5.4、Kimi 2.6、DeepSeek V4 Pro 的 token 定价），以及试验次数（例如 VirtualMLE 在 Baby/HSTU 上仅需 32 次试验达到最优，而贝叶斯优化需 464 次）。

## 5. 实验数量与充分性

- **主要实验组**：
  - 主要性能对比（表1）：3个数据集 × 2个骨干 × 8种方法，共 48 个指标结果（实际展示为每个骨干一组，每组包含多个方法）。
  - 搜索效率对比（表2）：2个数据集 × 2个骨干 × 3种方法（BO、OPRO、VirtualMLE），记录达到最佳验证 NDCG@10 的试验次数。
  - 消融实验（表3）：2个数据集 × 2个骨干 × 3种变体（完整、w/o Reflection、w/o Memory）。
  - 收敛行为分析（图2）：Baby/SASRec 上的三个变体对比，包括跨域初始化。
  - 定性分析：给出反思轨迹和认知摘要示例。
  - 鲁棒性实验（表4）：在 Baby/Beauty 上用不同 LLM 骨干（GPT-5.4, Kimi 2.6, DeepSeek V4 Pro）运行，比较精度和成本。
- **充分性与公平性**：
  - 对比方法均采用相同数据管道和验证协议，最终由验证集 NDCG@10 选择。
  - 网格搜索枚举了 729 个配置，贝叶斯优化运行 500 次，OPRO 和 VirtualMLE 共享相同动作接口，仅差异在反思/记忆模块。
  - 实验覆盖了不同骨干、不同数据集、不同 LLM 骨干，消融和鲁棒性实验充分。
  - **客观性与公平性**：较好，但未做统计显著性检验（如 t 检验），且仅使用三个数据集（均为 Amazon 同一来源），泛化性有限。

## 6. 主要结论与发现

1. **性能领先**：VirtualMLE 在所有 (数据集、骨干、指标) 上排名第一，相对普通骨干平均提升 21.4%–31.9%，并超越所有大型生成式推荐器（如 OpenOneRec）。
2. **效率巨大提升**：达到最佳验证 NDCG@10 所需的试验次数仅为贝叶斯优化的 1/5–1/27，比 OPRO 减少 3–8 倍。
3. **反思与记忆互补**：消融显示，单独去除反思和记忆都会导致显著下降，且效果大致可加，说明两者协同作用。
4. **跨数据集知识迁移有效**：用 Pet+Beauty 提炼的认知摘要初始化 Baby 的搜索，可缩短约 2/3 的试验（从28次降至10次），验证了启发式知识的可迁移性。
5. **鲁棒性**：不同 LLM 骨干（GPT-5.4、Kimi 2.6、DeepSeek V4 Pro）下性能差距在 ±1.5% 以内，弱 LLM 仅需更多试验但能达到相似质量，说明核心收益来自反思-记忆机制而非特定 LLM。

## 7. 优点

- **创新性**：首次将 LLM 代理的反思和层次化记忆应用于序列推荐调优，提出认知摘要实现跨数据集知识迁移。
- **实用性**：显著减少试验次数，降低计算成本；支持结构选择（不仅是标量超参），可处理更复杂的调优决策。
- **实验设计全面**：包含主要性能、效率、消融、收敛分析、定性案例和跨 LLM 鲁棒性验证，证据链完整。
- **公平对比**：与多种 AutoML 方法、大型生成式模型进行对比，且严格控制实验设置（沙盒、验证集选择、相同动作接口）。
- **开源贡献**：代码已公开，促进复现与应用。

## 8. 不足与局限

- **数据集单一性**：仅使用 Amazon-2014 的 3 个子集，未在更大规模、不同领域（如新闻、视频、社交媒体）的 SR 基准上验证，泛化性存疑。
- **计算开销未完整报告**：未说明训练骨干模型所需的 GPU 时间，仅给出 LLM 调用的 token 成本；实际部署中 LLM 调用次数（约 20–50 次）也可能产生可观延迟和费用。
- **统计显著性缺失**：未提供多次运行的方差或显著性检验（如 t 检验），虽然注明结果平均 3 次运行，但未展示波动范围。
- **消融仅针对单一骨干/数据集**：消融实验仅展示了 Baby 和 Beauty 两个数据集的结果，未覆盖 Pet，且未探讨记忆大小、反思频率等超参影响。
- **对弱 LLM 的假设**：虽然显示鲁棒性，但未测试更弱或开源的轻量 LLM（如 LLaMA-7B），实际部署时可能受限于推理能力。
- **认知摘要的可解释性**：定性示例虽展示了反思轨迹，但未量化这种抽象规则对搜索空间剪枝的实际效果（如减少了多少无效配置）。
- **搜索空间的限制**：VirtualMLE 的动作空间由一份 markdown 文件定义，若设计不完整可能遗漏重要调优维度。

（完）
