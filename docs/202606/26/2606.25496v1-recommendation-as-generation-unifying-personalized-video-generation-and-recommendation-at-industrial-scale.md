---
title: "Recommendation as Generation: Unifying Personalized Video Generation and Recommendation at Industrial Scale"
title_zh: 推荐即生成：工业规模下统一个性化视频生成与推荐
authors: "Yanhua Cheng, Bo Wang, Haotian Zhang, Xinyuan Gao, Zhihui Yin, Ben Xue, Yongzhi Li, Jieting Xue, Ye Ma, Minquan Wang, Jiahui Li, Tianyu Xu, Zhiqiang Liu, Xiao Lin, Shiyang Wen, Changcheng Li, Liu Liu, Quan Chen, Peng Jiang, Kun Gai"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25496v1"
tags: ["query:q1"]
score: 9.0
evidence: 统一生成式推荐与视频生成用于个性化视频广告
tldr: "传统短视频推荐系统受限于固定视频库，无法捕捉细粒度动态偏好。本文提出推荐即生成（RaG）范式，通过共享语义ID解耦内容与风格，统一生成式推荐与视频生成，并利用视频生成代理和跨域奖励学习机制，在工业平台实现个性化视频按需生成，在线测试广告收入提升1.87%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25496v1/fig-001.webp\", \"caption\": \"Figure 1: Recommendation paradigm shift. (a) DLRMs retrieve videos from a fixed content pool, leading to suboptimal matches when user interests fall outside the pool; (b) Our paradigm generates personalized videos on demand that both align with the user interests predicted by a GRM and are driven by real user feedback in a closed loop, breaking the fixed-pool limit.\", \"page\": 1, \"index\": 1, \"width\": 494, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2606.25496v1/fig-002.webp\", \"caption\": \"Table 4: Reward ablation with corresponding evaluation metrics. For each reward component, we report its dedicated metric on a corresponding evaluation set, comparing the policy trained with that reward (Ours) against the no-reward base policy (Base).\", \"page\": 7, \"index\": 2, \"width\": 492, \"height\": 279}, {\"url\": \"assets/figures/arxiv/2606.25496v1/fig-003.webp\", \"caption\": \"Figure 2: Architecture of the Recommendation-as-Generation (RaG) framework. Videos are encoded into Disentangled Semantic IDs (DSIDs) that decouple content and creative semantics, forming a shared latent interface for recommendation and generation. The Generative Recommendation Model (GRM) predicts a user’s interest D-SIDs from user context. The Instruction Model (IM) then translates these predicted D-SIDs, together with optional metadata, into shot-level production instructions, which are executed by the Video Generation Agents (VGAs) through hierarchical planning and refinement. The full pipeline is jointly optimized under Synergistic Cross-Domain Reward Learning (SCRL).\", \"page\": 3, \"index\": 3, \"width\": 1052, \"height\": 485}, {\"url\": \"assets/figures/arxiv/2606.25496v1/fig-004.webp\", \"caption\": \"Figure 4: Qualitative example of interest-driven personalized video generation in advertising scenarios.\", \"page\": 8, \"index\": 4, \"width\": 1056, \"height\": 319}]"
motivation: 传统推荐系统依赖预生产视频库，难以适应细粒度、动态的用户兴趣。
method: 提出RaG范式，使用共享语义ID统一推荐与生成，通过视频生成代理进行层次化视频创作，并引入协同跨域奖励学习优化。
result: "在工业平台部署，在线A/B测试显示广告收入提升1.87%，优于强基线。"
conclusion: 闭环生成系统是整合个性化视频生成与推荐的有前景范式，可进一步驱动收益增长。
---

## 摘要
传统的短视频推荐系统将用户兴趣与固定预制作视频池匹配，这限制了其捕捉细粒度、动态偏好的能力。我们提出“推荐即生成”（RaG）这一新范式，根据推断的用户兴趣按需生成个性化视频。我们的框架通过共享语义ID（SIDs）统一了生成式推荐与视频生成，将视频表示解耦为内容语义和创意风格语义，从而既能对用户兴趣进行细粒度建模，又能可控地生成与兴趣对齐的视频。我们进一步开发了视频生成智能体（VGAs），其以推断的SIDs为条件，驱动视频创作的分层规划与优化，包括视觉构图、音频对齐以及艺术效果增强。为优化该框架，我们有效引入了一种协同跨域奖励学习机制，共同强化兴趣对齐、用户反馈与视频质量评估。我们在拥有超过4亿日活跃用户的工业级平台上部署RaG，并在创收关键型广告场景中进行评估。在线A/B测试显示，与强生产GRM基线相比，广告收入提升高达1.87%，证明了其在生成式推荐之外进一步推动收入增长的有效性。我们的结果凸显了闭环生成系统作为将个性化视频生成融入推荐的有前途范式。

## Abstract
Traditional short-video recommendation systems match user interest to a fixed pool of pre-produced videos, which limits their ability to capture fine-grained and dynamic preferences. We propose Recommendation-as-Generation (RaG), a new paradigm that generates personalized videos on demand from inferred user interest. Our framework unifies generative recommendation and video generation through shared semantic IDs (SIDs), which disentangle video representation into content semantics and creative style semantics, enabling both fine-grained modeling of user interest and controllable generation of interest-aligned videos. We further develop Video Generation Agents (VGAs) that are conditioned on inferred SIDs to drive hierarchical planning and refinement for video creation, including visual composition, audio alignment, and artistic effect enhancement. To optimize the framework, we effectively introduce a synergistic cross-domain reward learning mechanism that jointly enforces interest alignment, user feedback, and video quality assessment.   We deploy RaG on an industrial-scale platform with over 400 million daily active users and evaluate it in a revenue-critical advertising scenario. Online A/B tests show up to 1.87% ad revenue improvement compared to a strong production GRM baseline, demonstrating its effectiveness in driving further revenue gains beyond generative recommendation. Our results highlight a closed-loop generative system as a promising paradigm for integrating personalized video generation into recommendation.

---

## 论文详细总结（自动生成）

# 详细中文总结：推荐即生成：工业规模下统一个性化视频生成与推荐

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：传统短视频推荐系统（DLRMs）依赖一个固定的预制作视频池，只能从池中检索最优内容，无法动态生成新内容来满足用户细粒度、长尾、动态变化的兴趣。即使先进的生成式推荐模型（GRMs）使用语义ID预测用户兴趣，仍受限于检索范式，无法打破固定池的限制。同时，推荐和视频生成在数据表示、学习目标和优化方式上存在根本性差异，难以统一；工业级场景下还需解决高质量、低成本、低延迟的个性化视频生成挑战。
- **整体含义**：本文提出“推荐即生成”（RaG）新范式，将推荐从“在固定池中检索”转变为“根据推断的用户兴趣直接生成个性化视频”，构建一个闭环生成系统，统一推荐与视频生成，实现按需内容生产。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
- 使用**解耦语义ID（Disentangled Semantic IDs, D-SIDs）**作为推荐和生成之间的统一接口。D-SIDs 将每个视频分解为**内容语义**（实体、主题）和**创意风格语义**（风格、节奏、氛围）两个独立部分，既支持细粒度兴趣建模，又支持可控视频生成。

### 关键技术细节

#### （1）解耦语义视频编码器
- **多模态表示学习**：基于 Qwen2.5-VL-7B-Instruct，通过内部密集描述模型生成内容描述和创意描述，与视觉特征联合编码，使用对比损失 (式6) 和正交性约束 (式7) 分离内容/创意表示，得到两个 L2 归一化向量 \( z_{\text{content}}, z_{\text{creative}} \)。
- **离散化**：对每个向量分别采用残差量化（RQ-KMeans）得到离散代码序列：内容 SIDs（\( s^{1:L}_{\text{content}} \)）和创意 SIDs（\( s^{1:L}_{\text{creative}} \)），构成 D-SIDs。每个模态独立码本，4层，每层8192个码字。

#### （2）生成式推荐模型（GRM）
- 输入用户上下文（静态特征+行为序列），自回归预测用户兴趣的 D-SIDs（式2）。GRM 使用 Transformer 解码器（7层、hidden size 768），优化交叉熵损失，随后通过 GDPO 强化学习微调。

#### （3）指令模型（IM）
- 将 D-SIDs 和可选元数据（产品信息、营销主题）翻译为镜头级视频生产指令（场景、运动、风格等）。使用 Qwen3-8B，通过三阶段训练：① 固定骨干，仅训练投影器对齐 D-SIDs 与语言空间；② 联合微调骨干；③ 使用奖励优化（SCRL）。损失为标准下一个词预测（式12）。

#### （4）视频生成智能体（VGAs）
- 将视频生成建模为顺序决策过程，包含三个角色子智能体（共享 Qwen2.5-32B 骨干）：**视觉规划智能体**（生成故事板）、**音频对齐智能体**（生成语音和背景音乐）、**效果增强智能体**（添加字幕、特效、行动按钮）。
- 智能体通过状态序列（共享前缀 + 阶段相关上下文 + 角色提示）区分，利用 KV-cache 复用降低延迟。每轮采用**有界反思循环**（最多2次迭代）改善跨模态一致性。

#### （5）协同跨域奖励学习（SCRL）
- 定义三类奖励：**视频质量**（视觉、音视频一致性、特效）、**兴趣对齐**（指令与 SID 对齐、视频与 SID 对齐）、**用户反馈**（真实交互 + 预估密集信号）。
- 公式化为约束优化：用户反馈为主目标，质量和对齐为不等式约束（式17），使用 GDPO 进行组解耦归一化和组相对优势计算（式18），再通过 PID 更新拉格朗日乘子，训练目标为式19。

## 3. 实验设计：数据集/场景、benchmark、对比方法

### 实验场景
- **部署平台**：快手广告系统，超过4亿日活跃用户。在**收入关键型广告场景**进行在线 A/B 测试。

### 在线实验
- **对比方法**：
  - 生产 DLRM 基线（基于特征的深度学习推荐模型）。
  - GRM 基线（仅用生成式推荐，仍检索固定池）。
  - 增强 GRM：GRM + D-SIDs（替换原有 SID，仍检索固定池）。
  - 完整 RaG：GRM + D-SIDs + IM + VGAs + SCRL（生成而非检索）。
- **指标**：广告收入（Rev.）相对提升。

### 离线实验（消融与评估）

#### （1）解耦 SID 质量
- **语义检索**：在产品级检索设置下，对比 VLM2Vec-V2、QARM、Qwen2.5-VL-7B，使用 Recall@1/5/10。
- **离散化质量**：对比 QARM（相同码本配置），指标为压缩率（Cpr.）和碰撞率（Col.）。

#### （2）指令模型配置
- 评估不同数据规模（100K vs 1M）和模型大小（8B vs 32B）下的解码保真度（余弦相似度）。

#### （3）视频生成系统级比较
- **基线**：传统工作流（固定顺序的 rough-cut + fine-cut 流水线）。
- **评估**：
  - 自动评分（平均/中位数，三位多模态评估器 GPT-5.1、Gemini-2.5 Pro、Claude-4.5 Sonnet）。
  - 自动胜率（Good-Same-Bad）。
  - 人工偏好评估（20名标注员，1,000对比较）。

#### （4）奖励贡献消融
- 在控制其他奖励的情况下，逐一添加视频质量子奖励（视觉、音频、效果）和兴趣对齐奖励，观察相应指标（自动胜率、兴趣对齐分数）变化。

#### （5）定性示例
- 展示一个端到端案例：用户画像 → GRM 预测 D-SIDs → IM 生成指令 → VGAs 生成最终视频。

## 4. 资源与算力
- 文中未全面说明整体算力。**部分提及**：
  - GRM 训练：8块 GPU（型号未明确），batch size 8192，使用 Adam、学习率 1e-4，推理 beam size 512，QPS 130。
  - VGAs 使用 Qwen2.5-32B 骨干，但训练/推理由多少资源支持未给出。
  - 解耦视频编码器基于 Qwen2.5-VL-7B，应需要大量GPU。
- **结论**：论文未提供完整算力账单（如总GPU小时、训练时长、模型并行策略），这是信息缺失之处。

## 5. 实验数量与充分性
- **实验数量**：
  - 在线 A/B 测试：1组主要对比（含多个变体），但未报告统计显著性细节（如p值、置信区间）。
  - 离线实验：① 解耦 SID 检索/离散化（1组对比，含4个方法）；② 指令模型配置（2×2组合）；③ VGAs vs 工作流（1组对比，含自动和人工）；④ 奖励消融（7个条件）；⑤ 定性示例（1个案例）。
  - 共计约10+个对比/消融条件。
- **充分性与客观性**：
  - **优点**：覆盖了表示、指令、生成、奖励各环节；奖励消融逐个检验子成分；人工评估使用多样化标注员和盲评。
  - **不足**：
    - 仅一个平台（快手）、一个场景（广告）。未在非广告推荐（如普通短视频推荐）上验证泛化性。
    - 未与端到端条件视频生成模型（如直接用 SID 作为条件输入扩散模型）对比。
    - 在线实验仅报告一个收入指标，未细观用户时长、点击率等；未讨论冷用户、长尾利益。
    - 离线评估依赖外部多模态评估器（GPT/Claude/Gemini），存在偏离真实用户偏好风险；人工评估样本量1,000对（20人×50对）相对有限。
    - 缺失部分关键细节（如GPT-5.1的版本、打分校准过程）。

## 6. 论文的主要结论与发现
- RaG 在工业级广告平台上实现**显著收入提升**：相比 DLRM 提升 5.462%，相比强 GRM 基线提升 1.870%，验证了生成驱动推荐的有效性。
- **D-SIDs** 改进了 GRM（增强版 GRM 收入提升 0.902%），且解耦表示具有更低的量化碰撞率和更高检索精度。
- **VGAs** 在自动和人工评估中显著优于传统工作流流水线，尤其在平均/中位数评分和胜率方面优势明显。
- 奖励消融表明所有组件（视觉、音频、效果、兴趣对齐）均有贡献，且联合优化效果最佳。
- 整体结论：闭环生成系统是将个性化视频生成集成到推荐中的有前途范式。

## 7. 优点：方法或实验设计上的亮点
- **范式创新**：首次提出“推荐即生成”，打破固定池限制，将用户兴趣直接转化为新内容。
- **统一表示**：解耦语义 ID 既保留细粒度语义，又实现可控生成，是推荐与生成桥梁。
- **工业级工程**：多智能体框架 + 共享骨干 + KV-cache 复用 + SID 索引缓存，使高开销生成可行。
- **奖励工程设计**：SCRL 通过约束优化和 GDPO 处理异质奖励，避免手动调权，稳定训练。
- **实验工程**：在真实大规模平台部署并进行在线验证，结果有实际说服力；离线评估结合自动和人工，较全面。
- **消融细致**：逐步验证了解耦表示、模型容量、奖励子成分的贡献。

## 8. 不足与局限
- **应用局限**：仅在广告场景测试，未验证对普通内容推荐的普适性；广告场景下收入可能受竞价限制，不直接反映用户满意度。
- **生成延迟瓶颈**：VGAs 当前为近线（～180s），无法实时按需生成；未来需加速至实时或近似实时（如蒸馏、推理优化）。
- **对比基线有限**：未与“直接使用扩散模型条件生成”或“图文生成+视频合成”等替代方案比较；仅与检索式 GRM 和工作流流水线对比。
- **评估偏差**：离线自动评估使用闭源 API 模型（GPT-5.1等），其内部更新会导致可复现性问题；人工评估样本量（1,000对）相对较小。
- **信息缺失**：缺少详细算力账单、在线实验的统计显著性、单一收入指标的局限性、长尾用户效果等。
- **可迁移性未知**：本文高度依赖快手内部模型（如 CapModel、Dense Captioning、广告元数据），外部复现难度大。

（完）
