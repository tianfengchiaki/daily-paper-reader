---
title: Fine-Tuned LLM as a Complementary Predictor Improving Ads System
title_zh: 微调大型语言模型作为互补预测器改进广告系统
authors: "Hui Yang, Daiwei He, Kevin Jiang, Taejin Park, Kungang Li, Jiajun Luo, Yuying Chen, Xinyi Zhang, Sihan Wang, Haoyu He, Yu Liu, Lakshmi Manoharan, David Xue, Shubham Barhate, Runze Su, Duna Zhan, Ling Leng, Siping Ji, Jinfeng Zhuang, Alice Wu, Leo Lu, Han Sun, Zhifang Liu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27856v1"
tags: ["query:q3"]
score: 9.0
evidence: 微调大语言模型作为广告系统的补充预测器
tldr: 本文提出在广告系统中使用微调的开源大语言模型作为辅助预测器，根据用户画像和历史预测广告商，从而增强候选生成并为下游排序提供先验信息。该方法在大规模生产广告系统中实现了显著的离线改进和在线业务影响，验证了LLM在广告推荐中的有效性，并展示了辅助预测如何解锁检索和排序阶段的端到端收益。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27856v1/fig-001.webp\", \"caption\": \"Figure 2: Serving architecture for large-scale inference. The deployment uses a distributed vLLM + Ray stackwith batching, prefix caching, fault tolerance, and incremental updates for efficient daily inference.\", \"page\": 5, \"index\": 1, \"width\": 482, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.27856v1/fig-002.webp\", \"caption\": \"Table 4: Feature ablation on the zero-shot 4B setup. Negative values indicate performance drops relative to the baseline.\", \"page\": 6, \"index\": 2, \"width\": 452, \"height\": 302}]"
motivation: 大语言模型在推荐系统特别是广告领域的实际应用较少，希望利用LLM的世界知识和预测能力提升广告推荐效果。
method: 微调开源LLM作为广告特定的辅助预测器，预测用户可能感兴趣的广告商，输出作为候选生成和下游排序的先验信息。
result: 在大规模生产广告系统中，方法带来显著的离线指标提升和可量化的在线业务增长。
conclusion: LLM作为辅助预测器能够有效增强广告系统，并通过端到端收益为LLM在推荐中的规模化应用提供可行路径。
---

## 摘要
推荐系统驱动着信息流、广告和短视频平台的用户参与和变现，但将大型语言模型的最新进展转化为推荐系统的实际收益仍然罕见，尤其是在广告和规模化生产的真实行业环境中。先前真实世界中LLM的成功应用通常分为三类：(a) 直接预测下一个候选项目的生成式检索，(b) 使用LLM进行后期重排序，(c) 利用LLM进行辅助信号增强。我们提出了一种面向广告的互补范式：微调的开源LLM并非用作排序器，而是作为广告特定的辅助预测器，根据用户画像和历史记录预测可能的广告主。这种LLM驱动的广告主预测增强了传统的候选生成，并为下游排序提供了信息丰富的先验知识。该方法在大规模生产广告系统中开发，产生了显著的离线改进和可衡量的在线业务影响，证明了LLM的世界知识和预测能力可以被高效利用。除了验证LLM在广告应用中的有效性，我们的结果表明，针对性的辅助预测能够在检索和后期排序阶段实现端到端的收益提升，为大规模LLM增强推荐提供了实用路径。

## Abstract
Recommendation systems power engagement and monetization across feeds, ads, and short-video platforms, but translating the latest advances in Large Language Models into Recommendation Systems (RecSys) gains remains rare, particularly in advertising and production-scale real-world industry setups. Prior real-world LLM successes typically fall into three buckets: (a) generative retrieval that directly predicts the next items for candidate generation, (b) late-stage re-ranking that uses LLMs, and (c) auxiliary signal enrichment with LLMs. We introduce a complementary paradigm for ads: a fine-tuned open-source LLM used not as a ranker, but as an ads-specific ancillary predictor, forecasting likely advertisers from user profiles and histories. This LLM-driven advertiser prediction augments conventional candidate generation and provides informative priors to downstream ranking. Developed in a large-scale production advertising system, our approach produces substantial offline improvements and measurable online business impact, demonstrating that LLM world knowledge and predictive capacity can be efficiently harnessed. Beyond validating LLMs for ads applications, our results show that targeted ancillary predictions can unlock end-to-end gains across both retrieval and late-stage ranking, offering a practical path to LLM-enhanced recommendation at scale.

---

## 论文详细总结（自动生成）

### 论文详细总结：Fine-Tuned LLM as a Complementary Predictor Improving Ads System

#### 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：大型语言模型（LLM）在推荐系统（RecSys）中的应用仍不成熟，尤其在广告领域和大规模生产环境中。主要挑战包括：工业推荐器依赖稀疏的ID特征（用户ID、商品ID等），与LLM的token语义不兼容；LLM的参数量和推理延迟难以满足实时广告流量需求。
- **整体含义**：先前LLM在RecSys中的成功集中在三类范式：生成式检索（直接预测下一个商品）、后期重排序、辅助信号增强。该论文提出一种**互补范式**：将微调的开源LLM用作广告特定的辅助预测器（而非主排序器），根据用户画像和历史行为预测可能转化的广告主，从而增强候选生成并为下游排序提供信息丰富的先验。该方法在Pinterest生产广告系统中实现了显著的离线改进和在线业务影响，表明LLM世界知识和预测能力可被高效利用，为大规模LLM增强推荐提供了实用路径。

#### 2. 方法论：核心思想、关键技术细节
- **核心思想**：微调开源LLM（文中未指定具体模型，可能是Llama或类似）作为辅助预测器，输出一组高转化意图的广告主列表和用户兴趣标签，这些输出同时注入检索阶段和排序阶段，作为互补信号。
- **关键技术细节**：
    - **数据管道**：
        - 用户选择：仅选取过去90天有活跃转化的美国Pinterest用户，以保证有意义的商业意图。
        - 标签构建：以预测窗口（第x+1天到第x+7天）内第一个转化广告主作为训练标签（单标签任务）。
        - 用户特征编译：收集用户画像（年龄、性别）、行为序列（站内搜索、站外归因转化、匹配转化、URL、品牌、类别等）以及预设广告主池。
    - **提示设计**：分三个阶段设计不同提示：
        - **SFT阶段**：要求模型预测单个下一个广告主（精确预测）。
        - **GRPO阶段**：要求模型输出20个排序广告主和5个用户兴趣（以XML格式），以增加奖励方差。
        - **推理阶段**：与GRPO相同格式，减少训练-测试不匹配。
    - **后训练策略**：
        - **监督微调（SFT）**：在单个广告主预测上优化，使用交叉熵损失。
        - **基于GRPO的强化学习**：奖励函数鼓励正确广告主出现在列表前部（位置折扣），并引入长度惩罚以保证格式严格性。
    - **Semantic ID (SID)增强**：
        - 使用RQ-VAE从多模态PinCLIP嵌入构建5级SID（每级20248个码）。
        - 多阶段持续预训练：第一阶段仅更新SID token嵌入（冻结其他参数），第二阶段全参数训练（混入通用领域数据防止灾难性遗忘），第三阶段任务特定微调（在提示中加入SID序列）。
    - **下游集成**：
        - **检索阶段**：开发新的候选生成器（CG），使用LLM预测的广告主作为定向过滤器，由双塔模型生成候选。
        - **排序阶段**：将预测广告主和兴趣作为特征输入下游转换模型（ctcvr, vtcvr）。
    - **服务架构**：使用分布式vLLM + Ray stack进行大规模批处理推理，包含前缀缓存、高效KV缓存管理（paged attention）、连续批处理，以及增量更新（仅对活动变化的用户重新推理）。

#### 3. 实验设计：数据集/场景、基准、对比方法
- **数据集/场景**：Pinterest内部生产数据。
    - **V0**：实验数据集，过滤出序列长度≥10的高信号用户。
    - **V1**：与在线服务流量对齐的数据集，标签为x+1到x+7的高优先级转化，更接近生产条件。
- **基准**：零样本推理（base open-source model，未微调）。
- **对比方法**：
    - 零样本提示
    - SFT + beam search
    - SFT + sampling
    - GRPO（带推理与不带推理）
    - SFT + GRPO（文本版 vs SID增强版）
- **下游模型评估**：
    - 转换模型（ctcvr, vtcvr）：对比加入LLM特征前后的损失、AUC、PR-AUC变化。
    - 候选生成器在线A/B测试：评估U.S. Shopping广告的Return on Ad Spend（RoAS）。
- **消融实验**：逐一移除不同输入特征组（如主动广告商、URL、搜索等），观察Recall@5变化。

#### 4. 资源与算力
- **文中未明确说明**：未提及训练使用的GPU型号、数量、训练时长。仅提到推理采用分布式vLLM + Ray stack，且每日推理限制在高价值用户群体（活跃转化用户）以控制成本。因此无法准确评估算力需求。

#### 5. 实验数量与充分性
- **实验数量**：论文进行了多组实验：
    - 广告主预测离线对比：表2（V0上6种方法）、表3（V1上6种方法+2种SID版本）。
    - 特征消融：表4（13种移/改操作）。
    - 下游转换模型影响：表5（ctcvr和vtcvr的ΔLoss, ΔAUC, ΔPR-AUC）。
    - 在线ROAS提升：表6（两个细分指标）。
    - 额外讨论：不同训练目标、CG quota的影响。
- **充分性评价**：
    - **充分性**：覆盖了主要对比方法、完整的消融、下游效果和在线验证，实验设计较为系统。
    - **客观性**：结果报告了离线Recall、AUC、PR-AUC和在线ROAS，并给出了p值（p=0.021和0.029），支持统计显著性。
    - **公平性**：同一数据集、相同评价指标，对比方法均在同一基座模型上进行（SFT、GRPO均基于相同开源模型），公平性较好。但未与其他同类方法（如生成式检索、两塔模型）直接对比，缺乏baseline多样性。
    - **局限性**：仅在一个平台（Pinterest）上验证，结论的泛化性有限。用户选择限于高价值用户，可能无法代表所有用户。

#### 6. 主要结论与发现
- 后训练（SFT、GRPO）显著优于零样本，SFT+GRPO组合达到最佳离线Recall@1=0.223（V1上文本版）和0.248（SID增强版）。
- SID增强带来>10%的Recall提升（Recall@1从0.223到0.248，Recall@5从0.461到0.515，Recall@20从0.683到0.755）。
- 输入特征中“主动广告商（有历史转化）”是最重要的；站外URL和站外搜索次之；用户画像贡献较小。
- LLM预测信号在离线转换模型上提升PR-AUC（ctcvr: +0.71%, vtcvr: +1.64%），AUC提升较小。
- 在线实验中，基于LLM的候选生成器在U.S. Shopping广告上使RoAS提升4.94%（全员）和6.69%（opt-in用户），且统计显著。
- 主动推理/思维方式（reasoning）对广告主预测任务无帮助，简单SFT或格式约束的GRPO更有效。

#### 7. 优点
- **方法创新性**：将LLM作为互补预测器而非主排序器，巧妙规避了LLM直接参与排序的延迟和成本问题，同时利用了LLM的世界知识。
- **实用性强**：设计了两阶段后训练（SFT+GRPO）和SID注入，并成功集成到生产系统的检索和排序两个阶段，证明了端到端收益。
- **实验设计严谨**：包含了离线Recall、下游模型指标、在线A/B测试，特征消融覆盖全面，统计显著性报告p值。
- **可扩展性**：通过增量推理（仅更新活动用户）和分布式推理框架降低了服务成本，具备生产级工程可行性。

#### 8. 不足与局限
- **实验覆盖不足**：仅在单一平台（Pinterest）和特定广告场景（U.S. Shopping Ads）上验证，结果可能不适用于其他平台或广告类型（如品牌曝光、应用安装）。未与其他流行的LLM推荐方法（如P5, RecGPT等）直接比较。
- **消融实验的完整性**：虽然移除了单个特征，但未展示特征组合或顺序的影响，且“top categories”的移除反而提升了Recall，说明特征噪声处理可能不完善。
- **SID增强的局限性**：SID增强仅离线评估，未在线验证，其实际在线收益未知。SID构建依赖PinCLIP多模态嵌入，可能在其他缺乏多模态数据的场景中难以复用。
- **资源透明度低**：未报告训练所需的GPU型号、数量、时间，无法评估方法的计算成本可复制性。
- **用户选择偏差**：用户被限制为有近期转化的活跃用户，模型可能对冷启动用户或低频用户效果不佳，限制了适用范围。
- **潜在偏差风险**：广告主预测模型可能放大现有转化模式中的数据偏差（如热门广告主），导致推荐结果缺乏多样性。论文提到需要调节CG quota避免候选集中过度集中。

（完）
