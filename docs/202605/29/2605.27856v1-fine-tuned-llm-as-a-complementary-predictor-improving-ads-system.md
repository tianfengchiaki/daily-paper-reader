---
title: Fine-Tuned LLM as a Complementary Predictor Improving Ads System
title_zh: 微调大语言模型作为互补预测器改善广告系统
authors: "Hui Yang, Daiwei He, Kevin Jiang, Taejin Park, Kungang Li, Jiajun Luo, Yuying Chen, Xinyi Zhang, Sihan Wang, Haoyu He, Yu Liu, Lakshmi Manoharan, David Xue, Shubham Barhate, Runze Su, Duna Zhan, Ling Leng, Siping Ji, Jinfeng Zhuang, Alice Wu, Leo Lu, Han Sun, Zhifang Liu"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27856v1"
tags: ["query:q3"]
score: 9.0
evidence: 微调LLM用于广告预测
tldr: 将微调的开源LLM作为广告系统的辅助预测器，利用用户画像和历史预测广告主，增强候选生成和下游排序。在大型生产系统中验证，取得显著离线改进和在线业务收益，表明LLM世界知识和预测能力可高效利用，为大规模LLM增强推荐提供了实用路径。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27856v1/fig-001.webp\", \"caption\": \"Figure 2: Serving architecture for large-scale inference. The deployment uses a distributed vLLM + Ray stackwith batching, prefix caching, fault tolerance, and incremental updates for efficient daily inference.\", \"page\": 5, \"index\": 1, \"width\": 482, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2605.27856v1/fig-002.webp\", \"caption\": \"Table 4: Feature ablation on the zero-shot 4B setup. Negative values indicate performance drops relative to the baseline.\", \"page\": 6, \"index\": 2, \"width\": 452, \"height\": 302}]"
motivation: 现有推荐系统难以直接利用LLM的最新技术进步，特别是在广告和工业级场景中，需要探索高效的LLM辅助范式。
method: 将开源LLM针对广告场景进行微调，作为候选生成阶段的辅助预测器，输出广告主预测结果以增强后续排序。
result: 在大型生产广告系统中，该方法带来显著的离线指标提升和可衡量的在线业务收益。
conclusion: 辅助性LLM预测能够有效利用世界知识，同时提升检索和排序阶段性能，为LLM增强广告推荐提供了可扩展的实践方案。
---

## 摘要
推荐系统驱动着信息流、广告和短视频平台上的用户参与和变现，但将大语言模型的最新进展转化为推荐系统的实际收益仍较为罕见，尤其是在广告和生产级工业场景中。此前大语言模型在真实世界的成功应用通常分为三类：(a) 直接预测下一项以生成候选的生成式检索；(b) 使用大语言模型进行后期重排序；(c) 利用大语言模型丰富辅助信号。我们为广告引入了一种互补范式：使用微调的开源大语言模型，并非作为排序器，而是作为广告特定的辅助预测器，从用户画像和历史记录中预测可能的广告主。这种基于大语言模型的广告主预测增强了传统的候选生成，并为下游排序提供了信息先验。该方法在大型生产级广告系统中开发，产生了显著的离线改进和可衡量的在线业务影响，证明了大语言模型的世界知识和预测能力可以被有效利用。除了验证大语言模型在广告应用中的有效性，我们的结果表明，有针对性的辅助预测可以解锁检索和后期排序两阶段的端到端收益，为大规模增强推荐系统提供了一条实用路径。

## Abstract
Recommendation systems power engagement and monetization across feeds, ads, and short-video platforms, but translating the latest advances in Large Language Models into Recommendation Systems (RecSys) gains remains rare, particularly in advertising and production-scale real-world industry setups. Prior real-world LLM successes typically fall into three buckets: (a) generative retrieval that directly predicts the next items for candidate generation, (b) late-stage re-ranking that uses LLMs, and (c) auxiliary signal enrichment with LLMs. We introduce a complementary paradigm for ads: a fine-tuned open-source LLM used not as a ranker, but as an ads-specific ancillary predictor, forecasting likely advertisers from user profiles and histories. This LLM-driven advertiser prediction augments conventional candidate generation and provides informative priors to downstream ranking. Developed in a large-scale production advertising system, our approach produces substantial offline improvements and measurable online business impact, demonstrating that LLM world knowledge and predictive capacity can be efficiently harnessed. Beyond validating LLMs for ads applications, our results show that targeted ancillary predictions can unlock end-to-end gains across both retrieval and late-stage ranking, offering a practical path to LLM-enhanced recommendation at scale.

---

## 论文详细总结（自动生成）

# 论文《Fine-Tuned LLM as a Complementary Predictor Improving Ads System》中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何在工业级大规模广告推荐系统中，有效利用大语言模型（LLM）的世界知识和语言理解能力，同时克服LLM在实时推理延迟、稀疏ID特征不匹配、训练成本高等方面的挑战。
- **研究动机**：传统推荐系统多阶段架构（召回→排序）依赖协同过滤和特征交叉，难以直接从LLM的最新进展受益。现有LLM在推荐中的应用主要限于生成式检索、后期重排序或辅助信号增强，但鲜有在广告场景下实现端到端的工业级验证。
- **整体含义**：本文提出将微调的开源LLM作为**辅助预测器**，而非替代现有排序或检索模型，用于预测用户最可能转化的广告主，从而在不改变主系统架构的前提下，提升召回覆盖率和排序精度，最终带来可衡量的在线业务收益。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

### 核心思想
> 使用微调的开源LLM（如Llama系列）根据用户画像、行为历史预测出**20个候选广告主**和**5个用户兴趣标签**，然后将这些预测结果分别注入广告检索阶段（作为候选生成器的过滤条件）和下游转化排序模型（作为额外特征）。

### 关键技术细节

1. **数据管线（Data Pipeline）**
   - **用户选择**：限制为近90天有有效转化行为的美国活跃用户，兼顾推理成本与业务价值。
   - **标签构建**：以预测窗口内用户首次转化的广告主作为训练标签（单标签预测），采用随机用户拆分保证无数据泄露。
   - **特征编译**：从用户画像（年龄/性别/用户状态）、行为序列（站内搜索、站外归因转化、匹配转化、URL、品牌、类别等）构造结构化文本输入。

2. **提示设计（Prompt Design）**
   - **SFT阶段**：要求模型预测单一“下一广告主”（1-advertiser prompt），输出自由文本。
   - **GRPO阶段**：要求模型输出20个排名广告主+5个兴趣标签（20-advertiser prompt），输出为结构化XML格式，便于下游消费。
   - **推理阶段**：与GRPO格式一致，减少训练-推理不匹配。

3. **后训练策略**
   - **SFT（监督微调）**：学习精准的下一广告主预测。
   - **GRPO（基于奖励的强化学习）**：优化20广告主的排序输出，奖励函数设计如下：
     \[
     R_{\text{total}} = R_{\text{match}} - P_{\text{adv\_len}} - P_{\text{interest\_len}}
     \]
     其中 \(R_{\text{match}}(i) = 0.1 \times (20 - i) + \text{bonus}(i \le 4 : 2.0)\)，长度惩罚确保输出格式准确。
   - **SID增强**：引入语义ID（Semantic ID），通过多阶段预训练融入多模态内容语义，进一步提升召回率。

4. **下游集成**
   - **检索阶段**：基于LLM预测的广告主构建新的候选生成器（CG），使用双塔模型优化点击/时长权重，作为补充召回通道。
   - **排序阶段**：将LLM预测的广告主和兴趣标签作为特征输入ctcvr/vtcvr等转化模型。

5. **服务化部署**
   - 采用分布式vLLM + Ray架构，支持前缀缓存、分页注意力机制和连续批处理，实现每日大规模批推理。
   - 通过增量更新（仅重新推理有最新活动的用户）和检查点虚拟化降低运维成本。

## 3. 实验设计：数据集/场景、基准、对比方法

### 数据集/场景
- **V0**：实验数据集，要求用户行为序列长度≥10，用于特征消融和早期模型评估。
- **V1**：与线上服务流量对齐的真实数据集，标签为高优先级转化（x+1至x+7天），更贴近生产环境但特征表达较弱。

### 基准方法
- **Zero-shot prompting**：直接使用未微调的开源LLM进行零样本推理。
- **SFT + beam search / sampling**：监督微调后采用不同解码策略。
- **GRPO (with/without reasoning)**：强化学习训练，并对比是否引入显式推理提示。
- **SID-enabled SFT + GRPO**：加入语义ID增强的版本。

### 对比指标
- **广告主预测质量**：Recall@1、Recall@5、Recall@20。
- **下游转化模型**：ΔAUC、ΔPR-AUC、ΔLoss（ctcvr和vtcvr）。
- **在线业务**：U.S. Shopping 广告的Return on Ad Spend (RoAS) 相对变化。

### 在线实验
- 部署于Home Feed、Related Pins、Search三种视图，仅对美国opt-in用户生效。
- 对比不同的检索模型训练目标（impression、click+时长、conversion），并精细调整L0配额防止候选过度集中。

## 4. 资源与算力

- **文中未明确给出GPU型号、数量及训练总时长**。
- 仅提及使用分布式vLLM + Ray堆栈进行大规模每日批推理，并提到需控制推理成本（通过用户选择、增量更新等措施）。具体算力细节（如使用A100还是H100、模型参数量级、训练GPU小时数）未披露。

## 5. 实验数量与充分性

- **主要实验数量**：约8组离线广告主预测实验（表2、表3）、1组特征消融实验（表4）、2组下游模型效果实验（表5）、1组在线A/B测试结果（表6）。此外，还包括SID增强的对比。
- **充分性评估**：
  - **正面**：覆盖了多种后训练策略、特征贡献、下游模型影响、在线业务效果，消融实验系统全面；使用了两个不同版本的数据集（V0/V1），兼顾实验探索和生产代表；在线实验采用统计显著性检验（p值）。
  - **客观性**：对比方法涵盖zero-shot、SFT、GRPO、SID增强等，且对推理显式性的必要性做了明确讨论（表明推理任务不需要长链推理）。
  - **不足**：仅有一个在线业务指标（RoAS），缺少用户满意度、广告多样性等维度的评估；消融实验基于零样本4B模型，未在高性能微调模型上复现；未报告训练/推理的成本具体数字（如延迟、QPS、GPU内存占用）。

## 6. 论文的主要结论与发现

1. **后训练是关键的**：所有微调变体均显著优于零样本，SFT+GRPO组合在Recall@20上提升达60%以上（V1上从0.422提升至0.755）。
2. **GRPO主要提升大K召回**（Recall@20），而SFT更擅长Recall@1；两者结合效果最佳。
3. **语义ID（SID）增强带来10%以上的绝对Recall提升**，说明多模态内容语义是对文本特征的强力补充。
4. **特征消融**发现，“活跃广告主（有历史转化）”是最重要的特征，而用户画像贡献微弱；行为序列中站外URL和站外搜索最重要。
5. **下游收益**：LLM预测的广告主特征可提升ctcvr和vtcvr的PR-AUC分别达0.71%和1.64%。
6. **在线业务**：LLM驱动的候选生成器在U.S. Shopping广告上提升RoAS 4.94%（全体），opt-in用户上提升6.69%（p<0.05），验证了适配用户分群和L0配额调整的重要性。
7. **显式推理对广告主预测任务无效**，简单SFT风格训练即可取得相似性能。

## 7. 优点

- **方法创新**：提出“互补预测器”范式，避免LLM替代主模型，降低了部署风险和成本。
- **实用性强**：完整覆盖数据构建、提示设计、后训练、服务化部署全流程，且所有技术细节均公开。
- **实验说服力**：离线和在线结合，消融实验系统，在线指标显著且统计显著。
- **可扩展性**：SID增强和增量更新设计便于持续迭代。
- **结构清晰**：论文写作逻辑严谨，公式、图、表、附录齐全。

## 8. 不足与局限

- **算力资源未公开**：无法判断方法的实际经济成本和可复现性。
- **单一业务指标**：在线实验仅报告RoAS，缺少对用户负面体验（如广告过多、相关性下降）的监测。
- **特征消融基于零样本模型**：其结果可能无法直接推广到更强大的微调模型。
- **用户选择受限**：仅针对有转化历史的高价值用户，未验证对冷启动用户的效果。
- **SID增强未在线验证**：文中明确说明SID版本尚未上线，其实证价值有限。
- **缺乏通用性讨论**：方法高度依赖广告主池和转化标签，难以直接迁移到无广告推荐场景。
- **未报告推理延迟**：虽然提到使用vLLM优化，但未给出p99延迟或吞吐量数据。

（完）
