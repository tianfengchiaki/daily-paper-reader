---
title: "PACMS: Submodular Context Selection as a Pluggable Engine for LLM Agents"
title_zh: PACMS：作为LLM代理可插拔引擎的子模上下文选择
authors: "Manu Ghulyani, Arunabh Singh, Karan Bharadwaj, Ankit Nath, Suranjan Goswami"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20047v1"
tags: ["query:llmapp"]
score: 6.0
evidence: 子模函数选择上下文用于大模型智能体记忆管理
tldr: 当前LLM智能体的上下文窗口会同时从用户对话、持久记忆和工具调用等多个方向填充。当超出token预算时，普遍采用的近期截断策略会丢弃早期但相关的信息，导致关键事实丢失。本文提出PACMS，一种基于子模函数的上下文选择引擎，可在组装提示时根据相关性从所有候选池（记忆、对话、工具输出）中挑选内容，替代盲目的近期截断。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20047v1/fig-001.webp\", \"caption\": \"Figure 3: PACMS as the OpenClaw context engine on a real workspace session. The user added Rust to their coding environment notes, then asked the assistant to verify Rust was installed; the tool calls (rustc, rustup) returned command not found; PACMS selected 17 of 168 pooled candidates to assemble the model’s prompt. The right-hand panel visualizes the keep/drop verdict per candidate, the strategy pill (pacms), the token budget, and the kept/dropped split. Transport-level heartbeat messages are omitted from the keep/drop grid for visual clarity; the underlying /select call ran on the full 168-message pool. All visible text in the figure is verbatim from a captured OpenClaw session.\", \"page\": 6, \"index\": 1, \"width\": 1070, \"height\": 755}, {\"url\": \"assets/figures/arxiv/2606.20047v1/fig-002.webp\", \"caption\": \"Table 1: Evidence-round recall (100 questions, template-based redundancy injection). Bold = best; underline = second-best. At 20% budget PACMS trails top-𝑘; at 45–70%, PACMS and lcmmr overtake top-𝑘 as 𝑅 grows. PACMS is consistently first or second.\", \"page\": 3, \"index\": 2, \"width\": 483, \"height\": 514}, {\"url\": \"assets/figures/arxiv/2606.20047v1/fig-003.webp\", \"caption\": \"Figure 1: PACMS overview. Candidates (memory, turns, tool outputs) form a query-anchored similarity graph. PACMS selects a token-budgeted subset (dark nodes) whose coverage halos account for all query-relevant items; the redundant duplicate (dup, dashed) lies inside a halo and is skipped.\", \"page\": 1, \"index\": 3, \"width\": 475, \"height\": 261}]"
motivation: 现有近期截断策略是话题盲的，会丢弃早期关键信息，而检索增强生成和上下文压缩方法无法仲裁智能体已有的池化上下文中。
method: 提出PACMS，将记忆条目、对话轮次和工具输出视为统一候选池，在提示组装时刻通过子模函数按相关性选择上下文。
result: PACMS作为可插拔引擎，能在不修改智能体核心的情况下，动态保留最相关的信息，避免近期截断的缺陷。
conclusion: PACMS通过子模选择机制有效提升了LLM智能体在长会话中的信息保持能力，是一种轻量且灵活的上下文管理方案。
---

## 摘要
对话式和工具使用型LLM代理在上下文窗口中操作，该窗口同时从多个方向填充。随着会话进行，代理会积累用户和助手的交互轮次、从持久记忆存储中提取的条目，以及通常最大的部分——工具调用的逐字输出，如文件读取、搜索结果和API响应。一旦累积的上下文超过模型的令牌预算，框架必须决定保留哪些内容。当前的主要机制是最近截断，有时结合定期摘要。这是主题盲目的：会话早期建立的事实仅仅因为陈旧而被丢弃，即使当前用户查询正是关于该事实；反之，冗长但无关的近期材料被保留。需要在多轮交互中回忆信息的代理——记忆的定义性情况——正是最近截断失败的地方。现有的替代方案位于代理的组装步骤之外。检索增强生成将外部文档提取到提示中，但不仲裁代理的“已有”池化上下文。上下文压缩方法通过重写或修剪文本来减少令牌数量，但操作时是查询盲目的且有损的。两者都没有将记忆条目、对话轮次和工具输出视为一个单一的候选池，在提示组装时根据相关性进行选择。

## Abstract
Conversational and tool-using LLM agents operate over a context window that fills from several directions simultaneously. As a session proceeds, the agent accumulates user and assistant turns, entries drawn from a persistent memory store, and often largest of all, the verbatim outputs of tool calls such as file reads, search results, and API responses. Once the cumulative context exceeds the model's token budget, the framework must decide what to keep.   The prevailing mechanism is recency truncation, sometimes paired with periodic summarization. This is topic-blind: a fact established early in a session is discarded simply because it is old, even when the current user query is about exactly that fact; conversely, verbose but irrelevant recent material is retained. Agents that must recall information across many turns, the defining case for memory, are precisely where recency truncation fails.   Existing alternatives sit outside the agent's assembly step. Retrieval augmented generation fetches external documents into the prompt but does not arbitrate the agent's \emph{already-present} pooled context. Context-compression methods reduce token count by rewriting or pruning text, but operate query-blind and lossily. Neither treats memory entries, conversation turns, and tool outputs as a single candidate pool to be selected from by relevance at the moment the prompt is assembled.