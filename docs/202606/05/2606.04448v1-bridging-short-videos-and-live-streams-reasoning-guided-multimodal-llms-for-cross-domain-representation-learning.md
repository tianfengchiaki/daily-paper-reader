---
title: "Bridging Short Videos and Live Streams: Reasoning-Guided Multimodal LLMs for Cross-Domain Representation Learning"
title_zh: 连接短视频与直播：推理引导的多模态大语言模型用于跨域表示学习
authors: "Le Zhang, Xiaolan Zhu, Yuchen Wang, Shilong Kang, Jiaqi Xue, Xiaoyu Zhang, Xiang Chen, Yalong Guan, Xiangyu Wu, Shijun Wang, Lantao Hu, Kun Gai"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04448v1"
tags: ["query:q6"]
score: 9.0
evidence: 多模态大模型用于短视频和直播跨域推荐
tldr: 针对短视频到直播的跨域推荐冷启动问题，提出RGCD-Rep框架，利用多模态大模型推理能力，通过推理感知蒸馏和可迁移性引导的表示分解学习可迁移物品表示，离线计算后整合推荐系统，在快手部署后显著提升核心指标，服务超4亿用户。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04448v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed RGCD-Rep framework.\", \"page\": 4, \"index\": 1, \"width\": 1042, \"height\": 464}]"
motivation: 直播场景用户行为稀疏导致冷启动严重，需从短视频迁移用户兴趣。
method: 提出RGCD-Rep，先通过冻结教师MLLM生成跨域推理知识蒸馏到轻量学生MLLM，再分解表示为可迁移和域残差部分，学习协作引导的可迁移表示。
result: 离线实验优于基线，在快手直播推荐系统上线后A/B测试显示核心业务指标显著提升。
conclusion: RGCD-Rep有效缓解跨域推荐冷启动，实现低成本的工业部署。
---

## 摘要
随着直播服务的增长，许多平台提供短视频和直播以满足多样化的需求。短视频承载着巨大的流量和丰富的行为信号，而直播作为核心转化场景，其行为数据稀疏，冷启动问题严重。将用户兴趣从短视频迁移到直播推荐可以缓解这些问题。同时，短视频和直播是复杂的多模态项目，整合多模态信号能提升推荐性能。尽管多模态大语言模型（MLLMs）展现出强大的多模态理解和推理能力，但它们在跨域推荐中的应用仍未被充分探索。为此，我们提出了推理引导的跨域表示学习（RGCD-Rep），这是一个从短视频到直播的跨域推荐推理引导框架。RGCD-Rep高效地引入MLLM推理，并通过两阶段训练学习由行为协同引导的可迁移项目表示。首先，推理感知蒸馏让冻结的教师MLLM生成结构化的跨域推理知识，并将其蒸馏到轻量级学生MLLM中。其次，迁移性引导的跨域表示学习将项目表示分解为可迁移表示和域残差表示。得到的表示离线计算并集成到下游检索任务中，实现低成本的工业部署。大量的离线实验证明了RGCD-Rep的优越性。在快手直播推荐系统中部署后，A/B测试显示多个核心业务指标显著提升，证实了其在真实工业场景中的有效性和实用性。RGCD-Rep已全面部署，每日服务超过4亿用户。

## Abstract
As live streaming services grow, many platforms offer short videos and live streams to meet diverse needs. Short videos carry substantial traffic and rich behavior signals, whereas live streaming is a core conversion scenario with sparse behavior data, making cold start severe. Transferring user interests from short videos to live streaming recommendation can alleviate these issues. Meanwhile, short videos and live streams are complex multimodal items, and integrating multimodal signals improves recommendation performance. Although Multimodal Large Language Models (MLLMs) show strong multimodal understanding and reasoning, their application to cross-domain recommendation remains underexplored. To this end, we propose Reasoning-Guided Cross-Domain Representation Learning (RGCD-Rep), a reasoning-guided framework for cross-domain recommendation from short videos to live streams. RGCD-Rep introduces MLLM reasoning resource-efficiently and learns transferable item representations guided by behavioral collaboration via two-stage training. First, reasoning-aware distillation lets a frozen teacher MLLM generate structured cross-domain reasoning knowledge and distills it into a lightweight student MLLM. Second, transferability-guided cross-domain representation learning decomposes item representations into transferable and domain residual representations. The resulting representations are computed offline and integrated into downstream retrieval tasks, enabling low-cost industrial deployment. Extensive offline experiments demonstrate RGCD-Rep's superiority. After deployment in Kuaishou's live streaming recommendation system, A/B tests show significant gains across multiple core business metrics, confirming its effectiveness and practicality in real industrial scenarios. RGCD-Rep is fully deployed and serves over 400 million users daily.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：短视频平台承载巨大流量和丰富行为信号；直播作为核心转化场景，用户行为（如送礼、关注）极其稀疏，新主播和用户面临严重冷启动问题。跨域推荐（CDR）成为缓解该问题的关键。
- **核心挑战**：
  - 现有基于预提取多模态特征的方法缺乏**深层语义推理**，无法解释为何不同域的物品可迁移。
  - 多模态大模型（MLLM）具备强理解与推理能力，但其应用面临三大障碍：
    - **Ch1**：大规模MLLM计算成本过高，难以工业部署；
    - **Ch2**：MLLM开放世界知识可能产生任务无关或错误的解释；
    - **Ch3**：跨域兴趣并非总一致，直接聚合共享与域特有信息可能导致**负迁移**。
- **研究目标**：高效引入MLLM推理能力，学习通用的、可离线部署的跨域可迁移物品表示。

### 2. 论文提出的方法论

- **核心思想**：将MLLM的推理能力通过两阶段训练迁移到轻量模型，并分解物品表示为可迁移与域残差两部分，以减轻负迁移。

- **关键技术细节**：
  1. **行为-协作对构建**：从用户日志中提取同时段内共同感兴趣的短视频-直播对，并融合行为共现与行为强度得到`b_i`（转移强度标签）。
  2. **结构化跨域推理生成**：冻结教师MLLM（35B）对每对输出三种语义维度（垂直领域、关键主体、内容主题）的标签与匹配结果，生成语义转移得分`r_i`。
  3. **Stage 1：推理感知蒸馏**
     - 将教师输出的**物品级**理解知识分解为单物品样本；
     - 学生MLLM（2B）采用可学习的**Chorus Tokens**（16个）聚合多模态信息，通过下一个词预测损失`L_NTP`进行微调，掌握结构化理解能力。
  4. **Stage 2：迁移性引导的跨域表示学习**
     - **权重共享编码器**：学生MLLM对短视频和直播编码，输出Chorus Tokens隐藏状态`H_x ∈ R^{N_C×d}`。
     - **可迁移-残差查询感知聚合**：两个独立可学习查询向量`q_T`和`q_R`分别对Chorus Tokens做加权求和，输出可迁移表示`z_T`和残差表示`z_R`。
     - **跨域对比学习**：对行为协作对，使用InfoNCE损失`L_con`（双向）拉近正对的`z_T`。
     - **可迁移路由**：为区分高低纯度对，计算两种空间的匹配分，用sigmoid路由概率`P_T`，结合行为与语义联合得分`q_i = b_i + r_i`构建路由标签（前/后20%），通过交叉熵损失`L_route`约束：高纯度对应主要由可迁移空间解释，低纯度对由残差空间吸收噪声。
  5. **总损失**：`L_stage2 = L_con + λ_route L_route`。
  6. **离线部署**：训练后，`z_T`离线生成并存入特征系统，检索时基于用户短视频兴趣做近似最近邻搜索。

### 3. 实验设计

- **数据集**：**Kuaishou数据集**——一周真实日志，随机采样109,523名同时访问短视频和直播的用户，训练集为前6天日志，测试集为第7天用户最后一条直播正反馈。物品含视频帧、OCR/ASR文本、标题等。
- **Benchmark**：Retrieval任务，指标为`HR@10`和`NDCG@10`。
- **对比方法**：
  - 单域多模态：FREEDOM、SMORE、AlphaRec。
  - 跨域单模态：UniSRec、VQ-Rec。
  - 跨域多模态：MISSRec、PMMRec、UniEmbedding（最强基线）。
- **消融实验**：移除Stage 1 / Stage 2 / Stage 2 MLLM（冻结骨干仅训练聚合模块）。
- **长尾分析**：按物品频次分为头/尾，测试性能。
- **冷启动分析**：按用户直播历史分为冷启动（无历史正反馈）与活跃用户。
- **在线A/B测试**：在快手及快手极速版直播推荐检索阶段部署一周。

### 4. 资源与算力

- **硬件**：8×NVIDIA H800（140GB GPU）。
- **模型**：教师Qwen3.6-35B-A3B（冻结），学生Qwen2-VL-2B-Instruct（可训练）。
- **训练**：Stage 1用6万样本，Stage 2用25万样本；LoRA rank=32；优化器Adam；Stage 1 Batch size=8，Stage 2 Batch size=64；学习率均为1e-4。
- **时间**：未明确给出具体训练时长。

### 5. 实验数量与充分性

- **离线主实验**（Table 2）：对比8种基线+3种消融，共12个结果，HR@10和NDCG@10各一组。
- **消融实验**：3个关键变体，验证了阶段1、阶段2及MLLM骨干微调的必要性。
- **长尾/冷启动实验**（Figure 2 & 3）：在头/尾物品和冷启动/活跃用户上分别对比RGCD-Rep与最强基线，展示了优势。
- **在线实验**（Table 3）：在两个独立应用（Kuaishou、Kuaishou Lite）上各报告4项指标（曝光、点击、有效进房、关注）。
- **充分性评价**：实验覆盖了离线匹配、消融、稀疏场景（长尾和冷启动）、在线部署，基准对比充分，消融完整，客观公正。但仅在一个内部数据集上做离线实验，未在公开数据集（如Amazon等）上验证，可能影响通用性结论。

### 6. 论文的主要结论与发现

- RGCD-Rep在所有离线指标上显著优于最先进的基线（HR@10提升17.59%，NDCG@10提升30.93%）。
- 每个组件（Stage1蒸馏、Stage2对比学习、路由机制、MLLM微调）均不可或缺，尤其Stage2带来的增益最大。
- 在长尾物品和冷启动用户上，RGCD-Rep的优势更加突出，证明其能有效缓解数据稀疏与冷启动问题。
- 线上A/B测试中，核心指标（如关注数）提升达+0.83%和+0.93%，系统每日服务超4亿用户。

### 7. 优点

- **首次将MLLM推理引入跨域推荐**，利用其深层语义理解能力解释转移原因。
- **资源高效**：通过蒸馏将大模型能力压缩至2B学生，且离线计算向量，部署成本极低。
- **表示分解**：可迁移-残差分离设计有效缓解负迁移，路由损失进一步区分高低纯度对。
- **实验扎实**：从离线到在线、从整体到边缘案例均进行了验证，消融清洗。

### 8. 不足与局限

- **行为协作对的构造依赖时间窗口与行为强度**，可能存在噪声或错标，影响下游训练质量。
- **教师模型35B仍可能引入偏见**，且其生成的结构化知识（三语义维度）是否最优未讨论。
- **仅在一个内部数据集上评估**，缺乏在公共多模态跨域基准（如Amazon、YOOCHOOSE等）上的验证，泛化性有待考证。
- **未与最新的非MLLM方法（如大语言模型仅作嵌入）比较**，也未计算线上实验统计显著性（p值未给出）。
- **未报告完整的训练时间与推理延迟**，仅提及“离线计算”，工业落地细节（如全量向量的更新频率）未披露。
- **路由标签采用固定百分比（20%）**，对噪声阈值敏感，未讨论自适应方案。

（完）
