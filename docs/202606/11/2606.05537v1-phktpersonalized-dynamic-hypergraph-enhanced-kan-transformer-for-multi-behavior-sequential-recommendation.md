---
title: "PHKT:Personalized Dynamic Hypergraph-enhanced KAN-Transformer for Multi-behavior Sequential Recommendation"
title_zh: "PHKT: 面向多行为序列推荐的任务个性化动态超图增强KAN-Transformer"
authors: "Ruijie Du, Hao Chen, Xin Zhang, Dongjing Wang, Ze Zhang, Xudong Shen, Runze Wu, Dongjin Yu"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05537v1"
tags: ["query:q1"]
score: 9.0
evidence: 多行为序列推荐
tldr: 本文针对多行为推荐中异质语义、用户特定权重和序列依赖建模的局限性，提出PHKT模型。它通过个性化动态超图模块捕获用户特定的高阶关系，并引入KAN替换Transformer中的MLP以增强对非线性模式的建模能力。在三个真实数据集上，PHKT在多个指标上优于九种基线模型，证明了其在多行为偏好建模和目标行为预测中的有效性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05537v1/fig-001.webp\", \"caption\": \"Figure 3: PHKT first generates three types of embeddings based on the user’s historical interaction information: item, location,\", \"page\": 6, \"index\": 1, \"width\": 1018, \"height\": 479}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-002.webp\", \"caption\": \"Figure 4: The impact of feature dimensionality on model performance\", \"page\": 11, \"index\": 2, \"width\": 817, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-003.webp\", \"caption\": \"Figure 5: The impact of the number of KAN network layers on model performance\", \"page\": 11, \"index\": 3, \"width\": 819, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-004.webp\", \"caption\": \"Table 4\", \"page\": 10, \"index\": 4, \"width\": 497, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-005.webp\", \"caption\": \"Table 5\", \"page\": 10, \"index\": 5, \"width\": 497, \"height\": 277}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-006.webp\", \"caption\": \"Table 3\", \"page\": 10, \"index\": 6, \"width\": 497, \"height\": 276}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-007.webp\", \"caption\": \"Figure 6: The impact of behavior weight combinations on\", \"page\": 12, \"index\": 7, \"width\": 402, \"height\": 856}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-008.webp\", \"caption\": \"Table 6\", \"page\": 12, \"index\": 8, \"width\": 497, \"height\": 568}, {\"url\": \"assets/figures/arxiv/2606.05537v1/fig-009.webp\", \"caption\": \"Table 1\", \"page\": 5, \"index\": 9, \"width\": 498, \"height\": 465}]"
motivation: 现有图/超图方法在异质语义、用户特定权重和序列依赖建模上存在不足，标准Transformer的共享前馈映射难以适应多行为场景中异质潜在模式的差异化需求。
method: 设计个性化动态超图模块进行行为感知的相似性加权以捕获用户特定高阶关系，同时采用KAN替换Transformer中的MLP以增强细粒度非线性建模能力。
result: 在Tmall、RetailRocket和IJCAI三个数据集上，PHKT一致优于九个强基线模型。
conclusion: PHKT能有效建模多行为偏好并准确预测目标行为，验证了其方法的优越性。
---

## 摘要
在多行为推荐中，点击、加入购物车和购买等辅助行为可以为预测目标行为提供更丰富的监督信息。尽管现有的图和超图方法能够建模用户、物品和行为之间的高阶关系，但在异质语义、用户特定权重和序列依赖建模方面仍存在局限性。虽然标准Transformer擅长序列建模，但其共享的前馈映射难以适应多行为场景中异质潜在模式的差异化需求。为此，本文提出了个性化超图增强Kolmogorov-Arnold网络Transformer（PHKT）。具体而言，我们设计了一个个性化动态超图模块，基于用户历史行为序列对物品相似度进行行为感知加权，以捕获用户特定的异质高阶关系。同时，采用Transformer作为时间骨架来建模短期和长期偏好的演变，并引入KAN替代前馈网络中的传统MLP，以增强对不同潜在模式非线性响应的细粒度建模能力。在Tmall、RetailRocket和IJCAI三个真实数据集上的实验表明，PHKT在多个评估指标上持续优于九个强基线模型，证明了其在多行为偏好建模和目标行为预测中的有效性。

## Abstract
In multi-behavior recommendation, auxiliary behaviors such as clicks, add-to-cart, and purchases can provide richer supervisory information for predicting target behaviors. Although existing graph and hypergraph methods are capable of modeling high-order relationships among users, items, and behaviors, they still have limitations in heterogeneous semantics, user-specific weighting, and sequence dependency modeling. While standard Transformers excel at sequence modeling, their shared feedforward mapping struggles to accommodate the differentiated requirements of heterogeneous latent patterns in multi-behavior scenarios. To address this, this paper proposes the Personalized Hypergraph-enhanced Kolmogorov-Arnold Network Transformer (PHKT). Specifically, we design a personalized dynamic hypergraph module that performs behavior-aware weighting of item similarities based on users' historical behavior sequences to capture user-specific heterogeneous high-order relationships. Meanwhile, a Transformer is used as the temporal backbone to model the evolution of short- and long-term preferences, and KAN is introduced to replace the traditional MLP in the feedforward network to enhance fine-grained modeling capability for nonlinear responses to different latent patterns. Experiments on three real datasets, Tmall, RetailRocket, and IJCAI, show that PHKT consistently outperforms nine strong baseline models across multiple evaluation metrics, demonstrating its effectiveness in multi-behavior preference modeling and target behavior prediction.

---

## 论文详细总结（自动生成）

# PHKT: 面向多行为序列推荐的个性化动态超图增强KAN-Transformer 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：在线服务平台中用户行为呈现多类型（浏览、加入购物车、收藏、购买），构成多行为序列。现有推荐方法主要面临两大局限：
  - **静态超图建模不足**：传统图/超图方法通常为（超）边分配静态权重，无法适应**用户特定的异质行为语义**及其动态演变，难以捕获“浏览→收藏→购买”这类行为链条以及高阶复杂依赖。
  - **统一前馈映射的局限**：标准Transformer的自注意力层虽然能编码多种潜在行为模式，但其前馈网络（FFN）采用**共享的MLP映射**，对所有潜在模式使用同一非线性变换模板，无法细粒度拟合不同模式对购买预测的差异化贡献（如“点击后购买”与“仅点击”模式的不同响应函数）。
- **整体意义**：本文提出**PHKT**，通过融合个性化动态超图与KAN-Transformer，旨在同时建模**异质行为的高阶结构关系**、**时间序列依赖**及**潜在模式的细粒度非线性响应**，从而更准确地预测目标行为（购买）。

## 2. 方法论

### 2.1 核心思想
- **双分支协同架构**：一条分支是**KAN-Transformer**，用于捕获序列时序依赖和模式特异性响应；另一分支是**个性化动态超图模块（PHDW）**，用于捕获用户特定的高阶异质行为关系。最终通过可学习系数融合两分支输出进行掩码预测。

### 2.2 关键技术细节

#### （1）行为感知的KAN-Transformer（KANT）
- **动机**：标准FFN（`FFN(x)=W2σ(W1x+b1)+b2`）对所有潜在模式共用激活模板，无法区分不同模式的非线性贡献。KAN允许每个输入维度独立学习径向基函数组合，实现模式特异性的非线性映射。
- **实现**：
  - 对每个输入维度 `x_i`，定义 `K` 个径向基函数 `B_i(x_i)=[b_{i,1}(x_i),...,b_{i,K}(x_i)]`。
  - 对基函数输出线性投影：`z_i = B_i(x_i)·ω_i`，`ω_i∈ℝ^{K×d_out}`。
  - 最终输出：`KAN(x)=∑_{i=1}^d z_i`（求和汇聚）。
  - 替换Transformer中的FFN，并保留残差连接和LayerNorm。

#### （2）个性化动态超图模块（PHDW）
- **步骤**：
  1. **初始行为权重**：为每种行为类型分配初始权重（例如浏览0.3，加购0.7，购买1.0，其他0.4）。
  2. **物品相似度计算**：基于物品嵌入 `X_v` 计算余弦相似度矩阵 `Sim ∈ ℝ^{L×L}`。
  3. **行为加权相似度**：利用用户历史行为权重对角矩阵 `W=diag(weight(t_1),...,weight(t_L))`，对相似度矩阵进行双向加权：`Sim_weighted = W·Sim·W`。这放大了高价值行为（如购买）对应的物品关联，抑制噪声行为（如误点）。
  4. **超边构建**：对每个物品，选取加权相似度最高的 `k` 个物品（包括自身）构成一条超边 `e_i`。所有超边构成超图邻接矩阵 `H`。
  5. **超图卷积**：采用HGNN传播公式 `G = D_v^{-1} H D_e^{-1} H^T`，`X_out^G = σ(G·X_in·W_h)`，学习节点表示。

#### （3）预测与融合
- 融合输出：`X_fused = φ·X_out^{KAN-Trans} + (1-φ)·X_out^G`，其中 `φ` 为可学习注意力标量。
- 训练采用掩码物品预测（Masked Item Prediction），损失为交叉熵，仅在掩码位置计算。

## 3. 实验设计

### 3.1 数据集与场景
- **Tmall**：中国电商平台，涵盖浏览、收藏、加购、购买四种行为，约100M交互。
- **RetailRocket**：零售电商日志，含浏览、加购、购买三种行为，约2.7M交互。
- **IJCAI**：IJCAI竞赛买家行为数据，行为类型与Tmall相同，约55M交互。
- 预处理：过滤用户交互<5次、物品交互<10次的数据；8:2划分训练/测试集。

### 3.2 基准方法（共9种）
- **通用序列推荐**：GRU4RecCPR、SASRecCPR、FEARec。
- **图神经网络序列推荐**：SURGE、MAERec、SelfGNN。
- **多行为推荐**：MBHT、PBAT、MBSRec。
- 注：MBHT是本文最直接的相关基线，同样使用超图+Transformer。

### 3.3 评价指标
- HR@5/10、NDCG@5/10、MRR@5。

## 4. 资源与算力

- **文中提及**：实验在一台配备 **NVIDIA GeForce RTX 5060 Ti** 显卡的 Windows 服务器上进行，基于 Python 3.8.0 和 PyTorch 框架。
- **未说明**：具体训练时间、轮数、批次大小等细节未提供，因此无法评估训练总成本。

## 5. 实验数量与充分性

- **主实验**：在三个数据集上对比9个基线，报告HR/NDCG/MRR指标，结果清晰（表3-5）。
- **消融实验**：设置两个变体——去掉KANT（换回标准FFN）、去掉动态超图（使用静态超图），在三个数据集上验证各模块贡献（表6）。
- **参数实验**：探究特征维度（50/100/150/200/300）、KAN层数（2/3/4/5）、初始行为权重组合（6种）的影响（图4-6）。
- **充分性评价**：
  - **充分**：覆盖了多个数据集、多种基线、关键模块消融和主要超参数。
  - **公平**：基线均为近年SOTA，且按技术路线分类对比；实验设置一致。
  - **客观**：指标选取标准，结果数据完整。

## 6. 主要结论与发现

- **整体性能**：PHKT在所有三个数据集上均取得最优结果，尤其在**IJCAI**上提升显著（HR@10较次优提升49.9%，NDCG@10提升56.7%）。
- **消融验证**：移除KANT或动态超图均导致性能下降，且移除KANT的下降幅度更大，说明**KAN-Transformer**在细粒度模式建模中起核心作用。
- **参数敏感性**：
  - 特征维度存在最优值（RetailRocket最优100维，IJCAI/Tmall最优200维）。
  - KAN层数2-3层最佳，过深导致过拟合。
  - 行为权重应强调购买（最高）、抑制浏览（最低），均匀权重效果差。
- **方法论优势**：图结构方法（如SelfGNN）显著优于纯序列方法；多行为方法（如MBSRec、MBHT）进一步优于单行为图方法；PHKT通过动态超图+KAN将多行为建模推向新高度。

## 7. 优点

- **创新性**：
  - 首次将**KAN**引入Transformer前馈网络用于推荐，实现潜在模式的非线性**差异化响应**，而非统一MLP。
  - 提出**个性化动态超图**，基于用户历史行为对物品相似度进行行为感知加权，使超图结构随用户行为分布自适应。
- **架构设计**：双分支（时序+结构）互补，通过可学习系数融合，简洁有效。
- **实验全面**：覆盖三种不同规模和行为分布的数据集，消融、参数分析充分，结论可靠。
- **可解释性启发**：通过KAN的维度独立基函数组合，潜在解释不同行为模式对预测的贡献差异（理论部分有定性解释）。

## 8. 不足与局限

- **计算资源与效率评价缺失**：未报告训练时间、推理速度，未与基线对比复杂度，无法评估实际部署成本。
- **KAN层超参数依赖**：KAN层数、基函数数量K等参数需根据数据集调优，文中未给出系统调参规则，可能影响复现性。
- **行为初始权重依赖人工设定**：初始权重（如[0.3,0.7,1,0.4]）基于先验，且不同数据集最优组合不同，缺乏自动化学习机制。
- **跨域/冷启动场景未验证**：仅评测了热门电商平台，未考虑新用户/新物品的冷启动问题，泛化性需进一步验证。
- **潜在偏差风险**：数据集可能存在交互稀疏性分布不均（如IJCAI用户行为少），但未讨论对模型公平性的影响。
- **理论解释深度有限**：虽然解释了KAN能学习模式特异性响应，但未提供定量分析（如可视化不同模式对应的响应函数）。

（完）
