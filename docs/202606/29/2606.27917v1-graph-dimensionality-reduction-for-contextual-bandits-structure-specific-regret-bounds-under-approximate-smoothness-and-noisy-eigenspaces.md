---
title: "Graph Dimensionality Reduction for Contextual Bandits: Structure-Specific Regret Bounds under Approximate Smoothness and Noisy Eigenspaces"
authors: "Joyanta Jyoti Mondal, Ibne Farabi Shihab, Anuj Sharma"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.27917v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于社交广告推荐的图波段方法
tldr: 针对具有图结构臂的上下文赌博机问题，本文提出GraphDR-LinUCB算法，将臂特征投影到图的低频谱子空间，并利用线性UCB进行决策。理论证明了首个基于谱投影的上下文赌博机的O(k√T)遗憾界，将维度依赖从d降到k；通过扰动分析扩展到噪声图，并建立了奖励平滑度与图估计误差的显式惩罚。实验表明，在合成和六个真实数据集（MovieLens、Amazon、LastFM等）上，GraphDR-LinUCB将累积遗憾降低15倍，并在五个数据集上优于现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.27917v1/fig-001.webp\", \"caption\": \"Table 3: Cumulative regret R(T ) against graph-aware bandits ( T=20000, 5 seeds synthetic; lowest per row in bold). GraphDRLinUCB beats both SpectralUCB and Laplacian-regularized LinUCB on the synthetic benchmark and on five of six real datasets. The single exception is ogbn-arxiv (ζk=0.96): even soft graph-aware methods beat hard bottom-k projection when the eigenspace is misaligned with the reward. The advantage of hard spectral truncation over soft eigenvalue-penalized smoothing is that truncation entirely removes the exploration cost of high-frequency directions rather than merely down-weighting it.\", \"page\": 7, \"index\": 1, \"width\": 706, \"height\": 192}, {\"url\": \"assets/figures/arxiv/2606.27917v1/fig-002.webp\", \"caption\": \"Table 1: Summary of the four theoretical regimes. The structure-specific bound (Thm. 5) tightens the generic worstcase linear bias to realized graph quantities SRLT andCRWT (Corollary 9); the estimated-residual variant (Thm. 7) makes it implementable.\", \"page\": 3, \"index\": 2, \"width\": 527, \"height\": 356}, {\"url\": \"assets/figures/arxiv/2606.27917v1/fig-003.webp\", \"caption\": \"Table 9: The subspace-capture margin Γk predicts the GraphDR-vs-content-PCA outcome where ζk cannot. Rows sorted by Γk; final column is the realized outcome (signed gap RGraphDR − RPCA in parentheses, negative = GraphDR wins). The fitted-threshold rule is leave-one-base-dataset-out correct on 6/6; the zero-parameter sign rule Γk > 0 is correct on 5/6 (lone miss: MIND at the near-tie Γk = −0.008, where both subspaces capture almost no reward energy yet GraphDR wins because content-PCA is itself far from the reward). Crucially, Γk orders ogbn-arxiv (−0.240, GraphDR’s documented failure) at the bottom while ζk cannot: MIND has the largest tail (0.99) yet GraphDR wins, exactly the inversion that defeated the scalar tail and the unit-weighted Gk.\", \"page\": 13, \"index\": 3, \"width\": 648, \"height\": 199}]"
motivation: 标准降维方法忽略图结构导致探索成本随d/k增长，本文旨在利用图结构减少探索代价。
method: 提出GraphDR-LinUCB，将臂特征投影到图的低频谱子空间，并在k维空间运行线性UCB。
result: 证明了O(k√T)遗憾界，并在六个真实数据集上实现15倍遗憾降低，优于五个数据集上的图感知方法。
conclusion: 高频奖励分量不必然带来线性惩罚，其实际代价取决于沿路径的实现影响；谱子空间对齐程度决定降维器性能。
---

## Abstract
Contextual bandits with graph-structured arms arise in recommendation, citation retrieval, and social advertising, where arms connected on a graph tend to share reward signal. Standard dimensionality reduction ignores this structure, inflating exploration cost by a factor of $d/k$. We propose GraphDR-LinUCB, which projects arm features onto the graph's low-frequency spectral subspace and runs linear UCB in the resulting $k$-dimensional space. We prove the first $\wtO(k\sqrt{T})$ regret bound for spectral-projection-based contextual bandits, reducing dimension dependence from $d$ to $k$; a perturbation argument extends this to noisy graphs, with an explicit penalty for reward-smoothness mismatch and graph-estimation error. Our central theoretical finding is that the high-frequency reward component need not incur a worst-case linear-in-$T$ penalty: its actual cost depends on its realized impact along the played path, not on its total energy. A simple spectral comparison between subspaces ($Γ_k$) predicts which reducer wins on a given dataset, correctly calling five of six real-dataset outcomes without any fitted threshold. Across a synthetic benchmark and six real datasets (MovieLens, Amazon, LastFM, ogbn-arxiv, MIND), GraphDR-LinUCB reduces cumulative regret by $15\times$ over full-dimensional LinUCB and outperforms competing graph-aware methods on five of six; the single failure is precisely where the graph's spectral subspace is misaligned with the reward.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：上下文赌博机（Contextual Bandits）在推荐系统、引文检索和社交广告中广泛应用，其臂（arms）往往具有图结构——图中的相邻节点倾向于共享奖励信号。
- **现有方法不足**：标准降维方法（如随机投影、PCA）完全忽略图结构，导致探索代价随特征维度 $d$ 增长因子 $d/k$ 膨胀。现有图感知方法（如拉普拉斯正则化、谱赌博机）虽然利用图信息，但并未通过硬投影（hard projection）实际降低探索维度，也未分析噪声图和不完美图平滑性的影响。
- **核心挑战**：高频奖励成分在传统误设定线性赌博机理论中会被处以线性于 $T$ 的惩罚，迅速淹没 $\sqrt{T}$ 探索收益。本文需要回答：硬谱投影能否带来真正收益？何时失效？奖励-图对齐程度如何量化？

## 2. 论文提出的方法论

### 2.1 核心思想
- **GraphDR-LinUCB**：将臂的特征向量投影到图拉普拉斯矩阵的低频特征子空间（底部 $k$ 个特征向量），然后在 $k$ 维空间运行标准的LinUCB算法。
- **关键洞察**：高频残差（$r_{\perp,k} = (I - \Pi_k)\theta^*$）的代价不必是线性于 $T$ 的；实际代价取决于两个沿实现轨迹的图量——**残差杠杆（SRL$_T$）** 和**候选集残差宽度（CRW$_T$）**，而非总量 $\zeta_k$。

### 2.2 关键技术细节
- **假设**：
  - 精确图平滑性：$\theta^* = E_k\alpha^*$（完美位于低频谱子空间）。
  - 近似平滑性与噪声图：$\|r_k\|\leq\zeta_k$，$\|\hat{L}-L\|\leq\varepsilon_L$，谱隙 $\Delta_k>0$。
- **算法流程**（Algorithm 1）：
  1. 从观测拉普拉斯 $\hat{L}$ 计算底部 $k$ 维标准正交基 $\hat{E}_k$。
  2. 初始化岭回归矩阵 $V_1 = \lambda I_k$，$b_1=0$。
  3. 每轮 $t$：计算参数估计 $\hat{\alpha}_t = V_t^{-1}b_t$；将每个臂特征投影为 $z_{t,a} = \hat{E}_k^\top x_{t,a}$；按UCB选取臂：$a_t = \arg\max (z^\top\hat{\alpha}_t + \beta_t \|z\|_{V_t^{-1}})$；观察奖励 $y_t$；更新 $V_{t+1}, b_{t+1}$。
- **置信半径**：$\beta_t = \beta_t^0 + B_T$，其中 $B_T$ 需上界残差杠杆SRL$_T$（或通过估计残差构造）。

### 2.3 理论贡献
- **Theorem 1（精确子空间）**：在完美图平滑性下，遗憾界 $\tilde{O}(k\sqrt{T})$，维度依赖从 $d$ 降至 $k$。
- **Theorem 4（通用鲁棒）**：在近似平滑性和噪声图下，遗憾界 $\tilde{O}(k\sqrt{T} + L_x(\zeta_k + S_k\varepsilon_L/\Delta_k)kT)$，线性偏差项与谱隙倒数成正比。
- **Theorem 5（结构特定遗憾）**：遗憾界 $2\beta_{T+1}^0 A_T + 2\ \text{SRL}_T A_T + \text{CRW}_T$，其中SRL$_T$ 和 CRW$_T$ 是沿实现轨迹定义的图量，高频残差代价可远小于最坏情况线性。
- **Theorem 7（可实施变体）**：使用从独立数据估计的残差 $\hat{r}$，遗憾界增加 $O(\epsilon_r kT)$，在良好预估下优于通用界。
- **子空间捕获边际 $\Gamma_k$**：$\Gamma_k = \|U_k^\top\theta\|^2 - \|P^\top\theta\|^2$，零参数统计量，直接比较图谱子空间与竞争降维器（如内容PCA）的奖励保持能量，预测5/6真实数据集结果。

## 3. 实验设计

### 3.1 数据集与场景
- **合成数据**：
  - 随机块模型（SBM）：$n=d=200$，$k^*=5$个社区，$T=20000$。
  - 随机几何图（RGG）：$n=200$，半径0.16。
  - 维度缩放实验：$d=n$从60到800。
  - 投影维度扫描实验：$k$从1到20。
- **真实数据集**（六个）：
  - 推荐图：MovieLens-100k/1M（物品-物品共评分kNN图），Amazon Digital Music（共评分图），LastFM/HetRec（共听kNN图）。
  - 非推荐图：MIND-small新闻（类别/共点击图），ogbn-arxiv引文（对称化论文引用图）。
  - 所有数据集臂特征为节点指示符（$x_{t,a}=e_a$），奖励来源于真实评分/播放/点击/标签。

### 3.2 基准方法
- Random（随机选择）
- LinUCB-full（完整 $d$ 维）
- PCA+LinUCB（主成分分析投影）
- JL+LinUCB（Johnson-Lindenstrauss随机投影）
- GraphDR-shuffled（图洗牌控制：用节点置换后的拉普拉斯特征向量投影，破坏图对齐但保持维度）
- SpectralUCB（谱赌博机：全拉普拉斯特征基带平滑权重，无硬截断）
- Laplacian-regularized LinUCB（拉普拉斯正则化LinUCB）

### 3.3 评估指标
- 累积遗憾 $R(T)$（$T=20000$，种子数5~8，报告均值±标准误）。

## 4. 资源与算力

- **文中未明确说明**所使用的GPU型号、数量或训练时长。所有实验很可能在CPU上完成（因为特征维度 $n$ 最大1500，LinUCB使用谢尔曼-莫里斯更新，计算量较小）。未提及额外算力开销。

## 5. 实验数量与充分性

### 5.1 实验数量
- **合成实验**：主比较（1组，8种子）、维度缩放（5个 $d$值）、第二图族（1组）、$k$扫描（7个 $k$值）、噪声压力测试（2组共16个配置）。
- **真实数据集**：6个数据集，每个重复5种子。还与图感知基线对比（SpectralUCB, Laplacian-reg），同样6数据集。另外包含诊断实验（150个实例的亚采样子图研究）。
- **消融/控制**：图洗牌控制、投影维度扫描、噪声水平扫描、残差预估质量测试。

### 5.2 实验充分性评价
- **正面**：覆盖合成和真实场景，包含多种基线（图盲、图感知、随机），进行了维度依赖性、投影维度选择、噪声鲁棒性等消融分析。图洗牌控制有效隔离了图结构增益。
- **客观性**：各方法均在同一协议下运行，遗憾值报告均值±标准误；理论预测（$\Gamma_k$ 符号）在6个数据集中正确5个，验证方法客观。
- **不足**：
  - 真实数据集仅6个，且均为节点指示符特征（$d=n$），未扩展到一般特征（如嵌入）。
  - 未在在线A/B测试或真实流量中评估，仅使用离线校准奖励。
  - 自适应 $k$ 选择未实现（仅扫描固定 $k$）。
  - 缺乏与低秩方法（如Bilinear Bandits）的直接对比。
  - 诊断Gk在150实例上的Spearman相关性低于 $\zeta_k$，表明理论驱动的标量聚合未实现预期效果。

## 6. 主要结论与发现

1. **理论**：GraphDR-LinUCB在精确图平滑性下实现 $\tilde{O}(k\sqrt{T})$ 遗憾，将维度依赖从 $d$降至 $k$。结构特定理论（Theorem 5）表明高频残差代价由沿轨迹的SRL$_T$和CRW$_T$控制，可远小于最坏情况线性。
2. **实验**：在合成数据上GraphDR将遗憾降低 **15倍** 于完整LinUCB，图洗牌控制崩溃至近随机水平。在六个真实数据集中，GraphDR在五个上打败完整LinUCB，在四个上打败PCA。唯一失败（ogbn-arxiv）对应 $\zeta_k=0.96$ 且 $\Gamma_k<0$，恰与理论预测一致。
3. **选择规则**：子空间捕获边际 $\Gamma_k$（零参数符号规则）在6个数据集上正确预测5个结果，优于标量尾 $\zeta_k$（仅2/6）和诊断Gk（2/6）。
4. **噪声鲁棒**：谱隙 $\Delta_k$ 保护算法免受图估计误差影响，验证了 $\varepsilon_L/\Delta_k$ 依赖。

## 7. 优点

- **理论深度**：首次为谱投影上下文赌博机提供 $\tilde{O}(k\sqrt{T})$ 遗憾界，并建立结构特定残差分析，摆脱最坏情况误设定理论。
- **可实践性**：提出 $\Gamma_k$ 选择规则（无需调参），在6个数据集上验证有效性。
- **实验严谨**：图洗牌控制、噪声压力测试、投影维度扫描、多个图族和真实场景全覆盖。
- **开放透明**：将高频率奖励代价与具体轨迹关联，而非简单用标量上界，提供更精细的遗憾分解。

## 8. 不足与局限

- **实验覆盖局限**：
  - 所有真实数据使用节点指示符特征（$x_{t,a}=e_a$），未测试图嵌入或一般特征输入。
  - 在线评估缺失（仅离线校准奖励），可能高估相对优势。
  - 自适应 $k$ 选择未实现，需依赖先验知识或枚举。
- **理论局限**：
  - SRL$_T$ 依赖未知 $\theta^*$，可实施版本（Theorem 7）需要独立预估且预估误差 $O(\epsilon_r)$ 仍导致线性项。
  - 未证明匹配下界（如 $\varepsilon_L/\Delta_k$ 依赖的下界）。
  - 诊断Gk在150实例子图研究中未超越 $\zeta_k$ 的Spearman相关性，表明理论聚合可能不够紧。
- **部署风险**：当图结构严重偏离奖励（如ogbn-arxiv），投影会损失关键信息，建议使用 $\Gamma_k$ 检测并切换到替代方法。
- **计算开销**：需要计算大图拉普拉斯特征分解（$n$可达1500），但未讨论更大规模图的近似方法（如Nyström）。

（完）
