---
title: "LRanker: LLM Ranker for Massive Candidates"
title_zh: LRanker：面向大规模候选集的LLM排序器
authors: "Tao Feng, Zijie Lei, Zhigang Hua, Yan Xie, Shuang Yang, Ge Liu, Jiaxuan You"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27810v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 用于大规模候选排序的LLM排序器
tldr: "针对LLM在百万级候选排序中受限于上下文长度和计算成本的问题，提出LRanker框架。其包含候选聚合编码器（K-means聚类建模全局信息）和基于图的测试时缩放机制（子集划分、多查询嵌入集成）。在RBench的七个任务上，小规模场景提升超30%，大规模场景MRR提升3-9%，超大规模（680万+）提升20-30%，消融实验验证了各组件的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27810v1/fig-001.webp\", \"caption\": \"Table 3. Model performance comparison with general ranking baselines and task-specific baselines across four scenarios on NDCG@10 and MRR. Left: Rec-Movie and Rec-Toy. Right: MS MARCO and ESCI. Bold and underline denote the best and second-best results.\", \"page\": 6, \"index\": 1, \"width\": 1002, \"height\": 298}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-002.webp\", \"caption\": \"Figure 4. The graph-based test-time ensemble produces richer query representations than a single embedding. t-SNE visualizations show that averaged embeddings from LRanker tend to lie closer to the ground-truth item.\", \"page\": 11, \"index\": 2, \"width\": 474, \"height\": 396}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-003.webp\", \"caption\": \"Table 10. Performance comparison in scenarios under candidates distribution shift across two scenarios on NDCG@10 and MRR. Left: Rec-Toy. Right: MS MARCO. Specifically, we train the models on the training sets of Rec-Toy and MS MARCO, where the candidates used during training are the original candidates of each dataset. During testing, we replace the candidate set of Rec-Toy with a mixed candidate pool constructed from both Rec-Movie and Rec-Toy. Similarly, for MS MARCO and ESCI, we replace each candidate set with mixed candidate pools that combine candidates from MS MARCO and ESCI. In addition, ∆ performance denotes the relative difference in MRR between the results obtained under the mixed-candidate setting and those obtained under the original candidate set.Bold and underline denote the best and second-best results.\", \"page\": 15, \"index\": 3, \"width\": 1000, \"height\": 525}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-004.webp\", \"caption\": \"Table 9. Performance comparison in scenarios with extremely irrelevant candidates across two scenarios on NDCG@10 and MRR. Left: Rec-Toy. Right: MS MARCO. Specifically, we train the models on the training sets of Rec-Toy and MS MARCO, where the candidates used during training are the original candidates of each dataset. During testing, however, we replace the candidates with a mixed pool that combines all candidates from Rec-Movie, Rec-Toy, MS MARCO, and ESCI. In addition, ∆ performance denotes the relative difference in MRR between the results obtained under the mixed-candidate setting and those obtained under the original candidate set.Bold and underline denote the best and second-best results.\", \"page\": 14, \"index\": 4, \"width\": 1000, \"height\": 512}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-005.webp\", \"caption\": \"Figure 1. Compared with existing LLM rankers on large-candidate tasks, LRanker incorporates advanced designs in both the representation of candidate information and the inference strategies used during testing. Note that the spark icon denotes models that require fine-tuning, while the snowflake icon denotes models with frozen weights. (a) Existing LLM rankers generally adopt four input formats (highlighted in the red box): (i) query only, (ii) query with a single candidate, (iii) query with candidate pairs, and (iv) query with the complete candidate list. The (iv) setting is fundamentally constrained by the limited context length of LLMs in massive-candidate scenarios, while the first three fail to incorporate global candidate-level information, leading to systematic biases in ranking. (b) LRanker’s training pipeline. In Stage 1 (data preparation), offline candidate embeddings are randomly split and clustered by K-means to obtain centroid embeddings. In Stage 2 (training), these centroids are passed through the learnable projector and injected into the LLM decoder via a special placeholder token, yielding query embeddings that already condition on global candidate information and are optimized with a ranking loss. (c) Graph-based test-time scaling. Step 1 obtains an initial query embedding E (0) q on the full candidate set. Step 2 uses E(0) q to retain top-ranked candidates within each partition and recompute partition-specific embeddings. Step 3 updates the query embedding by averaging embeddings from multiple partitions and repeating this elimination-and-update process for d rounds, producing {E(t) q }dt=0. Step 4 averages the scores computed from these embeddings to obtain the final ranking, enabling robust performance across diverse candidate scales.\", \"page\": 3, \"index\": 5, \"width\": 1013, \"height\": 370}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-006.webp\", \"caption\": \"Table 11. Performance comparison of the possible candidate aggregation encoder variants across four tasks.\", \"page\": 18, \"index\": 6, \"width\": 494, \"height\": 459}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-007.webp\", \"caption\": \"Table 15. Per-query inference latency comparison across different models on Rec-Music and MS MARCO.\", \"page\": 19, \"index\": 7, \"width\": 665, \"height\": 182}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-008.webp\", \"caption\": \"Table 12. Evaluating the direct task-solving capability of Qwen3 backbones of different sizes against general and task-specific baselines on Rec-Movie and Rec-Toy. Bold and underline denote the best and second-best results.\", \"page\": 19, \"index\": 8, \"width\": 657, \"height\": 492}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-009.webp\", \"caption\": \"Figure 7. Effect of the centroid dimensionality on the performance of LRanker across four tasks. Increasing the dimensionality generally improves the quality of centroid representations by preserving more semantic information, leading to consistent gains over the strongest baseline under all settings. Moderate dimensions (256–1024) already achieve strong results, indicating that LRanker does not require the full 1024-dimensional embeddings to maintain high effectiveness.\", \"page\": 17, \"index\": 9, \"width\": 999, \"height\": 705}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-010.webp\", \"caption\": \"Table 16. Optimal width/depth settings of LRanker and their relative latency overhead compared to direct ranking.\", \"page\": 20, \"index\": 10, \"width\": 804, \"height\": 353}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-011.webp\", \"caption\": \"Table 2. Detailed summarization of tasks used in our ranking experiments with varying candidate scales. We summarize the scenarios, task names, candidate sizes, and the number of queries.\", \"page\": 5, \"index\": 11, \"width\": 492, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2605.27810v1/fig-012.webp\", \"caption\": \"Figure 6. Effect of the number of centroids (k) on the performance of LRanker across four tasks. LRanker consistently outperforms the strongest baseline under all choices of k, and typically reaches peak performance at moderate values ( k = 10–50). Larger k introduces finer but noisier partitions, resulting in a slight performance drop.\", \"page\": 16, \"index\": 12, \"width\": 999, \"height\": 701}]"
motivation: 现有LLM排序方法因上下文长度和计算成本限制，难以处理百万级候选池。
method: 提出LRanker，包括候选聚合编码器（K-means聚类）和基于图的测试时缩放机制（子集划分、多嵌入集成）。
result: "在RBench的七个任务中，小规模场景提升超30%，大规模场景MRR提升3-9%，超大规模（680万+）提升20-30%。"
conclusion: LRanker在大规模候选排序中展现出鲁棒性、可扩展性和有效性。
---

## 摘要
大语言模型（LLMs）最近通过捕捉语义相关性和跨领域自适应展现了强大的排序潜力，但现有方法仍受限于有限的上下文长度和高计算成本，限制了其在候选池规模可达数百万的真实场景中的应用。为解决这一挑战，我们提出LRanker，一个专为大候选集排序设计的框架。LRanker包含一个候选聚合编码器，利用K-means聚类显式建模全局候选信息，以及一个基于图的测试时扩展机制，将候选集划分为子集，生成多个查询嵌入，并通过集成过程整合它们。通过聚合多样化的嵌入而非依赖单一表示，该机制增强了鲁棒性和表达能力，从而实现对大规模候选池的更准确排序。我们在RBench的三个场景、七个不同候选规模的任务上评估了LRanker。实验结果表明，在RBench-Small场景中，LRanker获得了超过30%的提升；在RBench-Large场景中，MRR提升了3-9%；在拥有超过680万候选集的RBench-Ultra场景中，LRanker保持了可扩展性，提升了20-30%。消融研究进一步验证了其关键组件的有效性。总之，这些发现证明了LRanker在大规模候选集排序中的鲁棒性、可扩展性和有效性。

## Abstract
Large language models (LLMs) have recently shown strong potential for ranking by capturing semantic relevance and adapting across diverse domains, yet existing methods remain constrained by limited context length and high computational costs, restricting their applicability to real-world scenarios where candidate pools often scale to millions. To address this challenge, we propose LRanker, a framework tailored for large-candidate ranking. LRanker incorporates a candidate aggregation encoder that leverages K-means clustering to explicitly model global candidate information, and a graph-based test-time scaling mechanism that partitions candidates into subsets, generates multiple query embeddings, and integrates them through an ensemble procedure. By aggregating diverse embeddings instead of relying on a single representation, this mechanism enhances robustness and expressiveness, leading to more accurate ranking over massive candidate pools. We evaluate LRanker on seven tasks across three scenarios in RBench with different candidate scales. Experimental results show that LRanker achieves over 30% gains in the RBench-Small scenario, improves by 3-9% in MRR in the RBench-Large scenario, and sustains scalability with 20-30% improvements in the RBench-Ultra scenario with more than 6.8M candidates. Ablation studies further verify the effectiveness of its key components. Together, these findings demonstrate the robustness, scalability, and effectiveness of LRanker for massive-candidate ranking.

---

## 论文详细总结（自动生成）

好的，这是基于您提供的论文内容生成的结构化、深入、客观的中文总结。

## 论文总结：LRanker: LLM Ranker for Massive Candidates（LRanker：面向大规模候选集的LLM排序器）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：如何利用大语言模型（LLM）对**百万级规模**的候选集进行高效、准确的排序。
- **背景与现状**：现有的LLM排序方法在处理小规模候选集时表现出色，但受限于两个关键瓶颈：
    1.  **有限的上下文长度**：LLM无法一次性处理包含数百万候选结果的完整列表。
    2.  **极高的计算成本**：逐一评估或生成排序结果的开销过大。
- **现有方法的缺陷**：
    - “查询+完整候选列表”的输入方式因上下文限制不可行。
    - “仅查询”、“查询+单候选”或“查询+候选对”的方式**无法建模全局候选信息**，引入排序偏差。
    - 基于Token空间生成排序结果的方法将排序质量与解码延迟耦合，限制了可扩展性。
- **研究意义**：该论文旨在解决LLM在真实世界大规模推荐、搜索等场景下的应用瓶颈，提出一个能够处理百万级候选集的通用框架。

### 2. 论文提出的方法论

论文提出 **LRanker** 框架，其核心思想是通过**聚合全局候选信息**和**多视角集成推理**来提升大规模排序的性能和鲁棒性。框架主要由两部分组成：

- **核心思想与框架设计**：LRanker采用编码器-解码器框架。关键创新在于：
    1.  **注入全局信息**：不再将原始候选列表输入LLM，而是通过一个**候选聚合编码器**，将大规模候选集压缩成紧凑的聚类质心，作为软提示注入LLM，使模型在生成查询表示时就能感知全局候选分布。
    2.  **多视角集成推理**：在测试时，通过**基于图的测试时缩放**机制，生成多个基于不同候选子集的查询嵌入，并集成为一个更鲁棒、表达能力更强的最终表示。
- **关键技术细节**：
    - **候选聚合编码器**：
        1.  **离线编码**：首先使用一个基础编码器（如Qwen3）为每个候选生成并缓存嵌入向量。
        2.  **K-means聚类**：对这些候选嵌入进行K-means聚类，得到K个聚类质心。
        3.  **投影与注入**：通过一个可学习的MLP投影器将质心向量映射到LLM的嵌入空间，并通过一个特殊占位符 `<|embedding|>` 将其作为软提示注入到LLM的输入中。
    - **LLM解码器**：LLM在接收到查询和聚合后的全局候选信息后，生成查询嵌入（通过平均查询词和`<eos>`位置的隐藏状态）。同时，每个候选也会通过同一个LLM（离线或批量处理）生成候选嵌入。
    - **排序与训练**：通过查询嵌入和候选嵌入的内积计算相关性分数。使用**InfoNCE损失**（交叉熵形式）进行训练，目标是让正样本对的得分高于负样本对。训练中还引入 **随机划分采样**，模型会随机选择候选集的一个子集进行聚类，以提升对不同候选集规模的鲁棒性。
    - **基于图的测试时缩放**：这是推理阶段的核心组件。流程包括：
        1.  **初始嵌入**：获得全局候选集上的初始查询嵌入 `E_q(0)`。
        2.  **迭代消除与更新**：多次重复以下步骤：
            - 将候选集划分为多个子集。
            - 使用当前查询嵌入，在每个子集内保留排名靠前的候选。
            - 基于这些缩减后的子集，K-means聚类并分别生成新的查询嵌入 `E_q(t, m)`。
            - 将所有新嵌入与初始嵌入求平均，得到更新的查询嵌入 `E_q(t+1)`。
        3.  **最终集成**：将**所有迭代步骤**生成的查询嵌入（`E_q(0)`, `E_q(1)`, ...）与候选嵌入进行内积打分，并**平均所有分数**作为最终排序依据。这个过程类似集成了多个不同“视角”的排序器。

### 3. 实验设计

- **数据集与场景**：作者在 **RBench** 基准的三个场景、**七个任务**上进行评估，候选集规模从20到680万不等。
    - **RBench-Small**：Rec-Music（音乐推荐），Routing-Balance（路由平衡）。候选数：20。
    - **RBench-Large**：Rec-Movie（电影推荐，3884个候选），Rec-Toy（玩具推荐，11925个候选），MS MARCO（段落检索，24697个候选），ESCI（商品搜索，4000个候选）。
    - **RBench-Ultra**：Rec-Clothing（服装推荐，680万+候选）。
- **评估基准**：对比了**通用排序方法**（BM25、Contriever）和**任务特定方法**。
    - **推荐任务**：FM、BERT4Rec、GRU4Rec、SASRec、Tiger。
    - **小型场景**：SASRec、BPR、R1-Rec、IRanker、GraphRouter等。
    - **检索/搜索任务**：RankBERT、Multilingual-E5、KaLM、BGE-Rerank、RankLLaMA。
- **评估指标**：Normalized Discounted Cumulative Gain (NDCG@10) 和 Mean Reciprocal Rank (MRR)。

### 4. 资源与算力

- 文中明确说明了实验的硬件配置和训练细节：
    - **GPU**：**1块 NVIDIA A6000 GPU**。
    - **骨干模型**：基于 **Qwen3-0.6B Embedding** 模型，使用 **LoRA (秩=32)** 进行参数高效微调。
    - **训练时间**：非常高效。例如，在Rec-Movie数据集上仅训练 **52分钟**，在MS MARCO数据集上训练 **21分钟**，远快于对比方法（如Tiger需21小时，RankLLaMA需6.5小时）。
    - **推理时间**：推理时使用预计算候选嵌入和质心，延迟极低（毫秒级），例如在MS MARCO上每查询 **20毫秒**，远低于RankLLaMA的21.67分钟。

### 5. 实验数量与充分性

- **实验数量**：非常丰富。
    - 在**7个任务**上进行了主要性能对比。
    - 进行了**3组核心消融实验**：移除全局信息、移除测试时集成、移除LoRA微调。
    - 进行了**全面的补充实验（附录）**，包括：
        - **零样本泛化**测试（Video Games, Software）。
        - **分布偏移**测试（混合不同来源的候选集）。
        - **参数敏感性**分析（聚类数k、质心维度）。
        - **与其他聚类方法**的对比（Set Encoder, PCA, 层次聚类）。
        - **骨干模型性能**对比（直接使用Qwen3零样本排序）。
        - **可扩展性分析**（在48M候选集上观察表现）。
        - **计算和运行时**效率分析，包括内存占用和延迟。
- **充分性与公平性**：
    - 实验设计**非常充分**，几乎覆盖了所有重要方面：性能、鲁棒性、可扩展性、效率。
    - 对比**公平**，所有基线使用相同配置和硬件，且LRanker的骨干模型较小（0.6B），与更大的模型（如8B的RankLLaMA）对比，展现了其方法的优越性。
    - 结论**客观**，不仅展示了优势，也通过消融实验分析了各组件贡献，并讨论了潜在局限（如测试时集成带来的额外但极小的延迟）。

### 6. 论文的主要结论与发现

- **SOTA性能**：LRanker在所有三个场景、七个任务上**一致性地超越了**所有现有方法，包括通用排序方法和任务特定的最强基线。
- **显著的性能提升**：
    - 在**小规模**场景（RBench-Small）实现了**超过30%** 的相对提升。
    - 在**大规模**场景（RBench-Large）MRR提升**3-9%**。
    - 在**超大规模**场景（RBench-Ultra，680万+候选）实现了**20-30%** 的提升。
- **关键组件有效**：消融实验证实，**全局候选信息注入**、**测试时集成机制**和**LoRA微调**三个模块均对性能有积极贡献。
- **极高的计算效率**：LRanker的训练和推理效率远高于现有的LLM排序器，能够在毫秒级处理百万级候选集，使其适用于生产环境。
- **良好的鲁棒性和泛化性**：在候选集分布偏移、存在大量无关候选等挑战下，LRanker的性能下降幅度最小，展现了其鲁棒性。零样本测试也证明了其良好的泛化能力。

### 7. 优点

- **方法创新性强**：巧妙地解决了LLM在超大候选集下的核心瓶颈。通过K-means聚合实现全局信息注入，是一种高效且信息无损的压缩策略；而基于图的测试时集成，则是一种计算友好、效果显著的推理增强方案。
- **实验极其全面且严谨**：从任务覆盖、基线对比、消融分析到泛化性、鲁棒性、效率分析，实验设计堪称典范，结论可信度高。
- **效率与性能兼顾**：LRanker不仅性能卓越，更重要的是其训练和推理效率极高。这得益于其双编码器结构，使得大多数计算（候选和质心编码）可以预计算和缓存，LLM只需处理查询和少量质心，因此计算量与候选集大小近似解耦。这对于工业应用至关重要。
- **设计简洁实用**：K-means、内积打分、LoRA微调等均为成熟技术，整个框架设计清晰、易于实现和部署。

### 8. 不足与局限

- **对候选嵌入质量的依赖**：模型的性能高度依赖于离线基础编码器生成的候选嵌入的质量。如果基础编码器对于某些领域或任务效果不佳，将直接影响LRanker的上限。论文未对此进行深度探讨。
- **K-means聚类的局限性**：K-means假设簇是球形的，对于分布不均匀或形状复杂的高维数据，聚类效果可能不佳。虽然实验结果良好，但理论上存在改进空间（如使用更先进的聚类方法）。
- **测试时缩放的开销**：虽然论文指出其延迟增加很小（<8%），但对于对延迟有极致要求的在线系统，额外的图迭代和多次编码仍可能构成挑战。文中使用的较浅深度和宽度（如深度6）可能是一种权衡。
- **实验场景的局限性**：所有实验都基于学术或公开数据集（RBench, Amazon）。虽然规模达到百万级，但距离真实互联网搜索引擎或推荐系统的海量数据（数亿）和超低延迟要求仍有差距。在不同模态（如图像、视频）的排序任务上验证其有效性也是一个未来方向。
- **与更前沿的排序方法对比不足**：虽然对比了包括RankLLaMA在内的多种方法，但未与一些更晚近的，基于推理或“思维链”的复杂排序模型进行对比（如Rank-R1，虽然论文时间线可能不允许）。

（完）
