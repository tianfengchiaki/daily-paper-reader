---
title: Constrained Auto-Bidding via Generative Response Modeling
title_zh: 通过生成式响应建模的约束自动出价
authors: "Eunseok Yang, Xingdong Zuo, Kyung-Min Kim"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27811v1"
tags: ["query:q2"]
score: 10.0
evidence: 生成响应模型用于自动出价
tldr: 自动出价系统需在预算和成本约束下最大化广告价值，但未来流量不确定。现有方法存在局限。本文提出生成式响应模型（GRM），通过序列模型预测流量及成本曲线，再结合轻量解析控制器进行约束优化。理论证明最优性差距有界且能限制违规。实验表明GRM提升约束稳定性和综合得分。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27811v1/fig-001.webp\", \"caption\": \"Table 3: GRM hyperparameters.\", \"page\": 11, \"index\": 1, \"width\": 494, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2605.27811v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the Generative Response Model (GRM) framework. (Left) GRM encodes state-action history via a causal transformer and predicts horizon-aggregate response curves (traffic 𝐼𝑡 :𝑇 , cost ̂̄𝐶𝑡 :𝑇 (·), value ̂̄𝑉 𝑡 :𝑇 (·)). Training uses future-sampled supervision, where for each anchor tick 𝑡 we sample future ticks 𝑘 ∼ U{𝑡, . . . ,𝑇 } from offline data and fit curves to realized outcomes. (Right) The min-pacing controller solves two 1D root equations for budget and CPA constraints, then executes 𝛼𝑡 = min{𝛼𝐵, 𝛼𝐶 }.\", \"page\": 3, \"index\": 2, \"width\": 1011, \"height\": 254}]"
motivation: 现有基于控制、强化学习或生成式的方法存在反应滞后、约束模糊或分布偏移等问题，需要更有效的约束处理方式。
method: 提出生成式响应模型（GRM），联合预测未来流量与聚合成本/价值曲线，然后通过一维寻根实现精确约束控制。
result: 在AuctionNet数据集上，GRM相比基线方法提升了约束稳定性与整体评分。
conclusion: GRM通过预测响应而非直接行动，结合轻量控制器，能有效应对非平稳环境，实现更优的自动出价。
---

## 摘要
自动出价系统旨在在预算约束和每次获取成本等比率目标下，长期最大化广告主价值，然而未来的流量和拍卖动态是非平稳且不确定的。现有方法面临鲜明的局限性：基于控制的调速方法对偏差做出反应但无法预见未来条件，而强化学习和生成方法将约束融入奖励信号，模糊了违规行为并在分布偏移下性能下降。我们通过生成式响应模型（GRM）将学习目标从动作转移到响应，这是一种基于历史条件的序列模型，联合预测未来流量以及作为单一竞价倍率函数的时域聚合成本/价值曲线。我们证明，在温和的单调性条件下，相对于完全逐拍控制的最优性差距受逐拍边际价值-成本离散度的限制。给定预测的响应，一个轻量级解析控制器通过一维求根步骤强制执行每个活动约束。我们证明该控制器对于单倍率问题是最优的，并在预测误差方面限制了滚动时域重新规划下的约束违反。在AuctionNet上的实验表明，与现有基线相比，GRM提高了约束稳定性和总体得分。

## Abstract
Auto-bidding systems aim to maximize advertiser value over long horizons under budget constraints and ratio targets such as cost-per-acquisition, yet future traffic and auction dynamics are non-stationary and uncertain. Existing approaches face distinct limitations: control-based pacing reacts to deviations but cannot anticipate future conditions, while RL and generative methods fold constraints into reward signals, obscuring violations and degrading under distribution shift. We shift the learning target from actions to responses with the Generative Response Model (GRM), a history-conditioned sequence model that jointly predicts future traffic volume and horizon-aggregate cost/value curves as functions of a single bid multiplier. We show that under mild monotonicity conditions, the optimality gap relative to full per-tick control is bounded by the dispersion of per-tick marginal value-per-cost. Given predicted responses, a lightweight analytic controller enforces each active constraint via a 1D root-finding step. We prove this controller is exact for the single-multiplier problem and bound constraint violations under receding-horizon replanning in terms of prediction error. Experiments on AuctionNet show that GRM improves constraint stability and overall score compared to existing baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：在线广告自动出价系统需要在长期预算约束和效率目标（如每次获取成本 CPA 或广告支出回报率 ROAS）下最大化广告主价值。然而，未来流量和拍卖环境是非平稳且不确定的，这给决策带来巨大挑战。
- **现有方法的局限性**：
  - **基于控制的调价方法**（如 PID、FTRL）：根据实际偏差反应式调整，但无法预见未来流量或竞争变化，容易滞后或震荡。
  - **强化学习（RL）方法**（如 CQL、IQL、DT）：通常将约束融入奖励信号，导致约束违反难以诊断，且在分布偏移下性能显著下降。
  - **生成式序列模型**（如 Decision Transformer）：通过返回-出行（RTG）隐式编码约束，缺乏显式约束处理机制，需要推理时指定 RTG，长期不确定性大。
- **核心问题**：如何在不依赖大量在线探索、且能显式处理长期约束的前提下，在非平稳环境中稳定地进行自动出价？

### 2. 论文提出的方法论：核心思想、关键技术细节、算法流程

- **核心思想**：**将学习目标从“动作”转变为“响应”**——预测环境对竞价乘数的响应曲线，而不是直接学习最优出价动作；然后基于预测曲线通过轻量解析控制器求解满足约束的乘数。
- **关键技术细节**：
  1. **生成式响应模型（GRM）**：一个因果序列模型（2层 Transformer），以历史上下文（特征、乘数序列、累积花费/价值）为条件，输出未来时域上的聚合响应包：
     - 剩余流量预测 `\hat{I}_{t:T}`；
     - 时域聚合成本曲线 `\bar{C}_{t:T}(\alpha)` 和价值曲线 `\bar{V}_{t:T}(\alpha)`，作为竞价乘数 `\alpha` 的函数。
  2. **曲线参数化**：采用“对数-正态CDF”形式（log-sigmoid），满足边界条件（`\alpha=0`无花费，`\alpha→∞`饱和），且保证单调性、平滑性，输出仅需 7 个参数（流量 + 6个曲线参数）。
  3. **训练方法——未来采样监督**：从离线数据中，对每个锚点时刻 `t`，采样多个未来时刻 `k`，用该时刻的实际观测（花费、价值）监督预测曲线在该乘数下的取值，并通过流量加权损失函数拟合。
  4. **轻量解析控制器（Min-Pacing Control）**：给定 GRM 预测，控制器执行两个一维求根（二分法）：
     - 预算乘数 `\alpha_B`：满足 `\hat{C}_{t:T}(\alpha_B) = B_t`（剩余预算）；
     - CPA 乘数 `\alpha_C`：满足 `\hat{C}_{t:T}(\alpha_C) - \tau \hat{V}_{t:T}(\alpha_C) = \Delta_t`（CPA 松弛）；
     - 最终执行 `\alpha_t = \min\{\alpha_B, \alpha_C\}`。
  5. **滚动时域重规划**：每个时刻根据最新历史重新预测并求解。
- **理论保证**：
  - 单乘数近似间隙受“效率离散度”控制（Theorem 5.2）：当各时刻边际价值-成本比波动较小时，单乘数策略接近最优。
  - Min-Pacing 对单乘数问题是最优的（Theorem 5.3）。
  - 约束违反程度与预测误差成线性关系（Theorem 5.4），并受滚动时域的自校正影响，流量误差仅贡献 `O(\log T)` 项。

### 3. 实验设计

- **数据集与环境**：使用 **AuctionNet**（NeurIPS 2024 自动出价挑战赛模拟器），包含多个交付周期（P7–P27），每个周期 48 个时刻、超过 50 万次竞价机会。日志包含竞价记录、预测转化价值、成本、结果等。
- **训练/评估划分**：所有模型在周期 P7–P13 上训练，在 P14–P20 上评估（共 7 个测试周期）。
- **对比基线**（共 8 种）：
  - 行为克隆（BC）
  - 保守 Q 学习（CQL）
  - 隐式 Q 学习（IQL）
  - 决策变换器（DT）
  - DiffBid（条件扩散模型）
  - EBaReT（专家引导奖励变换器）
  - **GRM-short**（单时刻变体，仅预测当前时刻响应）
  - **FTRL**（仅用于分布偏移实验的控制方法）
- **评估指标**：官方 AuctionNet 评分 = `score = p(cpa; d) * Σ Val_t`，其中 `p` 为 CPA 违反惩罚（β=2）。在分布偏移实验中同时报告评分降幅百分比。
- **实验类型**：
  1. **主性能对比**（Table 1）：7 个周期上的平均评分及标准差。
  2. **分布偏移鲁棒性**（Figure 2）：两种场景——竞争激增（竞对预算×1.1）和 CPA 收紧（目标 CPA×0.8），比较 GRM、DT 和 FTRL。
  3. **预测质量与性能关联**（Figure 3）：不同训练 checkpoint 的验证损失与测试评分相关性（r= −0.78）。
  4. **曲线函数形式消融**（Table 2）：比较线性、分段线性、Sigmoid、单调 MLP 与对数 Sigmoid。
  5. **超参数敏感性**（Table 5）：流量损失权重 `λ_I` 的影响。
  6. **各时域阶段预测质量**（Table 4）：早/中/晚期 MSE 与 MAPE。

### 4. 资源与算力

- **模型参数量**：约 85 万（∼850K），包含 2 层因果 Transformer（4个注意力头，128维隐藏层），2层 MLP 曲线解码器（64隐藏）。
- **训练配置**：AdamW 优化器（学习率 1e-3，权重衰减 1e-5），batch size 64，每个锚点采样 8 个未来时刻。
- **GPU 信息**：论文未明确说明 GPU 型号、数量、训练时长。仅提到使用单个消费级 GPU 即可完成训练（从参数量和 batch size 推测，如 2080Ti 或 A100 可能在数小时内收敛）。

### 5. 实验数量与充分性

- **实验数量**：主要表 1 涵盖 7 个测试周期 × 7 种方法（不含变体）共 49 个评分点；分布偏移实验 2 个场景 × 3 种方法；消融实验 4 种曲线形式；超参数实验 3 个值；预测质量分析涉及 10 个 checkpoint。总体上实验规模适中，覆盖主要场景。
- **充分性与客观性**：
  - 使用了公开基准（AuctionNet），所有方法在同一模拟器上评估，公平性较好。
  - 基线覆盖了主流离线 RL、生成式序列模型和工业控制方法。
  - 分布偏移实验设置了两种实际相关场景（竞争和约束变化），验证鲁棒性。
  - 消融实验和预测质量分析直接支持论文核心论点（预测误差→性能下降）。
  - 但实验仅在单一模拟环境（AuctionNet）上进行，缺少真实在线平台结果或更多不同设定的模拟器验证，**泛化性证据不足**。

### 6. 论文的主要结论与发现

1. **GRM 在主要性能上优于基线**：在 AuctionNet 测试集上平均评分 33.88，比最强基线 EBaReT（31.43）提升 7.8%，且方差处于合理水平。
2. **GRM 在分布偏移下更鲁棒**：竞争激增和 CPA 收紧场景中，GRM 评分降幅（7.2% / 5.0%）远小于 DT（22.6% / 13.9%）和 FTRL（9.0% / 6.9%），且约束违反率增加更少。
3. **预测质量直接影响性能**：验证损失与测试评分呈显著负相关（r = −0.78），支持理论定理 5.4。
4. **对数 Sigmoid 曲线形式最优**：比其他参数化（线性、分段线性、Sigmoid、MLP）表现更好，能捕捉递减边际收益。
5. **单时刻预测（GRM-short）性能下降**：表明时域聚合预测对于长期预算调节至关重要。

### 7. 优点

- **方法论创新**：将学习从“预测最优动作”转为“预测环境响应”，使得约束满足解析化、透明化，摆脱奖励塑造或后验搜索，直接避免分布偏移下的动作偏移。
- **理论坚实**：提供单乘数近似间隙、控制器最优性以及预测误差→约束违限的量化上界，使可解释性和保证性优于大多数端到端 RL 方法。
- **轻量高效**：控制器仅需两个一维求根，模型参数量小（850K），适合生产部署。
- **鲁棒性强**：在两种分布偏移下优于对比方法，归功于滚动时域重预测和解析约束求解。
- **训练简洁**：使用离线日志中的未来采样监督，无需显式探索或反事实评估，训练稳定。

### 8. 不足与局限

- **单乘数假设的局限性**：每时刻仅用单一乘数限制策略灵活性。当每时刻边际效率离散度大时（效率离散度 σ² 大），单乘数近似会产生不可忽略的最优性差距（Theorem 5.2）。论文未评估实际 σ² 大小，也未与多乘数优化方法比较。
- **实验环境单一**：仅在 AuctionNet 模拟器上验证，缺乏真实在线 A/B 测试或不同广告市场（如搜索广告、社交广告）的实证。模拟器的真实度可能影响泛化结论。
- **对历史数据覆盖的依赖**：GRM 依赖离线日志中乘数的多样性。如果日志中乘数范围或轨迹模式缺乏覆盖，学习到的曲线可能无法泛化到未见状态。论文仅简要提及“生产系统自然产生多样性”，未提供定量覆盖分析。
- **约束违规的延迟纠正**：由于重新规划是每时刻执行，但违反一旦发生只能通过未来减少竞价弥补，在预算刚性约束下可能仍有实际损失。论文未讨论零容忍场景下的处理。
- **缺少与在线学习/自适应控制方法的详细对比**：仅与 FTRL 在分布偏移下比较，而在主实验中没有包含更多的控制方法（如 PID 变体、在线凸优化方法）。
- **超参数调优不充分**：曲线参数化消融中最好与次好仅差 1.1%，但未在更大规模上验证泛化。流量损失权重 λ_I 只测试了三个值，优化空间可能更大。

（完）
