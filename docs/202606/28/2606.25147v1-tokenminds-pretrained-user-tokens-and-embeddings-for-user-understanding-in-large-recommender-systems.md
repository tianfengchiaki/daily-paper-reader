---
title: "TokenMinds: Pretrained User Tokens and Embeddings for User Understanding in Large Recommender Systems"
title_zh: "TokenMinds: 面向大型推荐系统中用户理解的预训练用户令牌与嵌入"
authors: "Qingyun Liu, Bo Yan, Yang Liu, Yuji Roh, Ekansh Sharma, Likang Yin, Emma Olowo, Min-hsuan Tsai, Yuxuan Li, Diego Uribe, Saksham Aggarwal, Siqi Wu, Yuan Hao, Vikas Kedigehalli, Lukasz Heldt, Lichan Hong, Li Wei, Xinyang Yi"
date: 2026-06-23
pdf: "https://arxiv.org/pdf/2606.25147v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 大型推荐系统中的用户标记和嵌入
tldr: TokenMinds提出一种工业级系统，通过编码器-解码器架构生成基于语义ID的离散用户令牌和密集嵌入，解决了传统用户表示维度固定、难以捕获序列行为的问题。在YouTube十亿用户规模上验证，该双输出设计提升了排序性能，且共享词汇表支持跨场景建模，降低训练成本。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25147v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 2136, \"height\": 882}, {\"url\": \"assets/figures/arxiv/2606.25147v1/fig-002.webp\", \"caption\": \"\", \"page\": 4, \"index\": 2, \"width\": 1798, \"height\": 694}, {\"url\": \"assets/figures/arxiv/2606.25147v1/fig-003.webp\", \"caption\": \"\", \"page\": 4, \"index\": 3, \"width\": 960, \"height\": 312}, {\"url\": \"assets/figures/arxiv/2606.25147v1/fig-004.webp\", \"caption\": \"\", \"page\": 5, \"index\": 4, \"width\": 940, \"height\": 399}, {\"url\": \"assets/figures/arxiv/2606.25147v1/fig-005.webp\", \"caption\": \"\", \"page\": 6, \"index\": 5, \"width\": 3549, \"height\": 811}, {\"url\": \"assets/figures/arxiv/2606.25147v1/fig-006.webp\", \"caption\": \"\", \"page\": 8, \"index\": 6, \"width\": 3585, \"height\": 850}, {\"url\": \"assets/figures/arxiv/2606.25147v1/fig-007.webp\", \"caption\": \"\", \"page\": 8, \"index\": 7, \"width\": 3585, \"height\": 848}]"
motivation: 现有用户建模依赖固定维度的密集嵌入，表示能力受限；基于LLM的文本令牌无法捕获序列行为且难与物品对齐，而基于SID的物品令牌化已有效，但用户侧离散表示仍缺失。
method: 从预训练LLM改编的编码器-解码器架构同时生成离散的SID用户令牌和密集用户嵌入，并通过异步基础设施解耦表示生成与下游评分。
result: 在YouTube全量用户（数十亿）上的离线实验和在线发布表明，该双输出方法在不同排序系统中提供互补价值，证实了SID用户令牌的工业可行性。
conclusion: 基于SID的用户离散表示与密集嵌入互补，能提升推荐系统性能，且共享词汇表支持高效跨场景建模。
---

## 摘要
工业推荐系统中的用户建模通常产生稠密嵌入，这些嵌入受限于固定维度向量的表征约束。一种新兴的离散用户表示方法——使用大语言模型生成基于文本的用户令牌——捕捉主题共现而非深层序列行为动态，并产生难以关联到物品属性的输出。与此同时，基于语义ID（SID）的物品分词已被证明能有效改进生成式推荐中的泛化能力，然而基于离散SID的用户表示仍基本未被探索。我们提出TokenMinds，一个工业级系统，将PLUM框架从物品检索扩展到用户建模，通过从预训练大语言模型改编的编码器-解码器架构生成基于SID的离散用户令牌和稠密用户嵌入。这种双输出设计提供了离散、语义化用户表示的互补优势，同时保持与依赖稠密嵌入的现有下游模型的兼容性。此外，共享的SID词汇表自然扩展到跨场景建模：通过将长视频和短视频行为统一到一个模型中，我们大幅降低了训练和服务成本。我们通过广泛的离线实验和在多个YouTube界面上的实时发布验证了TokenMinds，通过将表示生成与下游评分解耦的异步基础设施服务于全用户流量（数十亿用户）。以排序作为主要下游用例，我们的结果证实了基于SID的用户令牌在工业规模上的实际可行性，并表明令牌和稠密嵌入在不同生产排序系统中提供互补价值。

## Abstract
User modeling in industrial recommender systems typically produces dense embeddings, which suffer from representational constraints inherent to fixed-dimensional vectors. An emerging alternative for discrete user representation -- using LLMs to generate text-based user tokens -- captures topical co-occurrences rather than deep sequential behavior dynamics and produces outputs that are difficult to ground to item attributes. Meanwhile, Semantic ID (SID) based item tokenization has proven effective for improving generalization in generative recommendation, yet discrete SID-based representations for users remain largely unexplored. We propose TokenMinds, an industrial-scale system that extends the PLUM framework from item retrieval to user modeling, generating both discrete SID-based user tokens and dense user embeddings via an encoder-decoder architecture adapted from pre-trained LLMs. This dual-output design provides the complementary benefits of discrete, semantically grounded user representations while maintaining compatibility with existing downstream models that rely on dense embeddings. Additionally, the shared SID vocabulary naturally extends to cross-scenario modeling: by unifying long-form and short-form video behaviors into a single model, we substantially reduce training and serving costs. We validate TokenMinds through extensive offline experiments and live launches on multiple YouTube surfaces, served on full user traffic (billions of users) via an asynchronous infrastructure that decouples representation generation from downstream scoring. Focusing on ranking as the primary downstream use case, our results confirm the practical viability of SID-based user tokens at industrial scale and demonstrate that tokens and dense embeddings provide complementary value across different production ranking systems.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：工业推荐系统中，用户建模通常采用稠密嵌入（dense embeddings）作为用户表示，但其受限于固定维度，难以捕捉用户全部兴趣的细微差异。近年来，利用大语言模型（LLM）生成基于文本的用户令牌（text-based user tokens）成为一种替代方案，但这种方法仅能捕捉主题共现而非深层序列行为动态，且文本输出难以与物品属性对齐。与此同时，基于语义ID（Semantic ID, SID）的物品分词在生成式推荐中已证明能提升泛化能力，但基于SID的用户离散表示仍未被探索。
- **核心问题**：如何生成既能保持语义可解释性、又能与现有稠密嵌入下游模型兼容的用户表示，同时实现跨场景的高效建模？
- **整体含义**：论文提出TokenMinds系统，通过预训练LLM改编的编码器-解码器架构，同时生成离散的SID用户令牌和稠密用户嵌入，以互补方式提升推荐质量，并利用共享SID词汇表实现长/短视频（LFV/SFV）的统一建模，显著降低训练和服务成本。

## 2. 论文提出的方法论

### 核心思想
- 扩展PLUM框架从物品检索到用户建模，采用**编码器-解码器**架构，输入用户行为序列（包括观看历史、搜索查询、交互特征），编码器输出稠密嵌入，解码器自回归生成SID令牌。
- **双输出设计**：稠密嵌入兼容现有下游模型；离散SID令牌提供可解释、语义化的用户兴趣表示，两者互补。
- **跨场景建模**：通过条件令牌（如`<LFV>`、`<SFV>`）区分场景，并引入**多上下文解码**（multi-context decoding）：单次编码器前向传播后，解码器根据不同场景前缀并行生成场景特定令牌，避免冗余推理。

### 关键技术细节
- **输入表示**：每个视频用前缀长度为L（L=4）的SID表示（全长为8），非SID特征（如观看时长、设备）通过硬令牌或软令牌（MLP投影）编码。搜索查询用`<Search>`令牌分隔，与观看历史按时间交错。
- **训练目标**：使用**前瞻采样**（look-ahead sampling）从未来24小时窗口内随机选择N个目标（N=15），避免过拟合于下一个观看。损失函数为带奖励加权的负对数似然（式1），但实际采用按奖励权重采样后等权处理。
- **初始化**：从CPT（continued pre-training）检查点初始化，该检查点已通过PLUM的CPT将SID词汇与LLM原有知识对齐。
- **跨场景统一训练**：在输入序列中为每个观看前添加条件令牌，统一预测LFV和SFV目标。条件令牌不参与损失计算。

### 公式与算法流程（文字说明）
- 输入：用户历史 `[W_1,...,W_t]`，每个W包含条件令牌+前缀SID+软特征令牌。
- 编码器：Transformer编码器处理整个序列，输出上下文表征。
- 解码器：通过交叉注意力关注编码器输出，自回归生成每个目标视频的SID序列。
- 训练时：计算N个目标SID的交叉熵损失，梯度通过解码器传递到编码器。
- 服务时：提取编码器输出经池化得到稠密嵌入；解码器使用束搜索生成B个SID序列（B=40，其中LFV和SFV各20），每个序列截断到前缀长度L作为用户令牌。

## 3. 实验设计

### 数据集 / 场景
- **场景**：YouTube的长视频（LFV）和短视频（SFV）推荐，两个场景具有不同的视频ID空间和消费模式。
- **数据规模**：训练使用最新日数据，处理数百万样本/天。用户历史长度最多1200次观看（交错10条搜索查询），输入序列最大1024令牌。
- **离线评估**：在随机采样的7天训练、第8天测试的加速协议上评估Recall@10（预测正确SID前缀）。
- **在线评估**：在多个YouTube生产表面上进行7天A/B实验。

### Benchmark 与对比方法
- **离线对比**：对训练目标进行消融（如移除多目标、移除前瞻窗口、移除SID截断）；对初始化策略（CPT vs 预训练Gemini vs 随机初始化）；有无搜索查询。
- **在线对比**：
  - 令牌适应：前缀嵌入映射（EM）vs 可学习嵌入（LE，Unigram）。
  - 用户表示：仅稠密嵌入 vs 仅离散令牌 vs 两者联合。
  - 跨场景：统一模型 vs 两个独立模型（LFV-only和SFV-only）。
- **消融实验**：还包括模型架构变体（Balanced MoE、Balanced Dense、Unbalanced Dense）、输入长度缩放、批量大小缩放等。

### 主要结果
- **离线令牌准确率**：TokenMinds在Session Recall和Cold-Start Recall上分别达0.291和0.210，优于各消融变体。
- **在线排名效果**：
  - SFV场景：联合嵌入+令牌获+0.11%活跃用户和+0.62%满意参与度。
  - LFV场景：联合获+0.02%和+0.08%（略低于SFV，但仍有效）。
- **跨场景**：统一模型节省50%训练计算、31%服务计算，同时新鲜度指标显著提升（SFV新鲜参与度+0.33%，LFV+0.19%），核心指标持平。

## 4. 资源与算力

- **模型参数**：基于Gemini V1.5，370M参数MoE编码器 + 370M参数密集解码器。
- **训练框架**：内部JAX和Pathways框架（Google生态），连续训练，每天处理百万级样本。
- **服务基础设施**：异步处理，单用户生成耗时约339ms，缓存命中率96.4%，每秒处理144万读取请求。
- **算力明确说明**：论文未明确给出使用的GPU/TPU型号、数量或总训练时长。仅提及“bounded by the availability of pre-trained Gemini checkpoints”，并汇报了跨场景训练计算减少50%（相对于两个独立模型）。因此，具体算力细节未公开。

## 5. 实验数量与充分性

- **实验数量**：涵盖了广泛的消融和对比实验：
  - 3种训练目标消融（多目标、前瞻窗口、SID截断）
  - 3种初始化策略 × 2种搜索配置（共6种）
  - 在线令牌适应（2种方法）
  - 在线下游表示（3种组合×2个表面）
  - 在线跨场景（统一 vs 分开，质量+效率）
  - 模型架构缩放（3种变体）
  - 输入长度缩放（多个长度）
  - 批量大小缩放（3种）
- **充分性**：实验设计比较系统，覆盖了设计决策的关键维度，且均在实际工业流量上验证。在线实验持续7天，统计显著性标注（95%置信度）。但方法仅在YouTube内部验证，未在公开基准或不同领域测试，外部可重复性受限。

## 6. 论文的主要结论与发现

1. **SID用户令牌可行且有效**：在工业规模排序系统中，SID离散令牌可提供可衡量的改进（+0.04%~+0.62%指标提升）。
2. **稠密与离散互补**：联合使用嵌入和令牌效果优于单独使用，证明双输出设计的互补价值。
3. **CPT初始化至关重要**：从PLUM的CPT检查点初始化显著优于预训练Gemini或随机初始化，且搜索查询信号进一步放大收益。
4. **跨场景统一建模高效**：单模型统一LFV和SFV，通过多上下文解码节省约50%训练和31%服务计算，且新鲜度指标提升，核心指标不降。
5. **前瞻采样和SID截断是训练关键**：移除这些会导致Recall下降10%~17%。
6. **可学习嵌入优于静态映射**：下游模型中LE比EM效果更好。

## 7. 优点

- **创新性**：首次将SID离散表示从物品扩展到用户，并提出双输出架构，解决了稠密嵌入的局限。
- **实用性**：全部在YouTube十亿用户规模上部署，验证了工业可行性。
- **效率设计**：多上下文解码、异步服务基础设施有效降低计算开销。
- **跨场景自然扩展**：利用共享SID词汇表，无需额外对齐。
- **实验充分**：离线消融+在线A/B，涵盖多表面、多指标，且标注统计显著性。

## 8. 不足与局限

- **通用性受限**：所有实验在YouTube内部数据上完成，未在公开基准或不同领域（如电商、新闻）验证，可能不具有广泛可迁移性。
- **未公开细节**：模型架构、训练超参数、算力资源等细节有限，难以复现。
- **下游聚焦**：论文仅报告了排序作为下游任务的结果，检索等其他任务表现未知。
- **复杂性**：方法依赖于RQ-VAE预训练SID、CPT、编码器-解码器LLM等复杂组件，对中小规模系统可能过于沉重。
- **潜在偏差**：在线指标（活跃用户、满意参与度）是YouTube内部定义，可能存在特定优化偏向。
- **冷启动问题**：虽然提到了冷启动Recall，但对于全新用户或物品的极端冷启动场景未深入分析。

（完）
