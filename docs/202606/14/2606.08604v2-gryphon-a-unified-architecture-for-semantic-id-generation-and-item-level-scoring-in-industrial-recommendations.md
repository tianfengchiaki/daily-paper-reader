---
title: "Gryphon: A Unified Architecture for Semantic-ID Generation and Item-Level Scoring in Industrial Recommendations"
title_zh: "Gryphon: 一种用于工业推荐中语义ID生成和物品级评分的统一架构"
authors: "Daria Tikhonovich, Oleg Sorokin, Vladislav Dodonov, Mariia Ulianova, Ilya Murzin"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08604v2"
tags: ["query:q1"]
score: 9.0
evidence: 工业推荐中生成式检索和物品评分的统一架构
tldr: "生成式检索（GR）通过语义ID（SID）进行候选生成，但存在序列似然度校准不良和多个物品共享同一ID导致评分相同的问题。本文提出Gryphon，一种编码器-解码器架构，在SID生成基础上联合训练物品级评分组件，重用编码器用户表示对物品直接重评分。在工业音乐服务中，Gryphon的Recall@1000比原始GR高3.7%，比碰撞解决GR高2.5%，且部署后替换了超过15个候选生成器，系统简化且性能无显著变化。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08604v2/fig-001.webp\", \"caption\": \"Figure 1: Gryphon inference: beam search generates candidate SIDs, which are resolved to all matching items and re-scoredwith ILSM,while beam likelihood is discarded from ranking\", \"page\": 2, \"index\": 1, \"width\": 531, \"height\": 373}]"
motivation: 现有GR中，解码器的束搜索优化序列似然度而非物品相关性，且ID碰撞导致多个物品获得相同评分，需要引入物品级评分来解决这些问题。
method: Gryphon在编码器-解码器架构中增加联合训练的评分器，重用单次前向传播的用户表示，将解码出的SID解析为物品后直接计算物品级评分，替代累积似然度排序。
result: "在工业音乐服务中，Gryphon的Recall@1000比原始GR高3.7%、比碰撞解决GR高2.5%；其物品级排序比同候选集的束似然排序高4.2%；A/B测试中总收听时间无显著变化（+0.25%），替换了含15+候选生成器的管道。"
conclusion: Gryphon通过物品级评分有效消除了序列似然度偏差和ID碰撞问题，在保持性能的同时大幅简化了推荐系统候选生成流程。
---

## 摘要
生成式检索已成为候选生成的一种可扩展方法：每个物品被分配一个称为语义ID的短层次化token序列，下一个物品的语义ID通过自回归解码得到。一个实际限制是解码器的束搜索优化的是token序列的似然，而不是底层物品的相关性。当序列似然因束搜索错误累积而校准不佳，以及多个物品折叠到单个语义ID上并获得相同分数时，这些目标会发生偏离。我们提出Gryphon，一种编码器-解码器生成式推荐架构，它在语义ID生成的同时添加了一个联合训练的物品级评分组件，重用编码器在单次前向传播中计算的用户表示。Gryphon不是通过累积token似然对语义ID进行排序，而是将每个生成的语义ID解析为其具体物品，并直接对这些物品重新评分，从而避开了校准不佳的序列分数，并分离了碰撞到同一标识符上的物品。在一个工业音乐服务上，使用基于下一物品预测目标训练的物品级评分，Gryphon在物品级Recall@1000上取得了最高值，在参数数量和延迟相近的情况下，比最强基线（普通GR提高+3.7%，碰撞解决GR提高+2.5%）表现更好。Gryphon的物品级排序也超越了同一候选集的束似然排序（+4.2%增益），证明了物品级评分在GR中的优势。在为期7天的A/B测试中，Gryphon作为唯一的候选来源部署，总收听时间无统计学显著变化（+0.25%），同时取代了包含15个以上候选生成器和单独预排序阶段的管道，大大简化了候选生成系统。

## Abstract
Generative retrieval (GR) has become a scalable approach to candidate generation: each item is assigned a short hierarchical token sequence called a Semantic ID (SID), and the next item's SID is decoded autoregressively. A practical limitation is that the decoder's beam search optimizes the likelihood of token sequences, not the relevance of the underlying items. These objectives diverge when sequence likelihood is poorly calibrated due to beam search error accumulation, and when several items collapse onto a single SID and receive identical scores. We introduce Gryphon, an encoder-decoder generative recommendation architecture that adds a jointly trained item-level scoring component alongside SID generation, reusing the encoder's user representation computed in a single forward pass. Instead of ranking SIDs by accumulated token likelihood, Gryphon resolves each generated SID to its concrete items and re-scores those items directly, which sidesteps miscalibrated sequence scores and separates items that collide on the same identifier. On an industrial music service, with item-level scoring trained under a next-item-prediction objective, Gryphon attains the highest item-level Recall@1000, above the strongest baselines (+3.7% over vanilla GR and +2.5% over collision-resolved GR) at comparable parameter count and latency. Gryphon's item-level ranking also surpasses its beam-likelihood ranking of the same candidates (+4.2% gain), demonstrating the benefit of item-level scoring in GR. Deployed as the sole candidate source in a 7-day A/B test, Gryphon produced no statistically significant change in total listening time (+0.25%) while replacing a pipeline of more than 15 candidate generators and a separate preranking stage, substantially simplifying the candidate-generation system.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：生成式检索（GR）通过语义ID（SID）进行候选生成，但存在两个结构性问题：
  - **序列似然度校准不良**：束搜索依赖模型自身生成的前缀，早期token误差累积导致相关物品的SID似然度不准。
  - **语义ID碰撞**：多个物品共享同一SID时，束似然会给它们分配相同分数，无法区分相关性。
- **整体含义**：GR在工业推荐中本质上需要物品级排序，但传统方法错误地将SID序列似然作为物品相关性信号，导致评分与推荐目标不匹配。Gryphon通过解耦SID生成与物品评分，直接解决这一根本性矛盾。

## 2. 方法论：核心思想、关键技术细节与算法流程

- **核心思想**：采用共享编码器的编码器-解码器架构，在SID生成的同时联合训练一个物品级评分模块（ILSM），推理时束搜索仅用于生成候选SID集合，最终排序完全由ILSM完成，丢弃束似然。
- **关键技术细节**：
  - **SID生成**：使用残差K-Means（RQ-VAE）将每个物品量化为L个token的SID序列。解码器自回归预测下一个物品的SID。
  - **物品级评分模块（ILSM）**：对每个候选物品，将其SID和物品特征（如item-id hash）通过物品塔（item tower）得到物品查询嵌入 \( e_i \)，然后与共享编码器输出的用户状态 \( E_u \) 通过交叉注意力+MLP计算相关性分数 \( r_\phi(u,i) \)。
  - **训练目标**：联合优化生成损失 \( \mathcal{L}_{\text{gen}} \)（SID序列负对数似然）和物品级预测损失 \( \mathcal{L}_{\text{NIP}} \)（基于采样softmax的下一物品预测），总损失 \( \mathcal{L} = \mathcal{L}_{\text{gen}} + \lambda \mathcal{L}_{\text{NIP}} \)，其中 \( \lambda = 1 \)。
- **推理流程**：
  1. 用户历史序列通过共享编码器，计算用户状态 \( E_u \)。
  2. 解码器使用束搜索（beam size=2048）生成top-K个SID候选集合 \( \mathcal{B}_u \)。
  3. 将每个SID解析为所有匹配物品（碰撞组 \( \mathcal{C}_\sigma \)）。
  4. ILSM对解析出的所有物品计算分数 \( r_\phi(u,i) \)，取top-N作为最终候选。
- **与现有方法的关键区别**：不依赖SID序列似然作为排序依据，而是通过ILSM直接重评分物品，从而消除校准误差和ID碰撞的影响。

## 3. 实验设计

- **数据集**：来自大型音乐推荐平台一周的真实用户日志，包含数千万活跃用户和物品。
- **基准（Benchmark）**：对比以下三种方法：
  - **ARGUS**：部署的两塔Transformer候选生成器，使用采样softmax进行下一物品预测（强生产基线）。
  - **Vanilla GR**：标准生成式检索，束似然排序，存在SID碰撞。
  - **Vanilla GR Resolved**：在SID末尾附加唯一token解决碰撞（参考文献[11]方法，但作者认为不实用）。
- **实验设置**：
  - 所有模型使用隐层大小1024、用户序列长度512、束大小2048。
  - 超参数独立调优：SID码本数量1~4，码本大小1024~32000，编码器/解码器层数比例。
  - ILSM仅使用item-id特征（无其他物品特征），以保证与GR基线公平对比。
  - 离线指标：Recall@10和Recall@1000。

## 4. 资源与算力

- **未明确说明**：论文未提及训练所用GPU型号、数量、训练时长等具体算力信息。仅提及所有模型参数数量和推理时间相差小于1%，且使用Adam优化器、线性预热+线性衰减学习率。

## 5. 实验数量与充分性

- **离线实验**：包括与三个基线的对比（表1）、消融实验（表2：同一候选池下三种评分方式对比）。
- **在线A/B测试**：为期7天，4%用户分配到实验组，替换完整生产管线。
- **充分性评估**：
  - ✅ 消融实验有效隔离了ILSM带来的增益（物品级评分超越束似然天花板4.2%）。
  - ✅ 在线实验验证了实际部署效果，指标涵盖总收听时间、活跃用户比、未完成曲目。
  - ⚠️ 未进行多随机种子的正式显著性检验（仅初步估算变异±0.003，据此判断差异显著）。
  - ⚠️ 仅在一个平台（音乐推荐）测试，缺乏跨域或跨数据集的验证，泛化性存疑。

## 6. 主要结论与发现

- **离线结果**：Gryphon的Recall@1000达到0.8552，比Vanilla GR（0.8245）高3.7%，比Vanilla GR Resolved（0.8343）高2.5%。
- **消融关键发现**：同一候选池下，ILSM重评分（0.8552）显著高于束似然排序（0.8404），证明序列似然校准不良是限制因素，而非候选召回本身。
- **在线A/B测试**：作为唯一候选来源，总收听时间变化+0.25%（无统计显著），同时活跃用户比+0.43%（p<0.001），未完成曲目-1.3%（p<0.001），且将候选数从10,000降至1,000，去除了预排序阶段，简化了系统。

## 7. 优点

- **方法创新**：首次在GR中明确分离SID生成和物品级评分，直接解决序列似然校准和ID碰撞这两个核心缺陷。
- **架构高效**：共享编码器设计使得ILSM复用单次前向传播的用户表示，新增计算量极小（<1%参数和延迟）。
- **实验设计严谨**：消融实验隔离了ILSM效果，在线A/B测试验证了实用性和系统简化价值。
- **工业部署可行**：在真实音乐平台上替换了15+候选生成器和预排序，性能无显著下降，证明了方法的实际潜力。

## 8. 不足与局限

- **实验覆盖有限**：仅在一个音乐推荐平台测试，未在其他领域（如视频、电商）验证，泛化性不明。
- **ILSM训练目标简单**：当前仅使用下一物品预测（采样softmax），未探索多目标、蒸馏或长期价值优化等更丰富的目标，可能限制上限。
- **统计严谨性**：离线实验未进行多随机种子的正式显著性检验，仅依赖初步估计（±0.003），存在风险。
- **实用基线局限**：Vanilla GR Resolved因需要为每个碰撞项添加唯一token，导致动态目录下词汇表无限增长，作者认为不实用，但作为对比基线可能存在不公平（性能略低于Gryphon但差距不大）。
- **在线测试范围**：虽然替换了候选生成和预排序，但最终仍需要下游排序器（ranker），并未完全端到端化。
- **物品特征未利用**：ILSM为公平对比仅使用item-id，若加入更多内容特征可能获得更大提升，但未探索。

（完）
