---
title: "Bridging Short Videos and Live Streams: Reasoning-Guided Multimodal LLMs for Cross-Domain Representation Learning"
title_zh: 桥接短视频与直播：面向跨域表示学习的推理引导多模态大语言模型
authors: "Le Zhang, Xiaolan Zhu, Yuchen Wang, Shilong Kang, Jiaqi Xue, Xiaoyu Zhang, Xiang Chen, Yalong Guan, Xiangyu Wu, Shijun Wang, Lantao Hu, Kun Gai"
date: 2026-06-03
pdf: "https://arxiv.org/pdf/2606.04448v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 利用多模态LLM进行短视频与直播的跨域推荐
tldr: 针对直播推荐冷启动问题，提出推理引导的跨域表示学习框架RGCD-Rep，利用多模态大语言模型进行推理感知蒸馏和可迁移性引导的表示学习，实现从短视频到直播的用户兴趣迁移。在快手部署后，核心业务指标显著提升，服务超4亿用户。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.04448v1/fig-001.webp\", \"caption\": \"Figure 1: Overview of the proposed RGCD-Rep framework.\", \"page\": 4, \"index\": 1, \"width\": 1042, \"height\": 464}]"
motivation: 直播推荐面临冷启动问题，而短视频行为丰富，需要将用户兴趣从短视频迁移到直播以提升推荐效果。
method: 采用两阶段训练：先通过推理感知蒸馏将冻结教师MLLM的结构化知识蒸馏到轻量学生MLLM，再通过可迁移性引导的跨域表示学习分解为可迁移和域残差表示。
result: 离线实验和快手A/B测试表明，该方法在核心业务指标上取得显著提升，已全面部署服务超4亿用户。
conclusion: RGCD-Rep有效缓解了直播推荐冷启动，验证了多模态大语言模型在跨域推荐中的实用性。
---

## 摘要
随着直播服务的增长，许多平台提供短视频和直播以满足多样化需求。短视频承载着大量流量和丰富的行为信号，而直播作为核心转化场景，行为数据稀疏，冷启动问题严重。将用户兴趣从短视频迁移到直播推荐可以缓解这些问题。同时，短视频和直播是复杂的多模态项目，整合多模态信号可提升推荐性能。尽管多模态大语言模型（MLLMs）展现了强大的多模态理解与推理能力，但其在跨域推荐中的应用仍未充分探索。为此，我们提出推理引导的跨域表示学习（RGCD-Rep），这是一个从短视频到直播的跨域推荐推理引导框架。RGCD-Rep高效引入MLLM推理，并通过两阶段训练学习受行为协作引导的可迁移项目表示。首先，推理感知蒸馏使冻结的教师MLLM生成结构化的跨域推理知识，并将其蒸馏到轻量级学生MLLM中。其次，可迁移性引导的跨域表示学习将项目表示分解为可迁移表示和域残差表示。最终表示离线计算并集成到下游检索任务中，实现低成本的工业部署。大量离线实验证明了RGCD-Rep的优越性。在快手直播推荐系统部署后，A/B测试显示多个核心业务指标显著提升，证实了其在真实工业场景中的有效性和实用性。RGCD-Rep已全面部署，每天服务超过4亿用户。

## Abstract
As live streaming services grow, many platforms offer short videos and live streams to meet diverse needs. Short videos carry substantial traffic and rich behavior signals, whereas live streaming is a core conversion scenario with sparse behavior data, making cold start severe. Transferring user interests from short videos to live streaming recommendation can alleviate these issues. Meanwhile, short videos and live streams are complex multimodal items, and integrating multimodal signals improves recommendation performance. Although Multimodal Large Language Models (MLLMs) show strong multimodal understanding and reasoning, their application to cross-domain recommendation remains underexplored. To this end, we propose Reasoning-Guided Cross-Domain Representation Learning (RGCD-Rep), a reasoning-guided framework for cross-domain recommendation from short videos to live streams. RGCD-Rep introduces MLLM reasoning resource-efficiently and learns transferable item representations guided by behavioral collaboration via two-stage training. First, reasoning-aware distillation lets a frozen teacher MLLM generate structured cross-domain reasoning knowledge and distills it into a lightweight student MLLM. Second, transferability-guided cross-domain representation learning decomposes item representations into transferable and domain residual representations. The resulting representations are computed offline and integrated into downstream retrieval tasks, enabling low-cost industrial deployment. Extensive offline experiments demonstrate RGCD-Rep's superiority. After deployment in Kuaishou's live streaming recommendation system, A/B tests show significant gains across multiple core business metrics, confirming its effectiveness and practicality in real industrial scenarios. RGCD-Rep is fully deployed and serves over 400 million users daily.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：直播推荐面临严重的数据稀疏性和冷启动问题。短视频域承载着大量用户行为（点击、评论、点赞等），而直播域作为核心转化场景，其高价值行为（送礼、关注等）极为稀疏。如何将短视频中丰富的用户兴趣迁移到直播推荐，以缓解稀疏性问题，是跨域推荐（CDR）的核心挑战。
- **现有局限**：现有CDR方法主要依赖预先提取的模态特定表示，缺乏对多模态内容进行深度语义推理的能力，难以解释为何两个域的项目可以行为上可迁移。多模态大语言模型（MLLMs）具备强大的多模态理解与推理能力，但直接部署成本高昂，且其开放世界知识可能与推荐任务不相关，同时跨域用户兴趣并非总是一致，简单聚合可能导致负迁移。
- **本文目标**：提出RGCD-Rep，一种资源高效的推理引导跨域表示学习框架，首次将MLLM推理引入CDR，学习通用的可迁移项目表示，实现低成本工业部署。

## 2. 方法论：核心思想、关键技术细节

### 总体框架
- **两阶段训练**：
  - **阶段1：推理感知蒸馏**。冻结的教师MLLM（35B）对行为共现对（短视频+直播）生成结构化跨域推理知识，包括项目级多模态内容理解（垂直领域、关键主体、内容主题）和配对级迁移推理（三个语义维度的匹配标签）。将这些知识分解为独立项目级别的蒸馏数据集，训练轻量级学生MLLM（2B）。
  - **阶段2：可迁移性引导的跨域表示学习**。共享权重的学生MLLM编码器提取两个域的chorus token表示，再通过两个可学习的查询向量（可迁移查询和残差查询）进行加权聚合，得到可迁移表示 \( z^T_x \) 和残差表示 \( z^R_x \)。

### 关键技术点
- **行为协作对构建**：从用户日志中，在同一时间窗口内，同一用户对短视频和直播均表现出强兴趣的项目对作为训练数据，并计算行为共现强度和交互强度得到 \( b_i \)。
- **结构化推理生成**：教师MLLM输出三个语义维度的匹配标签 \( m^k_i \)（0/1），聚合为语义可迁移性分数 \( r_i \)。
- **学生MLLM**：采用Qwen2-VL-2B-Instruct，并插入16个可学习chorus tokens。
- **损失函数**：
  - 阶段1：标准下一个令牌预测损失 \( \mathcal{L}_{\text{NTP}} \)。
  - 阶段2：跨域对比学习损失 \( \mathcal{L}_{\text{con}} \)（InfoNCE）+ 可迁移路由损失 \( \mathcal{L}_{\text{route}} \)（基于行为与语义联合分数 \( q_i=b_i+r_i \) 将配对分为高纯度和低纯度，路由损失鼓励高纯度配对由可迁移空间解释，低纯度由残差空间解释）。总损失 \( \mathcal{L}_{\text{stage2}} = \mathcal{L}_{\text{con}} + \lambda_{\text{route}} \mathcal{L}_{\text{route}} \)。
- **部署方式**：学生MLLM离线生成所有项目的可迁移表示，存入特征系统，在线通过最近邻索引检索，作为新的检索通道。

## 3. 实验设计

### 数据集与场景
- **离线数据集**：快手平台连续一周的真实用户日志，随机采样109,523名同时使用短视频和直播的用户。训练集为前6天交互，测试集为第7天最后一次正交互的直播为预测目标。
- **在线测试**：部署在快手和快手极速版的直播推荐召回阶段，服务数亿用户。

### 基准方法
- **单域多模态方法**：FREEDOM、SMORE、AlphaRec。
- **跨域单模态方法**：UniSRec、VQ-Rec。
- **跨域多模态方法**：MISSRec、PMMRec、UniEmbedding。

### 评估指标
- 离线：HR@10、NDCG@10（召回任务）。
- 在线：曝光、点击、有效进房、关注等业务指标。

## 4. 资源与算力

- **硬件**：8张NVIDIA H800 GPU（140GB显存）。
- **框架**：PyTorch。
- **训练时间**：未明确给出具体时长，但提及阶段1样本60,000条，阶段2样本250,000条，LoRA秩32，批次大小分别为8和64，学习率1e-4。
- **推理成本**：学生模型为2B轻量级，离线生成所有项目表示，在线仅需索引检索，部署成本低。

## 5. 实验数量与充分性

### 实验组数
1. **主实验**（表2）：对比8种baseline，RGCD-Rep在HR@10和NDCG@10上分别领先最强基线UniEmbedding 17.59%和30.93%。
2. **消融实验**（表2）：去除阶段1、阶段2、冻结阶段2的MLLM骨干，共3组消融。
3. **长尾项目分析**（图2）：按出现频率分头部50%和尾部50%，对比RGCD-Rep与baseline。
4. **冷启动用户分析**（图3）：按历史行为将用户分为冷启动（无历史正反馈）和活跃用户，对比性能。
5. **在线A/B测试**（表3）：在快手和快手极速版各进行一周在线测试，报告4个指标提升百分比。

### 充分性与公平性
- 实验覆盖了多类baseline（单域、跨域、多模态），且对关键组件进行了消融，验证了每个阶段的必要性。
- 在线测试规模大（服务数亿用户），数据真实。
- 论文指出对baseline超参数进行了精细调优以获得最优性能，保证了比较公平性。
- 离线评估采用HR/NDCG标准召回指标，在线采用真实业务指标，评估客观。

## 6. 主要结论与发现

- RGCD-Rep在离线召回任务上显著优于所有现有方法，证明了MLLM推理引入CDR的有效性。
- 消融实验表明，阶段1的推理蒸馏提供了良好的语义初始化，阶段2的跨域对比学习和路由损失是实现性能提升的关键。
- 在长尾项目和冷启动用户上，RGCD-Rep的提升更显著，表明其有效缓解了数据稀疏和冷启动问题。
- 在线A/B测试中，快手和快手极速版的关注指标分别提升+0.83%和+0.93%，曝光、点击、有效进房等均有稳定增益，并在真实场景中服务超过4亿用户。

## 7. 优点

- **首创性**：首次将MLLM推理引入跨域推荐，提供了新的视角。
- **资源高效**：通过蒸馏将35B教师模型的能力迁移到2B学生模型，离线生成表示，在线部署成本低。
- **解决负迁移**：通过可迁移-残差分解和路由损失，区分了可迁移与域特定信息，防止因行为噪声或低迁移性配对导致负迁移。
- **工业友好**：两阶段设计便于离线处理，在线只需索引检索，符合工业低延迟需求。
- **全面实验**：既包含离线指标，也有大规模在线A/B测试，结果可靠。

## 8. 不足与局限

- **对配对构建的依赖**：行为共现对的质量直接影响训练效果；若配对不准确（如偶然共现），可能引入噪声。论文虽通过联合分数（行为+语义）进行路由调整，但未充分讨论数据中长尾配对的选择偏差。
- **域泛化性有限**：方法主要面向短视频→直播场景，虽然论文声称可扩展到多域，但未提供其他跨域（如视频→图文）的实验验证。
- **教师模型依赖**：需要35B级别的教师MLLM，虽然学生模型小，但教师模型的训练和推理仍有成本，且若教师模型偏见或错误，会通过蒸馏传递给学生。
- **可解释性不足**：虽然引入了推理标签，但最终表示是隐式的，难以追溯具体推理链条。
- **实验覆盖范围**：离线实验仅在快手单一数据集上进行，未使用公开数据集，可能无法证明方法在完全通用场景下的泛化能力。
- **计算资源未完全透明**：未报告教师模型推理耗时、阶段1/2确切训练时长，尤其蒸馏过程的GPU小时数。

（完）
