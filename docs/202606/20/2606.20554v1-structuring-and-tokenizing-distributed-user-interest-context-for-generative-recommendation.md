---
title: Structuring and Tokenizing Distributed User Interest Context for Generative Recommendation
title_zh: 结构化与分词化分布式用户兴趣上下文用于生成式推荐
authors: "Ruizhong Qiu, Yinglong Xia, Dongqi Fu, Hanqing Zeng, Ren Chen, Xiangjun Fan, Hong Li, Hong Yan, Hanghang Tong"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.20554v1"
tags: ["query:q1"]
score: 9.0
evidence: 为生成式推荐构建用户兴趣上下文
tldr: 生成式推荐是新兴范式，核心是物品标记化。现有方法在整合用户行为和物品语义上下文方面存在不足，图集成方法有可扩展性问题，语义标记化缺乏监督。本文提出G2Rec，统一了基于图的用户协同建模和语义标记化，用于工业规模生成式推荐，使模型能捕捉整体且语义化的用户兴趣原型，无需真实兴趣标签。在线部署和实验表明其优越性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.20554v1/fig-001.webp\", \"caption\": \"Table 2 Comparison with baseline methods on public datasets (best marked in bold). Our proposed G2Rec consistently outperforms all baseline methods on all four datasets.\", \"page\": 7, \"index\": 1, \"width\": 979, \"height\": 560}]"
motivation: 现有方法在同时有效组织和注入复杂用户行为及物品语义上下文方面存在困难，图方法可扩展性差，语义标记化缺乏监督。
method: 提出G2Rec框架，统一了基于图的用户协同建模和语义标记化，实现可扩展的工业级生成式推荐。
result: 在线部署和公开数据集实验表明G2Rec优于现有方法。
conclusion: G2Rec能捕捉整体且语义化的用户兴趣原型，提供更全面准确的行为上下文建模。
---

## 摘要
生成式推荐是一种新兴范式，在工业推荐系统中显示出潜力，旨在从用户历史行为预测其下一次交互。生成式推荐的核心是物品分词化，它连接了物品语义与推荐模型。然而，现有方法往往难以同时有效地组织和注入复杂的用户行为与物品语义上下文。一方面，现有的基于图的集成方法，如图序列化和图神经网络，要么面临可扩展性问题，要么仅利用局部图信息。另一方面，现有的语义分词化方法通常依赖启发式规则且缺乏明确的监督信号，可能导致不准确或次优的语义表示。为了解决用户兴趣上下文建模中的这些局限性，我们提出了G2Rec，一个可扩展的框架，将基于图的全局用户协同建模与语义分词化统一起来，用于工业规模的生成式推荐。总体而言，G2Rec使推荐模型能够捕捉到全局且具有语义基础的用户兴趣原型，无需依赖真实用户兴趣，从而在工业序列推荐中提供更全面、更准确的用户行为上下文建模。跨产品表面的在线部署以及在公开数据集上的广泛实验证明了G2Rec相对于现有方法的优越性。

## Abstract
Generative recommendation is an emerging paradigm that has shown promise in industrial recommendation systems, aiming to predict users' next interactions from their historical behaviors. At the core of generative recommendation lies item tokenization, which bridges item semantics and recommendation models. However, existing methods often struggle to effectively organize and inject complex user-behavioral and item-semantic contexts into recommendation models simultaneously. On the one hand, existing graph-based integration methods, such as graph serialization and graph neural networks, either suffer from scalability issues or exploit only local graph information. On the other hand, existing semantic tokenization methods typically rely on heuristics and lack explicit supervision signals, which may lead to inaccurate or suboptimal semantic representations. To address these limitations in user interest context modeling, we propose G2Rec, a scalable framework that unifies holistic graph-based user co-engagement modeling with semantic tokenization for industrial-scale generative recommendation. Overall, G2Rec enables recommendation models to capture holistic and semantically grounded user interest prototypes without requiring ground-truth user interests, thereby providing more comprehensive and accurate modeling of user behavior contexts in industrial sequential recommendation. Online deployment across product surfaces and extensive experiments on public datasets demonstrate the superiority of G2Rec over existing methods.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究问题**：现有生成式推荐方法难以同时有效地组织和注入**复杂的用户行为上下文**与**物品语义上下文**。具体体现为：
  - 基于图的方法（如图序列化、GNN）要么可扩展性差（序列化导致LLM计算成本高），要么仅利用局部图信息（GNN感知子图有限）。
  - 语义分词化方法通常依赖启发式目标、缺乏监督信号，导致语义表示不准确或次优。
- **核心目标**：设计一种可扩展的框架，统一**全局图协同建模**与**语义分词化**，在不依赖真实用户兴趣标签的前提下，捕捉全面且语义化的用户兴趣原型，提升工业级序列推荐的效果。
- **整体含义**：G2Rec通过构建物品-物品共参与图并进行软聚类，将图结构信息转化为可学习的兴趣轮廓令牌，使生成式推荐模型能够理解用户兴趣的演变模式，从而提供更准确的个性化推荐。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：先构造物品-物品共参与图（item-item co-engagement graph），然后用可微的软图聚类提取“兴趣原型”，再基于软聚类成员关系生成物品的兴趣轮廓令牌，最后将令牌与原始物品嵌入交替拼接形成用户兴趣转移序列，训练生成式推荐模型。
- **技术细节**：
  1. **稀疏化共参与图构造**：将每个用户的交互序列视为完全子图，对每个用户采样 O(log M) 条边（M 为总交互数），得到边数为 O(M log M) 的稀疏图，理论保证（定理2）其拉普拉斯矩阵与原图近似。
  2. **可扩展软图聚类**：提出可微的**软模块度**目标函数 Q_soft，将硬模块度中的指示函数替换为软成员概率的内积，公式为：
     - Q_soft(P) = (1/|E|) Σ_{(i,j)∈E} p_i^T p_j - γ ||P^T k||₂² / |E|²
     - 可通过GPU高效优化，每轮复杂度 O(ρM log M)（ρ 为每行稀疏的非零元数）。
  3. **兴趣轮廓令牌化**：
     - 每个兴趣原型 a 的嵌入 v_a = (Σ_i p_{i,a} x_i) / (Σ_i p_{i,a})（加权平均物品嵌入）
     - 每个物品 i 的兴趣轮廓令牌 y_i = Σ_a p_{i,a} v_a
  4. **序列格式**：用户交互序列变为 [BOS, x_{i1}, y_{i1}, x_{i2}, y_{i2}, ...]，即原始物品嵌入与兴趣轮廓令牌交替排列。
  5. **训练损失**：联合优化两个交叉熵损失：
     - 物品预测损失 L_item：预测下一个物品
     - 兴趣轮廓损失 L_profile：以物品软成员分布为标签，预测当前交互的兴趣原型
     - 总损失 L = L_item + λ L_profile

### 3. 实验设计：数据集、场景、对比方法

- **数据集**：四个公开的真实世界数据集
  - Beauty、Sports、Toys（Amazon评论子集）
  - Yelp（2019年1月1日之后的数据）
  - 全为稀疏数据（稀疏度>99.93%），用户交互通过5-core过滤。
- **评估场景**：序列推荐（按时间顺序排序，留一法划分：最后一项为测试，倒数第二项为验证，其余为训练）。
- **对比方法**：
  - **经典方法**：POP（基于热度）、MF（BPR损失）
  - **序列/分词化方法**：GRU4Rec、SASRec、BERT4Rec、Caser、EAGER
  - **图方法**：LightGCN、HeLLM
- **评价指标**：Recall@1/5/10、NDCG@5/10、MRR。
- **实验数量与充分性**：
  - 四个数据集 × 十种方法 × 六项指标 = 大量对比实验（表2、图1）。
  - 消融实验：软聚类 vs. 硬聚类（四个数据集上比较模块度，表4）；兴趣轮廓损失权重 λ 的影响（图2，单一数据集）。
  - 效率实验：训练和推理时间对比（表3，单个数据集Beauty）。
  - 在线部署：多个Meta产品表面的A/B测试，报告了多项用户参与度指标提升。
- **公平性**：所有基线使用作者开源代码并适配同一设置；序列长度限制50；超参数（如γ）按数据集调整；实验设置规范。

### 4. 资源与算力

- **论文中未明确说明使用的GPU型号、数量或训练总时长。**
- 提及的配置：使用 Llama 2 13B 作为推荐模型骨干，LoRA微调3个epoch（rank=16, dropout=0.05），Adam优化器（lr=0.0003，cosine schedule，100步warmup），最大序列长度50，嵌入维度64。
- 由于模型规模较大（13B参数），且经过LoRA微调，可推测需要多GPU（如A100或H100）支持，但具体资源未公开。

### 5. 实验数量与充分性评估

- **充分性**：实验覆盖全面，包括主流公开数据集、多种类型基线（经典/序列/图）、多维度指标（Recall/NDCG/MRR）、消融研究、效率分析以及大规模在线A/B测试。
- **客观性**：结果表明确实一致优于所有基线（平均排名1.04），消融实验验证了每个组件的贡献。
- **可能不足**：
  - 仅用四个数据集（均为国外平台），缺乏对中文或更复杂场景的验证。
  - 消融实验仅在Beauty上做了λ的完整曲线，其他数据集未展示；软聚类vs.硬聚类仅在模块度上比较，未直接对比推荐指标（如Recall）。
  - 在线测试的具体数值、置信区间未完全公开（受公司政策限制）。
  - 未与更多最新生成式推荐方法（如P5、TIGER等）直接对比。
- **总体评价**：实验设计较为严谨，结论具有说服力。

### 6. 论文的主要结论与发现

- **主要结论**：G2Rec能够**有效整合全局图结构与语义令牌化**，显著提升生成式序列推荐的准确性，同时保持工业可扩展性。
- **关键发现**：
  - 软聚类比硬聚类获得更高的模块度（提升23.8%在Sports），说明软成员关系更能刻画复杂兴趣。
  - 兴趣轮廓损失（λ>0）对推荐性能至关重要（去掉后MRR显著下降）。
  - 引入兴趣轮廓令牌对训练/推理时间影响极小（训练每batch多0.043秒，推理多0.0027秒）。
  - 在线A/B测试中多个用户参与度指标获得一致性改进（0.06%~0.19%）。

### 7. 优点

- **理论扎实**：提出可微模块度目标，具有闭合解和近线性复杂度证明（定理2、命题3/4）。
- **实用性高**：稀疏图构建 + GPU加速聚类，适合工业级数据规模；离线计算聚类，在线仅增加少量前向计算。
- **创新性强**：首次将软图聚类应用于生成式推荐的令牌化，形成“物品+兴趣轮廓”交替序列，使模型能学习兴趣转移。
- **实验充分**：包含公开数据集对比、消融、效率测试和在线A/B测试，结果一致优异。
- **可解释性**：聚类原型可视为潜在兴趣类别，有助于理解用户行为模式。

### 8. 不足与局限

- **实验覆盖**：公开数据集只有四个，且均为电商/评论类，缺乏短视频、新闻等推荐场景验证；基线中缺少更多最新的LLM+推荐方法。
- **偏差风险**：在线测试结果仅列出相对提升范围，未披露具体指标值和统计显著性细节，可能存在选择性报告。
- **参数敏感性**：超参数γ（聚类分辨率）和λ（损失权重）需对每个数据集调整，缺乏自动化调参指导。
- **冷启动问题**：论文假设所有物品在训练中出现（Σ Iu = I），未讨论新物品或新用户的处理。
- **假设限制**：理论保证（定理2）假设每个用户交互数N固定，实际中用户长度可能不均，边缘采样策略需进一步验证。
- **应用限制**：需大规模用户交互数据构建共参与图，对小平台或数据稀疏场景可能效果有限。

（完）
