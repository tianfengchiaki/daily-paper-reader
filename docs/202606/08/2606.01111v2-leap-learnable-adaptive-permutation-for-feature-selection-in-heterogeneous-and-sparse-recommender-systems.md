---
title: "LeAP: Learnable Adaptive Permutation for Feature Selection in Heterogeneous and Sparse Recommender Systems"
title_zh: "LeAP: 面向异构稀疏推荐系统中特征选择的可学习自适应排列"
authors: "Yihong Huang, Chen Chu, Fei Chen, Yu Lin, Ruiduan Li, Zhihao Li"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01111v2"
tags: ["query:q1"]
score: 9.0
evidence: 异构稀疏推荐系统的可学习特征选择
tldr: 针对工业推荐系统中异构稀疏特征的选择难题，提出LeAP（可学习自适应排列）模块，将随机排列转化为可学习机制，加速特征重要性评估，并引入自适应正则化策略处理异构维度和极端稀疏性。在四个公开数据集上达到最优，并在工业搜索排序模型中成功去除3600+冗余维度而无性能损失。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01111v2/fig-001.webp\", \"caption\": \"Table 4. Industrial Feature Pruning ROI and Ablation Analysis (Offline CTR GAUC Diff). 10−3 is usually considered as a significant difference for our metric.\", \"page\": 14, \"index\": 1, \"width\": 718, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2606.01111v2/fig-002.webp\", \"caption\": \"Table 3. Stepwise Validation of the LeAP’s feature importances (on MovieLens-1M)\", \"page\": 13, \"index\": 2, \"width\": 718, \"height\": 615}, {\"url\": \"assets/figures/arxiv/2606.01111v2/fig-003.webp\", \"caption\": \"Table 2. Feature Selection and Retrain AUC Results. Best results are highlighted in red. On ML-1M, all top-performing methods converged to the same feature subset, resulting in identical retrained AUC.\", \"page\": 12, \"index\": 3, \"width\": 616, \"height\": 555}]"
motivation: 现有特征选择方法无法有效处理异构特征维度和极端稀疏性，且传统排列方法计算代价高。
method: 提出LeAP模块，通过可学习机制替代随机排列，结合自适应正则化策略处理异构维度和稀疏性。
result: 在四个公开数据集上达到SOTA，工业部署中移除3600+冗余维度，性能无损失，是基线的2-10倍。
conclusion: LeAP是一种高效、模型无关的特征选择插件，适用于大规模异构稀疏推荐系统。
---

## 摘要
现代工业推荐系统依赖数千种异构特征——从低维标量（如统计值）到高维嵌入（如用户ID嵌入、MLP表示）——以实现高精度预测。鉴于训练相关的巨大计算成本，高效的特征选择至关重要。然而，现有方法面临三个主要瓶颈：（1）它们通常假设特征维度一致，或需要昂贵的映射到固定尺寸；（2）它们难以应对极端稀疏性，其中大部分特征（例如99%以上）保持默认值；（3）传统的基于排列的方法在大规模场景中计算成本过高。为应对这些挑战，我们提出了LeAP（可学习自适应排列），一种新颖的、模型无关的即插即用特征选择模块。LeAP将低效的随机排列过程转化为可学习机制，显著加速了特征重要性的评估。此外，我们引入了一种针对异构维度和极端稀疏性定制的自适应正则化策略，使得在非对称输入空间中获得更优的特征重要性排序结果。在四个公开推荐数据集上的实验表明，LeAP达到了最先进的性能。此外，LeAP已部署于一个日请求量超过十亿、模型参数规模达2TB的大规模工业搜索排序模型中。在这个包含超过12,000个总特征维度的实际场景中，LeAP成功识别并移除了超过3,600个冗余维度，且未出现性能下降，其能力是对比基线方法的2到10倍。

## Abstract
Modern industrial recommender systems rely on thousands of heterogeneous features -- ranging from low-dimensional scalars (e.g., statistical value) to high-dimensional embeddings (e.g., user-id embeddings, MLP representations) -- to achieve high-precision predictions. Given the immense computational costs associated with training, efficient feature selection is critical. However, existing methods encounter three primary bottlenecks: (1) they typically assume uniform feature dimensions or require costly mapping to a fixed size; (2) they struggle with extreme sparsity, where the majority of features (e.g., 99%+) remain at default values; and (3) traditional permutation-based approaches are computationally prohibitive in large-scale settings.   To address these challenges, we propose LeAP (Learnable Adaptive Permutation), a novel, model-agnostic plug-in module for feature selection. LeAP transforms the inefficient random permutation process into a learnable mechanism, significantly accelerating the evaluation of feature importance. In addition, we introduce an adaptive regularization strategy tailored for heterogeneous dimensions and extreme sparsity, enabling superior feature importance ranking results across asymmetric input spaces. Experiments on four public recommendation datasets demonstrate that LeAP achieves state-of-the-art performance. Furthermore, LeAP has been deployed in a large-scale industrial search ranking model with over a billion daily requests and a 2TB model parameter scale. In this real-world scenario involving 12,000+ total feature dimensions, LeAP successfully identified and removed over 3,600 redundant dimensions without performance degradation, which is 2 to 10 times the ability of compared baseline methods.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
现代工业推荐系统依赖数千种异构特征，包括低维标量（如统计值）和高维嵌入（如用户ID嵌入，可达256维）。这些特征维度差异巨大，并普遍存在极端稀疏性（超过99%的实例特征值为默认值）。训练和推理的计算成本极高，因此特征选择至关重要。现有方法面临三大瓶颈：
- **维度异构性**：多数方法假设特征维度一致或强制映射到固定尺寸，导致评估偏差（例如1D标量与256D嵌入难以公平比较）。
- **极端稀疏性**：稀疏特征因“低频激活”被误判为无用噪声而被过早剪枝，损害长尾个性化信号。
- **计算开销**：基于排列的特征重要性（Permutation Feature Importance）需要逐特征独立打乱并重新推理，计算复杂度为O(N × 模型开销)，在大规模模型中完全不可行。

## 2. 方法论：核心思想、关键技术细节
### 核心思想
将传统离散、计算昂贵的排列特征评估转化为**端到端可学习的门控机制**，通过单次前向传播即可评估所有特征的重要性，复杂度降至O(1 × 模型开销)。

### 关键技术细节
1. **可学习排列模块（Learnable Permutation Module）**：
   - 对每个特征i，在mini-batch内沿batch维度打乱其值得到噪声版本x_i'，保持边际分布和稀疏性。
   - 引入可学习门控变量g_i ∈ (0,1)（通过温度缩放Sigmoid参数化：g_i = σ(θ_i / τ)）。
   - 融合输出：\tilde{x}_i = g_i · x_i + (1 - g_i) · sg(x_i')（sg为stop-gradient，防止梯度传播至噪声）。
   - g_i反映模型对原始特征的依赖，接近1表示重要，接近0表示冗余。

2. **自适应正则化（Adaptive Regularization via Permutation Divergence）**：
   - 计算每个特征的排列散度Δ_i = (1/B) Σ ||x_i(b) - x_i'(b)||₂（L2范数差异的batch均值）。
   - 使用指数移动平均（EMA）平滑：\bar{Δ}_i^{(t)} = β·\bar{Δ}_i^{(t-1)} + (1-β)·Δ_i^{(t)}。
   - 自适应惩罚系数：λ_i = α · \bar{Δ}_i，总损失：L_total = L_task + Σ λ_i · g_i。
   - 作用：高维特征自动获得更大惩罚（因Δ_i更大），稀疏特征因Δ_i→0而惩罚极小，从而同时解决维度偏差和稀疏性歧视。

3. **理论保证**：基于凸函数性质证明，当特征信号强度ΔJ > λ_i时，门控梯度为负，驱动g_i→1；否则正则化主导，驱动g_i→0，实现自然极化。

4. **工业部署**：训练后直接使用绝对阈值（如0.5）或比例排序剪枝，移除门控模块并微调少量数据即可上线。

## 3. 实验设计
### 数据集
- **公开数据集**（均匀维度）：
  - Avazu（4043万样本，23字段，点击预测）
  - Criteo（4585万样本，39字段，CTR）
  - MovieLens-1M（100万样本，9字段，评分分类）
  - AliCCP（8532万样本，923字段，点击）
- **工业数据集**：来自Bilibili的搜索排序日志，日请求超10亿，模型包含500+特征字段，拼接后超过12000维，维度从1D到256D不等，含大量极端稀疏特征。

### 基准方法
- 启发式：Lasso, Random Forest, XGBoost
- 掩码法：AutoField, LPFS, SFS
- 排列法：SHARK（高效排列变体）

### 评价指标
- 公开：AUC、跨数据集聚类指标SAUC
- 工业：离线CTR预测用Group AUC（GAUC），在线用核心业务指标（视频观看、交互、观看时长）

## 4. 资源与算力
论文中**未明确说明**使用的GPU型号、数量或训练时长。仅提及：在工业部署中，模型参数规模达2TB，日请求超10亿，传统排列法因计算限制仅能在1%数据子集上运行；而LeAP在全量数据上完成特征重要性搜索的总时间甚至少于排列法在1%子集上的时间，体现了计算效率的巨大优势。但具体硬件配置未披露。

## 5. 实验数量与充分性
- **公开数据集实验**：在4个数据集上，以50%和25%特征保留率进行“搜索-重训练”两阶段评估，对比7种基线，报告了完整的AUC和SAUC。
- **机制分析实验**：在MovieLens-1M上通过逐步验证门控分数与特征实际效用的对应关系（表3），验证极化行为的合理性。
- **工业消融实验**：在工业数据上比较了Permutation、LeAP-Base（无自适应正则化）和完整LeAP在不同剪枝维度下的GAUC差异（表4），覆盖从~500到~6400维度的剪枝比例。
- **在线A/B测试**：部署剪枝3600+维度的策略，多周在线测试验证核心业务指标中性。
- 实验覆盖了不同维度设置、稀疏性、多个基线、消融分析和在线验证，较为充分和客观。但未进行跨模型结构（如不同深度网络）的泛化测试。

## 6. 主要结论与发现
- LeAP在4个公开数据集上均达到最高SAUC，优于所有基线。
- 门控分数自然极化：高分数对应强预测信号，低分数对应冗余或噪声，与理论一致。
- 自适应正则化至关重要：无自适应正则化的版本在剪枝3600+维度时GAUC下降2×10⁻²，而完整LeAP保持零损失。
- 工业部署中成功移除30%以上（3600+）的输入维度，解决带宽瓶颈，核心业务指标无退化（Zero Diff），剪枝能力是基线方法的2-10倍。
- LeAP将排列评估的计算复杂度从O(N)降至O(1)，并可在全量数据上高效运行。

## 7. 优点
- **创新性**：将离散排列转化为可学习门控，解决了排列法计算瓶颈，同时保留排列法的模型无关性和可解释性。
- **鲁棒性**：自适应正则化巧妙地利用排列散度的物理意义，同时解决维度异构和极端稀疏两大难题，理论推导清晰。
- **实用性强**：即插即用模块设计，适配工业流水线；极化门控允许直接阈值剪枝，无需重训练（仅微调），大幅降低部署成本。
- **实验充分**：从公开基准到大规模工业部署的全链路验证，包括消融分析和在线A/B测试，证据链完整。

## 8. 不足与局限
- **硬件资源未说明**：缺乏GPU型号、数量、训练能耗等细节，影响可复现性评估。
- **公开数据集维度均匀**：尽管在均匀维度上表现SOTA，但其核心优势在于处理异构稀疏特征，而公开实验并未直接模拟工业异构场景（仅工业实验展示了此优势），可能削弱对异构性效果的直接证明。
- **类别特征处理**：方法主要针对连续或嵌入特征，对于纯类别特征（如离散稀疏ID），打乱操作是否同样有效？论文未深入讨论。
- **超参数敏感性**：温度τ、全局稀疏系数α、EMA动量β的选取对结果影响如何？未提供敏感性分析。
- **仅限于推荐/排序模型**：方法是否可推广到其他领域（如NLP、CV）？未验证。
- **缺乏与其他动态维度选择方法（如AutoDim）的直接对比**：虽在相关工作中提到维度搜索方法，但实验未包含，对比不够全面。

（完）
