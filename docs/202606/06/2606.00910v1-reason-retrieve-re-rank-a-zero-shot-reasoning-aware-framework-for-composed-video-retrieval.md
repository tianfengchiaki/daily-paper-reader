---
title: "Reason, Retrieve, Re-rank: A Zero-Shot Reasoning-Aware Framework for Composed Video Retrieval"
title_zh: 推理、检索、重排序：面向组合视频检索的零样本推理感知框架
authors: Ali Alavi
date: 2026-05-30
pdf: "https://arxiv.org/pdf/2606.00910v1"
tags: ["query:llmapp"]
score: 7.0
evidence: 使用多模态大语言模型的零样本组合视频检索
tldr: "本文针对零样本组合视频检索任务，提出R3-CoVR框架，利用冻结的Qwen3-VL-8B模型推理编辑后效果并生成描述，SigLIP-2编码器初检，再通过同一模型进行约束感知重排，在挑战测试集上达到91.9% R@1和98.2% R@10，重排阶段贡献最大增益。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.00910v1/fig-001.webp\", \"caption\": \"Figure 1. R3-CoVR. A frozen multimodal LLM reasons about the after-effects of the edit and writes a concise target description (Stage 1); a frozen contrastive encoder embeds the description and the gallery and returns a top-K shortlist (Stage 2); the multimodal LLM then judges each shortlisted candidate against the intended result, and the judge score is blended with the retrieval rank to reorder the head of the list (Stage 3). No component is trained on challenge data.\", \"page\": 2, \"index\": 1, \"width\": 1036, \"height\": 313}]"
motivation: 现有方法依赖监督训练，零样本场景需利用推理能力提升检索精度。
method: 三阶段零样本流水线：多模态大模型推理编辑后效果并生成描述，对比编码器初检，再以大模型作为判别器进行约束感知重排。
result: "在挑战测试集上R@1达91.9%，R@10达98.2%，重排阶段使R@1从72.7%提升至91.9%。"
conclusion: 描述长度匹配编码器窗口和约束感知重排是关键因素，已开源三层实现。
---

## 摘要
组合视频检索（CoVR）旨在寻找通过对参考视频进行自由形式文本修改后得到的目标视频。我们应对CVPR 2026 VidLLMs研讨会上提出的'推理感知'CoVR（CoVR-R）挑战，其中检索严格为零样本。我们提出R3-CoVR（推理、检索、重排序），这是一个完全由冻结的基础模型构建的无需训练流程。多模态大语言模型（Qwen3-VL-8B）推理编辑所隐含的'后效'——状态转换、动作阶段、场景、镜头和节奏——并生成简洁的编辑后描述；对比视频-文本编码器（SigLIP-2）将该描述和候选库嵌入以进行第一阶段的检索；最后，一个约束感知的重排序阶段使用相同的多模态模型作为评判者，根据预期的编辑结果对每个入围候选者进行评分。在挑战测试集上，R3-CoVR达到了91.9%的R@1和98.2%的R@10。两个发现驱动了这些结果：（i）将描述长度与对比编码器的文本窗口匹配使R@1从67.5提升到72.7；（ii）仅对入围列表进行重排序的约束感知重排序器将R@1从72.7提升到91.9——这是最大的单一增益。我们分析了重排序器的行为、检索/重排序的混合以及入围列表深度，并发布了一个清晰的三层实现。

## Abstract
Composed Video Retrieval (CoVR) seeks the target video that results from applying a free-form textual modification to a reference video. We address the \emph{Reason-Aware} CoVR (CoVR-R) challenge at the CVPR~2026 VidLLMs workshop, where retrieval is strictly zero-shot. We present \textbf{R3-CoVR} (\emph{Reason, Retrieve, Re-rank}), a training-free pipeline built entirely from frozen foundation models. A multimodal large language model (Qwen3-VL-8B) reasons about the \emph{after-effects} an edit implies -- state transitions, action phases, scene, camera and tempo -- and verbalises a concise post-edit description; a contrastive video--text encoder (SigLIP-2) embeds this description and the gallery for first-stage retrieval; finally a constraint-aware re-ranking stage uses the same multimodal model as a judge that scores each shortlisted candidate against the intended edited result. On the challenge test set, R3-CoVR attains \textbf{91.9\% R@1} and \textbf{98.2\% R@10}. Two findings drive these results: (i)~matching the description length to the contrastive encoder's text window lifts \Rk{1} from $67.5$ to $72.7$; and (ii)~the constraint-aware re-ranker, which reorders only the shortlist, lifts \Rk{1} from $72.7$ to $91.9$ -- the single largest gain. We analyse the re-ranker's behaviour, the retrieve/re-rank blend, and the shortlist depth, and we release a clean three-layer implementation.