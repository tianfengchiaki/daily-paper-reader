---
title: "LeAP: Learnable Adaptive Permutation for Feature Selection in Heterogeneous and Sparse Recommender Systems"
title_zh: LeAP：用于异构稀疏推荐系统中特征选择的可学习自适应排列
authors: "Yihong Huang, Chen Chu, Fei Chen, Yu Lin, Ruiduan Li, Zhihao Li"
date: 2026-05-31
pdf: "https://arxiv.org/pdf/2606.01111v1"
tags: ["query:q1"]
score: 9.0
evidence: 推荐系统特征选择，可直接用于广告推荐
tldr: 针对工业推荐系统中异构特征和极端稀疏性导致的特征选择瓶颈，提出可学习自适应排列模块LeAP，将随机排列转为可学习机制，并引入自适应正则化。在四个公开数据集上达到SOTA，并在千亿请求、2TB参数的工业模型中成功移除3600+冗余维度，性能无损。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.01111v1/fig-001.webp\", \"caption\": \"Table 4. Industrial Feature Pruning ROI and Ablation Analysis (Offline CTR GAUC Diff). 10−3 is usually considered as a significant difference for our metric.\", \"page\": 14, \"index\": 1, \"width\": 718, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2606.01111v1/fig-002.webp\", \"caption\": \"Table 3. Stepwise Validation of the LeAP’s feature importances (on MovieLens-1M)\", \"page\": 13, \"index\": 2, \"width\": 718, \"height\": 615}, {\"url\": \"assets/figures/arxiv/2606.01111v1/fig-003.webp\", \"caption\": \"Table 2. Feature Selection and Retrain AUC Results. Best results are highlighted in red. On ML-1M, all top-performing methods converged to the same feature subset, resulting in identical retrained AUC.\", \"page\": 12, \"index\": 3, \"width\": 616, \"height\": 555}]"
motivation: 现有特征选择方法在异构维度、极端稀疏性和计算成本上存在瓶颈。
method: 提出LeAP，将随机排列转为可学习机制，并设计自适应正则化策略处理异构和稀疏特征。
result: 在四个公开数据集上达到SOTA，工业场景中从12000+特征中移除3600+冗余维度，无性能下降。
conclusion: LeAP是一种高效、模型无关的特征选择模块，适用于大规模异构稀疏推荐系统。
---

## 摘要
现代工业推荐系统依赖数千种异构特征——从低维标量（如统计值）到高维嵌入（如用户ID嵌入、MLP表示）——以实现高精度预测。鉴于训练相关的巨大计算成本，高效的特征选择至关重要。然而，现有方法面临三个主要瓶颈：（1）它们通常假设特征维度均匀或需要昂贵的映射到固定大小；（2）它们在极端稀疏性下表现不佳，其中大多数特征（例如，99%以上）保持默认值；（3）传统的基于排列的方法在大规模设置中计算成本过高。为了应对这些挑战，我们提出了LeAP（可学习自适应排列），这是一种新颖的、模型无关的插件式特征选择模块。LeAP将低效的随机排列过程转化为可学习机制，显著加速了特征重要性的评估。此外，我们引入了针对异构维度和极端稀疏性定制的自适应正则化策略，使得在非对称输入空间中获得更优的特征重要性排序结果。在四个公开推荐数据集上的实验表明，LeAP达到了最先进的性能。此外，LeAP已部署在一个拥有超过十亿日请求和2TB模型参数规模的大规模工业搜索排序模型中。在这个涉及12000多个总特征维度的实际场景中，LeAP成功识别并移除了超过3600个冗余维度，且没有性能下降，其能力是基线方法的2到10倍。

## Abstract
Modern industrial recommender systems rely on thousands of heterogeneous features -- ranging from low-dimensional scalars (e.g., statistical value) to high-dimensional embeddings (e.g., user-id embeddings, MLP representations) -- to achieve high-precision predictions. Given the immense computational costs associated with training, efficient feature selection is critical. However, existing methods encounter three primary bottlenecks: (1) they typically assume uniform feature dimensions or require costly mapping to a fixed size; (2) they struggle with extreme sparsity, where the majority of features (e.g., 99%+) remain at default values; and (3) traditional permutation-based approaches are computationally prohibitive in large-scale settings.   To address these challenges, we propose LeAP (Learnable Adaptive Permutation), a novel, model-agnostic plug-in module for feature selection. LeAP transforms the inefficient random permutation process into a learnable mechanism, significantly accelerating the evaluation of feature importance. In addition, we introduce an adaptive regularization strategy tailored for heterogeneous dimensions and extreme sparsity, enabling superior feature importance ranking results across asymmetric input spaces. Experiments on four public recommendation datasets demonstrate that LeAP achieves state-of-the-art performance. Furthermore, LeAP has been deployed in a large-scale industrial search ranking model with over a billion daily requests and a 2TB model parameter scale. In this real-world scenario involving 12,000+ total feature dimensions, LeAP successfully identified and removed over 3,600 redundant dimensions without performance degradation, which is 2 to 10 times the ability of compared baseline methods.

---

## 论文详细总结（自动生成）

# LeAP: 可学习自适应排列用于异构稀疏推荐系统特征选择 — 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

现代工业推荐系统依赖数千种异构特征（从1维标量到256维嵌入），导致模型规模达到TB级，训练和推理的计算成本极高。现有特征选择方法面临三个主要瓶颈：
- **维度异构性**：大多数方法假设特征维度均匀，或需要将所有特征映射到统一维度，导致信息失真和评估不公。
- **极端稀疏性**：99%以上的样本中特征为默认值，传统方法会错误地将“低激活频率”视为“无用噪声”，误删长尾重要信号。
- **计算成本过高**：基于排列的特征重要性方法（如Permutation Feature Importance）需要对每个特征单独打乱并重新推理，在数千特征的大规模模型中计算不可承受。

因此，论文提出**LeAP（Learnable Adaptive Permutation）**，旨在提供一种高效、模型无关、可端到端训练的特征选择插件，同时解决异构性和稀疏性问题。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
将传统离散、计算昂贵的排列特征重要性评估转化为**端到端可学习机制**，通过一次前向传播即可评估所有特征的重要性，将复杂度从O(N × model overhead)降至O(1 × model overhead)。

### 2.2 关键技术细节

**（1）可学习排列模块**
- 对每个特征`xi ∈ R^di`，在batch维度进行随机打乱得到噪声版本`x'_i`（保持边际分布和稀疏模式）。
- 引入可学习门控变量`gi = σ(θi/τ)` ∈ (0,1)，其中`θi`是学习参数，`τ`是温度超参数。
- 融合输出：`\tilde{x}_i = g_i · x_i + (1 - g_i) · sg(x'_i)`，其中`sg`为stop-gradient操作，确保梯度只流入门控权重。
- 门控权重`gi`反映模型对原始特征的依赖程度：`gi→1`表示依赖真实特征，`gi→0`表示依赖噪声。

**（2）自适应正则化（Permutation Divergence）**
- 计算每个特征在batch内的L2范数差异（置换散度）：`Δ_i = (1/B) Σ_b ||x_i^{(b)} - x'_i^{(b)}||_2`。
- 使用指数移动平均（EMA）平滑：`\bar{Δ}_i^{(t)} = β · \bar{Δ}_i^{(t-1)} + (1-β)·Δ_i^{(t)}`。
- 正则化权重：`λ_i = α · \bar{Δ}_i`，其中`α`是全局超参数。
- 总损失：`L_total = L_task + Σ_i λ_i · g_i`。

**（3）理论保证**
- 梯度解耦显示任务梯度与置换散度自然成比例，自适应正则化实现了惩罚与梯度的尺度对齐。
- 定理证明：若特征有用（信号强度ΔJ > λ_i），门控梯度为负，驱动`gi→1`；若特征冗余（ΔJ≈0），正则项主导，驱动`gi→0`，实现门控极化。

**（4）工业部署范式**
- 步骤1：将LeAP模块插入特征拼接层后，正常训练。
- 步骤2：门控收敛后，采用绝对阈值（如0.5）或Top-K比例直接硬剪枝。
- 步骤3：移除冗余特征和LeAP模块后，仅用少量数据微调即可上线。

## 3. 实验设计：数据集、基准、对比方法

### 3.1 公开数据集
- **Avazu**、**Criteo**、**MovieLens-1M**、**AliCCP**（推荐系统中常用CTR/评分预测基准）
- 特征维度被统一设置为均匀大小（以与其他方法公平对比）
- 评估指标：AUC、Normalized AUC（SAUC）

### 3.2 工业场景
- 来源：某长视频平台搜索排序模型，日请求超10亿，模型参数规模2TB
- 特征：500+特征字段，拼接后超过12,000维，维度从1到256不等，包含大量极端稀疏特征
- 评估指标：离线GAUC、在线核心业务指标（视频观看、互动、时长等）

### 3.3 对比方法（7种基线）
- **启发式**：Lasso、Random Forest、XGBoost
- **掩码型**：AutoField、LPFS、SFS
- **排列型**：SHARK（高效的排列近似方法）
- **消融变体**：LeAP-Base（无自适应正则化的版本）

## 4. 资源与算力

论文**未明确说明**训练GPU型号、数量或训练时长。但提及：
- 工业场景中，传统Permutation方法在1%数据采样上仍很慢，而LeAP在完整数据集上完成计算的时间更少。
- 公开数据集实验使用WideDeep作为骨干网络，遵循标准“Search-Retrain”两阶段协议，但无具体硬件信息。

## 5. 实验数量与充分性

### 实验数量
- 公开数据集：4个数据集 × 2种特征保留率（50%、25%）= 8组主实验结果（表2）。
- 机制分析（极化验证）：MovieLens-1M上的逐步验证（表3），包括9个逐步添加/移除特征的实验。
- 工业消融：不同剪枝维度（从500到6400）下，对比Permutation、LeAP-Base、LeAP在GAUC差异上的表现（表4）。
- 在线A/B测试：多周在线实验，验证核心业务指标。

### 充分性与公平性
- 公开数据集实验采用统一的骨干网络（WideDeep）和评估协议（搜索-重训练），对比方法均为该领域代表性方法，结果以SAUC聚合，减少随机波动。
- 工业场景中，由于计算限制，Permutation方法仅能在1%数据采样上运行，而LeAP在完整数据上运行，虽然对比不完全公平（采样偏差），但作者也承认此限制，并仍通过LeAP-Base的消融证明了自适应正则化的必要性。
- 消融实验覆盖了不同剪枝比例，证明了LeAP的鲁棒性。
- 总体而言，实验设计合理，覆盖了从标准基准到工业极大规模的多种场景，结论支撑充分。

## 6. 主要结论与发现

1. **公开数据集上达到SOTA**：在4个基准上，LeAP在50%和25%特征保留率下均取得最高SAUC（表2），优于所有基线。
2. **门控极化验证**：在MovieLens-1M上，LeAP的门控分数自然极化（接近0或1），且逐步验证表明高门控特征确实提升性能，低门控特征为冗余或噪声（表3）。
3. **工业剪枝ROI显著**：在2TB规模工业模型中，LeAP成功剪除30%+（3600+维）冗余特征，GAUC无损；而基线方法在剪除更少维度时就出现明显下降（表4）。
4. **自适应正则化不可或缺**：对比LeAP-Base（无自适应正则化）在剪枝3600维时GAUC下降2×10⁻²，完整LeAP保持零差异。
5. **在线验证**：剪枝后多周A/B测试显示核心业务指标完全中性，证明了实际部署价值。

## 7. 优点

- **方法创新**：将传统排列过程转化为可学习机制，极大提升计算效率（O(1) vs O(N)）。
- **针对性强**：自适应正则化同时解决维度异构和极端稀疏两大工业痛点，理论上有梯度尺度对齐和极化保证。
- **模型无关**：作为插件模块，可轻松集成到任何现有深度学习推荐模型中。
- **工业实用性强**：部署流程简洁（插入→训练→阈值剪枝→微调），并在超大规模系统中验证了30%+剪枝零损失。
- **实验全面**：覆盖标准基准和极大规模工业场景，有消融、机制分析和在线A/B测试。

## 8. 不足与局限

- **计算资源未披露**：论文未给出训练GPU类型、数量、时长等具体算力信息，影响可复现性和成本评估。
- **公开数据集维度均匀设置**：原文将公开数据集特征设为均匀维度，无法直接验证LeAP在异构维度上的优势。实际上，LeAP在异构场景中的效果仅由工业实验验证，而工业实验的对比基线（Permutation）受限于采样数据，公平性不足。
- **门控极化理论假设**：定理假设任务损失函数为凸，实际深度模型非凸，理论保证的适用性有限。
- **超参数敏感性**：温度τ、EMA动量β、全局稀疏度控制α等超参数的选择可能影响效果，论文未系统分析其鲁棒性。
- **剪枝策略的局限性**：采用的绝对阈值（0.5）或Top-K策略较为简单，未探讨门控值介于中间时的处理方式（如软剪枝）。
- **稀疏特征的保护机制**：虽然自适应正则化保护了稀疏特征，但也可能保留一些真正无用的默认值特征（若其置换散度极小但实际预测贡献也为0），论文未进一步区分这种情况。
- **在线实验范围**：仅在一个工业搜索排序模型上验证，未在其他推荐场景（如广告、流媒体推荐）中复现。

（完）
