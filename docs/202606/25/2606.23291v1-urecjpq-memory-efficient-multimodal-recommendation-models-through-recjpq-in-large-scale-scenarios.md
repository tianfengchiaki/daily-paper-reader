---
title: "URecJPQ: Memory-efficient Multimodal Recommendation Models through RecJPQ in Large-Scale Scenarios"
title_zh: "URecJPQ: 大规模场景下通过RecJPQ实现内存高效的多模态推荐模型"
authors: "Giuseppe Spillo, Zixuan Yi, Aleksandr Petrov, Cataldo Musto, Craig Macdonald, Iadh Ounis"
date: 2026-06-22
pdf: "https://arxiv.org/pdf/2606.23291v1"
tags: ["query:q6"]
score: 9.0
evidence: 通过乘积量化实现内存高效的多模态推荐
tldr: "针对大规模多模态推荐模型中ID嵌入内存消耗巨大的问题，提出URecJPQ联合乘积量化方法，将用户/物品表示为共享子嵌入拼接，大幅减少可训练参数和检查点大小（86-99%），平均召回率仅下降8.5%、NDCG下降16%，部分领域性能提升高达85%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.23291v1/fig-001.webp\", \"caption\": \"Fig. 2: Embedding quantization in RecJPQ: an 8-dimensional embedding is approximated by the concatenation of 4 sub-embeddings, each 2-dimensional.\", \"page\": 6, \"index\": 1, \"width\": 623, \"height\": 409}, {\"url\": \"assets/figures/arxiv/2606.23291v1/fig-002.webp\", \"caption\": \"Fig. 5: Impact of number of sub-spaces on Recall across the different datasets.\", \"page\": 14, \"index\": 2, \"width\": 389, \"height\": 1047}, {\"url\": \"assets/figures/arxiv/2606.23291v1/fig-003.webp\", \"caption\": \"Fig. 1: Overview of our URecJPQ method and its application in multimodal recommendation.\", \"page\": 3, \"index\": 3, \"width\": 546, \"height\": 348}, {\"url\": \"assets/figures/arxiv/2606.23291v1/fig-004.webp\", \"caption\": \"Fig. 3: Comparison between the number of trainable parameters of the BPR model on the Baby23 dataset, for 143K users and 44K items. The vanilla version has 12M total trainable parameters, while URecJPQ has only 32K.\", \"page\": 8, \"index\": 4, \"width\": 469, \"height\": 812}, {\"url\": \"assets/figures/arxiv/2606.23291v1/fig-005.webp\", \"caption\": \"Fig. 6: PCA applied to user and item embeddings learned by BPR and VBPR, for both vanilla models (on the left) and quantized models (on the right). Yellow scatters represent users, while blue scatters represent items.\", \"page\": 17, \"index\": 5, \"width\": 752, \"height\": 625}, {\"url\": \"assets/figures/arxiv/2606.23291v1/fig-006.webp\", \"caption\": \"Fig. 4: Model performance across ML1M, Baby23, and Sports23 datasets. The left column shows Recall@20 and the right column shows NDCG@20. The x-axis (log scale) reports the number of trainable parameters. Blue symbols represent the vanilla (full) models, while green symbols represent their corresponding quantized variants; arrows indicate parameter reduction. Marker size indicates the model checkpoint size. TOST confirms statistical equivalence for all three datasets.\", \"page\": 12, \"index\": 6, \"width\": 777, \"height\": 1053}]"
motivation: 大规模多模态推荐中ID嵌入内存消耗巨大，需在降低内存占用的同时保持推荐性能。
method: 通过乘积量化将用户和物品表示为共享子嵌入的拼接，替代完全学习的唯一嵌入，从而减少参数。
result: "在三个数据集上，检查点大小减少86%-98%，参数减少98%-99%，平均召回率下降8.5%、NDCG下降16%，部分领域性能提升85%。"
conclusion: URecJPQ能有效适用于大规模多模态推荐，显著降低内存需求且准确损失微小，甚至在某些场景提升性能。
---

## 摘要
在大规模工业数据集上训练最先进的推荐模型可能是一项具有挑战性的任务，因为用户和物品数量庞大，通常通过ID嵌入来表示。这些嵌入通常需要大量内存资源，而这些资源并不总是可用。在多模态推荐中，这一问题进一步加剧，因为多模态物品特征通常能提升推荐性能，但需要更多资源进行编码。在本文中，我们介绍了URecJPQ，这是一种专门为大规模和多模态top-k推荐任务设计的联合乘积量化方法。在这些任务中，大量的用户和物品结合可用的模态进一步增加了计算的内存需求。其核心思想是，不将每个用户/物品表示为一个完全学习的唯一嵌入，而是表示为共享学习子嵌入的串联，从而显著减少可训练参数的总数。我们在三个广泛使用的跨领域数据集（电影、婴儿和体育产品）上的实验表明，URecJPQ可以有效地应用于多模态推荐场景。在大规模场景中，我们观察到检查点大小和可训练参数数量的大幅减少（分别为86%到98%和98%到99%），而准确率仅有轻微下降（平均召回率下降8.5%，NDCG下降16%），在某些情况下，甚至出现了性能提升（高达85%），例如在婴儿产品领域。我们的代码库可在https://anonymous.4open.science/r/large_mmrecjpq-839B/README.md获取。

## Abstract
Training state-of-the-art recommendation models on large-scale industrial datasets can be a challenging task due to the high number of users and items which are typically represented through ID embeddings. Such embeddings typically require a large amount of memory resources, which are not always available. This problem is further exacerbated in multimodal recommendation, in which multimodal item features generally improve recommendation performance, but require more resources to encode. In this paper, we introduce URecJPQ, a Joint Product Quantization method specifically designed for large-scale and multimodal top-k recommendation tasks, in which the vast number of users and items, combined with the available modalities, further increases the memory demands for the computation. The core idea is to represent each user/item not as a fully learned, unique embedding, but rather as a concatenation of shared learned sub-embeddings, thereby significantly reducing the total number of trainable parameters. Our experiments on three widely-used datasets across different domains (movies, baby and sports products) show that URecJPQ can be effectively applied to multimodal recommendation settings. In large scale scenarios, we observe a substantial reduction in checkpoint sizes and the number of trainable parameters (ranging from 86% to 98%, and 98% to 99%, respectively), with only a marginal decrease in accuracy (8.5% on recall and 16% on NDCG, on average), and, in some cases, even performance improvements (up to 85%), as in the baby products domain. Our codebase is available at https://anonymous.4open.science/r/large_mmrecjpq-839B/README.md.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：在大规模工业推荐系统中，海量用户和物品通常使用唯一的ID嵌入向量表示，这导致巨大的内存消耗（尤其在GPU训练时）。当引入多模态信息（如图像、文本）以提升推荐效果时，这一内存问题更加严重。现有量化方法（如RecJPQ、TIGER）主要针对Next-item预测（顺序推荐）或仅压缩物品嵌入，未解决Top-k推荐中同时需要用户和物品嵌入的内存瓶颈。
- **整体含义**：本文旨在通过联合乘积量化（Joint Product Quantization）技术，在不显著牺牲推荐精度的前提下，大幅减少多模态Top-k推荐模型的参数量和检查点大小，使其能在大规模、内存受限的工业场景下部署。

#### 2. 方法论：核心思想、关键技术细节

- **核心思想**：放弃为每个用户和物品学习唯一的完整嵌入，而是将嵌入空间划分为多个子空间，每个子空间共享一个较小的子嵌入码本（codebook）。每个用户/物品由在多个子空间中索引到的子嵌入拼接而成，从而大大减少总可训练参数。
- **关键技术细节**：
  - **URecJPQ**：基于RecJPQ（原用于顺序推荐）扩展，同时量化用户和物品的ID嵌入。
  - **量化过程**：
    - 设原始嵌入维度为k，子空间数为m，则每个子嵌入维度为d = k/m。
    - 每个子空间维护一个大小为l（通常l=256，可存储于1字节）的码本，共m×l个子嵌入。
    - 每个用户/物品分配m个子ID（0~255），对应各子空间的码本索引。
  - **码分配策略**：
    - **随机策略（Random）**：随机分配子ID，简单但可能丢失语义一致性。
    - **截断SVD策略**：先对用户-物品交互矩阵进行SVD分解，得到低维隐因子，然后对每个维度进行分位数离散化，映射到子ID。可保留全局交互结构。
  - **训练方式**：子ID分配在训练开始时确定（不可导），码本和模型的其他参数通过反向传播端到端学习；推理时通过码本拼接重构完整嵌入。
  - **适配性**：可插入任何需要用户和物品ID嵌入的Top-k推荐模型（如BPR、VBPR、MMGCL、SLMRec）。

#### 3. 实验设计

- **数据集**：
  - **MovieLens-1M (ML1M)**：小规模、稠密，含文本和视觉模态。
  - **Amazon Reviews '23**的**Baby23**和**Sports23**：大规模、稀疏，含文本和视觉模态（使用MiniLM和ViT编码）。
  - 与2014旧版数据集对比，新版规模显著扩大（用户/物品/交互量级）。
- **Benchmark / 对比方法**：
  - 基础模型（未量化）：**BPR**、**VBPR**（多模态BPR）、**MMGCL**（多模态图对比学习）、**SLMRec**（自监督多模态学习）。
  - 自身量化版本：URecJPQ-BPR、URecJPQ-VBPR等。
- **评估指标**：
  - 推荐性能：Recall@20、NDCG@20。
  - 内存效率：检查点大小（checkpoint size）、可训练参数量。
- **实验设置**：
  - 数据划分：80%训练、10%验证、10%测试。
  - 嵌入维度k：{64,128,256}；子空间数m：{8,16,32,64,128}；码本长度l固定为1（256子嵌入/子空间）。
  - 使用TOST（Two One-Sided Tests）检验量化模型与原始模型的统计等价性（margin=0.1）。
  - 早停法（Recall@20验证，10轮无提升停止）。

#### 4. 资源与算力

- 论文明确提到：
  - **GPU型号**：NVIDIA A5000（24GB显存）。
  - **框架**：PyTorch 2.6，CUDA 12.4。
- **训练时长**：未详细报告每个实验的具体训练时间，仅说明基于MMRec框架实现。但指出在大规模数据集上，由于参数量大幅减少，训练和推理效率更高。
- **算力小结**：文中未提供精确的训练时间或GPU数量，但给出了所用硬件配置。

#### 5. 实验数量与充分性

- **实验数量**：
  - 3个数据集 × 4个模型 × (多种k、m、码分配策略) = 大量实验。
  - 主要结果展示：图4（各数据集性能-参数对比）、图5（子空间数敏感性）、表3（码分配策略对比）、图6（定性PCA可视化）。
  - 每组实验至少包含验证集上的Recall@20早停，并报告最优结果。
- **充分性与公平性**：
  - 对比实验覆盖了无量化、随机分配、SVD分配，且调整了关键超参数。
  - 使用相同的骨干模型和训练流程（MMRec框架），确保公平。
  - 使用TOST验证统计等价性，增强结果可靠性。
  - 消融方面：对子空间数、码分配策略进行了系统分析。
  - 不足：仅使用3个数据集，且全为Amazon和MovieLens领域，未覆盖其他类型（如社交、新闻推荐）。未探索更极大规模（如千万级用户）的场景。

#### 6. 主要结论与发现

- **RQ1（性能影响）**：URecJPQ在多数情况下性能与原始模型相当或更好。在Baby23上BPR/VBPR提升显著；在Sports23上略有下降（稀疏场景下CF信号瓶颈）；在ML1M上所有模型均有提升（正则化效果）。平均召回率下降8.5%，NDCG下降16%，但参数减少98-99%，检查点大小减少86-98%。
- **RQ2（子空间数）**：小/稠密数据集（ML1M）最优m=32；大/稀疏数据集（Baby23、Sports23）最优m=64；超过后性能下降（过分割）。
- **RQ3（码分配策略）**：简单模型（BPR、VBPR）在稀疏数据集上随机策略更好；复杂模型（SLMRec、MMGCL）两者差异小，SVD有时更优。两种策略在TOST下统计等价，但数值趋势有差异。
- **定性分析**：PCA可视化显示URecJPQ的嵌入更紧凑、集中在原点，说明共享子嵌入起到了正则化作用，聚类效果更好。

#### 7. 优点

- **方法创新**：首次将联合乘积量化同时应用于用户和物品ID嵌入，并成功适配多模态Top-k推荐，填补了已有工作（仅量化物品或用于顺序推荐）的空白。
- **极强内存节省**：参数量减少达99%，检查点大小减少98%，使得大模型可在有限GPU内存下训练。
- **性能保持良好**：在多数场景性能几乎无损失，甚至提升（正则化效果），并得到统计等价验证。
- **通用性**：可插入任何基于ID嵌入的推荐模型，包括复杂的多模态图模型。
- **实验设计严谨**：包含多种骨干模型、码分配策略、子空间数调优，以及统计检验和可视化分析。

#### 8. 不足与局限

- **实验覆盖有限**：仅使用3个数据集（MovieLens、Amazon两个子集），缺少其他领域（如视频、新闻、社交）和多语言数据。未在真正工业级千万级用户/物品上验证。
- **未压缩多模态特征本身**：本文仅量化了ID嵌入，而视觉/文本的编码器仍需要大量参数；未来可选择量化多模态特征。
- **稀疏场景退化**：在Sports23上，量化模型性能略有下降，说明在极高稀疏度下压缩会损失CF信号。
- **无效率实际测量**：虽然提到参数量减少，但未报告实际GPU训练时间、推理延迟、吞吐量等工程效率指标。
- **码分配策略选择依赖经验**：哪种策略最好需要根据模型和数据集尝试，缺乏自适应方法。

（完）
