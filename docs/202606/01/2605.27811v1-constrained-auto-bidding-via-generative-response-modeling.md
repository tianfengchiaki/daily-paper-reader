---
title: Constrained Auto-Bidding via Generative Response Modeling
title_zh: 通过生成式响应建模实现约束自动出价
authors: "Eunseok Yang, Xingdong Zuo, Kyung-Min Kim"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.27811v1"
tags: ["query:q2"]
score: 9.0
evidence: 基于生成响应模型的自动出价广告技术
tldr: 针对自动出价中非平稳流量与约束违规问题，提出生成响应模型（GRM）以预测未来流量和成本-价值曲线，结合轻量级分析控制器通过一维求根精确满足约束。实验证明GRM相比基线提升了约束稳定性和总体得分。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27811v1/fig-001.webp\", \"caption\": \"Table 3: GRM hyperparameters.\", \"page\": 11, \"index\": 1, \"width\": 494, \"height\": 386}, {\"url\": \"assets/figures/arxiv/2605.27811v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of the Generative Response Model (GRM) framework. (Left) GRM encodes state-action history via a causal transformer and predicts horizon-aggregate response curves (traffic 𝐼𝑡 :𝑇 , cost ̂̄𝐶𝑡 :𝑇 (·), value ̂̄𝑉 𝑡 :𝑇 (·)). Training uses future-sampled supervision, where for each anchor tick 𝑡 we sample future ticks 𝑘 ∼ U{𝑡, . . . ,𝑇 } from offline data and fit curves to realized outcomes. (Right) The min-pacing controller solves two 1D root equations for budget and CPA constraints, then executes 𝛼𝑡 = min{𝛼𝐵, 𝛼𝐶 }.\", \"page\": 3, \"index\": 2, \"width\": 1011, \"height\": 254}]"
motivation: 现有控制方法无法预测未来，RL和生成方法在分布偏移下约束易违规，亟需能联合预测未来并精准满足约束的出价方法。
method: GRM作为历史条件序列模型预测未来流量和聚合成本/价值曲线（出价乘数函数），再通过轻量级控制器进行一维求根以执行约束。
result: 在AuctionNet上，GRM相比基线提升了约束稳定性和总体得分，且控制器在预测误差下约束违规有界。
conclusion: 通过将学习目标从动作转向响应，GRM实现了对非平稳环境的鲁棒约束满足，为自动出价提供了高效可部署方案。
---

## 摘要
自动出价系统旨在长期预算约束和每次获取成本等比率目标下最大化广告主价值，然而未来流量和拍卖动态具有非平稳性和不确定性。现有方法面临显著局限：基于控制的节奏调整可对偏差做出反应，但无法预测未来条件，而强化学习和生成方法则将约束纳入奖励信号，模糊了违规行为并在分布偏移下性能退化。我们将学习目标从动作转向响应，提出生成式响应模型（GRM），这是一种基于历史条件的序列模型，联合预测未来流量和水平聚合成本/价值曲线（作为单个出价乘数的函数）。我们证明，在温和单调性条件下，相对于完全每刻控制的最优性差距受每刻边际价值-成本离散度的限制。给定预测响应后，轻量级解析控制器通过一维求根步骤强制执行每个活动约束。我们证明该控制器对于单乘数问题是精确的，并基于预测误差限制了滚动时域重规划下的约束违规。在AuctionNet上的实验表明，与现有基线相比，GRM提高了约束稳定性和整体分数。

## Abstract
Auto-bidding systems aim to maximize advertiser value over long horizons under budget constraints and ratio targets such as cost-per-acquisition, yet future traffic and auction dynamics are non-stationary and uncertain. Existing approaches face distinct limitations: control-based pacing reacts to deviations but cannot anticipate future conditions, while RL and generative methods fold constraints into reward signals, obscuring violations and degrading under distribution shift. We shift the learning target from actions to responses with the Generative Response Model (GRM), a history-conditioned sequence model that jointly predicts future traffic volume and horizon-aggregate cost/value curves as functions of a single bid multiplier. We show that under mild monotonicity conditions, the optimality gap relative to full per-tick control is bounded by the dispersion of per-tick marginal value-per-cost. Given predicted responses, a lightweight analytic controller enforces each active constraint via a 1D root-finding step. We prove this controller is exact for the single-multiplier problem and bound constraint violations under receding-horizon replanning in terms of prediction error. Experiments on AuctionNet show that GRM improves constraint stability and overall score compared to existing baselines.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）
自动出价系统需要在长期预算和效率（如目标CPA/ROAS）约束下最大化广告主价值。然而，未来流量和拍卖动态是非平稳且不确定的，这给实时出价决策带来巨大挑战。现有方法存在明显局限：  
- **基于控制的节奏调整**（如PID）虽能对实时偏差做出反应，但无法主动预测未来条件，容易导致约束违规。  
- **强化学习（RL）和生成式方法**（如Decision Transformer）将约束编码到奖励信号或返回条件中，不仅模糊了违规原因，还在分布偏移下性能大幅下降。  

因此，需要一种能够联合预测未来流量、成本与价值响应，并精确、鲁棒地满足多约束的方法。

### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：从“预测最优动作”转向“预测环境响应”。提出**生成式响应模型（GRM）**，它是一个基于历史条件的序列模型，预测未来**水平聚合响应包**：剩余流量 `𝐼_{𝑡:𝑇}`、基于单个出价乘数 `𝛼` 的聚合预期成本曲线 `̄C_{𝑡:𝑇}(𝛼)` 和聚合预期价值曲线 `̄V_{𝑡:𝑇}(𝛼)`。
- **关键技术细节**：
  - **曲线参数化**：使用归一化的对数-sigmoid（标准正态CDF）族参数化成本/价值曲线，确保边界条件（`̄C(0)=0, ̄C(∞)=a`）和单调性，且输出维度低（仅7个参数）。
  - **训练方式**：采用**未来采样监督**：对每个锚点时刻 `t`，从 `[t,T]` 均匀采样未来时刻 `k`，以加权回归方式拟合聚合曲线，权重为流量 `𝐼_k`，使高流量时刻对拟合影响更大。
  - **轻量级解析控制器**：给定GRM预测的响应，通过两个一维求根（如二分法）分别计算预算可行乘数 `𝛼_B` 和CPA可行乘数 `𝛼_C`，然后执行**最小节奏控制律** `𝛼_t = min{𝛼_B, 𝛼_C}`，保证同时满足所有约束。控制器在每一时刻会基于最新状态重新求解（滚动时域重规划）。
- **理论保证**：
  - **单乘数最优性差距**：相比每时刻独立选择乘数的完整问题，单乘数近似带来的差距受效率离散度 `𝜎²`（边际价值-成本比的加权方差）控制（定理5.2）。
  - **最小控制精确性**：对单乘数问题，`min{𝛼_B, 𝛼_C}` 是最优解（定理5.3）。
  - **约束违规界限**：在预测误差下，约束违规与预测误差呈线性增长，且流量误差仅贡献 `𝑂(𝜖_𝐼 ln T)`，主要依赖曲线误差（定理5.4）。

### 3. 实验设计
- **数据集/场景**：使用**AuctionNet**（NeurIPS 2024自动出价挑战赛环境），包含多个交付周期（P7–P27），每个周期48个时间片，超过5亿条记录。模型在P7–P13上训练，在P14–P20上评估。
- **Benchmark**：与代表性离线RL和生成式出价基线对比，包括：Behavioral Cloning (BC)、Conservative Q-Learning (CQL)、Implicit Q-Learning (IQL)、Decision Transformer (DT)、DiffBid、EBaReT，以及基于控制的方法FTRL。
- **评估指标**：官方AuctionNet分数 = `p(cpa; d) · Σ Val_t`，其中 `p(cpa; d)=min((d/cpa)^β,1)` 对CPA违规施加惩罚，β=2。
- **主要实验**：
  1. **整体性能**：P14–P20共7个时期上的平均分数。
  2. **分布偏移鲁棒性**：两种场景：竞争预算增加1.1倍、CPA目标收紧为0.8倍，对比正常条件和偏移条件下的分数下降百分比。
  3. **预测质量与性能关系**：分析验证损失与测试分数的相关性。
  4. **曲线族消融**：比较线性、分段线性、sigmoid、单调MLP和对数-sigmoid的影响。

### 4. 资源与算力
论文**未明确说明**所使用的GPU型号、数量或训练时长。仅提到GRM使用2层因果Transformer编码器（4头，128隐藏维），约85万参数，使用AdamW优化器，批量大小为64。训练环境未量化，但从参数规模看可以在单GPU上高效训练。

### 5. 实验数量与充分性
实验比较充分、客观、公平：
- **对比基线全面**：覆盖了行为克隆、保守Q学习、隐含Q学习、决策Transformer、扩散模型、奖励变换器等主流离线RL和生成式方法，并在相同策略类别（时间片乘数输出）下比较。
- **多角度评估**：不仅有整体分数，还有分布偏移下的鲁棒性、预测质量与性能的关联分析、曲线消融实验。
- **统计稳定性**：报告了多个时期的平均值和标准差，表明结果可靠。
- **考虑公平性**：所有方法使用相同的预算/CPA目标和输入特征，时间片粒度一致。

但是，未进行**真实在线A/B测试**，仅在模拟环境评估。此外，未对训练数据量、历史长度等超参数做敏感性分析。

### 6. 论文的主要结论与发现
- GRM在AuctionNet上平均分数达33.88，超越最强基线EBaReT（31.43）**7.8%**，且在所有时期表现一致。
- 在两种分布偏移下，GRM性能下降分别为**7.2%和5.0%**，显著优于DT（22.6%和13.9%）和FTRL（9.0%和6.9%），表明良好的鲁棒性。
- 验证损失与测试分数呈显著负相关（r=-0.78, p<0.01），验证预测质量直接转化为出价性能——与理论界限一致。
- 对数-sigmoid曲线族优于其他参数化，证明了log变换对捕捉赢率对数凹模式的必要性。
- 理论分析表明单乘数近似最优性差距受效率离散度控制，且约束违规随预测误差线性增长，提供清晰的可解释性。

### 7. 优点
- **创新性**：将学习目标从动作转向环境响应，结合预测与解析控制，提供了约束透明性、可诊断性和理论保证，与现有隐式约束处理方法形成鲜明对比。
- **理论与实践结合**：给出了三个关键定理（最优性差距、精确性、违规界限），并在实验中验证了预测误差与性能的直接关系。
- **简洁高效**：仅需预测单个聚合曲线和流量，避免了逐时刻预测的复杂性和高方差，控制器仅需两个一维求根，计算开销极低。
- **鲁棒性**：在分布偏移下表现远优于依赖离线数据的方法，归功于重规划机制——每时刻重新预测响应并求解约束。

### 8. 不足与局限
- **依赖单调性假设**：理论结果建立在曲线严格单调及CPA单调性条件上，尽管实验中98%情况满足，但极端饱和情况下可能失效，需设置回退规则。
- **未进行真实在线实验**：仅在AuctionNet模拟中评估，实际生产环境可能有更复杂的非平稳性和竞争演化，需要进一步验证。
- **资源与算力信息缺失**：未报告训练时间、GPU型号/数量，难以直接复现和评估计算成本。
- **超参数敏感性分析不足**：仅报告了流量损失权重λ_I的灵敏度，但未深入分析Transformer层数、隐藏维数、未来采样数M等影响。
- **应用限制**：需要充分的历史日志数据覆盖不同的α-历史组合，对于新广告主或冷启动场景，覆盖率可能不足，曲线预测可能不可靠。

（完）
