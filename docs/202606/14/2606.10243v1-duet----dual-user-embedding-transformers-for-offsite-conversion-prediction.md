---
title: DUET -- Dual User Embedding Transformers for Offsite Conversion Prediction
title_zh: DUET -- 用于站外转化预测的双用户嵌入变换器
authors: "Reazul Hasan Russel, Mingwei Tang, Rostam Shirani, Xinlong Liu, Navid Madani, Leo Ding, Yawen He, Xiangyu Wang, Mustafa Acar, Ashish Katiyar, Yuhai Li, Alan Yang, Metarya Ruparel, Derek Qiang Xu, Rupert Wu, Rui Yang, Liang Tao, Xinyi Zhao, Larry Zhang, Sri Reddy, Rob Malkin"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10243v1"
tags: ["query:q2"]
score: 9.0
evidence: 广告推荐中的站外转化预测
tldr: "针对离线转化率预测中点击信号丰富但转化信号稀疏、延迟且难以归因的挑战，提出DUET框架，将用户行为显式分为点击和转化两个领域，分别预训练专用Transformer编码器（多层自注意力用于密集点击流，交织交叉与自注意力用于稀疏转化流），生成互补嵌入供下游排序器使用，在严格服务延迟约束下实现性能提升，相比最强基线NE降低0.38%，A/B测试一致改进。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10243v1/fig-001.webp\", \"caption\": \"Table 2 Ablation study on the upstream encoders.\", \"page\": 11, \"index\": 1, \"width\": 632, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-002.webp\", \"caption\": \"Figure 3 End-to-end system: recurring pre-training with checkpoint validation followed by SIDE-based embedding compression and finally event-triggered inference and serving.\", \"page\": 7, \"index\": 2, \"width\": 968, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-003.webp\", \"caption\": \"Figure 6 Eval NE gains (%) on Different Types and Stages of downstream rankers. Higher is better.\", \"page\": 10, \"index\": 3, \"width\": 627, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-004.webp\", \"caption\": \"Figure 1 DUET unified framework: data routing, representation pre-training, and downstream ranker.\", \"page\": 4, \"index\": 4, \"width\": 894, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-005.webp\", \"caption\": \"Figure 4 Principal components of quantized user embeddings. ConvAUE and ClickAUE occupy distinct regions, indicating non-redundant information. Figure5 Orthogonality between ConvAUE and ClickAUE. Degree of complementarity varies, higher orthogonality indicating greater potential for additive information gain.\", \"page\": 9, \"index\": 5, \"width\": 977, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-006.webp\", \"caption\": \"Figure 2 DUET architecture. Left: the DLRM backbone with parallel sequence and non-sequence branches. Right: event tower structure with stacked attention blocks and DCN user summarization.\", \"page\": 5, \"index\": 6, \"width\": 968, \"height\": 518}]"
motivation: 点击和转化信号在统计特性上存在巨大差异，现有方法使用单一编码器处理所有数据，无法有效建模两种信号的区别。
method: 将用户行为划分为点击和转化两个领域，针对各自统计特性设计专用Transformer编码器，预训练后生成互补用户嵌入，由下游排序器联合使用。
result: "相比最强基线，归一化熵降低0.38%，线上A/B测试显示离线转化率预测精度一致提升。"
conclusion: DUET通过领域特定的双用户嵌入预训练，有效处理了点击和转化信号的异质性，在严格延迟约束下提升了离线转化率预测。
---

## 摘要
站外转化率（OCVR）预测是计算推荐系统中的一个重要排序问题。该任务面临建模挑战：点击信号丰富且时间范围短，而转化信号本质上稀疏、延迟长且常无法归因。尽管存在这些统计差异，两种信号类型都必须服务于在严格服务延迟限制内运行的模型。先前的预训练方法通过单一、无差异的编码器统一处理这两个数据流。我们提出DUET（双用户嵌入变换器），这是一个将用户行为数据明确划分为两个领域一致的数据流——点击和转化——的框架，并针对每个数据流的统计特性预训练专用变换器编码器：多层自注意力用于密集点击流，交错交叉和自注意力用于稀疏转化流。生成的互补嵌入由下游排序器联合使用，且不超出服务延迟预算。评估表明，相对于最强基线，归一化熵（NE）最多降低0.38%，A/B测试显示OCVR预测准确性持续提升。

## Abstract
Offsite conversion rate (OCVR) prediction is an important ranking problem in computational recommendation systems. This task presents a modeling challenge: click signals are abundant and exhibit short temporal horizons, whereas conversion signals are inherently sparse, long-delayed, and frequently unattributed. Despite these statistical disparities, both signal types must inform models that operate within strict serving-latency constraints. Prior pre-training approaches address this heterogeneity with a single, undifferentiated encoder applied uniformly across both data streams. We propose DUET (Dual User Embedding Transformers), a framework that explicitly partitions user behavioral data into two domain-coherent streams -- clicks and conversions -- and pre-trains dedicated transformer encoders with architectures tailored to each stream's statistical characteristics: multi-layer self-attention for the dense click stream and interleaved cross- and self-attention for the sparse conversion stream. The resulting complementary embeddings are jointly consumed by a downstream ranker without exceeding serving-latency budgets. Evaluation demonstrates up to 0.38% normalized entropy (NE) reduction relative to the strongest baseline, and A/B test shows consistent improvements in OCVR prediction accuracy.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义
- **研究动机**：站外转化率（OCVR）预测是推荐系统中的重要排序问题，但面临点击信号密集、转化信号稀疏、延迟长且难以归因（归因率常低于5%）的统计异质性挑战。现有预训练方法采用单一无差别的编码器处理所有行为数据，导致三个问题：①点击信号主导，转化模式被低估；②架构不匹配不同统计特性的数据流；③跨域知识迁移浅层。
- **核心目标**：在严格在线服务延迟约束下，通过显式分离点击与转化数据流并设计专用编码器，提升OCVR预测精度。

#### 2. 论文提出的方法论
- **核心思想**：将用户行为数据划分为两个领域一致的数据流（点击/站内转化流 与 站外转化流），分别预训练专用Transformer编码器，生成互补的ClickAUE和ConvAUE嵌入，由下游排序器以冻结方式集成。
- **关键技术细节**：
  - **领域感知数据路由**：点击/站内转化流（密集、短窗口）下采样负样本保留全正样本；站外转化流（稀疏、长窗口）不降采样，并引入合成数据（基于未归因转化推断最佳排名）作为弱监督。
  - **输入表示**：使用事件特征序列（EBF），包含时间戳、ID属性（物品/作者/媒体类型）、语义ID（通过KNN或RQ-VAE从内容导出），跨应用（广告交互、有机订阅、视频内容）。
  - **编码器架构**：共享DLRM主干（序列分支+非序列分支），序列分支内每个事件类型由专用事件塔（含Transformer块）处理，输出经DCN模块汇总为用户嵌入。点击流采用多层自注意力（LLaTTE范式，2层、2头、d=256）；转化流采用交错交叉注意力和自注意力（1层、8头、d=128），利用静态特征对序列表示进行正则化，降低过拟合和计算成本。
  - **预训练目标**：ClickAUN用多任务BCE损失（CTR+站内CVR），权重调优；ConvAUN用多任务BCE损失（归因OCVR+未归因OCVR）。
  - **下游集成**：冻结的ClickAUE和ConvAUE作为额外输入特征，下游模型无架构修改，多任务训练（主OCVR+辅助任务）。
  - **推理服务**：事件触发推理（ETI）异步生成嵌入，经SIDE量化压缩（4倍压缩），解码后由可学习MLP投影后输入下游模型。

#### 3. 实验设计
- **数据集与场景**：使用Meta内部广告推荐系统数据，针对6个不同下游OCVR模型（不同排名阶段、优化目标、流量特征），在离线评估（NE）和线上A/B测试（CVR lift）上进行验证。
- **基准方法**：基线为无预训练嵌入的下游排序器；对比配置包括仅ClickAUE、仅ConvAUE、DUET（两者结合）。未与对比学习/掩码预训练方法直接比较，因其目标函数不同。
- **对比方法**：未公开提及具体第三方方法，主要对比自身变体。计算了理论正交上限（0.51% NE下降）作为性能上界。

#### 4. 资源与算力
- **训练资源**：ClickAUN和ConvAUN均使用128块NVIDIA H100 GPU，离线训练速度约200K QPS。
- **训练时长**：文中未明确说明具体训练轮数或总时间。
- **说明**：仅提及足够支持持续训练和及时部署，未提供完整算力消耗明细。

#### 5. 实验数量与充分性
- **实验组数**：
  - 主结果：1张表（表1）报告单一下游模型上训练NE增益（4种配置）。
  - 跨模型验证：图6展示6个下游模型的评估NE增益（每条柱为不同配置）。
  - 消融实验：表2包含ConvAUE的4项消融（合成数据、注意力层数、用户旅程事件、物品曝光事件）和ClickAUE的3项消融（架构缩放、物品曝光事件、页面事件），分别报告预训练NE和下游NE损失。
  - 线上A/B测试：2个下游模型（Ranker 3和Ranker 4），统计显著。
- **充分性评估**：实验覆盖了不同场景和模型变体，消融实验系统探究了关键组件贡献，线上验证了离线增益可迁移。但仅基于Meta单一平台内部数据，缺乏公开数据集复现，且基线未包含最新跨域推荐方法，存在一定边际偏差。

#### 6. 论文的主要结论与发现
- DUET相比最强基线实现0.38% NE降低，恢复理论正交上限（0.51%）的约75%。
- ClickAUE和ConvAUE嵌入在PCA投影中占据不相交区域，余弦距离近似均匀分布，表明二者编码互补且近正交的信息。
- 六个下游模型均一致受益于DUET，增益幅度因模型而异（0.10%~0.36%），且ClickAUE的边际贡献（0.04%~0.21%）不为常数。
- 消融显示：用户旅程事件特征对ConvAUE最关键（0.30% PT NE损失）；架构缩放对ClickAUE最重要（0.13% PT NE损失）；合成数据贡献中等（0.13% PT NE），低传输比（23%）但仍有效。
- A/B测试中Ranker 3获得+0.66% CVR lift，Ranker 4获得+0.15% CVR lift，均统计显著。

#### 7. 优点
- **领域专用架构匹配**：针对点击流和转化流的不同统计特性（密度、窗口、稀疏性），分别采用自注意力和交错交叉-自注意力，消融验证了选择的有效性。
- **异步推理解耦**：事件触发推理（ETI）将嵌入生成与在线排名解耦，嵌入量化（SIDE）实现4倍压缩且NE损失可忽略，满足严格延迟预算。
- **跨应用输入丰富**：融合有机订阅、内容语义ID等异构特征，扩展了用户表示知识源。
- **理论框架**：证明分离模型与联合模型在线性假设下近似等价，同时提供架构灵活性和运维优势。
- **充分实验验证**：包含离线评估、多模型验证、消融分析和线上A/B测试，结论稳健。

#### 8. 不足与局限
- **数据路由静态规则**：当前基于标签语义和窗口硬划分，未探索学习式多流分区。
- **注意力配置未全面对比**：仅验证了所选配置（自注意力 vs. 交叉+自注意力），未测试其他分配（如转化流用纯自注意力、点击流用混合注意力）的可行性。
- **嵌入冻结限制**：下游训练时嵌入固定，可能无法适应线上数据分布漂移；论文建议通过适配器层轻量调优作为未来工作。
- **可复现性有限**：实验完全基于Meta内部数据，未公开数据集或代码，其他团队难以直接复现或公平比较。
- **域外泛化未知**：仅针对广告推荐场景，未验证在非广告推荐（如电商、视频）上的有效性。
- **消融PT→DR传输比差异**：数据级变更传输比（23%）低于架构级（30%~40%），提示高层修改对下游影响更直接，但未深入机制分析。

### 附录补充
论文附录证明了在假设线性交互近似下，分离上游模型与联合模型等价，支持双嵌设计合理性。

（完）
