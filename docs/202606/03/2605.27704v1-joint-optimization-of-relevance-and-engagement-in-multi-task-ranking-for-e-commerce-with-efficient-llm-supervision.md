---
title: Joint Optimization of Relevance and Engagement in Multi-Task Ranking for E-Commerce with Efficient LLM Supervision
title_zh: 高效LLM监督下电商多任务排序中相关性与参与度的联合优化
authors: "Luming Chen, Jiaqi Xi, Raghav Saboo, Kenny Chi, Martin Wang, Sudeep Das, Danny Nightingale, Aditya Dodda, Elyse Winer, Akshad Viswanathan"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.27704v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 大模型监督用于电商搜索多任务排序
tldr: "本文针对电商搜索排序中仅优化用户行为信号导致的偏差问题，提出一个多任务排序系统，将语义相关性作为主要优化目标。系统采用有序回归的相关性预测头，并与用户行为信号通过统一价值模型融合，实现可控制的相关性-参与度权衡。利用微调轻量级大模型生成三级相关性标签，高效标注超亿级数据。离线指标NDCG@10和在线实验表明，该方法显著提升语义对齐效果且不损害核心参与目标。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27704v1/fig-001.webp\", \"caption\": \"Figure 1: Multi-task ranking architecture with unified relevance–engagement optimization\", \"page\": 3, \"index\": 1, \"width\": 456, \"height\": 363}, {\"url\": \"assets/figures/arxiv/2605.27704v1/fig-002.webp\", \"caption\": \"Table 2: Online experiment results\", \"page\": 4, \"index\": 2, \"width\": 485, \"height\": 446}]"
motivation: 现有排序模型偏向热门或价格锚定商品，忽视语义意图，需要引入可控的相关性-参与度权衡。
method: 构建多任务排序架构，包含有序回归相关性预测头与行为预测头，通过统一价值模型融合，并利用轻量级LLM生成三级相关性标签进行监督。
result: "离线NDCG@10和在线A/B实验显示语义对齐显著提升，同时保持核心参与目标。"
conclusion: 该方法能有效平衡语义相关性与用户行为信号，适用于工业级电商排序系统。
---

## 摘要
仅针对用户参与信号优化工业搜索排序模型通常会引入系统性偏差，优先展示可能不满足语义意图的热门或价格锚定商品。我们提出了一个生产级多任务排序系统，将语义相关性作为主要优化目标，实现显式且可控的相关性-参与度权衡。我们的架构采用序数相关性头部，预测相关性阈值上的累积概率，保留标签的内在顺序。这些输出通过统一的价值模型评分函数与参与度头部集成，实现语义质量与短期行为信号的系统平衡。为了为此多任务框架提供高质量监督，我们利用微调后的轻量级大语言模型（LLM）生成三级序数相关性标签：不相关、中等相关和高度相关。我们解决了标签分布敏感性问题，并确保与人工标注高度一致，以实现对超过1亿个查询-商品对的高效标注。在包括NDCG@10在内的离线指标和在线A/B实验中的评估表明，我们的方法在保持核心参与目标的同时，显著提升了语义对齐度。

## Abstract
Optimizing industrial search ranking models solely for user engagement signals often introduces systematic biases, prioritizing popular or price-anchored items that may not satisfy semantic intent. We present a production-scale multi-task ranking system that integrates semantic relevance as a primary optimization objective, enabling explicit and controllable relevance-engagement trade-offs. Our architecture employs an ordinal relevance head that predicts cumulative probabilities over relevance thresholds, preserving the inherent ordering of labels. These outputs are integrated with engagement heads through a unified value model scoring function, enabling systematic balancing of semantic quality and short-term behavioral signals. To provide high-quality supervision for this multi-task framework, we utilize fine-tuned lightweight Large Language Models (LLMs) to generate three-level ordinal relevance labels: irrelevant, moderately relevant, and highly relevant. We address challenges regarding label distribution sensitivity and ensure high alignment with human annotations to enable efficient labeling for over 100 million query-item pairs. Evaluation across offline metrics, including NDCG@10, and online A/B experiments demonstrates that our approach significantly improves semantic alignment while preserving core engagement objectives.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：工业级电商搜索排序模型通常仅针对用户参与信号（如点击率CTR、加购率ATCR、转化率CVR）进行优化，这会导致系统性偏差：热门商品、价格锚定商品或仅具有词汇重叠的商品被过度提升，即使它们并不满足用户的语义意图。长期来看，这会降低搜索质量感知、增加查询重写率、侵蚀用户信任。
- **核心挑战**：生产规模下缺乏高质量语义相关性监督。人工标注昂贵、缓慢且难以维护（动态目录、长尾查询）。行为信号虽丰富但受位置偏差、曝光偏差和反馈循环影响。
- **研究意义**：本文提出一种高效利用LLM生成大规模序数相关性标签，并直接将其作为多任务排序的第一类优化目标的方法，实现了在保持核心业务指标的同时显著提升语义对齐度。该工作展示了LLM监督在工业搜索中的可操作化路径。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- 构建一个多任务深度神经网络（DNN）排序架构，共享底层表示，分别预测CTR、ATCR、CVR和语义相关性。
- 相关性采用**三级序数标签**（0=不相关，1=中等相关，2=高度相关），通过**序数回归头**（ordinal relevance head）预测累积概率 \( p(r \ge 1) \) 和 \( p(r \ge 2) \)。
- 利用微调后的轻量级LLM（gpt-4o-mini）离线生成超过1亿条查询-商品对的相关性标签。
- 最终排序分数通过**统一价值模型评分函数**综合行为预测和相关性分数，实现显式可调的相关性-参与度权衡。

### 关键技术细节
1. **LLM标签生成流程**（参照文献[16]）：
   - 收集约60万条人工标注作为ground truth。
   - 对人工标注进行审计：将人工标签为0但行为信号（ATCR、CVR）排名前50%的样本，使用更大LLM（GPT-4o、o3）重新评估；利用Query-to-Taxonomy (Q2T) 模型解决冲突，最终修正标签。
   - 在审计后的标注上微调gpt-4o-mini，并在10.5万条测试集上达到三分类准确率89%，within-1准确率98%。
   - 利用该轻量模型对超过1亿查询-商品对进行离线大规模标注。

2. **序数相关性头**：
   - 输出两个概率：\( \hat{p}(r \ge 1) \) 和 \( \hat{p}(r \ge 2) \)。
   - 最终标量相关性得分：\( \hat{s}_{rel} = \hat{p}(r \ge 1) + \hat{p}(r \ge 2) \)，等价于期望等级 \( E[r] \)。

3. **统一价值模型评分函数**：
   \[
   S(q,i) = \alpha \hat{y}_{CTR} + \beta \hat{y}_{ATCR} + \gamma \hat{y}_{CVR} + (1-\alpha-\beta-\gamma) \hat{s}_{rel}
   \]
   - 超参数 \(\alpha,\beta,\gamma \in [0,1]\) 控制各目标权重，可在线调优。

4. **模型架构效率**：
   - 共享底层（高基数嵌入查找是主要计算成本）。
   - 相关性任务复用已有特征，无需额外特征提取。
   - 单次前向传播输出所有目标，避免多次服务调用。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：200万查询-商品对，相关性标签由微调LLM生成（基于60万人工标注审计后获得）。
- **离线指标**：AUC用于参与预测，NDCG@10用于排序质量和相关性评估。
- **对比方法**：
  - **MTL-Engagement**：仅优化CTR、ATCR、CVR的标准多任务模型。
  - **MTL-Softmax**：使用三分类softmax头进行相关性分类的多任务模型。
  - **MTL-Regression**：将相关性等级视为连续值、使用MSE损失回归的多任务模型。
  - **Ours (Auxiliary Only)**：使用序数头但仅在训练时作为辅助任务，推理时不使用相关性分数。
  - **Ours (Ordinal)**：本文完整方案，序数头并直接用于价值模型。

- **在线实验**：生产环境A/B测试，持续三周，统计显著性p<0.05。
  - 指标：ATCR、CVR、总订单价值（GOV）。

## 4. 资源与算力

- 论文**未明确说明**所使用的GPU型号、数量、训练时长等硬件资源细节。
- 仅提及微调的是gpt-4o-mini（OpenAI提供的轻量模型），但未报告OpenAI调用成本或本地部署的算力需求。
- 离线LLM标注涉及多次调用（GPT-4o、o3用于审计，gpt-4o-mini用于大规模生成），但未给出具体计算量估计。
- 整体训练和推理的计算开销被描述为“可忽略的额外代价”，主要因为共享结构和特征复用。

## 5. 实验数量与充分性

- **离线实验**：主表（Table 1）对比了5种方法在点击、加购、转化三个参与任务的AUC和NDCG@10，以及相关性NDCG@10。实验覆盖了主要对比和消融。
- **消融分析**：包含“辅助任务only”变体，证明仅将相关性作为辅助任务而不用于最终分数效果有限。
- **权衡分析**（Figure 2）：通过改变相关性权重绘制相关性与转化NDCG的trade-off曲线，比较序数、softmax、回归三种公式。
- **在线实验**：一个A/B实验，报告了三项指标的相对提升幅度和p值。
- **充分性评价**：
  - 离线实验数量适中，对比设计合理，消融充分，但缺少超参数敏感性分析（如\(\alpha,\beta,\gamma\)的具体取值及对结果的影响）。
  - 仅提供了单个在线实验结果，未展示多组不同权重配置的在线对比。
  - 未涉及长尾查询、冷启动物品等特定场景的详细分析。

## 6. 主要结论与发现

1. **相关性可无损集成**：所有包含相关性头的模型在参与指标上均与纯参与基线持平，证明语义优化不损害核心业务目标。
2. **序数公式最优**：本文提出的序数头在相关性NDCG@10上达到0.962，优于softmax（0.961）和回归（0.959），且trade-off曲线更优。
3. **直接用于排序比仅辅助训练更有效**：“辅助任务only”变体相关性NDCG仅0.823，远低于直接参与排序的0.962，说明将相关性纳入价值模型是必要的。
4. **在线收益显著**：A/B实验显示ATCR提升1.16%、CVR提升1.10%、GOV提升0.50%，p值均<0.05，验证了离线假设。
5. **权衡可控**：通过调节价值模型权重，可实现语义质量与参与度的灵活折中。

## 7. 优点：方法与实验设计亮点

- **方法层面**：
  - 将LLM监督与多任务排序有机结合，实现端到端的可操作化，无需在线调用LLM。
  - 序数回归头的设计自然保留了相关性等级顺序，且概率输出易于校准和阈值设定。
  - 标签审计流程（人类标注 → 冲突检测 → Q2T模型辅助裁决 → LLM微调）有效保证了大规模标签质量。
  - 共享底层架构保证了工业级效率。
- **实验层面**：
  - 离线+在线实验相结合，线上统计显著，确认了实际业务价值。
  - 提供了trade-off分析，展示了方法的灵活性。
  - 消融实验清晰分离了不同设计组件（辅助vs直接使用、不同标签公式）的贡献。

## 8. 不足与局限

- **实验覆盖**：
  - 仅在一个数据集（200万条）上进行离线评估，未报告跨数据集或跨领域的泛化性。
  - 在线A/B实验仅呈现一个权重配置的结果，未展示不同权衡下的线上表现。
  - 未对长尾查询、冷启动物品、新商家等边缘场景做专门分析。
- **偏差风险**：
  - LLM标签依赖GPT-4o-mini，其本身可能存在与人工标注的系统偏差（虽通过审计部分缓解，但审计使用更大LLM同样有偏差风险）。
  - 标签分布敏感性问题虽被提及，但未展示具体的分布统计和采样策略细节。
  - 在线实验周期仅三周，可能存在置信区间宽泛或季节性影响。
- **应用限制**：
  - 该方法需要大量人工标注（60万条）作为基础，对于小型平台成本较高。
  - 需要访问商业LLM API（GPT-4o、o3）用于审计，存在成本与延迟问题。
  - 价值模型的超参数需要离线调优和在线验证，调优过程未详细说明。
- **信息缺失**：
  - 未报告模型参数量、推理延迟、训练时间等工程细节。
  - 未提供LLM微调的具体超参数和计算开销。
  - 未公开代码或数据，可复现性受限。

（完）
