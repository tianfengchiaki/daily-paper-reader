---
title: "SIREN: Unified Multi-Granularity Semantic Interaction for Multi-Modal Lifelong User Interest Modeling"
title_zh: SIREN：统一多粒度语义交互的多模态终身用户兴趣建模
authors: "Yaqian Zhang, Ruyi Yu, Tianyi Li, Bohan Liu, Maoquan Ye, Ke Wang, Shifeng Wen, Junwei Pan, Lijie Wang, Qi Zhou, Yeshou Cai, Chengguo Yin, Lifeng Wang, Hui Li, Lei Xiao, Haijie Gu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25726v1"
tags: ["query:q6"]
score: 9.0
evidence: 提出统一的多粒度语义交互框架用于工业推荐中的多模态长期用户兴趣建模
tldr: "针对多模态特征与协同特征的对齐难题，提出SIREN统一多粒度语义交互框架，通过多模态相似度软检索和SemID硬检索实现通用搜索，结合粗粒度相似度桶和细粒度前缀编码SemID进行精确目标感知交互。离线SOTA GAUC，在线微信场景GMV提升2.28%/3.87%/1.61%，已全量部署。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25726v1/fig-001.webp\", \"caption\": \"Figure 2: CTR distributions of Semantic ID groups within target–behavior similarity buckets. Although pairs in the same bucket have similar multi-modal proximity to the target item, their CTRs vary substantially across Semantic ID groups, especially in high-similarity regions. This withinbucket heterogeneity shows that similarity buckets provide only coarse target-aware relevance, motivating the use of fine-grained SemIDs to preserve semantic and collaborative distinctions.\", \"page\": 2, \"index\": 1, \"width\": 529, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2605.25726v1/fig-002.webp\", \"caption\": \"Figure 3: Overview of SIREN. SIREN follows the two-stage GSU–ESU paradigm. The GSU retrieves target-relevant behaviors via similarity-based soft retrieval or SemID-based hard retrieval. The ESU incorporates SemIDs and similarity buckets as side information for unified target-conditioned sequence modeling.\", \"page\": 3, \"index\": 2, \"width\": 519, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.25726v1/fig-003.webp\", \"caption\": \"Table 1: Overall offline performance comparison on GAUC. The best result is highlighted in bold.\", \"page\": 5, \"index\": 3, \"width\": 508, \"height\": 248}]"
motivation: 现有方法将多模态序列与行为序列分离建模并晚融合，导致多模态表示粗粒度、集成有限。
method: 通用搜索单元采用多模态相似度软检索和SemID硬检索；精确搜索单元通过粗粒度相似度桶与细粒度前缀编码SemID实现目标感知交互。
result: "离线数据集达GAUC最佳，在线A/B测试中微信朋友圈/公众号/视频号GMV分别提升2.28%/3.87%/1.61%。"
conclusion: SIREN框架有效融合多模态信息，已成功部署于腾讯广告平台全流量服务。
---

## 摘要
工业推荐系统越来越多地利用用户终身行为历史和丰富的多模态内容来捕捉不断变化的用户偏好。然而，由于多模态空间与协同空间之间的固有错位，将多模态特征有效整合到终身兴趣建模中仍然具有挑战性。现有范式通常依赖于多模态序列和行为序列的单独建模，并通过后期融合来缓解模态差异，这导致了粗粒度的多模态表示和有限的集成。在本文中，我们提出SIREN，一个统一的多粒度语义交互框架，用于多模态终身用户兴趣建模。在通用搜索单元阶段，我们引入了两种替代检索策略：基于多模态相似性的软检索以提高检索效果，以及基于语义ID（SemID）的硬检索以实现高效的工业服务。在精确搜索单元阶段，我们通过粗粒度相似度桶和细粒度前缀编码的SemID显式融入目标感知相关性，从而在目标条件Transformer架构中实现与协同ID特征的统一交互。在离线数据集上的大量实验表明，SIREN达到了最先进的GAUC。在线A/B测试进一步证明了在多个生产场景中的一致GMV提升，包括微信朋友圈+2.28%、微信公众号+3.87%和微信视频号+1.61%。自2025年7月起，SIREN已在腾讯广告平台全面上线，服务全流量。

## Abstract
Industrial recommender systems increasingly leverage lifelong user behavior histories and rich multi-modal content to capture evolving user preferences. However, effectively integrating multi-modal features into lifelong interest modeling remains challenging due to the inherent misalignment between multi-modal and collaborative spaces. Existing paradigms typically rely on separate modeling of multi-modal sequence and behavior sequence, and late fusion to alleviate the modality gap, which results in coarse-grained multi-modal representation and limited integration. In this paper, we propose SIREN, a unified multi-granularity semantic interaction framework for multi-modal lifelong user interest modeling. In the General Search Unit stage, we introduce two alternative retrieval strategies: multi-modal similarity-based soft retrieval for retrieval effectiveness, and Semantic ID (SemID)-based hard retrieval for efficient industrial serving. For the Exact Search Unit stage, we explicitly incorporate target-aware relevance via coarse similarity buckets and fine-grained prefix-encoded SemIDs, enabling unified interaction with collaborative ID features within the target-conditioned transformer architecture. Extensive experiments on the offline dataset demonstrate that SIREN achieves a state-of-the-art GAUC. Online A/B tests further demonstrate consistent GMV gains across multiple production scenarios, including +2.28% in Weixin Moments, +3.87% in Weixin Official Accounts, and +1.61% in Weixin Channels. From July 2025, SIREN has been fully launched for full-traffic serving in Tencent's advertising platform.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **背景**：工业推荐系统依赖用户终身行为序列捕捉动态偏好，并引入多模态内容（如图像、文本）以增强表示。然而，多模态预训练嵌入与协同 ID 特征之间存在明显的**空间错位**，直接融合会引入噪声。
- **核心问题**：现有主流范式将多模态序列与行为序列**分离建模**，仅在序列聚合后进行**晚期融合**，导致多模态表示粗糙、交互不充分；同时，目标-行为相似度仅提供粗粒度相关性，忽略了协同空间中的语义异质性。
- **整体含义**：本文提出 SIREN 框架，旨在通过**统一的多粒度语义交互**，实现多模态特征与协同特征的**早期融合**，从而更精准地建模用户终身兴趣。

### 2. 论文提出的方法论：核心思想、关键技术细节

**核心思想**：在工业常用的两阶段范式（GSU-ESU）中，将多模态信息以不同粒度（粗粒度的相似度桶、细粒度的 SemID）注入检索与精确建模阶段，实现统一的、目标条件的序列建模。

**关键技术细节**：

- **多模态特征构建**：
  - **Semantic ID（SemID）**：使用 RQ-VAE 将每项的多模态嵌入离散化为层次化代码序列（如 [c1, c2, ...]），再通过**前缀编码**（prefix encoding）生成多粒度 token 序列，映射为可学习嵌入 \( e_{\text{sem}} \)。
  - **相似度桶**：计算行为与目标的多模态余弦相似度 \( s_{i,t} \)，离散化为 \( B \) 个桶索引，并映射为嵌入 \( e_{\text{Sim}} \)。

- **通用搜索单元（GSU）**：
  - **相似度软检索**：基于余弦相似度从全量行为序列中检索 Top-K 行为。
  - **SemID 硬检索**：基于目标顶层 SemID（c1）建立倒排索引，快速匹配相同顶层编码的行为，实现近常数时间检索，降低在线延迟与存储。

- **精确搜索单元（ESU）**：
  - **统一物品表示**：每个行为表示为 \( h_i = \text{Concat}(e_{\text{id}}^i, e_{\text{sem}}^i) \)，目标类似。
  - **目标条件 Transformer**：使用多头目标注意力，将 \( e_{\text{Sim}} \) 作为侧信息拼接至 query 与 key/value，得到用户兴趣 \( u_t \)。
  - **目标条件交互**：引入元素级乘法 \( \tilde{u}_t = u_t \odot h_t \)，捕捉细粒度相关性，最终用于 CTR 预测。

### 3. 实验设计：数据集、场景、基准和对比方法

- **离线数据集**：**Taobao-MM**（99M 交互样本，8.79M 用户，35.4M 物品，每用户序列≤1000），含 128 维预训练多模态嵌入。
- **离线基准方法**：DIN、SIM-Hard、SIM-Soft、TWIN、MISS、MUSE，以及 SIREN 的两个变体（SemID-GSU、Sim-GSU）。
- **在线场景**：腾讯微信广告系统三大场景——Weixin Moments、Weixin Official Accounts、Weixin Channels，生产基线为 SIM-Hard。
- **评估指标**：离线 GAUC，在线 GMV 提升。

### 4. 资源与算力

论文**未明确报告**所使用的 GPU 型号、数量及训练时长。仅提及：
- 训练配置：单个 epoch，batch size=1000，使用 AdamW（lr=2e-4）和 SparseAdam（lr=2e-3）。
- 在线部署规模：微信广告平台每日处理数十亿广告请求，全流量部署。

### 5. 实验数量与充分性

- **实验数量**：包括 1 组整体离线对比（8 种方法）、1 组消融实验（4 种变体）、表示判别性分析（含 4 个子图）、条件熵分析、桶粒度分析，以及 1 组在线 A/B 测试（含细分冷启分析）。
- **充分性与公平性**：
  - 消融实验逐步添加组件，验证了 SimBucket、SemID、TI 的贡献。
  - 离线对比中统一了 GSU 检索长度（Top-50）和训练 epoch，确保公平。
  - 在线 A/B 测试覆盖不同场景和管道阶段，并进行了冷启细分分析。
  - 实验覆盖了主效果验证、组件分析、表示质量、粒度必要性和实际部署效果，**较为充分**。

### 6. 论文的主要结论与发现

- **SIREN 达到离线 SOTA**：GAUC 0.6155，相对提升 +2.48%，优于 MUSE 等强基线。
- **在线 GMV 显著提升**：微信朋友圈 +2.28%、公众号 +3.87%、视频号 +1.61%。
- **SemID 硬检索高效可用**：近 90% 的延迟和存储节省，性能与软检索相当。
- **SimBucket 与 SemID 互补**：SimBucket 提供粗粒度目标相关性，SemID 保留细粒度语义/协同异质性，两者结合优于任一单独使用。
- **冷启效果突出**：低活跃用户和冷启新广告的 GMV 提升整体平均值的 1.7~3.6 倍。

### 7. 优点（方法或实验设计上的亮点）

- **统一早期融合**：将多模态信息以项级粒度融合进序列建模，而非晚期聚合，使协同与语义特征充分交互。
- **双检索策略**：软检索保证效果，硬检索保证效率，为在线部署提供灵活选择。
- **多粒度语义交互**：粗粒度相似度桶与细粒度 SemID 互补，前者捕捉目标相关性，后者捕捉语义结构，共现增强表示判别力。
- **表示质量分析**：利用互信息量化表示判别力，并验证目标条件交互和侧信息组合的增益，设计新颖。
- **大规模在线验证**：在腾讯广告全流量部署，多个场景一致性提升，论证充分。

### 8. 不足与局限

- **算力透明度不足**：未提供 GPU 数量、型号及训练时间，不利于复现和成本评估。
- **离线数据单一**：仅使用 Taobao-MM 一个离线数据集，缺乏跨域或更多公开数据集的验证。
- **未对比端到端长序列模型**：如 LONGER（RecSys 2025）等直接处理 10k 序列的方法，讨论“现有范式”时未将其纳入对比。
- **SemID 依赖量化质量**：RQ-VAE 的量化层数 M 和前缀深度 K 未做充分消融，可能影响泛化。
- **冷启分析粒度有限**：仅按用户活跃度和广告新度分组，未分析物品模态缺失等更细粒度的冷启场景。
- **在线效果偏长期短期混合**：A/B 测试 14 天，未报告长期稳定性或周期间波动。

（完）
