---
title: DUET -- Dual User Embedding Transformers for Offsite Conversion Prediction
title_zh: DUET —— 用于站外转化预测的双用户嵌入Transformer
authors: "Reazul Hasan Russel, Mingwei Tang, Rostam Shirani, Xinlong Liu, Navid Madani, Leo Ding, Yawen He, Xiangyu Wang, Mustafa Acar, Ashish Katiyar, Yuhai Li, Alan Yang, Metarya Ruparel, Derek Qiang Xu, Rupert Wu, Rui Yang, Liang Tao, Xinyi Zhao, Larry Zhang, Sri Reddy, Rob Malkin"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10243v1"
tags: ["query:q2"]
score: 9.0
evidence: 面向广告的站外转化预测
tldr: "针对离线转化率预测中点击信号丰富、转化信号稀疏且延迟的异质性问题，现有单一编码器预训练方法效果有限。本文提出DUET框架，将用户行为分为点击和转化两个域，分别使用多层自注意力Transformer和交错交叉自注意力Transformer进行预训练，生成互补嵌入供下游排序器使用。实验表明，该方法在归一化熵上比最强基线降低0.38%，在线A/B测试持续提升预测精度。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10243v1/fig-001.webp\", \"caption\": \"Table 2 Ablation study on the upstream encoders.\", \"page\": 11, \"index\": 1, \"width\": 632, \"height\": 251}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-002.webp\", \"caption\": \"Figure 3 End-to-end system: recurring pre-training with checkpoint validation followed by SIDE-based embedding compression and finally event-triggered inference and serving.\", \"page\": 7, \"index\": 2, \"width\": 968, \"height\": 481}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-003.webp\", \"caption\": \"Figure 6 Eval NE gains (%) on Different Types and Stages of downstream rankers. Higher is better.\", \"page\": 10, \"index\": 3, \"width\": 627, \"height\": 460}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-004.webp\", \"caption\": \"Figure 1 DUET unified framework: data routing, representation pre-training, and downstream ranker.\", \"page\": 4, \"index\": 4, \"width\": 894, \"height\": 300}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-005.webp\", \"caption\": \"Figure 4 Principal components of quantized user embeddings. ConvAUE and ClickAUE occupy distinct regions, indicating non-redundant information. Figure5 Orthogonality between ConvAUE and ClickAUE. Degree of complementarity varies, higher orthogonality indicating greater potential for additive information gain.\", \"page\": 9, \"index\": 5, \"width\": 977, \"height\": 334}, {\"url\": \"assets/figures/arxiv/2606.10243v1/fig-006.webp\", \"caption\": \"Figure 2 DUET architecture. Left: the DLRM backbone with parallel sequence and non-sequence branches. Right: event tower structure with stacked attention blocks and DCN user summarization.\", \"page\": 5, \"index\": 6, \"width\": 968, \"height\": 518}]"
motivation: 现有预训练方法用单一编码器统一处理点击和转化两种统计特性迥异的数据，忽视了二者的异质性，导致模型无法充分捕捉稀疏转化信号。
method: 将用户行为数据分为点击和转化两个域，分别用多层自注意力Transformer处理密集点击流、用交错交叉和自注意力Transformer处理稀疏转化流，生成互补的用户嵌入。
result: "离线评估中相对最强基线降低0.38%归一化熵，在线A/B测试持续提升OCVR预测准确率。"
conclusion: 通过为不同统计特性的行为数据设计专用编码器并联合使用，DUET有效提升了离线转化率预测性能，同时满足了服务延迟约束。
---

## 摘要
站外转化率（OCVR）预测是计算推荐系统中一个重要的排序问题。该任务存在建模挑战：点击信号丰富且时间跨度短，而转化信号本质稀疏、延迟长且常无法归因。尽管存在这些统计差异，两种信号都必须服务于在严格服务延迟约束下运行的模型。现有的预训练方法采用单一、无区分的编码器统一处理两种数据流。我们提出DUET（双用户嵌入Transformer）框架，将用户行为数据显式划分为两个领域相干流——点击流和转化流——并针对每种流的统计特性分别预训练专有的Transformer编码器：针对密集点击流采用多层自注意力，针对稀疏转化流采用交叉注意力与自注意力交替的结构。由此得到的互补嵌入由下游排序器联合使用，且不超出服务延迟预算。评估表明，相对于最强基线，归一化熵（NE）降低可达0.38%，A/B测试显示OCVR预测准确性持续提升。

## Abstract
Offsite conversion rate (OCVR) prediction is an important ranking problem in computational recommendation systems. This task presents a modeling challenge: click signals are abundant and exhibit short temporal horizons, whereas conversion signals are inherently sparse, long-delayed, and frequently unattributed. Despite these statistical disparities, both signal types must inform models that operate within strict serving-latency constraints. Prior pre-training approaches address this heterogeneity with a single, undifferentiated encoder applied uniformly across both data streams. We propose DUET (Dual User Embedding Transformers), a framework that explicitly partitions user behavioral data into two domain-coherent streams -- clicks and conversions -- and pre-trains dedicated transformer encoders with architectures tailored to each stream's statistical characteristics: multi-layer self-attention for the dense click stream and interleaved cross- and self-attention for the sparse conversion stream. The resulting complementary embeddings are jointly consumed by a downstream ranker without exceeding serving-latency budgets. Evaluation demonstrates up to 0.38% normalized entropy (NE) reduction relative to the strongest baseline, and A/B test shows consistent improvements in OCVR prediction accuracy.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：站外转化率（OCVR）预测中，点击信号（密集、短时间窗口）与转化信号（稀疏、长延迟、常无法归因）存在显著的统计特性差异。现有预训练方法使用单一、无区分的编码器统一处理两种数据流，导致模型被密集点击信号主导，无法有效捕捉稀疏但关键的转化模式，从而限制了 OCVR 预测性能。
- **研究背景**：随着零售媒体网络扩张和第三方 Cookie 废止，平台需依赖自有行为数据进行精准转化预测；但 OCVR 任务面临标签稀疏（转化率低于 5%）、归因窗口长、部分不可归因等挑战，且模型需在严格的在线推理延迟约束下运行。这促使研究者探索解耦设计：上游模型离线预训练丰富用户嵌入，下游排序器异步消费嵌入，以平衡表达能力和延迟约束。

### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：将异构行为数据划分为统计特性相干的点击流和转化流，分别预训练专有 Transformer 编码器，生成互补的用户嵌入（ClickAUE 和 ConvAUE），供下游排序器联合使用。
- **关键技术细节**：
  - **Domain-Aware Data Routing**：将训练数据分为 CTR/站内转化流（密集、短窗口）和 OCVR 流（稀疏、长窗口；包含合成不可归因转化数据以增加训练量）。
  - **Upstream Encoder Design**：
    - 共享 DLRM 骨干架构（序列分支+非序列分支），序列分支内对每种事件类型使用专用 Event Tower。
    - ClickAUN（点击流）：采用多层自注意力（LLaTTE 范式），适合密集信号的高阶交互模式捕捉。
    - ConvAUN（转化流）：采用交叉注意力与自注意力交替的结构，利用静态特征锚定稀疏序列表示，提供隐式正则化并降低计算成本。
  - **Training Objectives**：ClickAUN 优化 CTR 和站内转化细 ECB 损失；ConvAUN 优化归因和不可归因的站外转化细 ECB 损失；均含多任务权重调优。
  - **Downstream Integration**：冻结 ClickAUE 和 ConvAUE 作为额外输入特征，下游排序器无架构修改；支持独立调度训练。
  - **Asynchronous Serving**：通过事件触发推断（ETI）异步生成嵌入，配合检查点验证、节流、SIDE 量化压缩（4 倍压缩比），确保低延迟。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集/场景**：论文基于 Meta 公司的工业推荐平台，使用实际用户行为数据（包含点击、站内转化、站外转化、有机交互等事件特征）。具体数据集规模未公开，但提及序列长度最大 1000（ClickAUN）和 200（ConvAUN）。
- **基准**：现有的下游 DLRM 排序器，不包含预训练嵌入（Baseline）。
- **对比方法**：
  - Baseline（无预训练嵌入）
  - ClickAUE Only（仅添加点击嵌入）
  - ConvAUE Only（仅添加转化嵌入）
  - DUET（同时添加两种嵌入）
  - 另外还计算理论最优上界（假设两种嵌入完全正交）
- **评价指标**：归一化熵（NE），衡量相对未归一化对数损失的改善，消除类别不平衡影响。离线报告 NE 降低百分比（% ∆NE），在线报告 OCVR Lift。

### 4. 资源与算力

- 论文明确提及：ClickAUN 和 ConvAUN 均在 **128 块 NVIDIA H100 GPU** 上训练，达到约 **200K 离线训练 QPS**（每秒查询数），足以在延迟约束下实现持续训练和及时部署。
- 未给出具体训练时长（如小时/天），也未说明下游排序器的训练资源。

### 5. 实验数量与充分性

- 实验组数：
  - **主实验**（表 1）：训练 NE 降低对比，含基线、单嵌入、DUET 及理论上限，共 4 个配置。
  - **跨模型评估**（图 6）：在 6 个不同下游 OCVR 排序器上报告评估 NE 增益，覆盖不同阶段和目标，验证泛化性。
  - **消融研究**（表 2）：对 ConvAUE 和 ClickAUE 分别进行组件级消融，包括消除合成数据、减少注意力层数、移除特定事件特征等，共 8 个子实验。
  - **嵌入分析**（图 4、图 5）：PCA 可视化和余弦距离分布，验证嵌入互补性。
  - **在线 A/B 测试**（表 3）：在 2 个下游模型上运行，报告 OCVR lift（+0.66% 和 +0.15%），统计显著性（p<0.05）。
- **充分性评估**：实验覆盖了关键维度（数据流分离、架构选择、特征贡献、在线效果），并包含与理论最优的对比。消融实验系统性地验证了每个设计选择的必要性。实验较为充分且客观，但主要基于 Meta 内部数据，未在公开数据集上验证。

### 6. 论文的主要结论与发现

- DUET 相比最强基线（Baseline）实现 **0.38% NE 降低**，接近理论上限 0.51%（恢复约 75% 的可加性增益），明显优于单独使用 ClickAUE（0.21%）或 ConvAUE（0.30%）。
- 两种嵌入在 PCA 空间中占据不同区域，余弦距离近似均匀分布，表明它们编码了非冗余的互补信息。
- 在 6 个不同下游模型上，DUET 持续优于单一嵌入，且 ClickAUE 和 ConvAUE 的贡献因模型而异（添加 lift 范围 0.04%–0.21%），证实了跨模型的泛化性。
- 消融实验显示：用户旅程事件特征对 ConvAUE 影响最大（PT NE 降 0.30%）；缩放序列架构对 ClickAUE 增益显著（0.13%）；合成数据（不可归因转化）为稀疏流提供了有价值的弱监督（贡献 0.13% PT NE）。
- 在线 A/B 测试显示统计学显著的 OCVR 提升（+0.66% 和 +0.15%），且延迟开销可忽略。

### 7. 优点

- **问题驱动的设计**：明确识别点击与转化信号的异质性，并据此设计数据路由和架构，而非盲目堆砌模型。
- **灵活性**：上游预训练与下游解耦，允许使用不同容量和架构的编码器，且下游无需修改；嵌入量化压缩保证了工业级部署可行性。
- **实验严谨性**：包括离线/在线评估、跨模型验证、消融研究、嵌入空间分析，并与理论最优比较，结论扎实。
- **实用性**：事件触发推断、检查点验证、节流等工程优化确保了实时服务中的性能和稳定性。

### 8. 不足与局限

- **静态路由**：领域分配基于手工规则（点击 vs. 转化），未考虑更细粒度的数据划分或学习路由机制。
- **架构选择未充分搜索**：注意力配置（自注意力 vs. 交叉/自注意力）仅通过消融验证，未与替代配置（如全交叉注意力、混合缩放）进行比较，可能非最优。
- **嵌入冻结**：下游训练期间嵌入固定，无法适应分布漂移；虽利于解耦，但可能损失适应能力。论文提及可引入适配层（Adapter）作为未来工作。
- **依赖专有数据**：评估完全基于 Meta 内部系统，未在公开数据集（如 Amazon、Yelp）上复现，限制了外部可复现性和通用性。
- **A/B 测试覆盖有限**：仅测试 2 个下游模型，论文解释源于实验资源限制；更广泛场景下的表现尚需验证。
- **理论等价性附录**：附录 7.2 在点积假设下推导了联合嵌入与分离嵌入的近似等价，但承认实际非线性层会引入偏差，该推导的实操意义有待进一步检验。

（完）
