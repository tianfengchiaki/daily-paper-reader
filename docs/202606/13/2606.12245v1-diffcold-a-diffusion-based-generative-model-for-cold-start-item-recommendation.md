---
title: "DiffCold: A Diffusion-based Generative Model for Cold-Start Item Recommendation"
title_zh: DiffCold：一种针对冷启动物品推荐的基于扩散的生成模型
authors: "Kangning Zhang, Yingjie Qin, Weinan Zhang, Yong Yu, Jianghao Lin"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.12245v1"
tags: ["query:q1"]
score: 9.0
evidence: 用于冷启动物品推荐的扩散模型
tldr: 针对冷启动物品推荐中的跷跷板困境（提升冷物品性能会损害热物品性能），本文提出基于扩散的生成模型DiffCold。通过条件扩散从内容特征重建热物品嵌入，保留流形结构；设计检索增强聚合器与对比学习对齐模块，统一冷热物品表示。实验表明DiffCold解决了跷跷板困境，全面超越现有方法。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.12245v1/fig-001.webp\", \"caption\": \"Table 7. The efficiency analysis of different models. The experiments are conducted on a Tesla V100 GPU card with 32 GB memory.\", \"page\": 15, \"index\": 1, \"width\": 548, \"height\": 281}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-002.webp\", \"caption\": \"Fig. 1. The seesaw dilemma for three representative cold-start recommendation methods (i.e., Heater [39], GAR [2] and ALDI [11]), where they fail to improve the performance for cold and warm items simultaneously. In contrast, our DiffCold can address the seesaw dilemma and achieve consistent improvement for all three types of task.\", \"page\": 2, \"index\": 2, \"width\": 722, \"height\": 477}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-003.webp\", \"caption\": \"Table 3. Overall, Cold and Warm recommendation performance of all models. The R@20 and N@20 mean Recall@20 and NDCG@20, respectively. The improvements are calculated by comparing DiffCold to the best baseline (underline) on each dataset.\", \"page\": 10, \"index\": 3, \"width\": 719, \"height\": 404}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-004.webp\", \"caption\": \"Fig. 5. The hyperparamter result of α1, α2 and β1, β2 on Citeulike based on MF.\", \"page\": 14, \"index\": 4, \"width\": 723, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-005.webp\", \"caption\": \"Table 5. The ablation study (Recall@20) about DiffCold.\", \"page\": 14, \"index\": 5, \"width\": 690, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-006.webp\", \"caption\": \"Fig. 4. The distribution of learned ID representations of users, warm and cold items on Movielens.\", \"page\": 13, \"index\": 6, \"width\": 558, \"height\": 399}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-007.webp\", \"caption\": \"Fig. 3. The comparison between different starting representation methods based on MF backbone.\", \"page\": 12, \"index\": 7, \"width\": 714, \"height\": 327}, {\"url\": \"assets/figures/arxiv/2606.12245v1/fig-008.webp\", \"caption\": \"Fig. 2. The overall procedure of training and inference in DiffCold. The detailed algorithm of training and inference is summarized in Algorithm 1 and 2.\", \"page\": 5, \"index\": 8, \"width\": 707, \"height\": 404}]"
motivation: 现有冷启动方法因冷热物品嵌入分布不一致（行为流形 vs 语义流形）导致跷跷板困境，需统一表示。
method: DiffCold利用条件扩散模型从内容特征重建热物品嵌入，结合检索增强聚合器初始化生成与基于对比学习的表示对齐模块。
result: 在三个基准数据集上，DiffCold所有指标均超越现有最优方法，成功消除跷跷板困境。
conclusion: 扩散生成模型可有效统一冷热物品表示，为冷启动推荐提供新范式。
---

## 摘要
冷启动物品推荐由于缺乏交互历史，在实际系统中仍然是一个持续的挑战。尽管先前的模型尝试利用物品内容特征来弥补这一差距，但它们普遍面临“跷跷板困境”：提升冷物品的性能不可避免地会降低热物品的性能，反之亦然。我们指出，这一困境源于根本性的分布差异：热物品嵌入占据了一个由丰富交互信号塑造的复杂“行为流形”，而冷物品嵌入则被限制在仅从辅助内容中导出的“语义流形”上。现有方法通常强制在这些不一致的空间之间进行刚性映射，导致模型为了适应冷物品而牺牲热物品表示的精度。为了解决这一问题，我们提出了DiffCold，一种基于扩散的生成模型，统一了冷热表示。与GAN或VAE不同，DiffCold利用条件扩散从内容中重建热物品嵌入，保留了底层的流形结构而不退化。我们进一步通过两种特定设计定制了这一范式：一种检索增强聚合器，利用语义相似的热物品初始化生成，以避免低效噪声；以及一种基于模拟的表示对齐模块，通过对比学习强制生成嵌入与真实嵌入之间的分布一致性。在三个基准上的实验证实，DiffCold解决了跷跷板困境，在所有指标上持续优于最先进的方法。

## Abstract
Cold-start item recommendation remains a persistent challenge in real-world systems due to the absence of interaction histories. While prior models attempt to bridge this gap using item content features, they universally suffer from the \textbf{seesaw dilemma}: enhancing performance for cold items inevitably degrades performance for warm items, and vice versa. We identify that this dilemma stems from a fundamental \textbf{distributional disparity}: warm item embeddings occupy a complex ``behavioral manifold" shaped by rich interaction signals, whereas cold item embeddings are constrained to a ``semantic manifold" derived solely from auxiliary content. Existing methods often force a rigid mapping between these inconsistent spaces, causing the model to sacrifice the precision of warm representations to accommodate cold ones. To address this, we propose \textbf{DiffCold}, a diffusion-based generative model that unifies warm and cold representations. Unlike GANs or VAEs, DiffCold leverages conditional diffusion to reconstruct warm item embeddings from content, preserving the underlying manifold structure without degradation. We further tailor this paradigm with two specific designs: a \textbf{Retrieval-enhanced Aggregator} that initializes generation using semantically similar warm items to bypass inefficient noise, and a \textbf{Simulation-based Representation Alignment} module that enforces distribution consistency between generated and real embeddings via contrastive learning. Experiments on three benchmarks confirm that DiffCold resolves the seesaw dilemma, consistently outperforming state-of-the-art methods across all metrics.

---

## 论文详细总结（自动生成）

# 论文总结：DiffCold — 一种针对冷启动物品推荐的基于扩散的生成模型

## 1. 核心问题与整体含义（研究动机和背景）

- **问题背景**：在推荐系统中，冷启动物品因缺乏历史交互数据而难以进行准确预测。现有方法利用物品内容特征（如文本、图像）来弥补交互缺失，但普遍存在“**跷跷板困境**”：提升冷物品推荐性能往往导致热物品性能下降，反之亦然。
- **根本原因**：论文指出这一困境源于**分布差异**——热物品嵌入由用户交互信号塑造，形成复杂的“**行为流形**”；冷物品嵌入仅从内容特征导出，形成“**语义流形**”。现有方法（如Dropout-based、生成式、对齐式）试图在这两个不一致的空间间建立刚性映射，从而牺牲其中一方的表示精度。
- **整体目标**：提出一种能够同时提升冷、热物品性能，且不互相损害的推荐框架。

## 2. 方法论

### 2.1 核心思想
- 采用**条件扩散模型**（DDPM）将热物品的ID嵌入作为目标分布进行学习，并利用内容特征作为条件指导生成过程。训练时对热物品嵌入加噪并重建，使模型学会从内容到行为空间的映射；推理时对冷物品从初始化表示开始逐步去噪，生成与热物品分布一致的新嵌入。
- 结合**检索增强聚合器**解决起始点问题，以及**基于模拟的表示对齐模块**保证分布一致性。

### 2.2 关键技术细节
1. **前向-逆向过程**：
   - 前向过程：对热物品的原始ID嵌入 \( \mathbf{e}_{i}^{0} \) 添加不同尺度的高斯噪声，得到 \( \mathbf{e}_{i}^{t} = \sqrt{\bar{\alpha}_{t}} \mathbf{e}_{i}^{0} + \sqrt{1 - \bar{\alpha}_{t}} \epsilon \)。
   - 逆向过程：通过神经网络 \( f_{\theta}(\mathbf{e}_{i}^{t}, \mathbf{c}_{i}, t) \) 预测原始嵌入 \( \hat{\mathbf{e}}_{i}^{0} \)，使用重建损失 \( \mathcal{L}_{\text{recon}} = \mathbb{E}[\| \mathbf{e}_{i}^{0} - \hat{\mathbf{e}}_{i}^{0} \|^2] \) 和BPR冷物品损失 \( \mathcal{L}_{\text{bpr-cold}} \)。
2. **检索增强聚合器**：
   - 对每个物品 \( i \)，基于内容特征余弦相似度检索前 \( k \) 个最相似的热物品，将其ID嵌入平均作为冷物品的去噪起始点 \( \bar{\mathbf{e}}_{i}^{0} = \frac{1}{|\mathcal{I}_{i}^{R}|} \sum_{j \in \mathcal{I}_{i}^{R}} \mathbf{e}_{j} \)。
3. **基于模拟的表示对齐**：
   - 在训练阶段，用热物品模拟冷物品生成过程：取热物品 \( i \)，用上述聚合器得到模拟起始点 \( \bar{\mathbf{e}}_{i}^{0} \)，再加噪、去噪得到 \( \hat{\bar{\mathbf{e}}}_{i}^{0} \)，然后通过InfoNCE对比损失 \( \mathcal{L}_{\text{sim-align}} \) 将其与真实热嵌入 \( \mathbf{e}_{i} \) 对齐。
4. **热物品增强表示学习**：
   - 同时优化热物品的BPR损失 \( \mathcal{L}_{\text{bpr-warm}} \) 和用户-热物品对比损失 \( \mathcal{L}_{\text{warm-aug}} \)，防止热物品表示退化。
5. **总体损失**：
   \[
   \mathcal{L}_{\text{overall}} = \mathcal{L}_{\text{recon}} + \alpha_{1}\mathcal{L}_{\text{bpr-cold}} + \beta_{1}\mathcal{L}_{\text{sim-align}} + \alpha_{2}\mathcal{L}_{\text{bpr-warm}} + \beta_{2}\mathcal{L}_{\text{warm-aug}}
   \]

### 2.3 算法流程（文字说明）
- **训练阶段**：从交互数据采样三元组 (u, i, j)，对正样本 i：
  1. 随机采样时间步 t，计算加噪嵌入 \( \mathbf{e}_{i}^{t} \)。
  2. 用扩散模型预测 \( \hat{\mathbf{e}}_{i}^{0} \) 并计算重建损失和BPR损失。
  3. 用聚合器生成模拟表示 \( \hat{\bar{\mathbf{e}}}_{i}^{0} \) 并计算对齐损失。
  4. 对热物品计算BPR和对比损失，更新模型参数。
- **推理阶段**：对冷物品 i：
  1. 用检索增强聚合器得到起始点 \( \bar{\mathbf{e}}_{i}^{0} \)。
  2. 通过 T 步逐步去噪（公式15）生成最终冷嵌入 \( \tilde{\mathbf{e}}_{i}^{0} \)。
  3. 用 \( \tilde{\mathbf{e}}_{i}^{0} \) 与用户嵌入交互计算推荐分数。

## 3. 实验设计

- **数据集**：三个公开数据集——MovieLens（6040用户，3706物品）、Citeulike（5551用户，16980物品）、Xing（106881用户，20519物品）。每个数据集选取20%的物品作为冷物品（交互划分为1:1验证/测试），80%为热物品（8:1:1训练/验证/测试）。
- **Backbone**：使用三种经典推荐模型作为冷/热物品嵌入的预训练基础——MF、LightGCN、SimGCL。
- **对比方法**：
  - Dropout-based：DropoutNet、Heater
  - Generative：MetaEmb、GAR、GoRec
  - Alignment-based：CLCRec、ALDI、PAD-CLRec
- **评价指标**：Recall@20 和 NDCG@20，分为Overall、Cold、Warm三个类别。

## 4. 资源与算力

- 文中仅在**效率分析**（第4.7节）中提及使用 Tesla V100 GPU 32GB 进行实验，未明确说明GPU数量、训练总时长或总资源消耗。各模型epoch级时间在表7中给出（如MF backbone上DiffCold每epoch 14.04秒，共21个epoch收敛）。整体资源需求属中等水平。

## 5. 实验数量与充分性

- **对比实验**：在3个数据集×3种backbone上，对比了8种现有方法，共9组（含DiffCold）总计27个实验条目（表3），充分覆盖冷、热、整体三类指标。
- **消融实验**：设计了5种变体（W/O Aggregator、W/O Sim-Align、W/O BPR-Cold、W/O BPR-Warm、W/O Warm-Aug），在2个数据集×2种backbone上进行验证（表5），共20个结果，能清晰反映各模块贡献。
- **超参数分析**：对α1、α2、β1、β2和扩散步数、调度器进行了详细实验（图5、表6），验证了稳健性。
- **分布一致性分析**：通过Wasserstein距离、MMD、余弦相似度等指标量化（表4），并可视化t-SNE分布（图4），增强了说服力。
- **起始点问题分析**：比较了5种不同起始策略（图3），证明了检索增强聚合器的优势。
- **效率分析**：与Heater、GAR、CLCRec、ALDI在运行内存、epoch时间、收敛epoch数上对比（表7）。
- 总体实验覆盖全面、设计合理，对比方法属于当前主流baseline，实验条件控制一致（均使用ColdRec框架实现），公正性较好。

## 6. 主要结论与发现

1. DiffCold在所有三个数据集和三种backbone上，**同时提升了冷、热、整体推荐性能**，彻底解决了跷跷板困境，而所有baseline均无法同时改善冷、热物品。
2. 关键模块中，**检索增强聚合器**对冷物品性能贡献最大（去除后Recall@20下降显著）；**模拟对齐损失**和**BPR-Cold损失**对冷物品至关重要；**BPR-Warm**和**Warm-Aug**损失则主要保护热物品性能。
3. 生成的冷物品嵌入与热物品嵌入在分布上高度一致（低Wasserstein距离、低MMD），且物品区分度好（内类相似度低）。
4. 扩散步数选择20、线性调度器为最优配置；超参数在合理范围内表现稳健。

## 7. 优点

- **问题洞察深刻**：首次将跷跷板困境归因于行为流形与语义流形的刚性耦合，并从生成模型视角提出解决方案。
- **方法创新性强**：将扩散模型引入冷启动推荐，并针对性设计了检索增强聚合器（解决起始点问题）和模拟对齐模块（解决分布不一致问题），两者协同效果好。
- **实验充分且直观**：不仅给出指标对比，还通过可视化、分布度量等方式验证表示对齐效果，可信度高。
- **效率可控**：相比某些方法（如CLCRec需210秒/epoch），DiffCold在单epoch时间上接近最轻量的Heater（14.04 vs 14.55秒），且收敛较快（21 epoch），实用性较好。

## 8. 不足与局限

- **计算资源未详细披露**：未说明训练总GPU小时数、是否多卡并行等，难以完全复现资源需求。
- **冷物品比例固定**：仅实验20%冷物品场景，未探讨不同冷比例（如5%、50%）下的表现，可能影响泛化结论。
- **对内容特征质量依赖**：检索增强聚合器依赖内容特征的余弦相似度，若内容特征质量差或语义相似性不准确，可能影响生成效果。
- **未在更大规模工业数据集上验证**：虽然使用Xing（10万用户）较大，但真实工业场景物品数可能达百万级，且行为模式更复杂，需要进一步验证。
- **扩散步数选择线性调度器**：表中余弦/二项式调度器效果明显较差，但未深入分析原因，可能存在调度器依赖。
- **未见与最新大型语言模型（LLM）或多模态方法的对比**：近年来LLM可用于冷启动，文中未涉及此类最新方法，对比基线虽全面但仍有更新空间。

（完）
