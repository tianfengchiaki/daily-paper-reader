---
title: Generative Long-term User Interest Modeling for Click-Through Rate Prediction
title_zh: 面向点击率预测的生成式长期用户兴趣建模
authors: "Jiangli Shao, Kaifu Zheng, Hao Fang, Huimu Ye, Zhiwei Liu, Bo Zhang, Shu Han, Xingxing Wang"
date: 2026-05-15
pdf: "https://arxiv.org/pdf/2605.15905v1"
tags: ["query:q1"]
score: 9.0
evidence: 针对广告点击率预测的生成式长期用户兴趣建模
tldr: 针对点击率预测中长期用户兴趣建模中现有方法因目标中心化导致兴趣特征不全面有偏、匹配检索耗时且忽略行为交互等问题，提出生成式长兴趣模型GenLI，由兴趣生成模块（IGM）产生多样化目标无关分布、行为检索模块（BRM）实现O(1)复杂度的简单查找、兴趣融合模块（IFM）通过门控机制融合特征，在提升兴趣多样性的同时避免了复杂匹配，实现了准确率与效率的更好平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.15905v1/fig-001.webp\", \"caption\": \"Figure 1: The framework of GenLI. GenLI consists of an interest generation module (IGM), a behavior retrieval module (BRM), and an interest fusion module (IFM).\", \"page\": 3, \"index\": 1, \"width\": 1056, \"height\": 392}, {\"url\": \"assets/figures/arxiv/2605.15905v1/fig-002.webp\", \"caption\": \"Figure 3: Performance of GenLI when taking different lengths of short-term behaviors as the input of interest generation module.\", \"page\": 8, \"index\": 2, \"width\": 506, \"height\": 454}]"
motivation: 现有两阶段框架因目标中心化导致兴趣特征不全面有偏，且匹配式检索耗时并忽略行为间交互。
method: GenLI包含目标无关的兴趣生成模块（IGM）生成多样化兴趣分布、简单查找的行为检索模块（BRM）实现O(1)复杂度、以及门控机制的兴趣融合模块（IFM）。
result: GenLI提升了用户兴趣多样性，避免了复杂匹配式检索，在准确率和效率间取得更好平衡。
conclusion: 通过生成式建模，GenLI有效改善了长期兴趣建模的完整性和效率。
---

## 摘要
在广告和推荐系统中，利用海量历史用户行为对长期用户兴趣进行建模可以提升点击率（CTR）预测性能。通常，两阶段框架被广泛采用：首先由通用搜索单元（GSU）检索与目标物品最相关的top-k行为，然后由精确搜索单元（ESU）通过定制化注意力机制生成兴趣特征。然而，当前以目标为中心的GSU会忽略其他潜在用户兴趣，导致兴趣特征不完整且有偏差。此外，GSU中基于匹配的检索过程依赖于目标物品与每个历史行为之间的成对相似度分数，这不仅随着用户行为持续增长而变得耗时，还忽略了用户行为之间的交互信息。为了解决这些问题，我们提出了一种名为GenLI的生成式长期用户兴趣模型，用于点击率预测。GenLI包含兴趣生成模块（IGM）、行为检索模块（BRM）和兴趣融合模块（IFM）。IGM生成多个兴趣分布以指示实时用户兴趣的不同方面，该模块与目标无关，并融合了行为间的交互信息，确保了兴趣特征的完整性和多样性。BRM通过简单的查找操作选择相关行为，将每个行为的加权时间复杂度降低到O(1)。最后，IFM使用精细的门控机制生成兴趣特征。基于生成过程，GenLI提高了用户兴趣的多样性，避免了复杂的基于匹配的行为检索，在CTR预测的准确性和效率之间实现了更好的平衡。

## Abstract
Modeling long-term user interests with massive historical user behaviors enhances click-through rate (CTR) prediction performance in advertising and recommendation systems. Typically, a two-stage framework is widely adopted, where a general search unit (GSU) first retrieves top-$k$ relevant behaviors towards the target item, and an exact search unit (ESU) generates interest features via tailored attention. However, current target-centered GSU would ignore other latent user interests, leading to incomplete and biased interest features. Additionally, the matching-based retrieval process in GSUs depends on the pairwise similarity score between target item and each historical behavior, which not only becomes time-consuming for online services as user behaviors continue to grow, but also overlooks the interaction information among user behaviors. To combat these problems, we propose a \textbf{Gen}erative \textbf{L}ong-term user \textbf{I}nterest model named GenLI for CTR prediction. GenLI consists of an interest generation module (IGM), a behavior retrieval module (BRM), and an interest fusion module (IFM). The IGM generates multiple interest distributions to indicate different aspects of real-time user interests, which is target-independent and incorporates interaction information among behaviors, ensuring complete and diverse interest features. The BRM selects related behaviors via a simple lookup operation, reducing the time complexity for weighting each behavior to $O(1)$. Finally, the IFM uses delicate gating mechanisms to generate interest features. Based on the generation process, GenLI improves the diversity of user interests and avoids complex matching-based behavioral retrieval, achieving a better balance between accuracy and efficiency for CTR prediction.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：在广告和推荐系统中，准确预测点击率（CTR）依赖于对用户长期兴趣的建模。海量历史行为数据蕴含丰富兴趣模式，但现有两阶段框架存在三个关键缺陷：
  - **目标中心化偏差**：通用搜索单元（GSU）仅检索与目标物品最相关的行为，忽略用户其他潜在兴趣，导致兴趣特征不完整、有偏。
  - **匹配式检索效率低**：GSU需要计算目标物品与每个历史行为的成对相似度，时间复杂度随行为序列增长而线性增加，在线服务中成为瓶颈。
  - **忽略行为间交互**：成对匹配方式无法利用行为序列中的交互信息，导致相似度度量不精确。
- **整体含义**：论文首次将生成式方法引入长期用户兴趣建模，提出GenLI框架，旨在同时提升兴趣建模的完整性、多样性和检索效率，实现准确率与效率的更好平衡。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：放弃传统的目标中心式检索，改为生成式思路——先利用短期行为生成多个**目标无关的兴趣分布**，再通过简单的**查找操作**（lookup）为每个历史行为赋分并检索，最后融合成长期兴趣特征。
- **关键模块**：
  - **兴趣生成模块（IGM）**：输入最近 \(l\) 个短期行为，通过多头注意力（MHA）得到隐藏兴趣嵌入 \(h\)，再经MLP和softmax生成离散概率分布。为捕捉不同层面的兴趣，同时生成三种分布：
    - **显式分布 \(p_E\)**：以用户实际点击物品为标签，最小化负对数似然损失 \(L_{explicit}\)。
    - **隐式分布 \(p_I\)**：以曝光物品（平台根据历史投放的物品）为标签，最小化 \(L_{implicit}\)。
    - **相对分布 \(p_R = \text{softmax}(p_E - p_I)\)**：表示兴趣变化趋势与对比关系。
  - **行为检索模块（BRM）**：对每个历史行为，通过取模运算 \(s(b_i, p) = p_{\text{ID}(b_i) \% N}\) 直接得到其在该分布下的分数，时间复杂度 \(O(1)\)。分别从三种分布中各自选取top-\(k\)行为，共 \(K=3k\) 个。
  - **兴趣融合模块（IFM）**：分别对每类检索出的行为使用MHA（以目标物品嵌入为query）聚合，得到三个兴趣嵌入 \(z_I, z_E, z_R\)，拼接后通过门控机制 \(\boldsymbol{g} = \sigma(MLP(z_F))\) 生成最终长期兴趣特征 \(x_l\)。
- **损失函数**：端到端优化，总损失 \(L = L_{ctr} + \alpha L_{implicit} + \beta L_{explicit}\)，其中 \(L_{ctr}\) 为交叉熵。
- **算法流程**（文字说明）：
  1. 从完整行为序列中提取短期行为；
  2. 通过MHA计算隐藏兴趣嵌入；
  3. 分别生成显式、隐式、相对三种分布；
  4. 对每种分布，通过lookup操作为历史行为打分，选出top-\(k\)；
  5. 分别聚合三类行为，经门控融合得到长期兴趣特征；
  6. 与其它特征拼接后输入MLP预测CTR，反向传播更新参数。

## 3. 实验设计：使用了哪些数据集 / 场景，它的 benchmark 是什么，对比了哪些方法

- **数据集**（三类）：
  - **Amazon (Book)**：75,053用户，358,367物品，150,016样本，行为序列截断/填充至100。
  - **Taobao**：987,994用户，34,196,612物品，7,956,431样本，序列长度500。
  - **Industrial**（美团广告系统）：2.4亿用户，1.1亿物品，11亿样本，序列长度1000。
- **基准（benchmark）**：所有模型保持其他CTR预测部分相同，仅替换长期兴趣建模部分。最终CTR预测使用3层MLP（隐藏维度200→80）。
- **对比方法**：
  - 短兴趣模型：DIN、DIEN（仅用短期行为）。
  - 长兴趣基线：Avg-Pooling-long、SIM-hard、SIM-soft、ETA、SDIM、TWIN（两阶段SOTA）。
- **评价指标**：离线AUC；在线CTR提升率和RPM提升率；推理时间（毫秒）。

## 4. 资源与算力：如果文中有提到，请总结使用了多少算力（GPU 型号、数量、训练时长等）。若未明确说明，也请指出这一点。

- 论文未明确说明使用的GPU型号、数量及训练时长。仅提及“在工业数据集上训练60天样本，测试1天”，但未提供具体算力配置。
- 在效率实验中报告了“平均推理时间（8192样本/批）”，但未说明推理设备的规格。

## 5. 实验数量与充分性：大概做了多少组实验，这些实验是否充分、是否客观、公平

- **实验组数**：
  - **总体性能对比**：在两个公开数据集上重复多次（给出均值和标准差），并给出工业数据集上的AUC与推理时间。
  - **效率对比**：不同序列长度下的推理时间（图2）。
  - **兴趣分布消融**：去除IGM模块或删除某种分布（表5）。
  - **短期行为长度影响**：变化 \(l\) 值（图3）。
  - **检索数量 \(K\) 影响**：分别对比SIM-soft、TWIN、GenLI在不同 \(K\) 下的AUC（图4）。
  - **分布维度 \(N\) 影响**：变化 \(N\) 观察AUC变化（图5）。
  - **在线A/B测试**：在美团真实流量30%上进行6天实验，报告CTR和RPM提升。
- **充分性与公平性**：
  - **充分**：涵盖了主要消融、超参数影响、效率分析、在线验证，实验设计较全面。
  - **客观**：公开数据集结果给出均值和标准差，工业结果基于实际业务指标。
  - **公平**：消融实验中控制其他参数不变；与基线保持相同检索总数（\(K=60\)）和相同短/长序列定义；但部分基线（如DIN/DIEN）只使用短序列，而GenLI同时利用了长序列，对比可能存在不一致。此外，Amazon和Taobao数据集缺乏曝光物品记录，用“最近类似物品”替代，可能引入噪声。

## 6. 论文的主要结论与发现

- GenLI在Amazon和Taobao数据集上AUC分别达到0.7564和0.9552，优于所有基线，较最优基线TWIN分别提升0.61%和1.03%。
- 工业数据上，GenLI相比TWIN提升AUC约0.22%，且推理时间更短（4.6ms vs 7.9ms）。
- 在效率上，GenLI的瓶颈在于 \(O(L)\) 的lookup操作，远低于匹配式方法的 \(O(L d_h)\)，在长序列下有显著优势。
- 三种兴趣分布（显式、隐式、相对）均对性能有正向贡献，其中显式分布最关键（去除后AUC下降最多）。
- 在线A/B测试中，GenLI带来CTR提升0.776%、RPM提升1.567%，并已在美团平台部署服务数亿用户。
- 生成式方法成功避免了目标中心化偏差，能够捕获更完整、多样的用户兴趣。

## 7. 优点：方法或实验设计上有哪些亮点

- **方法层面**：
  - 首次将**生成式方法**用于CTR中的长期兴趣建模，摆脱对目标物品的依赖，显著提升兴趣多样性。
  - 利用**短期行为生成分布**，自然融合短/长期兴趣信息。
  - 通过**取模运算实现O(1)行为赋分**，极大降低检索复杂度，在线部署友好。
  - 提出**三种互补的兴趣分布**（显式、隐式、相对），全面刻画用户兴趣的即时性与演化趋势。
- **实验层面**：
  - 在**三个不同规模的数据集**（含超大型工业集）上验证，泛化性强。
  - 进行**充分的消融与超参数分析**，明确各组件贡献及参数选择依据。
  - 提供了**效率对比**（图2）和**检索数量影响**（图4），证明GenLI在低检索数下优势更明显。
  - 完成**在线A/B测试**并给出真实业务指标，证明实际应用价值。

## 8. 不足与局限：包括实验覆盖、偏差风险、应用限制等

- **实验覆盖局限**：
  - 仅在**书籍和淘宝商品**两个公开数据集上评估，未涉及新闻、视频等其他领域。工业数据集来自美团（本地生活），场景可能泛化能力需进一步验证。
  - 缺乏**与最新生成式推荐模型（如GPT4Rec、Generative Retrieval）** 的直接对比，论文仅对比了传统两阶段方法。
  - 未分析**分布 \(N\) 过小时碰撞增加对检索质量的具体影响**。
- **偏差风险**：
  - Amazon和Taobao数据集无真实曝光物品，使用“最近相似物品”替代隐式兴趣的标签，可能引入**近似偏差**。
  - 在线A/B测试仅进行6天，未讨论**长期效果与用户兴趣漂移**。
- **应用限制**：
  - GenLI需要**预定义短/长序列的分界**（如 \(l=10\) 或100），不同场景下最佳值可能差异大，调参成本较高。
  - 对**极端稀疏用户**（行为极少）可能难以生成有意义的分布，论文未讨论该情况。
  - 方法依赖**离散分布取模寻址**，当物品ID分布不均匀时可能出现**哈希冲突导致检索质量下降**。

（完）
