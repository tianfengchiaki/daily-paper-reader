---
title: "Meta-Modal Agent: Sequential Evidence Routing for Missing-Modality Candidate Reranking"
title_zh: 元模态智能体：面向缺失模态候选重排序的序列化证据路由
authors: "Jinze Wang, Yangchen Zeng, Tiehua Zhang, Lu Zhang, Yuze Liu, Zhishu Shen, Jiong Jin, Zhu Sun"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.25007v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 基于大模型的多模态推荐重排序
tldr: "针对多模态推荐系统中模态缺失导致性能下降的问题，提出元模态智能体（MMA），将缺失模态视为顺序证据路由问题，通过掩码模态情节的平衡缺失任务强化学习训练。MMA在候选池上重排序，结合自动工具（MMA-Auto）或交互式澄清（MMA-Interactive）。实验表明，MMA-Auto在目标正例OOMA NDCG@10提升4.0%，全目录重排序提升12.7%，优于非交互基线，且学习路由优于确定性融合。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25007v1/fig-001.webp\", \"caption\": \"Table 4: Per-modality OOMA NDCG@10 under the target-positive 100-item protocol. Each cell reports mean ± std over 5 seeds.\", \"page\": 7, \"index\": 1, \"width\": 602, \"height\": 232}, {\"url\": \"assets/figures/arxiv/2605.25007v1/fig-002.webp\", \"caption\": \"Figure 2: Performance and ablation analysis on OOMA NDCG@10. (a) Interactive upper-bound where MMAInteractive keeps Ask_User enabled. (b) Ablation study results (mean ± std, 5 seeds) showing MMA-Interactive as an upper bound.\", \"page\": 7, \"index\": 2, \"width\": 506, \"height\": 250}, {\"url\": \"assets/figures/arxiv/2605.25007v1/fig-003.webp\", \"caption\": \"Figure 1: Overview of MMA. The agent receives a shared candidate pool, routes among available evidence tools, treats Null returns as observations, and outputs candidate scores that are fused with first-stage retrieval scores for reranking.\", \"page\": 3, \"index\": 3, \"width\": 1028, \"height\": 535}, {\"url\": \"assets/figures/arxiv/2605.25007v1/fig-004.webp\", \"caption\": \"Table 1: Missingness-task families under leave-one-modalityout (LOMO). Each task is defined by a fixed mask m = [text, img, beh].\", \"page\": 4, \"index\": 4, \"width\": 493, \"height\": 261}]"
motivation: 现有缺失模态恢复方法（如插补、传播、生成）在幸存信号弱时会注入无依据证据，导致推荐质量下降。
method: 将缺失模态建模为顺序证据路由问题，基于大语言模型的候选池重排序器MMA，通过掩码模态情节的平衡缺失任务强化学习训练，并融合自动工具或交互式澄清。
result: "MMA-Auto在OOMA NDCG@10上提升4.0%，在固定池全目录重排序上提升12.7%，且学习路由（MMA-Auto）优于确定性融合（RuleRouter-Fuse）。"
conclusion: 将模态缺失视为顺序证据路由问题并结合强化学习训练的方法，有效提升了缺失模态场景下的推荐鲁棒性和准确性。
---

## 摘要
缺失模态会导致多模态推荐系统严重失效。在冷启动场景中，用户历史、物品文本和视觉证据经常缺失，而正是在这些场景下推荐质量最为重要。现有方法通过插补、特征传播或生成式重建来恢复缺失信号，但当存活信号较弱时，这些策略可能会注入缺乏依据的证据。我们提出元模态智能体（MMA），一种基于大语言模型的候选池重排序器，将缺失问题视为序列化证据路由问题。MMA 通过基于缺失任务平衡的强化学习在掩码模态情节上进行训练，并评估了两种变体：MMA-Auto（仅使用自动化文本、图像和图形工具）和 MMA-Interactive（额外允许基于存活模态的澄清问题，作为上界诊断）。MMA 在第一阶段检索器生成候选池后运行；它对候选进行评分，而非从全量目录中检索物品。最终重排序融合 MMA 分数与基于验证数据选择的第一阶段检索分数。我们的评估围绕四个证据检查展开，这些检查对于稳健的缺失模态主张是必要的：无预言机单观测模态可用性（OOMA）鲁棒性、逐模态 OOMA 分解、固定池全目录重排序，以及确定性路由机制控制。相比最强的非交互式基线，MMA-Auto 在目标正样本 OOMA NDCG@10 上提升 4.0%，在固定池全目录重排序 NDCG@10 上提升 12.7%。而 RuleRouter-Fuse 使用相同工具和融合规则但无学习策略更新，其性能低于 MMA-Auto，支持了超越确定性工具融合的学习路由。在可获得澄清信息时，MMA-Interactive 增加了 4.1% 的上界增益。

## Abstract
Missing modalities cause severe failures in multimodal recommender systems. User histories, item text, and visual evidence are frequently absent during cold-start scenarios, exactly when recommendation quality matters most. Existing approaches recover absent signals through imputation, feature propagation, or generative reconstruction, but these strategies can inject unsupported evidence when the surviving signals are weak. We introduce the Meta-Modal Agent (MMA), a large language model based candidate-pool reranker that treats missingness as a sequential evidence-routing problem. MMA is trained with balanced missingness-task reinforcement learning over masked-modality episodes and is evaluated in two variants: MMA-Auto, which uses only automated text, image, and graph tools, and MMA-Interactive, which additionally permits clarification questions grounded in surviving modalities as an upper-bound diagnostic. MMA operates after a first-stage retriever has produced a candidate pool; it scores those candidates rather than retrieving items from the full catalog. Final reranking fuses MMA scores with first-stage retrieval scores selected on validation data. Our evaluation is organized around four evidence checks required for a robust missing-modality claim: oracle-free one-observed-modality availability (OOMA) robustness, per-modality OOMA breakdowns, fixed-pool full-catalog reranking, and a deterministic-router mechanism control. MMA-Auto improves target-positive OOMA NDCG@10 by 4.0% and fixed-pool full-catalog reranking NDCG@10 by 12.7% over the strongest non-interactive baseline. RuleRouter-Fuse, which uses the same tools and fusion rule without learned policy updates, underperforms MMA-Auto, supporting learned routing beyond deterministic tool fusion. MMA-Interactive adds a 4.1% upper-bound gain when clarification is available.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：多模态推荐系统在冷启动场景中经常面临模态缺失（用户历史、物品文本、视觉证据缺失），导致推荐质量严重下降。
- **现有方法局限**：传统方法通过插补、特征传播或生成式重建来恢复缺失模态，但当幸存信号较弱时，这些策略可能注入不可靠的证据，反而损害排序。
- **本文贡献**：首次将缺失模态问题重新定义为顺序证据路由问题，提出元模态智能体（Meta-Modal Agent, MMA），利用大语言模型作为候选池重排序器，通过动态选择可用的证据工具（文本、图像、图谱）来评分候选物品，而不是试图恢复缺失模态。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：将候选池重排序建模为部分可观测马尔可夫决策过程（POMDP）。代理（LLM）在每个时间步决定调用哪个证据工具，工具返回证据或Null（表示模态缺失），Null成为改变后续路由路径的观测。
- **模块化框架**（如图1所示）：
  - **模块1：情景接口与候选池** – 从第一阶段检索器获得固定大小的候选池（含真实目标物品和硬负样本），并附带每项物品的存活模态摘要。
  - **模块2：证据路由代理** – 基于Llama-3-8B-Instruct，使用LoRA微调，通过强化学习学习选择工具的策略。动作空间包括：`Analyze_Text`、`Analyze_Image`、`Retrieve_Graph`（自动工具），以及`Ask_User`（仅交互式变体）。工具返回Null表示该模态缺失。
  - **模块3：候选评分与融合** – 最终动作`Score_Candidates`输出物品相关性评分，经min-max归一化后与第一阶段检索分数加权融合（融合权重α在验证集上网格搜索确定）。
  - **模块4：平衡缺失任务训练** – 定义7个缺失任务族（如表1），包括全模态可用、留一模态缺失（LOMO）、单模态可用（OOMA）。训练时均匀采样各任务，采用PPO优化，奖励函数包含NDCG@10（最终排序质量）和步骤代价（工具调用-0.02、用户询问-0.10、无效动作-0.20）。
- **关键设计**：CMA-Auto仅使用自动工具，作为主要部署方法；MMA-Interactive启用Ask_User，仅作为上界诊断。

## 3. 实验设计
- **数据集**：Amazon-Baby、Amazon-Sports（亚马逊产品数据，含文本、图像、交互图谱）和Yelp（业务评论数据，含文本、CLIP视觉特征、用户-业务交互）。按时间划分训练/验证/测试集（80%/10%/10%）。
- **评估协议**：
  - **OOMA（单观测模态可用性）**：在候选池中插入真实目标物品，其余为来自幸存模态的硬负样本（BM25、CLIP、ANN）。评估重排序质量。
  - **固定池全目录重排序**：使用真实第一阶检索器（如BM25+CLIP）生成候选池，MMA在该池上重排序。
- **对比方法**：
  - 静态基线：LightGCN、SASRec（CF）、MMGCN、LRMM（多模态CF）、DGMRec、GRE-MC（完成式/生成式）、MACL（对比学习）。
  - LLM代理基线：ReAct零样本、AgentCF、MMA-SFT、MMA-Pooled（无任务均衡）、MMA-Auto、MMA-Interactive。
  - 控制基线：RuleRouter-Fuse（固定确定性的工具调用顺序，无学习策略，使用相同工具和融合规则）。
- **指标**：HR@10、NDCG@10（主要），统计显著性采用配对Wilcoxon符号秩检验和Cliff's δ效应量。

## 4. 资源与算力
- **文中提及**：
  - 基础LLM：Llama-3-8B-Instruct，8位量化，LoRA（r=16, α=32）微调q_proj/v_proj。
  - 优化：PPO，学习率1e-5，batch 64，GAE λ=0.95，500次迭代，最大回合长度T=8步。
  - 推理成本（附录A表8）：MMA-Interactive每查询约1.250秒/8.6 GB/8.2×10^11 FLOPs，平均3.9次前向；静态基线（DGMRec）约0.012秒/2.1 GB。
  - **未明确说明**：使用的GPU数量、总训练时长。仅在附录提到“单张NVIDIA A100 GPU”用于推理成本测量，训练可能使用更多设备但未报告。

## 5. 实验数量与充分性
- **实验分组**：
  - 主实验（RQ1）：表3 – OOMA NDCG@10，三个数据集，对比MMA-Auto与两个最强静态基线（DGMRec、GRE-MC）。
  - 逐模态分解（RQ2）：表4 – 9个单元格（3数据集 × 3模态），MMA-Auto全部优于最强基线。
  - 全目录重排序（RQ3）：表5 – 固定池第一阶检索后的重排序，对比静态管线。
  - 交互式上界（RQ4）：图2a – MMA-Interactive对比MMA-Auto。
  - 消融与机制（RQ5）：图2b（消融）、表6（统计检验）、表7（vs RuleRouter-Fuse）、附录A表9（路由指标）。
- **充分性评价**：
  - **充分且客观**：覆盖了多种缺失场景（OOMA、LOMO）、多个数据集、多种基线类型（传统CF、多模态、完成式、LLM代理、确定性控制）；进行了统计显著性检验和效应量报告。
  - **公平性**：所有LLM代理共享相同基础模型、上下文预算、解码参数；MMA仅训练LoRA适配器和价值头，基础权重冻结。第一阶检索分数和融合权重均在验证集上选择。
  - **稍显不足**：仅在3个数据集上评估，且均为英文商业数据；未在更广泛领域（如视频、音频、社交推荐）验证。缺少对训练过程中PPO超参数敏感性的分析。

## 6. 主要结论与发现
- **核心结果**：
  - MMA-Auto在OOMA NDCG@10上比最强静态基线DGMRec平均提升4.0%（绝对值+0.0066）。
  - 在固定池全目录重排序中，NDCG@10相对提升12.7%，HR@10从0.0385升至0.0427，Recall@100不变（说明提升来自排序而非召回）。
  - MMA-Interactive在MMA-Auto基础上再提升4.1%（上界）。
  - 学习路由（MMA-Auto）显著优于确定性路由（RuleRouter-Fuse）：平均NDCG@10从0.1578升至0.1711，同时失败调用率更低（47.0% vs 65.8%），回合步数更少（2.5 vs 3.8）。
- **机制证据**：
  - 零样本ReAct最差（NDCG@10 0.0928）；SFT提升至0.1421；PPO进一步至0.1711，表明需要经历失败证据路径。
  - 任务均衡（MMA-Auto vs MMA-Pooled）差异不大，但防止了易任务主导。
  - 所有9个数据集-模态OOMA单元格均获胜，表明路由策略普遍有效。

## 7. 优点
- **问题重新定义**：将缺失模态从静态表示学习问题转化为动态证据路由问题，更符合实际冷启动场景（代理需主动探索可用证据）。
- **清晰的方法分离**：明确区分自动化部署变体（MMA-Auto）与交互式诊断上界（MMA-Interactive），避免过度承诺。
- **严格的评估协议**：采用固定候选池、目标正例协议，控制召回变量，直接测量重排序质量；包含全目录重排序检验，避免人为插入目标。
- **强有力的机制对照**：通过RuleRouter-Fuse基线证明学习路由的增量价值，而不仅仅是工具访问和融合规则。
- **统计严谨性**：使用配对检验和效应量，避免小幅度提升被过度解读。

## 8. 不足与局限
- **增益幅度有限**：绝对NDCG提升约0.006-0.007，在用户感知上可能不显著。尽管统计显著，但Cliff's δ为0.15-0.19（中等效果），实际部署效益需进一步验证。
- **协议局限性**：主要实验在人工构造的候选池（插入目标物品+硬负样本）上进行，虽然全目录实验缓解了这一点，但该协议可能高估代理的排序能力。
- **推理延迟高**：MMA-Interactive延迟1.25秒/查询，远高于静态基线（0.012秒），不适合高实时性场景。MMA-Auto虽略快但仍远高于传统方法。
- **交互式工具理想化**：Ask_User的响应基于幸存模态自动合成，而非真实用户交互，可能高估交互式增益。
- **仅覆盖文本、图像、行为三种模态**：未考虑音频、视频等更多模态，且数据集均为英文商业领域，泛化性未知。
- **训练资源未完整报告**：缺少GPU型号数量、训练总时长、超参数敏感性分析，复现难度较大。
- **未与最新生成式推荐方法对比**：如TIGER、P5等，虽然它们可能也面临缺失模态问题，但本文未纳入比较。

（完）
