---
title: "Fast and Feasible: Permutation-based Constrained Reranking for Revenue Maximization"
title_zh: 快速且可行：基于排列的受限重排序以实现收入最大化
authors: "Svetlana Shirokovskikh, Anastasiia Soboleva, Ekaterina Solodneva, Aleksandr Katrutsa, Roman Loginov, Egor Samosvat"
date: 2026-06-26
pdf: "https://arxiv.org/pdf/2606.28059v1"
tags: ["query:q1"]
score: 9.0
evidence: 电子商务中付费推广的重新排序以最大化收入
tldr: "搜索推荐系统为增加平台收入对结果重排序，但需平衡相关性和风险。本文提出整数线性规划建模约束重排序，并设计轻量级排列算法PermR，通过相邻交换近似优化。离线取得ILP收益的63%，在线5600万查询测试中收入提升2%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.28059v1/fig-001.webp\", \"caption\": \"Table 2: For queries with N = 50 items, we identify # iterations (I) for PermR, the time limit (T ) for GA, and the best ILP solver from HiGHS and MOSEK (M) on different threads. ILP solvers provide an upper bound on revenue uplift (∆Rev(%) = +6.9). To solve ILPs, we use MOSEK with 12 threads, as it achieves the lowest average runtime. Based on the production time limit, we set I = 750 and the GA time limit to 0.05.\", \"page\": 5, \"index\": 1, \"width\": 783, \"height\": 217}, {\"url\": \"assets/figures/arxiv/2606.28059v1/fig-002.webp\", \"caption\": \"Figure 2: ILPs achieve the maximum possible revenue uplift of +4.1%, but exceed prod time limits by 0.05 seconds for inference. PermR yields a +2.6% revenue uplift with no drop in time limit, while GA achieves only +0.4% revenue.\", \"page\": 5, \"index\": 2, \"width\": 976, \"height\": 414}, {\"url\": \"assets/figures/arxiv/2606.28059v1/fig-003.webp\", \"caption\": \"Table 3: Revenue gain (%) by category compared to the production ranking. Offline: ∆ Revenue for ILP solvers, PermR, and genetic algorithm (GA) on logs. Online: ∆ Revenue from a 14-day A/B test with PermR (I=750, 56M queries, 35% traffic). Volume shows the query share in a category. The confidence intervals in A/B test are evaluated with p-value < 0.05.\", \"page\": 6, \"index\": 3, \"width\": 698, \"height\": 341}]"
motivation: 仅最大化收入会降低用户体验，需在重排序中同时满足相关性等约束。
method: 提出PermR算法，通过迭代交换相邻项逼近整数线性规划解，满足在线延迟要求。
result: "离线达到ILP收入改进的63%，在线A/B测试14天5600万查询中收入增加2%。"
conclusion: PermR是轻量级、在线可行的重排序方法，有效提升收入且保持约束。
---

## 摘要
搜索和推荐系统已经能够产生高度相关的搜索结果。在电子商务中，此类系统发展的自然下一步是对这些结果进行重排序，以增加平台来自付费推广产品的收入。然而，仅最大化收入可能会降低相关性或增加欺诈风险，从而损害用户体验。为避免这种情况，我们将重排序问题表述为一个整数线性规划（$ILP$），该规划在每查询的其他指标（如相关性）约束下最大化收入。由于对每个查询精确求解$ILP$对于在线服务部署而言速度较慢，我们提出了一种轻量级的基于排列的重排序近似算法PermR。每一步，该算法选择一对相邻项目并交换它们，以改进目标或修复违反的约束。我们在一个大型分类平台的多个类别上，在离线和在线设置下评估了PermR。PermR在保持所有约束的情况下，在生产延迟限制内实现了约63%的ILP收入提升。在一项涵盖5600万搜索查询的14天在线A/B测试中，PermR使收入增加了2%。

## Abstract
Search and recommender systems have produced highly relevant search results. A natural next step in the development of such systems in e-commerce is to rerank these results to increase the platform's revenue from paid promotion products. However, maximizing revenue alone may degrade the user experience by reducing relevance or increasing fraud risk. To avoid this, we state the reranking problem as an integer linear program ($ILP$) that maximizes revenue subject to per-query constraints on other metrics, e.g., relevance. Since solving $ILP$ exactly for every query is slow for deployment to the online service, we propose a lightweight permutation-based reranking approximation algorithm PermR. At each step, the algorithm selects a pair of neighboring items and swaps them to either improve the objective or repair a violated constraint. We evaluate PermR across multiple categories of a large classified platform in offline and online settings. PermR achieves about 63\% of the ILP revenue improvement, within production latency limits, preserving all constraints. In a 14-day online A/B test over 56 million search queries, PermR increased revenue by $2$\%.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义

- **研究动机**：现代搜索与推荐系统已能产生高度相关的结果。在电商平台中，自然下一步是通过重排序增加付费推广产品的收入。但单纯最大化收入会损害相关性、增加欺诈风险，降低用户体验。
- **核心问题**：如何在不违反其他指标（如相关性、质量）约束的前提下最大化收入，同时保证重排序算法的在线延迟可接受。
- **整体含义**：将重排序建模为整数线性规划（ILP），但精确求解ILP太慢，因此需要设计轻量级近似算法，在有限时间内取得接近最优的收入提升。

## 2. 方法论

- **核心思想**：提出一种基于排列的贪心交换算法（PermR），通过迭代交换相邻物品来逐步优化目标或修复约束，避免全局求解ILP。
- **关键技术细节**：
  - 问题形式化为ILP：最大化 $\sum_{i,j} \gamma_j d_{ij} f^{(i)}_0$，约束包括全量约束（$F_m \ge F^*_m$）和前缀约束（$F_m@K \ge F^*_m@K$），其中$d_{ij}$为二进制分配矩阵，$\gamma_j$为位置偏置衰减因子（$\gamma_j = 0.97^j$）。
  - PermR算法流程（I次迭代）：
    - 检查当前排列是否满足所有约束。
    - 若全部满足：以概率 $\propto \max(0,\ f^{s_{j+1}}_0 - f^{s_j}_0)$ 采样相邻对并交换，以提高目标函数$F_0$。
    - 若存在违反约束：随机选择一个违反的约束$m$，以概率 $\propto \max(0,\ f^{s_{j+1}}_m - f^{s_j}_m)$ 采样相邻对并交换；若没有可提高的交换（对前缀约束），则从剩余物品中选择$f_m$最高的插入到当前有序前缀之后。
  - 最终返回迭代过程中满足约束且$F_0$最大的排列。

## 3. 实验设计

- **数据集与场景**：
  - 采用大型分类平台（Avito）的Goods类别（约2亿商品）。离线实验使用27,000条真实日志查询（3天数据），大多数SERP长度为50项。
  - 在线A/B测试覆盖14天、5600万查询、35%搜索流量。
- **基准方法**：
  - ILP精确求解器：HiGHS、MOSEK（多线程1-12）。
  - 遗传算法（GA）：来自pymoo框架，设置不同时间限制$T$。
  - 生产基线：当前线上排序结果。
- **对比指标**：收入提升百分比（$\Delta Rev$）、推理时间、约束满足情况。

## 4. 资源与算力

- **文中未明确说明使用的GPU型号、数量或训练时长**。原因是PermR为零训练方法（无需训练模型），仅需在线推理。
- 仅提及MOSEK求解ILP时使用12线程的CPU（从1线程到12线程测试，12线程平均0.612秒，仍超出生产限制0.05秒）。GA和PermR均在CPU上运行。

## 5. 实验数量与充分性

- **离线实验**：
  - 对27K查询进行整体统计。
  - 对比ILP、GA、PermR在不同SERP长度下的推理时间和收入提升（图2）。
  - 探究PermR不同迭代次数（100-10000）和GA不同时间限制（0.01-1.0秒）的影响（表2）。
  - 按11个子类别分别报告离线收入提升（表3）。
- **在线实验**：14天A/B测试，覆盖多个子类别，报告收入提升及置信区间（p<0.05）。
- **充分性评估**：
  - 实验设计较充分，同时评估了离线近似率、在线实际收益、延迟约束。
  - 但仅有单一平台数据，未跨平台验证；GA参数网格搜索但结果仍差，可能未调优到最佳；未进行消融实验（如移除某条约束的影响）。整体客观性较好。

## 6. 主要结论与发现

- 离线结果：ILP精确求解获得+4.1%收入提升，PermR（750迭代）获得+2.6%（约63%近似），GA仅+0.4%。
- 在线A/B测试：PermR在5600万查询中平均收入提升2.0%±0.4%（p<0.05），所有子类别均有提升（Home & Garden最大+6.0%）。
- 延迟满足：PermR最高时间0.038秒（I=750），低于生产限制0.05秒；ILP求解器在N=50时平均0.612秒，不可接受。
- 结论：PermR在近似最优收入的同时，满足全部约束和延迟要求，适合在线部署。

## 7. 优点

- **方法层面**：基于排列的贪心交换思路新颖，无需训练模型，轻量级，可处理多个约束（包括前缀约束）。
- **实验层面**：同时进行离线详尽对比和在线A/B测试，结果具有实际说服力；采用p-value评估统计显著性；公开部分代码和数据集子集，促进可复现性。
- **实用性**：直接针对生产延迟限制设计（0.05秒），算法简单易实现，适合电商实时系统。

## 8. 不足与局限

- **泛化性有限**：实验仅基于一个分类平台（Avito）的Goods类别，其他领域（如商品推荐、视频搜索）未验证。
- **对比方法**：遗传算法（GA）表现极差（+0.4%），可能因默认参数或种群大小不合适，未充分调优，削弱了对比公平性。
- **近似率**：750迭代下仅63%近似，更高迭代（2500）可达80%但延迟增加；在延迟更严格的场景下近似率可能更低。
- **约束定义依赖**：约束边界$F^*_m$取自当前生产排序，若生产排序本身非最优，可能限制潜在收入。
- **未讨论鲁棒性**：对多约束冲突或退化情况（如所有物品f_m相同）的处理未深入分析。
- **代码开源程度**：仅提供部分子集，完整可复现性受限。

（完）
