---
title: "Ocean4Rec: Offline LLM-Derived OCEAN Profiles for Request-Time VOD Reranking"
title_zh: "Ocean4Rec: 离线LLM推导的OCEAN画像用于请求时VOD重排"
authors: "Wonkyun Kim, Sehyun Bae, Kwanki Ahn, Mungyu Bae, Saeun Choi, Soyeon You, Chandra Prabhakar, Sehyun Kim"
date: 2026-05-22
pdf: "https://arxiv.org/pdf/2605.27429v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 离线大模型特征用于视频点播重排序
tldr: "针对工业VOD推荐中LLM重排序的请求时延高、部署复杂问题，提出Ocean4Rec，离线利用LLM从元数据提取项目OCEAN人格画像，并在请求时结合用户画像、基础分数和新近度进行数值重排序，无需在线LLM调用。在三星智能电视日志上，NDCG@20比强基线提升7.6%~61.5%，验证了该辅助特征的可部署性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.27429v1/fig-001.webp\", \"caption\": \"Table 3: Secondary diagnostics. VOD rows use the same temporal holdout and Top1000 candidate sets as Table 2. Public rows use sampled public subsets and Top20 candidates, so absolute values must not be compared across VOD and public blocks. HR@20 on public Top20 rows is candidate-set recall rather than a rank-sensitive reranking metric. These rows are protocol sanity checks, not the main evidence.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 240}, {\"url\": \"assets/figures/arxiv/2605.27429v1/fig-002.webp\", \"caption\": \"Table 2: Same-candidate Top1000 temporal-holdout ablation. Values are rounded offline point estimates within the same generator and cutoff; relative deltas in text use full precision.\", \"page\": 6, \"index\": 2, \"width\": 1056, \"height\": 269}, {\"url\": \"assets/figures/arxiv/2605.27429v1/fig-003.webp\", \"caption\": \"Figure 1: Ocean4Rec overview. Item OCEAN profiles are generated offline from content metadata, user OCEAN profiles are computed from recent profiled interactions, and request-time reranking combines base model score, OCEAN compatibility, and catalog recency without an online LLM call.\", \"page\": 3, \"index\": 3, \"width\": 1056, \"height\": 529}]"
motivation: LLM作为重排序器在请求时重复执行提示构建、生成、解析等操作，导致延迟、吞吐量和容量隔离问题，难以部署于高流量实时服务。
method: 离线使用LLM从视频元数据生成五维OCEAN画像，用户画像通过时间衰减聚合近期点击项目构建，请求时联合预计算画像与基础分数进行数值重排序。
result: "在匿名VOD日志上，Ocean4Rec使NCF和LightGCN生成的候选集NDCG@20分别提升7.6%和61.5%，HR@20对LightGCN提升67.3%。"
conclusion: Ocean4Rec提供了一种免请求时LLM的辅助内容品味特征，兼顾效果与部署优势，可作为工业推荐系统的轻量级重排序层。
---

## 摘要
工业视频点播（VOD）推荐系统需要更丰富的内容理解，但基于LLM作为重排序器的设计会在每次请求中重复进行提示构建、令牌生成、模型调用、输出解析和回退处理。在高吞吐量、低延迟的服务中，这些请求时操作使吞吐量规划、尾部延迟控制、容量隔离和可预测运维变得复杂。本文提出Ocean4Rec，一种重排序层，仅使用LLM离线从内容元数据中物化项目的OCEAN画像。项目被映射为开放性、尽责性、外向性、宜人性和神经质分数，而用户画像则通过时间衰减聚合最近点击和深度链接的项目在同一五维空间中构建。在请求时，Ocean4Rec联结预计算的项目画像、用户画像、基础推荐器分数和目录新鲜度，然后执行数值重排序，无需调用LLM。在匿名三星智能电视VOD日志上，相同候选集的Top1000时间留出离线评估显示，对于NCF生成器，Ocean4Rec将NDCG@20相对于更强的非OCEAN基础+新鲜度排序提高了7.6%，对于LightGCN生成器提高了61.5%。HR@20对于NCF不确定，而对于LightGCN提高了67.3%，这反映了稀疏的精确项目回放标签以及新鲜度作为工业基线的强度。该结果应解读为对一种有界辅助内容品味特征的离线回放证据，该特征保留了无请求时LLM服务路径的可部署性优势。

## Abstract
Industrial video-on-demand (VOD) recommenders need richer content understanding, but LLM-as-reranker designs repeat prompt construction, token generation, model invocation, output parsing, and fallback handling for each request. In high-volume latency-sensitive services, these request-time operations complicate throughput planning, tail-latency control, capacity isolation, and predictable operation. This paper presents Ocean4Rec, a reranking layer that uses an LLM only offline to materialize item OCEAN profiles from content metadata. Items are mapped into Openness, Conscientiousness, Extraversion, Agreeableness, and Neuroticism scores, while user profiles are built by time-decayed aggregation of recently clicked and deep-linked items in the same five-dimensional space. At request time, Ocean4Rec joins precomputed item profiles, user profiles, base recommender scores, and catalog recency, then performs numeric reranking without an LLM call. On anonymized Samsung Smart TV VOD logs, same-candidate Top1000 temporal-holdout offline evaluations show that Ocean4Rec improves NDCG@20 over a stronger non-OCEAN Base+Recency ordering by 7.6% for an NCF generator and 61.5% for a LightGCN generator. HR@20 is inconclusive for NCF and improves by 67.3% for LightGCN, reflecting sparse exact-item replay labels and the strength of recency as an industrial baseline. The result should be read as offline replay evidence for a bounded auxiliary content-taste feature that preserves the deployability advantage of a request-time-LLM-free serving path.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **背景**：工业视频点播（VOD）推荐系统需要更丰富的内容理解来提升排序效果。现有方法常将大语言模型（LLM）作为重排序器，但在每次请求时重复执行提示构建、令牌生成、模型调用、输出解析和回退处理等操作。
- **问题**：这些请求时操作在高吞吐量、低延迟的实时服务中引入了严重的吞吐量规划难度、尾部延迟控制问题、容量隔离困难和运维不可预测性，导致LLM-as-reranker架构难以实际部署。
- **意义**：如何在不牺牲“内容品味”特征的前提下，消除在线LLM调用带来的部署复杂性，是工业推荐系统面临的紧迫挑战。

## 2. 论文提出的方法论

### 核心思想
- 将LLM从在线推理路径中完全移除，仅利用LLM**离线**从视频元数据中提取项目的OCEAN人格画像（开放性、尽责性、外向性、宜人性、神经质五维度分数）。
- 用户画像通过时间衰减聚合最近点击及深度链接的项目在同一五维空间中构建。
- 请求时，将预计算的项目画像、用户画像、基础推荐器分数以及目录新鲜度进行联结，执行数值重排序，**无需任何在线LLM调用**。

### 关键技术细节
- **项目OCEAN画像生成（离线）**：使用LLM（模型类型未明确，推测为通用LLM）从视频元数据（如标题、描述、标签等）映射出五维OCEAN分数。该过程在后台一次性完成，结果持久化存储。
- **用户OCEAN画像构建（离线/准在线）**：对每个用户，聚合其近期交互（点击、深度链接）的项目OCEAN向量，采用时间衰减加权（如近期交互权重更高），得到用户当前偏好向量。
- **请求时重排序计算**：
  - 输入：基础推荐器（如NCF或LightGCN）生成的候选集及其分数 \(s_{base}\)，预计算的项目画像 \(p_i\)，用户画像 \(p_u\)，目录新鲜度 \(t_i\)（如项目上线天数或最近更新时间）。
  - 输出：重排序分数 \(s_{rerank} = f(s_{base}, \text{cosine}(p_u, p_i), t_i)\) 。具体函数形式未在摘要中详述，但推测为线性或简单加权组合。
  - 由于所有特征均已物化，该计算仅需数值运算，延迟极低，且无需模型推理。

## 3. 实验设计

### 数据集与场景
- **数据集**：匿名三星智能电视VOD日志（来自真实工业环境）。
- **场景**：Top1000候选集的时序留出（temporal holdout）离线评估，模拟真实推荐请求的时刻分布。

### Benchmark与对比方法
- **基础推荐器**：NCF（神经协同过滤）和LightGCN两种广泛使用的协同过滤模型作为候选集生成器。
- **基线**：更强的“非OCEAN基础+新鲜度排序”（Base+Recency ordering），即仅使用基础分数和目录新鲜度进行排序，不含OCEAN特征。
- **对比指标**：NDCG@20（归一化折损累计增益）和HR@20（命中率@20）。

## 4. 资源与算力

- 论文中**未明确说明**训练或推理使用的GPU型号、数量、训练时长等算力信息。
- 仅可推断：离线LLM生成项目OCEAN画像需要一定算力，但并非频繁操作；在线重排序仅需CPU/内存进行数值计算，对资源需求极低。

## 5. 实验数量与充分性

- **实验组数**：主要结果来自两个基础推荐器（NCF、LightGCN）的对比，且包含消融分析（Table 2中的Base+Recency vs Ocean4Rec）。此外有次级诊断实验（Table 3，在公共子集上验证协议正确性）。
- **充分性评估**：
  - ✅ **合理**：时序留出设置、相同候选集比较、多指标（NDCG、HR）报告，符合工业推荐离线评估惯例。
  - ⚠️ **不足**：仅在一个数据集（三星VOD日志）上验证，且缺乏在线A/B实验；缺乏对不同LLM模型、不同OCEAN生成方式的敏感性分析；仅测试了两个基础模型，更多协同过滤变体未覆盖。

## 6. 论文的主要结论与发现

- Ocean4Rec在离线回放评估中显著提升排序质量：
  - 对于NCF生成器，NDCG@20比强基线（Base+Recency）**提升7.6%**。
  - 对于LightGCN生成器，NDCG@20**提升61.5%**，HR@20**提升67.3%**。
  - 注意：HR@20对NCF提升不确定（inconclusive），因为稀疏的精确项目回放标签以及新鲜度基线本身较强。
- 结论：Ocean4Rec提供了一种**有界、可部署的辅助内容品味特征**，仅在离线使用LLM，保留了无请求时LLM服务路径的优势，可作为工业推荐系统的轻量级重排序层。

## 7. 优点

- **部署友好**：完全消除请求时LLM调用，规避了延迟、吞吐量、容量隔离等工业部署痛点。
- **简单有效**：利用成熟的人格心理学OCEAN框架，将内容理解转化为数值向量，易于计算和集成。
- **通用性**：OCEAN画像可独立于基础推荐模型，作为额外特征增强任何候选生成器。
- **离线成本可控**：LLM调用仅发生在数据预处理阶段，后续在线推理成本极低。

## 8. 不足与局限

- **实验覆盖有限**：仅在一个工业VOD数据集上验证，且未在公开基准（如MovieLens、Netflix）上报告，可能缺乏可复现性和泛化性证明。
- **OCEAN画像质量依赖**：完全依赖LLM从元数据中提取人格属性，若元数据不完整或LLM误判，画像可能有噪音；论文未分析OCEAN预测的准确性。
- **消融不够深入**：未对比不同聚合策略（如用户画像的时间衰减系数）、不同组合函数（如加法vs乘法）、不同新鲜度定义的影响。
- **无在线结果**：仅为离线回放证据，无法证明在实时流量中能带来业务指标提升。
- **偏差风险**：OCEAN框架本身是否适合描述视频内容品味？可能存在文化或用户群体偏差（如不同地区对人格特质的解读可能不同）。

（完）
