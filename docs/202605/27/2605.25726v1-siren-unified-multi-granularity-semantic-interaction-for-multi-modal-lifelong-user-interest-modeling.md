---
title: "SIREN: Unified Multi-Granularity Semantic Interaction for Multi-Modal Lifelong User Interest Modeling"
title_zh: "SIREN: 统一多粒度语义交互的多模态终身用户兴趣建模"
authors: "Yaqian Zhang, Ruyi Yu, Tianyi Li, Bohan Liu, Maoquan Ye, Ke Wang, Shifeng Wen, Junwei Pan, Lijie Wang, Qi Zhou, Yeshou Cai, Chengguo Yin, Lifeng Wang, Hui Li, Lei Xiao, Haijie Gu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25726v1"
tags: ["query:q1"]
score: 9.0
evidence: 多模态终身用户兴趣建模用于推荐
tldr: 针对推荐系统中多模态特征与协作空间不对齐导致兴趣建模粗粒度的问题，提出SIREN统一多粒度语义交互框架，采用多模态相似性软检索和SemID硬检索策略，通过粗粒度相似桶和细粒度前缀编码实现目标感知交互，离线GAUC最优，在线广告平台GMV提升显著，已全量上线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25726v1/fig-001.webp\", \"caption\": \"Figure 2: CTR distributions of Semantic ID groups within target–behavior similarity buckets. Although pairs in the same bucket have similar multi-modal proximity to the target item, their CTRs vary substantially across Semantic ID groups, especially in high-similarity regions. This withinbucket heterogeneity shows that similarity buckets provide only coarse target-aware relevance, motivating the use of fine-grained SemIDs to preserve semantic and collaborative distinctions.\", \"page\": 2, \"index\": 1, \"width\": 529, \"height\": 361}, {\"url\": \"assets/figures/arxiv/2605.25726v1/fig-002.webp\", \"caption\": \"Figure 3: Overview of SIREN. SIREN follows the two-stage GSU–ESU paradigm. The GSU retrieves target-relevant behaviors via similarity-based soft retrieval or SemID-based hard retrieval. The ESU incorporates SemIDs and similarity buckets as side information for unified target-conditioned sequence modeling.\", \"page\": 3, \"index\": 2, \"width\": 519, \"height\": 489}, {\"url\": \"assets/figures/arxiv/2605.25726v1/fig-003.webp\", \"caption\": \"Table 1: Overall offline performance comparison on GAUC. The best result is highlighted in bold.\", \"page\": 5, \"index\": 3, \"width\": 508, \"height\": 248}]"
motivation: 现有方法无法有效对齐多模态与协作空间，导致兴趣建模粗粒度且融合不足。
method: 提出SIREN框架，包含通用搜索单元（软/硬检索）和精确搜索单元（目标感知相似桶与前缀编码），统一交互协同特征。
result: "离线GAUC达到最优，在线广告场景GMV提升2.28%~3.87%。"
conclusion: SIREN已全量部署于腾讯广告平台，证明了多模态终身兴趣建模的有效性。
---

## 摘要
工业推荐系统越来越多地利用终身用户行为历史和丰富的多模态内容来捕捉不断变化的用户偏好。然而，由于多模态空间和协同空间之间固有的不对齐，将多模态特征有效整合到终身兴趣建模中仍然具有挑战性。现有范式通常依赖于分别对多模态序列和行为序列进行建模，并通过后期融合来缓解模态差距，这导致多模态表示粗粒度且集成有限。在本文中，我们提出SIREN，一个统一的多粒度语义交互框架，用于多模态终身用户兴趣建模。在通用搜索单元阶段，我们引入了两种可选的检索策略：基于多模态相似性的软检索以获得检索效果，以及基于语义ID（SemID）的硬检索以进行高效的工业服务。在精确搜索单元阶段，我们通过粗略相似度桶和细粒度前缀编码的SemID显式地融入目标感知相关性，从而在目标条件Transformer架构内实现与协同ID特征的统一交互。离线数据集上的大量实验表明，SIREN达到了最先进的GAUC。在线A/B测试进一步表明，在多个生产场景中，GMV持续增长，包括微信朋友圈+2.28%，微信公众号+3.87%，微信视频号+1.61%。自2025年7月起，SIREN已在腾讯广告平台全面上线，用于全流量服务。

## Abstract
Industrial recommender systems increasingly leverage lifelong user behavior histories and rich multi-modal content to capture evolving user preferences. However, effectively integrating multi-modal features into lifelong interest modeling remains challenging due to the inherent misalignment between multi-modal and collaborative spaces. Existing paradigms typically rely on separate modeling of multi-modal sequence and behavior sequence, and late fusion to alleviate the modality gap, which results in coarse-grained multi-modal representation and limited integration. In this paper, we propose SIREN, a unified multi-granularity semantic interaction framework for multi-modal lifelong user interest modeling. In the General Search Unit stage, we introduce two alternative retrieval strategies: multi-modal similarity-based soft retrieval for retrieval effectiveness, and Semantic ID (SemID)-based hard retrieval for efficient industrial serving. For the Exact Search Unit stage, we explicitly incorporate target-aware relevance via coarse similarity buckets and fine-grained prefix-encoded SemIDs, enabling unified interaction with collaborative ID features within the target-conditioned transformer architecture. Extensive experiments on the offline dataset demonstrate that SIREN achieves a state-of-the-art GAUC. Online A/B tests further demonstrate consistent GMV gains across multiple production scenarios, including +2.28% in Weixin Moments, +3.87% in Weixin Official Accounts, and +1.61% in Weixin Channels. From July 2025, SIREN has been fully launched for full-traffic serving in Tencent's advertising platform.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- 工业推荐系统广泛利用终身用户行为历史和多模态内容来建模动态变化的用户偏好。
- 当前面临的关键问题：多模态特征空间与协同信号空间存在**先天不对齐**（misalignment），导致现有方法通常采用分离建模（分别处理多模态序列和行为序列）加后期融合的方式，使得多模态表示粗粒度，且与协同特征的交互受限。
- 具体表现为：①多模态信号主要在序列聚合后才与ID特征融合，无法在序列建模过程中充分互动；②目标-行为相似度仅提供粗粒度的相关性，忽视了相似度桶内因语义ID不同而产生的协作异质性（CTR差异大）。

## 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：提出**SIREN**（统一多粒度语义交互框架），在工业两阶段范式（GSU-ESU）中实现多模态与协同特征的早期、细粒度融合。
- **整体架构**：
  - **GSU阶段**（通用搜索单元）：
    - 提供两种可选检索策略：
      - *相似性软检索*：基于目标与历史行为的多模态余弦相似度，检索top-K个行为（效果较好但计算开销大）。
      - *SemID硬检索*：使用离散语义ID（SemID）的顶层代码匹配（倒排索引），实现近乎常数时间检索（效率高、存储低）。
  - **ESU阶段**（精确搜索单元）：
    - 引入两种互补的多模态特征作为**侧信息**：
      - *SemID*：通过RQ-VAE将多模态嵌入量化为层次化码序列，并采用**前缀编码**（prefix encoding）保留从粗到细的语义。
      - *相似桶*：将目标-行为余弦相似度离散化为桶索引，并映射为可学习嵌入。
    - 构建**统一物品表示**：每个行为物品的表示由ID特征和SemID前缀嵌入拼接而成。
    - 采用**目标条件Transformer**（多头目标注意力）：将行为表示、相似桶嵌入、目标表示统一输入，使协同信号、语义信号、目标感知相关性在序列聚合前共同影响注意力和表示学习。
    - 引入**目标条件交互**（元素级乘法）进一步对齐用户兴趣与目标表示，用于最终CTR预测。

## 3. 实验设计
- **离线数据集**：Taobao-MM（大规模公开多模态终身用户行为基准），包含99M交互样本、8.79M用户、35.4M商品，每个用户历史最长1000条行为，每件商品附带128维预训练多模态嵌入。
- **离线对比方法**：DIN、SIM-Hard、SIM-Soft、TWIN、MISS、MUSE，以及SIREN的两个变体（SemID-GSU、Sim-GSU）。所有两阶段方法均检索top-50行为。
- **在线场景**：腾讯微信广告系统，包含三个生产场景——朋友圈（Moments）、公众号（Official Accounts）、视频号（Channels），占20%流量，最长14天A/B测试。生产基线为SIM-Hard。

## 4. 资源与算力
- 论文**未明确说明**使用的GPU型号、数量及训练时长。
- 仅提及训练超参数：学习率（AdamW 2×10⁻⁴，SparseAdam 2×10⁻³）、批次大小1000、训练1个epoch；未给出具体硬件配置。

## 5. 实验数量与充分性
- **离线实验**：
  - 整体性能对比（表1）：8个方法，GAUC指标。
  - 消融实验（表2）：逐步添加SemID、SimBucket、目标交互，验证各组件贡献。
  - 表示可区分性分析（图4左）：互信息比较（SIREN vs MUSE，以及SimBucket/SemID消融）。
  - 条件熵分析（5.5.1节）：SemID组与相似桶组的信息增益对比。
  - 桶内CTR分布分析（图2、5.5.2节）：展示相似桶内SemID组的CTR差异。
  - 桶粒度实验（图4右）：不同桶数量对GAUC的影响。
- **在线A/B测试**：三个场景各阶段GMV提升，并做冷启动细分分析（用户活跃度、商品新度）、SemID硬检索效率对比。
- **充分性评价**：
  - 实验数量充分，覆盖了效果、表示质量、消融、可解释性、效率、冷启动等多维度。
  - 公平性：所有两阶段模型采用相同的GSU检索长度（top-50），训练epoch数一致（1个epoch），超参数与MUSE等对齐。但离线仅一个公开数据集，泛化性可能受限。

## 6. 论文的主要结论与发现
- **离线性能**：SIREN（Sim-GSU）取得最高GAUC 0.6155，相对提升+2.48%（相较于DIN），比最强基线MUSE高0.11%（绝对值，但工业界认为0.1% level有意义）。
- **消融结果**：
  - 相似桶贡献更大（+1.02%），SemID提供精细语义区分（+0.25%），两者互补。
  - 目标条件交互进一步小幅提升GAUC，但显著增强表示可区分性（互信息）。
- **表示可区分性**：SIREN的互信息持续高于MUSE及其组件，表明学习得到更富区分性的用户兴趣表示。
- **SemID硬检索**：在线效率较软检索降低超90%延迟和存储，且性能几乎无损失（相对于软检索GAUC仅差0.07%），提供了有效的效率-效果折中。
- **在线A/B测试**：全场景GMV提升显著（朋友圈+2.28%，公众号+3.87%，视频号+1.61%），冷启动用户和广告提升幅度更大。
- **已全面上线**：自2025年7月起，SIREN已部署在腾讯广告平台全流量服务。

## 7. 优点：方法或实验设计上的亮点
- **方法创新性**：首次在终身兴趣建模的GSU-ESU框架内实现多模态特征的**早期、细粒度融合**，而非传统的后期融合；同时引入SemID硬检索兼顾工业效率。
- **多粒度设计**：利用粗粒度相似桶（目标相关）与细粒度SemID（内容语义）互补，有效捕捉了相似度桶内无法区分的协作异质性。
- **实验完备性**：既有大规模公开离线基准，又有三个真实生产场景的在线A/B测试，且包含冷启动分析、效率对比、表示分析等，验证充分。
- **实用性**：提供了两种GSU检索策略（软/硬），方便不同资源约束下的部署；代码已开源（通过HuggingFace数据集引用）。

## 8. 不足与局限
- **离线提升幅度有限**：GAUC绝对提升仅0.11%（对比MUSE），虽工业界认为有意义，但学术上略显不足；可能意味着多模态融合的改进空间逐渐缩小。
- **依赖预训练多模态嵌入**：SemID和相似桶均基于预训练嵌入（如SCL），嵌入质量直接影响框架效果，且RQ-VAE训练需额外计算资源。
- **实验场景局限**：离线仅使用一个公开数据集（Taobao-MM），尽管在线场景丰富，但不同平台的多模态特性可能不同，泛化能力需更多验证。
- **未讨论负面效应或偏差**：如多模态信号引入可能导致推荐多样性下降或用户隐私风险（利用视觉/文本内容）。
- **消融实验未包含所有组合**：例如未比较同时移除SemID和SimBucket的基线，也未测试不同前缀深度的影响。
- **训练配置简化**：所有模型仅训练1个epoch，虽为工业标准实践，但可能限制模型容量发挥，且不同方法收敛速度未必相同。

（完）
