---
title: "OneRank: Unified Transformer-Native Ranking Architecture for Multi-Task Recommendation"
title_zh: "OneRank: 面向多任务推荐的统一Transformer原生排序架构"
authors: "Jiakai Tang, Sunhao Dai, Kun Wang, Zhiluohan Guo, Yu Zhao, Cong Fu, Kangle Wu, Yabo Ni, Anxiang Zeng, Xu Chen, Jun Xu"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.16838v1"
tags: ["query:q1"]
score: 9.0
evidence: 统一的Transformer原生排序用于多任务推荐
tldr: 多任务学习在推荐系统中至关重要，但现有Transformer架构将特征编码与多任务预测解耦，导致信息瓶颈、梯度干扰和性能瓶颈。本文提出OneRank，一种Transformer原生的多任务排序框架，通过任务私有通道实现前向的任务特定表示学习和反向的梯度隔离，并用动态匹配评分替代静态MLP。在大规模工业数据集上的离线与在线实验表明，OneRank显著优于现有方法，同时保持计算效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.16838v1/fig-001.webp\", \"caption\": \"Figure 1: Architectural comparison between (a) traditional encoder–predictor paradigm and (b) our proposed OneRank framework. OneRank internalizes multi-task reasoning within the Transformer-native stack, enabling taskspecialized representation learning, dynamic context-aware ranking, and controlled cross-task knowledge transfer without architectural transitions.\", \"page\": 2, \"index\": 1, \"width\": 487, \"height\": 260}, {\"url\": \"assets/figures/arxiv/2606.16838v1/fig-002.webp\", \"caption\": \"Table 1: Offline performance comparison under different encoder architectures and multi-task learning strategies, together with model size (Params) and computational cost (FLOPs). Best results are highlighted in bold.\", \"page\": 7, \"index\": 2, \"width\": 942, \"height\": 417}, {\"url\": \"assets/figures/arxiv/2606.16838v1/fig-003.webp\", \"caption\": \"Table 2: Statistics of the Shopee dataset. Abbreviations: M = Million (106), B = Billion (109).\", \"page\": 7, \"index\": 3, \"width\": 503, \"height\": 474}, {\"url\": \"assets/figures/arxiv/2606.16838v1/fig-004.webp\", \"caption\": \"Figure 4: Scaling performance analysis of OneRank w.r.t. encoder depth and hidden size. 4.4 Scaling Analysis\", \"page\": 8, \"index\": 4, \"width\": 493, \"height\": 336}, {\"url\": \"assets/figures/arxiv/2606.16838v1/fig-005.webp\", \"caption\": \"Figure 2: Overall architecture of OneRank. The input is organized into a unified token sequence with structured tokenization, including interaction history (IH), preference anchoring (PA), and candidate-task token groups. Task-specific token injection with mutual invisibility enables early task specialization. Candidate-aware contextualization aggregates cross-candidate signals via situational descriptors, while flexible cross-task relational attention facilitates controlled knowledge transfer across tasks. OneRank adopts a matching-based scoring formulation for dynamic context-aware personalized ranking.\", \"page\": 4, \"index\": 5, \"width\": 981, \"height\": 519}]"
motivation: 现有Transformer在推荐系统中作为任务无关编码器，导致信息瓶颈、梯度干扰和表示学习与预测的不兼容，限制了多任务学习的性能与可扩展性。
method: OneRank设计任务私有通道，前向通过任务条件信息选择、候选感知上下文和受控跨任务交互学习任务特定表示，反向通过跨任务梯度隔离防止负迁移，并用动态匹配评分替代静态MLP。
result: 在大规模工业数据集上的离线与在线实验中，OneRank显著优于当前最优基线，且计算效率相当。
conclusion: OneRank通过将多任务推理内部化到Transformer堆栈中，建立了一个统一且可扩展的架构范式，有效解决了现有方法的局限性。
---

## 摘要
多任务学习在推荐系统中至关重要，能够实现不同用户反馈之间的互补学习。虽然现代工业实践已从DNN转向以Transformer为中心的架构，以加强序列建模和扩展能力，但它们仍然将特征编码与多任务预测解耦，将Transformer视为任务无关的编码器。这种设计从根本上限制了性能和可扩展性：(1) 在异构任务目标下造成信息瓶颈，(2) 诱发梯度干扰导致跷跷板现象，(3) 强制数据流转换，使得基于注意力的上下文自适应表示学习转化为静态前馈任务预测，且信息读写动态不兼容。我们提出OneRank，一种原生Transformer多任务排序框架，消除了编码器-预测器分离，并为前向表示学习和反向优化引入了任务私有通道，实现了任务专业化学习，同时减少了任务间干扰。在前向传播中，OneRank通过任务条件信息选择、候选感知上下文化和受控的跨任务交互，自底向上学习任务特定表示。在反向传播中，跨任务梯度分离将任务私有参数更新与共享知识提取模块隔离，防止负迁移。我们进一步用基于动态匹配的评分替代静态任务特定MLP评分器，实现上下文感知的个性化排序。通过将多任务推理内化于Transformer栈中，OneRank建立了一个统一且可扩展的架构范式。在大规模工业数据集上的离线和在线实验表明，OneRank在保持计算效率的同时显著优于最先进的基线方法。

## Abstract
Multi-task learning (MTL) is essential in recommender systems to enable complementary learning among diverse user feedback. While modern industrial practices have shifted from DNNs to Transformer-centric architectures to strengthen sequence modeling and scaling capacity, they still decouple feature encoding from multi-task prediction, treating the Transformer as a task-agnostic encoder. This design fundamentally limits the performance and scalability by (1) creating an information bottleneck under heterogeneous task objectives, (2) inducing gradient interference that leads to the seesaw phenomenon, and (3) forcing a dataflow transition in which attention-based, context-adaptive representation learning is converted to static feed-forward task prediction with incompatible information read-write dynamics.   We propose OneRank, a Transformer-native multi-task ranking framework that eliminates encoder-predictor separation and introduces task-private channels for forward representation learning and backward optimization, enabling task-specialized learning while reducing inter-task interference. In the forward pass, OneRank learns task-specific representations bottom-up through task-conditioned information selection, candidate-aware contextualization, and controlled cross-task interaction. In the backward pass, cross-task gradient detachment isolates task-private parameter updates from shared knowledge extraction modules, preventing negative transfer. We further replace static task-specific MLP scorers with dynamic matching-based scoring for context-aware personalized ranking. By internalizing multi-task reasoning within the Transformer stack, OneRank establishes a unified and scalable architectural paradigm. Offline and online experiments on large-scale industrial datasets show that OneRank significantly outperforms state-of-the-art baselines while maintaining computational efficiency.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：现代工业推荐系统广泛采用多任务学习（MTL）以联合建模多样的用户反馈（如点击、加购、下单）。然而，尽管架构已从DNN转向Transformer以增强序列建模和扩展能力，现有方法仍沿袭“编码器-预测器”分离范式（`G(Z=F(X))`），即先用Transformer作为任务无关的编码器提取共享表示Z，再使用独立的MLP任务塔进行预测。这种设计导致三个根本性问题：
  1. **信息瓶颈**：共享表示Z将任务特定信号与共享知识混合，下游预测器被迫从融合嵌入中分离任务特异性信息，且预测阶段建模能力通常受限。
  2. **梯度干扰与跷跷板现象**：共享参数上的冲突梯度可能导致优化一个任务时损害其他任务。
  3. **数据流转换不兼容**：基于注意力的上下文自适应表示学习被转换为静态前馈任务预测，打断了端到端的任务推理和计算缩放。

- **整体含义**：需要一种新的架构范式，将多任务推理内化于Transformer内部，消除编码器-预测器分离，实现任务专业化学习、可控的跨任务知识迁移，并解决训练-服务不一致问题。

## 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：OneRank是一个原生Transformer多任务排序框架，通过在前向传播中自底向上构建任务私有通道（任务私有token、候选感知上下文、受控跨任务交互），并在反向传播中使用跨任务梯度隔离，实现任务特定学习与最少任务间干扰。最终用动态匹配评分替代静态MLP评分器。

- **关键技术细节**：
  1. **结构化分词（Structured Tokenization）**：
     - 将异构输入组织成统一token序列：交互历史（SIH）+ 偏好锚（SPA，检索增强的序列）+ 候选-任务token组（Gi = [eC_i, t1, t2, ..., tK]），其中任务token参数{t_k}跨候选共享。
  2. **任务特定编码（Task-Specific Encoding）**：
     - 通过结构化注意力掩码实现任务token间的互不可见，同时保持共享用户上下文的因果注意力。每个任务token只能看到用户上下文、自身候选嵌入和自身，无法看到其他任务token。这实现了早期任务专业化。
  3. **候选感知上下文化（Candidate-Aware Contextualization）**：
     - 引入情境描述符s（包含用户、查询、会话元数据），使用任务特定投影函数f_k将s映射为查询q_k，再通过任务特定的多头交叉注意力（MHCA_k）聚合全局候选信息，得到任务级全局表示h_k。
  4. **多任务预测（Multi-Task Prediction）**：
     - **跨任务注意力**：对{h_k}应用可配置掩码的MHSA，允许可控的跨任务信息流。
     - **梯度分离机制**：反向传播时仅允许对角线梯度流动，阻止跨任务梯度干扰，使跨任务注意力成为只读的知识迁移存储器。
     - **动态匹配评分**：最终得分s_ik = z_k^T · r_ik，其中z_k是经跨任务注意力增强的全局表示，r_ik是任务特定候选表示，通过内积实现上下文自适应评分。
  5. **联合学习目标**：结合List-wise InfoNCE损失和Point-wise BCE损失，加权求和。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：大规模工业数据集，来自Shopee（东南亚和拉美电商平台），包含2025年12月连续30天的用户日志，覆盖点击、加购、下单三个信号。统计：用户33M，物品118M，查询105M，展示26.6B，点击1.05B，加购251M，下单40M。该数据集为专有数据集，需结合丰富的序列特征、多任务标注和排序候选集。
- **基准（Baseline）**：对比了多种编码器架构（DNN、MTGR、OneTrans）与多种多任务学习策略的组合，包括：
  - 编码器：DNN（生产基线）、MTGR（Meituan生成式推荐）、OneTrans（ByteDance统一Transformer）。
  - 多任务策略：noMTL（单任务）、NSE（共享嵌入）、MMoE、PLE、DCMT、ResFlow。共形成多种组合对比。
  - 评估指标：AUC和GAUC用于三个任务（C、A、O）。
- **在线A/B测试**：在Shopee主排序场景进行7天测试，对照组为既有生产基线（优化后的Transformer编码器+多任务预测器），治疗组使用OneRank。流量分配各10%。指标包括GMV/UU、Paid GMV/UU、AR/UU和Bad Query Rate。

## 4. 资源与算力

- **文中未明确说明**：论文未提及训练所用的GPU型号、数量、总训练时长等具体算力信息。仅提到模型参数量和FLOPs，例如OneRank参数4.9M，FLOPs 1.0G（比某些基线高但参数量更紧凑）。在线部署时涉及每个请求最多4096个候选，分8×512组并行处理。未披露训练资源细节。

## 5. 实验数量与充分性

- **实验组数**：
  - 离线整体性能对比：包含多种编码器×多任务策略组合（超过20种配置），结果汇总为一张表。
  - 消融实验：6种变体（V1-V6），分别移除关键组件，结果以图展示。
  - 缩放分析：深度（1-6层）和宽度（128-512）变化，对比AUC和GAUC增量。
  - 参数敏感性：温度（0.05-2）和损失权重（1:2,1:1,2:1）的影响，各有图表。
  - 在线A/B测试：7天，两组（治疗vs对照）对比。
- **充分性与公平性**：
  - 对比方法覆盖了多种主流架构和MTL策略，且在同一实验环境下实现（如文中声明），确保公平。
  - 消融实验验证了每个组件的必要性，缩放分析展示了可扩展性，参数敏感性分析了关键超参数。
  - 在线实验验证了实际部署效果。
  - 不足之处：仅使用单一专有数据集，缺乏在公共数据集上的验证（但论文解释公共数据集不满足工业要求）；对比方法中DCMT在Transformer编码器下表现反常（过校正），但论文有所说明。

## 6. 论文的主要结论与发现

- **离线实验结论**：
  - MTL在DNN编码器下一致有益，更强的Transformer编码器进一步放大了MTL效果。
  - OneRank在所有指标上显著优于所有基线组合，证明统一Transformer原生架构的有效性。
  - 消融实验显示每个组件（任务特定token、候选感知上下文、跨任务注意力、梯度分离、情境描述符、可配置掩码）均不可或缺。
  - OneRank具有良好的缩放能力：增加深度和宽度均持续提升性能。
- **在线实验结论**：
  - OneRank相比生产基线实现：GMV/UU +1.01%，Paid GMV/UU +1.17%，AR/UU +0.81%，Bad Query Rate降低2.29%。
  - 说明OneRank既提升了商业指标又改善了用户体验。
- **整体发现**：消除编码器-预测器分离，将多任务推理内化于Transformer内部，是更优的工业级推荐架构范式。

## 7. 优点：方法与实验设计亮点

- **创新性**：
  - 首次提出“Transformer原生”的多任务排序框架，完全消除编码器-预测器解耦。
  - 任务特定token注入+互不可见掩码实现早期任务专业化，有效缓解跷跷板现象。
  - 候选感知上下文化（通过情境描述符和交叉注意力）显式建模候选集竞争关系，弥合训练-服务差距。
  - 跨任务注意力梯度分离机制创新性地实现前向知识迁移+反向优化隔离。
  - 动态匹配评分替代静态MLP，使评分自适应上下文。
- **实验设计亮点**：
  - 使用了大规模工业数据（亿级用户、百亿级展示），结果具有强实际参考价值。
  - 对比基线全面，同时变化编码器和MTL策略，能够归因于不同因素。
  - 在线A/B测试验证了商业收益和用户体验提升。
  - 参数敏感性分析全面（温度、损失权重）。

## 8. 不足与局限

- **实验覆盖局限性**：
  - 仅使用单一专有数据集，缺乏在多个公开异构数据集上的验证，可能影响泛化性（但论文给出了合理理由）。
  - 未与最新的基于LLM的推荐方法对比（如HSTU、RankMixing等，但这些方法也属于Transformer范畴，论文在相关工作部分提及但未直接对比）。
  - 在线测试仅持续7天，更长时间的稳定性未验证。
- **潜在偏差与风险**：
  - 梯度分离机制可能阻碍某些有益的跨任务梯度交互（论文选择阻止非对角线梯度，但并非所有任务间冲突都是负面的）。
  - 可配置掩码依赖于领域专业知识，对于任务关系不明的场景，可能选择不当导致次优。
  - 跨任务注意力的计算复杂度随任务数二次增长，虽远小于候选数，但在任务数很多时仍需注意。
- **应用限制**：
  - 需要大规模GPU资源用于训练（虽未报告算力，但1.0G FLOPs在工业规模下仍需大量计算）。
  - 对特征工程和结构化分词有一定依赖，新场景需要重新设计tokenization。
  - 未提供代码或公共复现细节，可复现性有限。

（完）
