---
title: "PEARL: Unbiased Percentile Estimation via Contrastive Learning for Industrial-Scale Livestream Recommendation"
title_zh: PEARL：通过对比学习实现工业级直播推荐的无偏百分位估计
authors: "Blake Gella, Wei Wu, Yuhao Yin, Zexi Huang, Zikai Wang, Emily Liu, Junlin Zhang, Wentao Guo, Qinglei Wang"
date: 2026-05-20
pdf: "https://arxiv.org/pdf/2605.21752v1"
tags: ["query:q1"]
score: 9.0
evidence: 工业级直播推荐系统的无偏分位数估计
tldr: "针对推荐系统中用户行为强度不平衡导致反馈信号失真问题，提出PEARL框架，利用对比学习直接估计百分位偏好，无需辅助分布模型。通过理论证明无偏性，引入bootstrap平滑、值加权和协同训练策略。在工业直播推荐中，离线实验缓解偏差，在线A/B测试实现观看时长+2.10%、消费+0.80%、互动率+1.49%、举报率-6.91%。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.21752v1/fig-001.webp\", \"caption\": \"Figure 2: The comparison of the UAUC among different user active level cohorts. The non-live cohort corresponds to users who have not watched livestreams in the past month, while the high-active cohort corresponds to users who consumes livestreams content on a daily basis.\", \"page\": 8, \"index\": 1, \"width\": 457, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2605.21752v1/fig-002.webp\", \"caption\": \"Figure 1: The overall architecture of PEARL, which consists of two core components: A user-keyed reservoir pool (𝑁 = 50) that dynamically maintains an unbiased historical distribution. (2) Contrastive Learning: A multi-sample comparator that cross-evaluates model predictions against historical samples to compute the multi-sample contrastive loss (L𝑀𝐵𝐶𝐸 ) without explicit distribution tracking.\", \"page\": 4, \"index\": 2, \"width\": 973, \"height\": 340}]"
motivation: 用户交互数据中的行为强度不平衡使模型放大高活跃用户信号，降低推荐质量。
method: 提出非参数对比百分位近似框架PEARL，通过对比样本直接估计百分位偏好，结合bootstrap平滑、值加权和协同训练。
result: "离线实验缓解偏差，在线A/B测试中观看时长+2.10%、消费金额+0.80%、互动率+1.49%、举报率-6.91%。"
conclusion: PEARL有效缓解行为偏差，提升推荐鲁棒性和性能，适合工业级应用。
---

## 摘要
基于用户交互数据训练的推荐系统容易受到行为强度不平衡的影响——这是一种由用户间异质参与模式导致的系统性偏差。这种不平衡扭曲了反馈信号，使得观察到的交互不再真实反映用户偏好，导致模型过度放大高活跃用户信号而低估其他用户，最终损害大规模推荐系统的质量和鲁棒性。为解决该问题，我们提出非参数对比百分位近似框架PEARL，该框架建模相对偏好信号而非绝对参与量级。基于相对优势去偏方法，PEARL直接利用真实对比交互样本近似百分位关系，无需依赖辅助分布估计模型。我们提供理论证明表明，此类成对比较能产生基于百分位的偏好信号的无偏估计。为提升适用性，我们引入基于预测的自举机制进行百分位平滑以处理稀疏离散反馈，同时结合广义值加权公式和协同训练策略以增强模型灵活性与表征学习。大量离线实验表明，PEARL有效缓解行为偏差，并在多个排序目标上持续提升推荐性能。在拥有数十亿用户组合的生产级直播平台部署后，在线A/B测试证实了显著的现实增益：观看时长+2.10%，消费金额+0.80%，互动率+1.49%，举报率-6.91%。

## Abstract
Recommender systems trained on user interaction data are susceptible to behavioral intensity imbalance--a systematic distortion arising from heterogeneous engagement patterns across users. This imbalance skews feedback signals such that observed interactions no longer faithfully reflect true preferences, causing models to disproportionately amplify signals from highly active users while underrepresenting others, which ultimately degrades recommendation quality and robustness at scale. To address this issue, we propose a nonparametric contrastive percentile approximation framework, PEARL, that models relative preference signals instead of absolute engagement magnitudes. Building upon relative advantage debiasing, PEARL leverages real contrastive interaction samples to approximate percentile relationships directly, without relying on auxiliary distribution estimation models. We provide theoretical justification demonstrating that such pairwise comparisons yield unbiased estimates of percentile-based preference signals. For broader applicability, we introduce a prediction-based bootstrapping mechanism for percentile smoothing to handle sparse and discrete feedback, alongside a generalized value-weighted formulation and a co-training strategy to enhance both modeling flexibility and representation learning. Extensive offline experiments demonstrate that PEARL effectively mitigates behavioral bias and consistently improves recommendation performance across multiple ranking targets. Deployed in a production livestream platform with a combined user base of billions, online A/B testing confirms substantial real-world gains: +2.10% Watch Duration, +0.80% Consumption Amount, +1.49% Interaction Rate, and -6.91% Report Rate.

---

## 论文详细总结（自动生成）

# PEARL: 通过对比学习实现工业级直播推荐的无偏百分位估计——论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：推荐系统依赖用户交互数据训练，但用户行为存在**强度不平衡**——高活跃用户产生密集信号，低活跃用户交互稀疏，导致观察到的交互不能真实反映偏好，模型被高活跃用户主导，低活跃用户被低估，最终降低推荐质量和鲁棒性。
- **研究背景**：现有去偏方法（如反事实学习、因果推理、表征去偏）多假设用户反馈同质，或依赖辅助分布估计（如RAD），增加了建模复杂度和估计误差。直播推荐场景下，用户对直播内容的行为与短视频差异大，存在**内容格式偏好偏差**，加剧了用户侧偏差。
- **整体意义**：提出轻量级、无分布假设的对比百分位近似框架PEARL，直接通过对比样本学习相对偏好，消除对绝对量级的依赖，从根源上缓解行为强度偏差，在大规模工业系统中取得显著线上收益。

## 2. 方法论：核心思想、关键技术细节

### 2.1 核心思想
将预测目标从**绝对量级**（如观看时长）转换为**用户级百分位**，利用用户自身历史分布进行对比，学习相对排序信号。通过对比学习直接逼近百分位，无需显式维护用户分布或辅助模型。

### 2.2 关键技术细节

1. **问题形式化**  
   - 对用户 \( u \) 的交互量级 \( y \)，定义百分位 \( p = F_u(y) = P(Y_u \le y) \)。目标是学习映射 \( f(x;\theta) \approx p \)，输入为特征向量 \( x \)，输出为 \([0,1]\) 的百分位估计。

2. **单样本对比学习（Section 3.2）**  
   - 从用户历史分布中采样一个随机样本 \( Y' \)，构建二值指示 \( I(y > Y') \)。**定理1**证明：该指示的期望等于真实百分位 \( \text{CDF}_u(y) \)。采用BCE损失优化，使模型输出逼近百分位。
   - 由于单样本方差大，引入多样本策略。

3. **多样本对比学习（Section 3.3）**  
   - 采样 \( N=50 \) 个历史样本，得到平均指示 \( \bar{p} = \frac{1}{N}\sum_i I(y > Y'_i) \)。**定理2**证明方差降低为原来的 \( 1/N \)。使用Multi-sample BCE（MBCE）损失，等价于软标签的BCE，优化目标为 \( \bar{p} \)。
   - 在工业系统中，通过**水库采样**维护每个用户大小为50的历史池，保证无偏、低延迟的样本分布。

4. **值加权对比学习（Section 3.4）**  
   - 为融合量级信息，提出VWBCE损失：每个历史样本按其量级 \( Y'_i \) 加权，得到值加权标签 \( \bar{p}_{vw} \)。**定理3**证明最优预测为**部分期望比**：累计量级占总量级的比例。平衡了排序与绝对价值。

5. **自举对比学习（Section 3.5）**  
   - 针对离散稀疏标签（如0/1举报），提出用**连续先验预测值** \( \hat{y} \) 替代原始标签进行对比。通过历史先验预测的百分位作为软标签，提供更平滑的监督，避免退化。

6. **百分位协同训练（Section 3.6）**  
   - 对于需要保留绝对量级校准的任务（如广告eCPM），采用双头训练：回归损失 \( L_{reg} \) 保持绝对值预测，PEARL损失 \( L_{PEARL} \) 作为排序正则化项，总损失 \( L_{total} = L_{reg} + \lambda L_{PEARL} \)。推理时使用回归头的绝对输出。

7. **大规模系统实现（Section 3.7）**  
   - **水库采样**：Parameter Server存储用户级历史池（50个最近样本），通过门控概率更新，保证无偏。  
   - **梯度门控**：对历史样本数不足阈值（\( \tau=10 \)）的用户，跳过百分位梯度的更新，避免噪声污染。

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：工业级生产数据集，来自排名阶段，包含 **1680亿实例**、4552个特征、98个目标（见表1）。
- **场景**：全球领先的直播推荐平台，数十亿用户。
- **基准方法**：
  - 原始回归/分类模型（Baseline）
  - **CQE**（Conditional Quantile Estimation）：条件分位数估计，非个性化去偏
  - **RAD**（Relative Advantage Debiasing）：相对优势去偏，依赖用户级CDF
- **共享骨干**：现代排名架构（Zenith），各任务有独立MLP Tower。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量或训练时长。仅提到训练优化器为RMSPropV2，学习率0.0015，动量0.999999。考虑到工业级规模（1680亿样本），推测使用了大规模分布式训练集群，但具体硬件细节未披露。

## 5. 实验数量与充分性

- **离线实验**：在**5个重要目标**上进行了评估（交互、会话观看时长、消费、举报、长期观看时长），每个目标应用了不同的PEARL变体（多样本、值加权、自举、协同训练）。同时展示了不同用户活跃度分层的UAUC对比（图2）。
- **在线A/B测试**：进行了**多维度的周级在线实验**，覆盖观看会话、观看时长、消费金额、互动率、举报率等指标。
- **消融分析**：通过对比CQE、RAD等基线方法，以及不同PEARL变体在不同目标上的表现，间接验证了各组件有效性。
- **公平性与充分性**：实验较充分，离线结果与线上结果一致；对比方法为当前主流去偏方法；分用户层分析展示了PEARL对低活跃用户的显著改进。但未进行显式的消融实验（如单独去掉值加权或自举），也未讨论不同池大小 \( N \) 或阈值 \( \tau \) 的敏感性。

## 6. 主要结论与发现

1. **PEARL在所有测试目标上均优于基线和其他去偏方法**：离线UAUC提升显著（交互+6.93%，长期观看时长+16.1%，举报+4.96%等）。
2. **有效缓解用户行为偏差**：分用户层分析显示，对于非直播用户和低活跃用户，百分位目标UAUC远高于原始量级目标（非直播用户从0.501提升至0.698），而高活跃用户基本持平。
3. **线上收益显著**：观看时长+2.10%，消费金额+0.80%，互动率+1.49%，举报率-6.91%。
4. **协同训练能兼顾绝对值校准与排序提升**：在长期观看时长目标上，协同训练后的回归目标UAUC达0.800（原始0.689），证明百分位建模的知识迁移效果。

## 7. 优点

- **方法创新**：将百分位估计转化为对比学习问题，理论严密（三个定理），无需辅助分布模型，轻量且可扩展。
- **工业级实用性**：设计的水库采样、梯度门控、值加权、自举等机制均针对大规模系统约束，实现了低延迟、低存储、高稳定性。
- **实验扎实**：离线+在线A/B测试结合，覆盖多种目标类型（连续、离散、稀疏），并分析了用户分层效果，证明去偏的有效性。
- **代码与复现友好**：虽然未公开代码，但方法描述详细，公式清晰，工程实现考虑周到。

## 8. 不足与局限

- **硬件资源未公开**：无法评估训练成本，降低对其可复现性的信心。
- **消融实验不完整**：未单独验证水库采样大小 \( N \)、梯度门控阈值 \( \tau \)、协同训练权重 \( \lambda \) 的敏感性；未展示去掉某个扩展（如值加权、自举）后的性能变化。
- **对比方法有限**：仅对比CQE和RAD，未与近期流行的去偏方法（如D2Co、DVR、AutoDebias等）比较；也未与基于逆倾向评分（IPS）的方法对比。
- **依赖先验预测**：自举对比学习需依赖一个连续先验模型，引入额外依赖，且先验性能可能影响效果。
- **泛化性验证不足**：仅在直播场景验证，未在短视频、新闻、电商等其他推荐场景测试；也未讨论对冷启动用户的处理（阈值 \( \tau=10 \) 可能导致冷启用户无梯度）。
- **理论假设**：假设用户历史分布可采样，对于新用户或极稀疏用户，水库池无法有效构建，可能导致性能退化。

（完）
