---
title: Query-aware Routing for Filtered Approximate Nearest Neighbors Search
title_zh: 面向查询的自适应路由用于过滤近似最近邻搜索
authors: "Qianqian Xiong, Mengxuan Zhang"
date: 2026-06-18
pdf: "https://arxiv.org/pdf/2606.19898v1"
tags: ["query:q1"]
score: 7.0
evidence: 用于检索的过滤近似最近邻查询感知路由
tldr: 过滤近似最近邻搜索是向量数据库和RAG的核心操作。研究发现，不同数据集和谓词下无单一最优方法，甚至同一数据集内最佳方法也因查询而异。为此提出查询感知路由框架，用轻量ML模型预测各方法的召回率，结合离线基准表选择最佳召回-QPS权衡。将候选特征压缩至3个，采用回归预测。在5个未见验证集上达到SOTA，且延迟开销可忽略。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19898v1/fig-001.webp\", \"caption\": \"Figure 3: Query-aware ML routing pipeline.\", \"page\": 6, \"index\": 1, \"width\": 1056, \"height\": 454}, {\"url\": \"assets/figures/arxiv/2606.19898v1/fig-002.webp\", \"caption\": \"Figure 5: Recall–QPS Pareto on all combinations of dataset and predicate type.\", \"page\": 11, \"index\": 2, \"width\": 1036, \"height\": 863}, {\"url\": \"assets/figures/arxiv/2606.19898v1/fig-003.webp\", \"caption\": \"Figure 2: Recall–QPS comparison of all benchmarked filtered ANN methods across datasets and predicate types.\", \"page\": 4, \"index\": 3, \"width\": 1056, \"height\": 1007}, {\"url\": \"assets/figures/arxiv/2606.19898v1/fig-004.webp\", \"caption\": \"Table 3: Parameter sweep ranges for training data collection.\", \"page\": 9, \"index\": 4, \"width\": 1056, \"height\": 229}]"
motivation: 现有过滤ANN方法在不同数据集和谓词下表现差异大，且同一数据集内查询也需不同方法，因此需要查询感知的路由策略。
method: 轻量ML模型预测每个候选方法在查询上的召回率，路由器基于离线基准表（方法→召回率和QPS）选择最优召回-QPS权衡的方法，使用3个关键特征和回归目标。
result: 在5个未见验证集上，路由器的召回率和QPS平衡优于所有现有过滤ANN基线，且延迟开销极小。
conclusion: 查询感知路由框架能有效提升过滤ANN搜索性能，实现自适应方法选择。
---

## 摘要
过滤近似最近邻搜索将向量相似性与属性谓词相结合，是现代向量数据库和检索增强生成中的核心原语。我们在多个数据集上对三种谓词下的所有主要分类过滤ANN方法进行了基准测试，发现没有单一方法占据主导地位。此外，即使在同一个数据集和谓词类型下，不同查询的最佳方法也可能不同。因此，我们提出了一种查询感知的路由框架。一个轻量级机器学习模型预测每种候选方法在查询上的召回率，路由器查询一个离线的基准表（该表将每种方法和参数设置映射到其测量的召回率和QPS），然后选择在召回率-QPS权衡中表现最佳的方法。我们的消融研究将22个候选特征缩减为最小的三个特征集，并采用回归而非分类作为预测目标以提高准确性。我们的模型在六个真实世界数据集上训练，并在五个未见过的验证数据集上应用。最终结果表明，与现有的过滤ANN基线相比，我们的路由器在所有五个验证数据集上实现了最先进的召回率和QPS平衡，同时延迟开销可忽略不计。

## Abstract
Filtered ANN search, which combines vector similarity with attribute predicates, is a core primitive in modern vector databases and retrieval-augmented generation. We benchmark all major categorical filtered ANN methods across multiple datasets under three predicates and find that no single method dominates. Moreover, even within a single dataset and predicate type, the best method for a query can vary. Therefore, we propose a query-aware routing framework. A lightweight ML model predicts each candidate method's recall on the query, and the router consults an offline benchmark table that maps every method and parameter setting to its measured recall and QPS, then selects the method with the best recall--QPS trade-off. Our ablation study narrows 22 candidate features to a minimal set of three and we adopt regression rather than classification as the prediction target to sharpen accuracy. Our model is trained on six real-world datasets and applied to five unseen validation datasets. The final result shows that our router achieves state-of-the-art recall and QPS balance across all five validation datasets compared to existing filtered ANN baselines, while incurring negligible latency overhead.