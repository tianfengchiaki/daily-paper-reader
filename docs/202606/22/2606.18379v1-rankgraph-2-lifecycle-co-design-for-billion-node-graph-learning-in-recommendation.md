---
title: "RankGraph-2: Lifecycle Co-Design for Billion-Node Graph Learning in Recommendation"
title_zh: RankGraph-2：面向推荐中十亿节点图学习的生命周期协同设计
authors: "Renzhi Wu, Zikun Cui, Junjie Yang, Tai Guo, Hong Li, Xian Chen, Li Yu, Ke Pan, Sri Reddy, Mahesh Srinivasan, Nipun Mathur, Haomin Yu, Hong Yan"
date: 2026-06-16
pdf: "https://arxiv.org/pdf/2606.18379v1"
tags: ["query:q1"]
score: 9.0
evidence: Meta的十亿节点图学习框架用于推荐中的相似性检索
tldr: "针对十亿节点图检索中图构建、表示学习与实时服务孤立的问题，提出RankGraph-2框架，在Meta部署，通过生命周期协同设计，利用带偏差校正的子采样、个性化PageRank预计算和残差量化聚类索引，将服务计算成本降低83%，召回率比基线提升3.8倍，并带来CTR+0.96%、CVR+2.75%的收益。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.18379v1/fig-001.webp\", \"caption\": \"Figure 1 Lifecycle co-design in RankGraph-2. Serving and training impose requirements (dashed red, left) on upstream stages. Each requirement is resolved by a codesigned solution (solid blue, right), creating mutual dependencies across the pipeline.\", \"page\": 2, \"index\": 1, \"width\": 436, \"height\": 329}, {\"url\": \"assets/figures/arxiv/2606.18379v1/fig-002.webp\", \"caption\": \"Table 1 Cross-stage design coupling in billion-scale recommendation systems. For competing systems, “Cross-stage gaps” highlights dependencies left unaddressed. For RankGraph-2, the same column shows the co-design couplings that close these gaps.\", \"page\": 4, \"index\": 2, \"width\": 968, \"height\": 297}, {\"url\": \"assets/figures/arxiv/2606.18379v1/fig-003.webp\", \"caption\": \"Table 4 Learned index with vs without the regularization module for code imbalance\", \"page\": 9, \"index\": 3, \"width\": 478, \"height\": 366}, {\"url\": \"assets/figures/arxiv/2606.18379v1/fig-004.webp\", \"caption\": \"Figure 2 The RankGraph-2 system architecture for billion-node graph learning and retrieval. (A) Graph Construction transforms raw user-item interaction logs into a tractable, heterogeneous graph via popularity bias correction, edge subsampling, and Personalized PageRank (PPR) multi-hop neighbor pre-computation. (B) Model Training leverages multi-head type-aware feature encoders and a heterogeneous aggregator to optimize node representations using a contrastive link prediction objective. (C) Index Co-Learning utilizes residual quantization to jointly train a regularized cluster index, preventing codebook collapse and enabling KNN-free U2U2I retrieval that reduces serving cost by 83%.\", \"page\": 5, \"index\": 4, \"width\": 972, \"height\": 537}]"
motivation: 现有方法孤立处理图构建、表示学习和实时服务，无法满足十亿节点检索场景下三者紧密耦合的需求。
method: 级联设计：构建阶段使用带流行度偏差校正的子采样和个性化PageRank预计算邻域；训练阶段联合学习残差量化索引；服务阶段利用共学索引避免在线KNN。
result: "服务计算成本降低83%；召回率比GAT+DGI高3.8倍，比PyTorch-BigGraph高2.1倍；CTR提升0.96%，CVR提升2.75%。"
conclusion: 生命周期协同设计使简单架构在十亿节点图检索中取得显著效果，已支撑Meta 20+检索上线。
---

## 摘要
十亿节点规模的基于图的检索需要联合解决三个紧密耦合的问题——图构建、表示学习和实时服务——然而现有工作各自孤立地处理这些问题。我们提出了RankGraph-2，一个部署在Meta的框架，它为基于相似度的检索（U2U2I和U2I2I）协同设计了所有三个生命周期阶段，每个阶段的需求塑造其他阶段。服务需要一个协同学习的聚类索引来避免昂贵的在线KNN——这促使将索引协同训练纳入训练目标。训练受益于一个观察：基于相似度的检索容忍预计算的邻域，从而消除了在线图基础设施——这要求构建产生自包含的数据。构建还必须支持小时级别的刷新以保持物品覆盖。针对这些级联需求，RankGraph-2通过带流行度偏差修正的子采样将数万亿条边减少到数千亿条，通过个性化PageRank预计算多跳邻域，并协同学习一个残差量化聚类索引，将服务计算成本降低了83%。这种生命周期协同设计使得一个简单架构能够在二分图上实现比GAT+Deep Graph Infomax模型高3.8倍的召回率，在物品检索上比PyTorch-BigGraph高2.1倍。RankGraph-2带来了高达+0.96%的CTR和+2.75%的CVR提升，并已在主要面上支持了20多次检索发布。

## Abstract
Graph-based retrieval at billion-node scale requires jointly solving three tightly coupled problems -- graph construction, representation learning, and real-time serving -- yet existing work addresses each in isolation. We present RankGraph-2, a framework deployed at Meta that co-designs all three lifecycle stages for similarity-based retrieval (U2U2I and U2I2I), where each stage's requirements shape the others. Serving requires a co-learned cluster index to avoid expensive online KNN -- this pushes index co-training into the training objective. Training benefits from the observation that similarity-based retrieval tolerates pre-computed neighborhoods, eliminating online graph infrastructure -- this requires construction to produce self-contained data. Construction must also support hour-level refresh for item coverage. Acting on these cascading requirements, RankGraph-2 reduces hundreds of trillions of edges to hundreds of billions via subsampling with popularity bias correction, pre-computes multi-hop neighborhoods via personalized PageRank, and co-learns a residual-quantization cluster index that reduces serving computational cost by 83%. This lifecycle co-design enables a simple architecture to achieve 3.8 x higher recall than a GAT + Deep Graph Infomax model on a bipartite graph and 2.1 x higher than PyTorch-BigGraph on item retrieval. RankGraph-2 delivers up to +0.96% CTR and +2.75% CVR, and has powered 20+ retrieval launches across major surfaces.

---

## 论文详细总结（自动生成）

# 详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **问题**：基于图的检索在十亿节点规模下需要联合解决三个紧密耦合的问题——图构建、表示学习和实时服务。现有工作孤立地优化每个阶段，忽视了跨阶段依赖关系，导致整体性能瓶颈。
- **背景**：在Meta等大规模推荐系统中，图构建需要处理数百亿条交互记录，表示学习需在庞大图上训练，实时服务需要低延迟检索。学术研究侧重小规模模型，工业系统（如GiGL、LiGNN）优化训练基础设施但服务成本高昂且假设图已给定。
- **核心观点**：生命周期协同设计——让每个阶段的需求反向塑造上游阶段——可以解锁单独优化无法达到的性能。

## 2. 方法论：核心思想、关键技术细节、流程

- **核心思想**：三个生命周期阶段（构建→训练→服务）通过级联需求相互约束，每个阶段的输出适配下游阶段。
- **图构建**（Section 4.2）：
    - 构建异构协交互图，包含三种边类型：U-I（用户-物品）、U-U（用户-用户）、I-I（物品-物品），所有边从同一组互动数据导出。
    - **流行度偏差修正**：对I-I边权重进行修正：`w' = w × (w_out / ∑w_out)^α`，α=0.3，减少热门物品的过度连接。
    - **边子采样**：保留约0.1B高业务价值节点；对每个节点按权重保留top-K<sub>CAP</sub>条边，将边从数百亿缩减到数百亿。
    - **预计算重要邻居**：使用个性化PageRank（PPR）在骨干图上识别每个节点的K<sub>IMP</sub>个最重要邻居（随机游走R次，长度L，重启概率0.15），并包装成自包含的边格式，无需在线图基础设施。
- **模型训练**（Section 4.3）：
    - 输入预计算的边格式数据，类型感知特征编码器 + 异构聚合器（AGG）产生节点嵌入。
    - 损失函数：结合Margin Ranking Loss和InfoNCE Loss，对每种边类型加权（权重由不确定性加权学习）。
    - 负采样：批量内负样本 + 批量外滚动池 + 多头增强负样本。
    - 效率：数据获取、特征预处理、负采样在CPU上进行，与GPU模型训练重叠。
- **服务索引**（Section 4.4）：
    - **残差量化聚类索引**：两层码书（5000×50=250,000个聚类），学习将嵌入向量分解为聚类码。
    - **码书不平衡问题**：提出正则化损失L<sub>reg</sub>和偏倚码选择策略，防止连续训练下码书崩溃。
    - **U2U2I服务**：用户被分配聚类，每个聚类维护一个最近活跃用户的物品队列，服务时直接读取队列——替代在线KNN，减少83%服务成本。

## 3. 实验设计：数据集/场景、基准、对比方法

- **应用场景**：Meta的两大推荐面（Surface 1和Surface 2），每个服务数十亿用户。
- **数据集**：内部生产数据，图由24小时互动数据构建，每3小时完全重建（~1小时完成），包含数十亿节点和数百亿边。
- **对比方法**：
    - 用户嵌入评估：GAT-DGI（图注意力网络+深度图互信息）、HSTU（万亿参数序列基础模型）。
    - 物品嵌入评估：PBG（PyTorch-BigGraph）、HSTU。
- **评估指标**：
    - 离线：召回率@5,10,50,100（用户：100K样本；物品：1000条边采样）。
    - 在线A/B测试：14天测试，主要指标CTR（点击率）和CVR（转化率）。

## 4. 资源与算力

- 文中未明确说明使用的GPU型号、数量或训练时长。
- 训练配置：连续训练，批量大小32,768，使用AdaGrad（lr 0.02）和AdamW（lr 0.004）。嵌入维度256。端到端延迟从互动数据到刷新约2小时。
- 服务成本减少83%被量化，但具体基础设施（如GPU数量）未提供。

## 5. 实验数量与充分性

- **实验数量**：四组主要实验：
    - 用户嵌入质量（对比GAT-DGI和HSTU）
    - 物品嵌入质量（对比PBG和HSTU）
    - 学习索引质量（有/无正则化）
    - 消融实验：三种异质边类型、三种邻居选择策略（随机、顶权重、PPR）、流行度修正（有/无）
    - 在线A/B测试（两个曲面×两种检索类型）
- **充分性**：实验覆盖了主要设计维度，基线选择合理（包括强基线如HSTU、PBG），消融证明了每个组件的贡献。但离线评估均基于采样（100K用户、1000条边），可能无法完全代表全局分布；在线测试提供了真实场景验证。整体充分且客观。

## 6. 主要结论与发现

- **性能提升**：
    - 用户嵌入召回率@5：0.143 vs GAT-DGI 0.038（3.8倍）；物品嵌入召回率@100：0.775 vs PBG 0.374（2.1倍）。
    - 在线：CTR +0.96%（U2I2I Surface 1），CVR +2.75%；U2U2I也有显著提升。
    - 服务成本降低83%（通过集群索引替代在线KNN）。
- **关键发现**：收益主要来自生命周期协同设计（异质边、PPR邻居、流行度修正、联合学习索引），而非更复杂的模型架构。
- **互补性**：图模型与序列基础模型（如HSTU）互补，图嵌入可作结构先验特征供排名模型使用。

## 7. 优点

- **创新思想**：生命周期协同设计是首创，明确识别并解决了跨阶段依赖，实用性强。
- **简化基础设施**：证明相似度检索不需要在线图基础设施，预计算邻居可达到或超过在线采样质量。
- **联合学习索引**：训练过程中协同学习聚类索引，直接优化服务质量，避免后处理量化损失。
- **广泛部署验证**：在Meta多个面部署，支撑20+检索上线，在线指标显著提升。
- **消融完整**：逐一验证异质边、PPR邻居、流行度修正的贡献，结论可信。

## 8. 不足与局限

- **适用范围**：专门针对基于相似度的检索（U2U2I, U2I2I），不适用于需要实时行为特征的直接用户-物品匹配。
- **预计算限制**：离线预计算邻居无法支持需要查询依赖邻域探索的任务；3小时刷新周期对极端时间敏感场景可能不够。
- **资源细节缺失**：未提供GPU型号、数量、训练时长等算力信息，限制了可复现性评估。
- **采样偏差**：离线评估基于采样（100K用户、1000条边），可能存在选择偏差。
- **公开基准缺失**：未使用Amazon、MovieLens等公开数据集，因为规模不匹配，但生产环境独特性也意味着无法独立复现。

（完）
