---
title: "DREAM: Dynamic Refinement of Early Assignment Mappings"
title_zh: DREAM：早期分配映射的动态优化
authors: "Liwei Guan, Huanjie Wang, Hongwei Zhang, Linxun Chen, Zhaojie Liu"
date: 2026-06-05
pdf: "https://arxiv.org/pdf/2606.06947v1"
tags: ["query:llmapp"]
score: 8.0
evidence: 通过动态精炼语义ID的生成式推荐，利用类大语言模型生成
tldr: 生成式推荐使用语义ID进行检索，但现有方法对冷启动项采用静态分配，导致编码差。DREAM通过意图感知分词器生成候选ID、冻结推荐骨干作为评估器、动态波束机制保持多假设，实现对早期分配的动态优化，在三个Amazon基准上显著提升冷启动性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.06947v1/fig-001.webp\", \"caption\": \"Figure 4: UC3 conservative gating and CPDE multi-path recovery (Sports). (a) Of 18,548 vote-summary items, 56.8% fall back at the support gate, 12.4% at the margin gate, and only 10.6% actually switch SID; (b) winner items exhibit systematically higher\", \"page\": 10, \"index\": 1, \"width\": 1040, \"height\": 305}, {\"url\": \"assets/figures/arxiv/2606.06947v1/fig-002.webp\", \"caption\": \"Figure 3: CART prior-support repair. Three deterministic Sports cold-start items whose static SID is absent from the CART\", \"page\": 10, \"index\": 2, \"width\": 1040, \"height\": 314}, {\"url\": \"assets/figures/arxiv/2606.06947v1/fig-003.webp\", \"caption\": \"Table 4: Warm-item recommendation performance (R@10 /\", \"page\": 8, \"index\": 3, \"width\": 1052, \"height\": 271}, {\"url\": \"assets/figures/arxiv/2606.06947v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of the DREAM framework. The top trajectory shows SID evolution from the fragile static path Φ0 to CART candidate pool P𝑖 , UC3 primary SID ΦC, CPDE beam paths B𝑖 , and a union trie for cold-hit recovery. CART injects collaborative\", \"page\": 4, \"index\": 4, \"width\": 1056, \"height\": 561}]"
motivation: 现有语义ID方法因早期静态分配导致冷启动项编码不佳，成为性能瓶颈。
method: DREAM通过三阶段框架：意图感知分词器生成行为对齐候选、冻结推荐骨干作为评估器选择最优候选、动态波束机制维护多假设以防止过早收敛。
result: 在三个Amazon基准上，DREAM在冷启动指标上显著优于现有生成式和顺序推荐方法。
conclusion: 动态细化早期分配映射可有效缓解冷启动问题，桥接分词与生成的目标。
---

## 摘要
生成式推荐通过将物品检索重构为语义ID（Semantic IDs, SIDs）的自回归生成来推进物品检索，SID是编码物品语义的紧凑令牌序列。虽然SID提供了强大的语义先验，但当前基于SID的方法在观察到足够的用户反馈之前，通过离线分词为每个物品分配一个单一的静态标识符。对于冷启动物品，这种一次性提交会产生辨别力差的编码，生成不对齐的路径，并且由于相关令牌在训练期间很少被采样，这些路径无法得到优化。我们识别出这种早期静态提交（而非模型容量）是基于SID的生成式推荐中冷启动的根本瓶颈。为了克服这一瓶颈并弥合分词和生成的不一致目标，我们提出了DREAM（早期分配映射的动态优化），这是一个三阶段框架，通过渐进优化解决这一缺陷。首先，一个意图感知的分词器通过反事实对比学习重建SID空间，为每个冷启动物品生成一个多样化的行为对齐候选池。其次，冻结的推荐主干作为评估器，基于多上下文用户支持选择最可靠的候选，无需重新训练。第三，一个动态束机制在训练和推理过程中维护多个加权的SID假设，防止过早坍缩为单一分配。在三个Amazon基准上的大量实验表明，DREAM在冷启动指标上显著优于最先进的生成式和序列基线。

## Abstract
Generative recommendation advances item retrieval by reformulating it as autoregressive generation of Semantic IDs (SIDs), compact token sequences that encode item semantics. While SIDs offer a strong semantic prior, current SID-based methods assign each item a single static identifier through offline tokenization before sufficient user feedback is observed. For cold-start items, this one-shot commitment produces poorly discriminative codes, generating misaligned paths that remain unrefined because the associated tokens are rarely sampled during training. We identify this early static commitment, not model capacity, as the fundamental cold-start bottleneck in SID-based generative recommendation. To overcome this bottleneck and bridge the disjoint objectives of tokenization and generation, we propose DREAM (Dynamic Refinement of Early Assignment Mappings), a three-stage framework that resolves this flaw through progressive refinement. First, an intent-aware tokenizer rebuilds the SID space through counterfactual contrastive learning, generating a diverse pool of behavior-aligned candidates per cold-start item. Second, the frozen recommendation backbone serves as an evaluator, selecting the most reliable candidate based on multi-context user support without retraining. Third, a dynamic beam mechanism maintains multiple weighted SID hypotheses throughout training and inference, preventing premature collapse to a single assignment. Extensive experiments on three Amazon benchmarks show that DREAM substantially outperforms state-of-the-art generative and sequential baselines on cold-start metrics.