---
title: "L2Rec: Towards Dual-View Understanding of LLMs for Personalized Recommendation"
title_zh: L2Rec：面向大语言模型双重视图理解实现个性化推荐
authors: "Pingjun Pan, Tingting Zhou, Peiyao Lu, Tingting Fei, Hongxiang Chen, Chuanjiang Luo"
date: 2026-05-26
pdf: "https://arxiv.org/pdf/2605.26717v1"
tags: ["query:llmapp"]
score: 9.0
evidence: 具备双视图理解的LLM用于个性化推荐
tldr: 为适应个性化推荐，大型语言模型需对齐用户偏好并融合行为与语义信号。现有方法在输入或输出层融合存在分布差距或缺乏端到端监督。本文提出L2Rec，在参数级通过双视图个性化混合专家（DPMoE）机制施加低秩扰动，实现行为和语义的互补适应，再经自适应跨视图融合得到统一用户偏好。在四个数据集和工业A/B测试中显著优于基线。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26717v1/fig-001.webp\", \"caption\": \"Figure 1: The overall framework of L2Rec.\", \"page\": 2, \"index\": 1, \"width\": 494, \"height\": 291}]"
motivation: 现有方法在输入或输出层融合行为和语义信号，导致分布差距或缺乏端到端监督。
method: L2Rec在LLM参数级别通过DPMoE施加视图特定低秩扰动，并利用自适应融合模块整合双视图输出。
result: 在四个数据集和工业A/B测试中，L2Rec一致优于最先进基线，关键参与指标显著提升。
conclusion: L2Rec通过参数级融合有效对齐行为和语义视图，为推荐提供更优的个性化适应。
---

## 摘要
将大语言模型（LLMs）适配于个性化推荐，需要在有效利用行为信号与语义信号的同时，将其通用能力与用户特定偏好对齐。现有方法通常在输入层面（例如，将行为嵌入注入令牌空间）或输出层面（例如，对独立编码器进行对比对齐）整合这些信号，但存在分布差异或缺乏端到端任务监督的问题。在本工作中，我们提出了L2Rec，它在LLMs的参数层面统一了行为与语义理解。我们的核心洞察是，同一组Transformer参数可以作为两种视图的共享媒介：通过一种双视图个性化混合专家（DPMoE）机制应用视图特有的低秩扰动，L2Rec使得单个LLM主干能够为每个用户生成互补的行为与语义适配，且表示层面的不对齐最小化。一个自适应跨视图融合模块进一步将双视图输出整合为统一的用户偏好。在四个数据集上的实验表明，L2Rec持续优于最先进的基线方法，并且在一个大规模工业平台上的在线A/B测试验证了关键参与指标的显著提升。

## Abstract
Adapting large language models (LLMs) for personalized recommendation requires aligning their general-purpose capabilities with user-specific preferences while effectively leveraging both behavioral and semantic signals. Existing approaches typically integrate these signals at either the input level (e.g., injecting behavioral embeddings into the token space) or the output level (e.g., contrastive alignment of separate encoders), suffering from distribution gaps or lack of end-to-end task supervision. In this work, we introduce L2Rec, which unifies behavioral and semantic understanding at the parameter level of LLMs. Our key insight is that the same set of Transformer parameters can serve as a shared medium for both views: by applying view-specific, personalized low-rank perturbations via a Dual-view Personalized Mixture-of-Experts (DPMoE) mechanism, L2Rec enables a single LLM backbone to produce complementary behavioral and semantic adaptations for each user with minimal representation-level misalignment. An adaptive cross-view fusion module further integrates the dual-view outputs into a unified user preference. Experiments on four datasets show that L2Rec consistently outperforms state-of-the-art baselines, and online A/B testing on a large-scale industrial platform validates significant improvements in key engagement metrics.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **研究动机**：将大语言模型（LLMs）用于个性化推荐时，需解决两个交织的挑战：**任务对齐**（通用语言建模目标与用户偏好预测之间的差距）和**信号整合**（用户行为模式与物品语义信息的融合）。现有方法要么在**输入层**融合（如将行为嵌入注入令牌空间），要么在**输出层**融合（如对比对齐两个独立编码器），但都存在分布差异或缺乏端到端任务监督的问题。
- **背景**：传统推荐方法（如SASRec）擅长捕捉行为模式但缺乏语义理解；LLM-based方法（如LLaRA、LEARN）虽更强，但输入/输出层面的融合方式限制了其潜力。作者提出在**参数空间**进行融合，认为同一组Transformer参数可作为行为与语义两种视图的共享媒介，从而避免表示空间的对齐困难。

#### 2. 论文提出的方法论：核心思想、关键技术细节
- **核心思想**：在LLM的**参数层级**对行为与语义信号进行统一。通过对冻结的LLM主干施加**视图特定的个性化低秩扰动**（通过双视图个性化混合专家DPMoE），使同一主干为每个用户生成互补的行为和语义适配，再通过自适应跨视图融合模块整合为统一的用户偏好。
- **关键技术细节**：
  - **双视图输入构建**：语义视图将物品描述拼接成完整令牌序列（保留细粒度语义）；行为视图将物品压缩为单个向量（建模序列模式），并通过投影器对齐维度。
  - **DPMoE（双视图个性化混合专家）**：维护一个专家池，包括**共享专家**（捕捉跨视图共性）和**视图特定专家**（SPMoE用于语义、BPMoE用于行为）。采用**个性化路由器**，结合上下文、用户表示和交互信号，通过加法-乘法融合与Top-N稀疏化，为每个用户动态选择最合适的专家组合。
  - **自适应跨视图融合（ACF）**：对双视图输出进行残差投影后，通过动态门控机制融合，自适应地强调每个用户更信息量的视图。
  - **训练目标**：包含三类损失：对比推荐损失（L_Rec）、双向偏好对比损失（L_BPC，对齐双视图偏好）、负载平衡损失（L_LB，防止路由坍塌）。训练时仅更新DPMoE和ACF参数（约5%），LLM主干冻结。

#### 3. 实验设计：数据集、场景、对比方法
- **数据集**：三个Amazon Review子集（Scientific, Instruments, Arts）和一个大规模工业平台数据集（1.5M用户、611K物品、41.9M交互）。Amazon采用域迁移设置（预训练7个源类别，微调3个目标类别）；工业数据集使用前三个月日志预训练、后一个月微调。
- **评估指标**：Recall@10和NDCG@10。
- **对比方法**：7种基线，分为三类：
  - **ID-based**：SASRec、BERT4Rec
  - **Text-enhanced**：S3-Rec、UniSRec、RecFormer
  - **LLM-based**：LLaRA（Llama2-7B）、LEARN（Baichuan2-7B）

#### 4. 资源与算力
- 论文明确提到所有实验运行在**NVIDIA A100 GPU**上，但**未具体说明GPU数量、显存容量和训练时长**。仅提到Qwen3-0.6B作为主骨干，且L2Rec仅更新约32M参数（约5%），因此训练开销相对较低。

#### 5. 实验数量与充分性
- **实验组数**：非常充分。
  - 主实验：在4个数据集上对比7个基线，报告了NDCG@10和Recall@10（共8个指标）。
  - **消融实验**：包含5种变体（w/o Adapt, w/o Semantic, w/o Behavioral, w/o BPC, w/o PR），在3个数据集上验证了各组件的必要性。
  - **数据效率分析**：在Scientific和Instruments上使用5种训练比例（1%, 5%, 10%, 50%, 100%），比较了UniSRec、RecFormer、LEARN和L2Rec。
  - **超参数分析**：在工业数据集上分析了LoRA秩（2~32）和激活专家数（2~12）。
  - **在线A/B测试**：在~1.5M DAU平台进行为期一个月的实验，6%流量分配，统计显著性p<0.01。
- **公平性**：基线使用各自原始配置和骨干；作者额外将L2Rec扩展到相同大骨干（Llama2-7B、Baichuan2-7B、Qwen3-8B）进行控制，排除了骨干差异的干扰。实验设计较严谨。

#### 6. 论文的主要结论与发现
- **L2Rec在所有数据集上一致优于SOTA基线**，NDCG@10相对提升3.87%~8.02%。
- **参数级融合优于输入/输出级融合**：消融实验显示移除任何一项组件（语义视图、行为视图、BPC、个性化路由）都会显著下降。
- **数据效率高**：在仅10%数据时即超越LEARN全量数据表现。
- **在线A/B测试显著提升**：点击率+9.24%，回复率+3.15%（p<0.01）。
- **骨干扩展仍有效**：在更大骨干上（如Qwen3-8B）性能继续提升，且同一骨干下L2Rec优于LLaRA/LEARN。

#### 7. 优点：方法或实验设计上的亮点
- **创新性**：首次提出在LLM参数空间融合行为与语义信号，避免了传统表示空间的对齐困难。
- **技术精巧**：DPMoE中的个性化路由器结合了上下文、用户特征和交互信号，实现了细粒度的用户自适应；共享+视图特定专家的设计兼顾了共性与个性。
- **实验全面**：涵盖了公共基准、工业大规模数据、离线消融、数据效率分析、超参数分析以及线上A/B测试，验证了方法的鲁棒性和实用性。
- **效率高**：冻结主干，仅更新5%参数，适合工业部署。

#### 8. 不足与局限
- **骨干依赖性**：性能仍受限于所选LLM（Qwen3-0.6B）的质量；更大骨干虽提升但计算成本可能增加。
- **序列长度限制**：最大序列长度（L=80, T=2048）可能限制长序列建模能力。
- **在线测试的指标有限**：仅报告了点击率和回复率，未涉及更全面的业务指标（如用户留存、多样性等）。
- **负载平衡损失**虽用于防止路由坍塌，但实验中未单独消融其效果。
- **开放性问题**：未讨论模型在冷启动用户/物品场景下的表现；也未与最新LLM生成式推荐（如GPT-4级模型）对比，可能低估了大型闭源模型的潜力。

（完）
